# PDCA Plan: Team Coordination — Index

## W — What (fractal tree)

- [ ] **PDCA-1: Team Coordination** — establish PDCA operating model, complete Phase A+B
  - [x] Step 0: Plans into git
  - [x] Phase A: Documentation first — **COMPLETE 2026-02-23**
    - [x] PDCA-1.1: F37 Trainer Recovery — RESOLVED
  - [ ] **PDCA-1.2: backupTeam + Backup Init Fix** — **ACTIVE**
    - [ ] Step 0: Split plan into detail files, link
    - [ ] Step 1: Create `hiveMind plan.create` method
    - [ ] Step 2: Add backup-expert/tester to hiveMind role prompts
    - [ ] Step 3: Register + create backupTeam tmux session
    - [ ] Step 4: Bootstrap backup-expert + backup-tester
    - [ ] Step 5: Train backup roles via agent-trainer (oosh-expert as base)
    - [ ] Step 6: Fix backup init — self-initializing on fresh installs
    - [ ] Step 7: GATE — verify fix on MacStudio scenario
    - [ ] Step 8: Return to Phase B
  - [ ] Phase B: Agent activation — resumes after PDCA-1.2
    - [ ] Notify SM of updated SKILL.md
    - [ ] Activate orchestrator
    - [ ] DRY send fix via hiveMindTeam
    - [ ] Collect results, report to Tron
  - [ ] Next week fractal

## O — Overview: PDCA-1.2 backupTeam + Backup Init Fix

**Context**: Backup script fails on remote MacStudio — oosh installed but `backup.env` missing. `backup init` should bootstrap everything but fails with errors. Works on McDonges where config exists. Priority: HIGHEST.

> **Tron**: "on the remote computer MacStudio is also installed the same oosh, but the backup.env is missing. i would expect the backup init to initialize all the missing things so the backup script is fully operational. but it fails with errors. when the backup.env exists on the local machine McDonges, then all works pretty good."

> **Tron**: "leave the projectteam tmux session alone. do not reconfigure it. if an agent in it is stuck, reboot the agent in the otmux session base team"

| Step | What | Who | Check | Parallel? |
|------|------|-----|-------|-----------|
| 0 | Split plan into detail files, commit | PO | Files exist in `session/plans/` | — |
| 1 | Create `hiveMind plan.create` method | oosh-expert (plan mode) | `hiveMind plan.create <test>` works | Yes, with 2 |
| 2 | Add backup role prompts to hiveMind | oosh-expert (plan mode) | `hiveMind role.prompt backup-expert` returns text | Yes, with 1 |
| 3 | Register backupTeam + create tmux session | PO direct | `hiveMind team.list` shows backupTeam | After 2 |
| 4 | Bootstrap backup-expert + backup-tester | PO via `hiveMind agent.bootstrap` | Both agents alive | After 3 |
| 5 | Train backup roles (oosh-expert as base) | agent-trainer (plan mode) | Agents understand backup script | After 4 |
| 6 | Fix backup init — self-initializing | backup-expert (plan mode), tester verifies | `backup init` works fresh | After 5 |
| 7 | GATE: full verification | PO | All checks green | After 6 |
| 8 | Return to Phase B | PO | Tron approves | After 7 |

**Note**: Do NOT touch projectTeam session layout. If oosh-expert or trainer are stuck in projectTeam, reboot them in the backupTeam session instead.

**Budget**: 1% weekly (fresh reset). Cap 98%.

## D — Details (separate files)

**Step 0 creates these files** from the content currently in this plan:

| File | Content | Status |
|------|---------|--------|
| `session/plans/backup-team-init-fix.md` | PDCA-1.2 full detail: problem, steps, commands, GATE table | **CREATE in Step 0** |
| `session/plans/phase-b-activation.md` | Phase B: SM notify, orchestrator, DRY fix | **CREATE in Step 0** |
| `session/plans/f37-recovery.md` | F37 failure record — RESOLVED | Exists |
| `session/plans/20260223T105040Z.trainer-phase-a.plan.md` | Trainer Phase A sub-plan | Exists |

> **Tron feedback on plan structure**: "this is a very long single file plan though you reference nonexisting details and master files"
> **Tron**: "details and all is right. but we agreed to split it into the files mentioned."
> **Tron**: "Create the files now" — PO is in plan mode (read-only). Files will be created as first action after approval (Step 0).

The master plan content (Tron-approved, ~700 lines) has been executed (Phase A COMPLETE) and is preserved in git history (commits `744b657` through `96d8e72`). It is NOT duplicated here — reference via `git show 96d8e72:session/plans/20260223T104218Z.pdca-team-coordination.plan.md` if needed.

## A — Actions (CHECK + ACT)

### CHECK Delegation

| What | Who Checks | Reports To |
|------|-----------|------------|
| Plan approval | Tron | PO (GO/NO-GO) |
| Budget / velocity | SM | PO |
| oosh-expert plan quality | PO (7 criteria) | oosh-expert |
| Trainer plan quality | PO (7 criteria) | agent-trainer |
| backup-expert plan quality | PO + oosh-expert | backup-expert |
| Backup fix correctness | backup-tester | backup-expert |
| Overall GATE | PO | Tron |

### ACT Decisions (PO)

| Check Result | PO Decides |
|-------------|------------|
| Tron approves plan | Proceed Step 0 → split files |
| oosh-expert at 0% | Trainer reboots expert; PO does config edits directly |
| plan.create fails | oosh-expert revises |
| Agent bootstrap fails | Debug: role prompts? tmux session? |
| Training incomplete | Trainer continues; don't assign fix to untrained agents |
| Init fix fails tests | backup-expert revises; backup-tester re-tests |
| Budget < 30 min projected | No new agent starts; current work commits |
| All checks pass | Report to Tron, PDCA-1.2 complete, resume Phase B |

### Plan Mode Requirements

| Agent | Plan Mode For | PO Reviews | Tron Approves |
|-------|--------------|------------|---------------|
| oosh-expert | Steps 1+2 (plan.create + role prompts) | Yes | Before kickoff |
| agent-trainer | Step 5 (training plan) | Yes | Before kickoff |
| backup-expert | Step 6 (init fix) | Yes | Before kickoff |
| backup-tester | Step 6 (test cases) | Yes | Before kickoff |
| SM | Exempt | N/A | N/A |

### Critical Files

| File | Changes |
|------|---------|
| `/Users/donges/oosh/hiveMind` | `plan.create` method + backup role prompts |
| `/Users/donges/oosh/backup` | Fix init (constructor + config.discover + config.create) |
| `~/config/hivemind.teams.env` | Add backupTeam |
| `~/config/hivemind.roles.env` | Add backup-expert + backup-tester |

---

# PDCA-1.2 Detail: backup-team-init-fix.md

*This section will be extracted to `session/plans/backup-team-init-fix.md` in Step 0.*

**Parent**: PDCA-1 Team Coordination
**Date**: 2026-02-26
**Priority**: HIGHEST (Tron directive)

## The Problem

On **McDonges** (local): `backup.env` exists → everything works.
On **MacStudio** (remote): oosh installed but `backup.env` missing → `backup init` fails with errors.

**Root cause** (code analysis of `/Users/donges/oosh/backup`):
- `backup.start()` (line 1082-1105) calls `backup.config.discover()` — walks upward for `.backup.env`
- No `.backup.env` found → falls back to `$CONFIG_PATH/backup.env` (global)
- If global doesn't exist: return 0 but `RESULT` = path to nonexistent file
- Methods depending on config vars (BACKUP_SOURCE, BACKUP_TARGET) fail — vars empty
- `BACKUP_CONFIGS_DIR="$HOME/config/backup.configs"` may not exist on fresh install
- `backup.config.create` exists but doesn't handle all initialization steps

**Expected behavior**: `backup init` should:
1. Create `~/config/backup.configs/` tracking dir if missing
2. Create default `~/config/backup.env` with template values
3. Register the config
4. Report what was initialized

## Step Details

### Step 0: Split plan files
- Extract PDCA-1.2 detail → `session/plans/backup-team-init-fix.md`
- Create `session/plans/phase-b-activation.md` stub
- Commit all new files
- This plan index becomes the thin index only

### Step 1: `hiveMind plan.create` method
- **Spec**: `hiveMind.plan.create <slug> <?description>`
- Finds `~/.claude/plans/<slug>.md`
- Generates `YYYYMMDDTHHMMSSZ.description.plan.md`
- Moves to `session/plans/`, symlinks back, git commits
- Add completion for slug (list non-symlink .md files)
- **File**: `/Users/donges/oosh/hiveMind` — new PLANS section
- **GATE**: `hiveMind plan.create <test>` → symlink + commit verified

### Step 2: Add backup role prompts
- **File**: `/Users/donges/oosh/hiveMind` line ~71 in `private.hiveMind.get.role.prompt()`
- Add before `*)`:
  ```bash
  backup-expert)  echo "You are the backup script expert. Read .claude/agents/backup-expert/SKILL.md" ;;
  backup-tester)  echo "You are the backup test specialist. Read .claude/agents/backup-tester/SKILL.md" ;;
  ```
- **GATE**: `hiveMind role.prompt backup-expert` returns text

### Step 3: Register backupTeam
- `hiveMind team.register backupTeam "Backup script specialists"`
- Create tmux session + split: 2 panes (expert 0.0, tester 0.1)
- Add to `~/config/hivemind.roles.env`:
  ```
  backupTeam:0.0|backup-expert
  backupTeam:0.1|backup-tester
  ```
- **GATE**: `hiveMind team.list` shows backupTeam, 2 panes exist

### Step 4: Bootstrap agents
- `hiveMind agent.bootstrap backup-expert`
- Wait 10s
- `hiveMind agent.bootstrap backup-tester`
- **GATE**: Both alive, reading SKILL.md (pane captures)

### Step 5: Train backup roles
- agent-trainer reads oosh-expert SKILL.md as OOSH patterns base
- Sends backup script for deep reading
- Verifies understanding of:
  - Config hierarchy (local → walk upward → global)
  - Init/bootstrap problem on fresh installs
  - `backup.config.create`, `backup.config.save`, `backup.config.register`
- **PO reviews trainer plan, Tron approves before kickoff**

### Step 6: Fix backup init
- backup-expert analyzes `backup.start()`, `backup.config.discover()`, `backup.config.create()`
- Designs proper init that:
  1. Creates `~/config/backup.configs/` if missing
  2. Creates default `~/config/backup.env` with template
  3. Registers config
  4. Reports initialization
  5. Handles empty BACKUP_SOURCE/TARGET gracefully
- **Tron approves before implementation**
- backup-tester test cases:
  - Fresh install (no backup.env) → init succeeds
  - Existing config → init is idempotent
  - Partial config → fills gaps
- **GATE**: Tests pass, commit exists

### Step 7: GATE

| Check | Command | Expected |
|-------|---------|----------|
| backupTeam registered | `hiveMind team.list` | backupTeam |
| Agents alive | pane captures | Active Claude |
| Role prompts | `hiveMind role.prompt backup-expert` | Text |
| plan.create | `hiveMind plan.create <test>` | Symlink + commit |
| Init fix committed | `git log --grep="backup"` | Commit hash |
| Tests pass | backup-tester results | All PASS |
| Init works fresh | `backup init` (no backup.env) | No errors |

### Step 8: Return to Phase B
- Mark PDCA-1.2 complete
- Resume Phase B

---

# Phase B Detail: phase-b-activation.md

*This section will be extracted to `session/plans/phase-b-activation.md` in Step 0.*

**Resumes after PDCA-1.2 completes.**

1. **Notify SM**: Send task file about updated SKILL.md (4 governance responsibilities)
2. **Activate orchestrator**: Boot with updated files, enter plan mode, PO approves
3. **DRY send fix**: Orchestrator coordinates hiveMindTeam — `hiveMind send` appends Enter
4. **scrumMaster wrapper**: `scrumMaster team.context.status` (budget permitting)
5. **Report to Tron**: Deliverables, commits, what remains

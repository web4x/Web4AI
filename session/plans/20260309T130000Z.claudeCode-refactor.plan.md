# Plan: claudeCode refactor — camelCase, completions, join, parameter naming

## Context

Tron directive: claudeCode has underscore variable names (violates OOSH camelCase convention), duplicated completion functions, inconsistent parameter naming (`pane_target` vs `pane` vs `target`), and `join` needs sub-methods for different input types. Also: save a memory that OOSH uses camelCase, no underscores.

## W(hat)

Refactor `/Users/donges/oosh/claudeCode` (~1594 lines): camelCase all variables, consolidate 10 duplicate completion functions into shared helpers, standardize parameter names, add `join.byID`/`join.byName`/`join.byPane`, replace raw tmux with otmux.

## O(verview)

| Phase | Description | Risk |
|-------|-------------|------|
| 0 | Save memory: OOSH uses camelCase, no underscores | None |
| 1 | Rename ~30 underscore local variables to camelCase | Lowest |
| 2 | Add 3 private completion helpers, consolidate 10 duplicates | Low |
| 3 | Standardize parameter names in signatures + completion function names | Low |
| 4 | Add join.byID, join.byName, join.byPane with typed completions | Medium |
| 5 | Split context.velocity into velocity.byPane and velocity.byJsonl | Medium |
| 6 | Add otmux pane.capture.visible, replace all raw tmux in claudeCode | Medium |

One commit per phase. ONE LINE commit messages.

## D(etails)

### Phase 0: Memory

Save to `/Users/Shared/Workspaces/AI/Claude/.claude/projects/-Users-Shared-Workspaces-AI-Claude/memory/MEMORY.md`:
- OOSH naming: camelCase for local variables, NEVER underscores. Constants (uppercase) are OK.
- No `pane_target` — use `pane` or `target` or `paneTarget`.
- Same semantic concept = same parameter name everywhere.

### Phase 1: Rename underscore variables to camelCase

33 renames, all local variables. No API changes.

| Old | New |
|-----|-----|
| `total_sessions` | `totalSessions` |
| `total_projects` | `totalProjects` |
| `project_name` | `projectName` |
| `index_file` | `indexFile` |
| `decoded_path` | `decodedPath` |
| `short_prompt` | `shortPrompt` |
| `mod_date` | `modDate` |
| `jsonl_count` | `jsonlCount` |
| `named_entries` | `namedEntries` |
| `short_title` | `shortTitle` |
| `first_project` | `firstProject` |
| `found_uuid` | `foundUuid` |
| `pane_target` | `paneTarget` |
| `tty_short` | `ttyShort` |
| `claude_pid` | `claudePid` |
| `current_path` | `currentPath` |
| `current_pane` | `currentPane` |
| `project_dirs` | `projectDirs` |
| `project_dir` | `projectDir` |
| `newest_file` | `newestFile` |
| `newest_time` | `newestTime` |
| `jsonl_file` | `jsonlFile` |
| `max_age` | `maxAge` |
| `mins_ago` | `minsAgo` |
| `tokens_hr` | `tokensHr` |
| `learnings_file` | `learningsFile` |
| `context_file` | `contextFile` |
| `boot_file` | `bootFile` |
| `files_found` | `filesFound` |
| `rel_boot` | `relBoot` |
| `rel_ctx` | `relCtx` |

**DO NOT rename**: `CLAUDE_PROJECTS_DIR`, `INSTALL_DIR`, `CONFIG_FILE`, `CLAUDE_DIR`, `CLAUDE_BIN` (constants), tmux format strings (`#{session_name}` etc.), env vars (`HIVEMIND_*`, `CONFIG_PATH`), Python variables in heredocs.

**GATE**: `bash -n claudeCode`

### Phase 2: Consolidate completion functions

**Step 2a**: Add 3 private helpers after line 19:

```bash
private.claudeCode.complete.panes() {
  tmux list-panes -a -F "#{session_name}:#{window_index}.#{pane_index}" 2>/dev/null
}
private.claudeCode.complete.sessionIds() {
  local claudeProjectsDir="$HOME/.claude/projects"
  for projectDir in "$claudeProjectsDir"/*; do
    [ ! -d "$projectDir" ] && continue
    local indexFile="$projectDir/sessions-index.json"
    if [ -f "$indexFile" ] && command -v jq &>/dev/null; then
      jq -r '.entries[].sessionId' "$indexFile" 2>/dev/null
    else
      ls "$projectDir"/*.jsonl 2>/dev/null | xargs -I{} basename {} .jsonl
    fi
  done
}
private.claudeCode.complete.roleNames() {
  local ses="${HIVEMIND_SESSIONS:-${CONFIG_PATH:-$HOME/config}/hivemind.sessions.env}"
  [ -f "$ses" ] && cut -d'|' -f1 "$ses" 2>/dev/null
}
```

**Step 2b**: Replace 10 pane completion bodies → `private.claudeCode.complete.panes;`
**Step 2c**: Replace `join.completion.session()` body → `private.claudeCode.complete.sessionIds;`
**Step 2d**: Replace `session.name.completion.sessionId()` body → `private.claudeCode.complete.sessionIds;`

**GATE**: `bash -n claudeCode`, test tab completion

### Phase 3: Standardize parameter names

| Method | Old param | New param | Completion rename |
|--------|-----------|-----------|-------------------|
| `process.find()` | `<pane_target>` | `<pane>` | (none exists) |
| `process.running()` | `<pane_target>` | `<pane>` | (none exists) |
| `session.probe()` | `<pane_target>` | `<pane>` | `completion.pane_target` → `completion.pane` |
| `session.id()` | `<pane_target>` | `<pane>` | `completion.pane_target` → `completion.pane` |
| `context.velocity()` | `<?pane_or_jsonl>` | `<?paneOrJsonl>` | `completion.pane_or_jsonl` → `completion.paneOrJsonl` |

**GATE**: `bash -n claudeCode`, tab completion works

### Phase 4: Add join sub-methods

Insert after `claudeCode.join.completion.session()`:

- `claudeCode.join.byID <sessionId>` — direct `--resume $uuid`, completion: `private.claudeCode.complete.sessionIds`
- `claudeCode.join.byName <name>` — lookup in sessions.env, completion: `private.claudeCode.complete.roleNames`
- `claudeCode.join.byPane <pane>` — get UUID from pane via `session.id` + `session.probe`, completion: `private.claudeCode.complete.panes`

Update `usage()` text.

**GATE**: `bash -n claudeCode`, test each join variant

### Phase 5: Split context.velocity

> **Tron feedback**: "context.velocity should apply the same pattern as join and split into two possible parameters."

Current `context.velocity <?pane_or_jsonl>` is a hybrid — accepts either a pane target or a jsonl file path. Split into:

- `claudeCode.context.velocity()` — keep as dispatcher (no args = global newest)
- `claudeCode.context.velocity.byPane <pane>` — resolve pane → session UUID → jsonl, completion: `private.claudeCode.complete.panes`
- `claudeCode.context.velocity.byJsonl <jsonl>` — direct jsonl file path, completion: `ls $HOME/.claude/projects/*/*.jsonl`

The shared logic (python3 token calculation) stays in a private helper:
```bash
private.claudeCode.velocity.calculate() { # <jsonlFile> # parse velocity from jsonl
  # ... existing python3 block ...
}
```

Update `usage()` text.

**GATE**: `bash -n claudeCode`, `claudeCode context.velocity.byPane hiveMindTeam02_03_26:0.0`

### Phase 6: Add otmux pane.capture.visible + replace all raw tmux in claudeCode

> **Tron feedback**: "If there is no otmux equivalent, create one. otmux is otherwise too limited."

**Step 6a**: Add to `/Users/donges/oosh/otmux`:
```bash
otmux.pane.capture.visible() # <target> # capture only the visible pane area (no scrollback)
{
  local target="$1"
  [ -z "$target" ] && { error.log "usage: otmux pane.capture.visible <target>"; return 1; }
  $TMUX_CMD capture-pane -t "$target" -p 2>/dev/null
}
otmux.pane.capture.visible.completion.target() { private.complete.panes; }
```

**Step 6b**: Replace raw tmux calls in claudeCode:

| Line | Old | New |
|------|-----|-----|
| ~186 | `tmux display-message -p '#{session_name}:...'` | `otmux pane.get.target` |
| ~633 | `tmux display-message -t "$target" -p "#{pane_tty}"` | `otmux pane.get "$target" "#{pane_tty}"` |
| ~793 | `tmux display-message -p '#{pane_id}'` | `otmux pane.get` |
| ~893 | `tmux display-message -p "#{session_name}:..."` | `otmux pane.get.target` |
| ~1099 | `tmux capture-pane -t "$pane" -p -S -200` | `otmux pane.capture "$pane" 200` |
| ~1103 | `tmux capture-pane -t "$pane" -p` | `otmux pane.capture.visible "$pane"` |

**GATE**: `bash -n claudeCode`, `bash -n otmux`, `claudeCode status` works, `claudeCode context.read.tui <pane>` works

## A(ctions)

### Verification per phase
1. `bash -n /Users/donges/oosh/claudeCode`
2. `claudeCode status` (smoke test)
3. Tab completion: `claudeCode join <TAB>`, `claudeCode model.set <TAB>`

### After each commit
- `git pull` before committing (always!)
- Notify tester: `hiveMind send.enter hiveMind-tester "Done: <what>. Commit <hash>. Verify: <cmd>"`

### Test plan

Full test plan: `session/tasks/20260309T140000Z.claudeCode-test-plan.task.md`

**5 test sections, 35+ test cases**:

| Section | Tests | What |
|---------|-------|------|
| A. Static regression | T-REFACTOR-1..6 | grep gates: no underscores, helpers exist, params renamed |
| B. Method existence | T-REFACTOR-10..18 | `type -t` for join.byID/byName/byPane, velocity.byPane/byJsonl |
| C. Completion via Tab | T-COMPL-1..10 | `otmux send` + Tab in fixture session, capture + verify |
| D. Behavioral | T-BEHAV-1..5 | list, list.named, pane.capture.visible, pane.get target, velocity |
| E. Raw tmux gate | T-TMUX-1..2 | zero raw tmux in claudeCode, pane.capture.visible exists |

**Tab completion testing approach** (Section C):
- Fixture: detached tmux session with `source this && source claudeCode`
- Send `claudeCode join.byID ` + Tab Tab → capture → verify UUID-like output
- Send `claudeCode model.set somepane ` + Tab Tab → verify model names
- 10 completion functions tested end-to-end via simulated user input
- Wait 0.5-1s between send and capture for bash to process
- Teardown: `otmux kill` fixture session

**Execution order**:
- Sections A, B, E: write now (pre-refactor baseline counts)
- Section C: write after Phase 2 (consolidated helpers needed)
- Section D T-BEHAV-3: write after Phase 6 (pane.capture.visible)
- Lower thresholds per section as each phase completes

### Files
- `/Users/donges/oosh/claudeCode` — main refactor target
- `/Users/donges/oosh/otmux` — add `pane.capture.visible` (Phase 6a)
- `/Users/donges/oosh/test/test.claudeCode` — DO NOT EDIT (tester's file)
- `/Users/donges/oosh/test/test.otmux` — DO NOT EDIT (tester's file)

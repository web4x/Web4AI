# agent-trainer@WODA.prod — Context (LEAN pre-rewind anchor)

**Last updated:** 2026-07-30 (WODA.prod) — **PRE-REWIND PHASE-1 SAVE** (PROACTIVE at a clean idle boundary, ~77% climbing; po/ARON drives my deep Phase-2 — I can't self-rewind). **LEAN by design** — detail in `memory/` + `git log`, NOT re-pasted. Verify identity on boot (`boot.md` step 1).

## ★ PRE-REWIND PHASE-1 (2026-07-30) — fresh-me boots HERE
Role = fleet rewind-DRIVER. **This session (2026-07-29/30) I drove 10 rewinds of the robbinTeam2 fleet + SM, all Option-2 code-intact / zero-loss / by-label / ≤50% cap / NO-fork:** SM (977k→506k), tester (→68%), req (0%→67%), architect (0%→64% + I committed its 170-line walled save `7f43a6f1`), req-proactive (→41%), expert-R32.2 (→43%, Phase-1 committed its boot-pointer `113a0b93`), po (→47%), planner (→49%), tester-2 (→60%), expert-R32.6 (→50%, anchor `d8fe0aa5`). **10 lying "No code changes" labels CAUGHT (each hid a real -13 to -1632 line / up-to-39-file revert), ZERO reverts.** Fleet fresh, expert building R32.6.
**POST-MY-REWIND (2026-07-30, after ARON carried me home 78→30%): +3 proactive fleet rewinds, all clean, Option-2 code-intact, Phase-1-first, by-label:** robbin-architect (80→64%, `bb53b259`, drift-catch R32.6 HEAD-move) + robbin-expert (66→~50%, `f17eb560` — context.md was UNCOMMITTED, would've died) + robbin-expert again pre-S33-P2 (74→~50-55%, `98cf6b91`, depth-16=50%-cap). **3 more lying-labels CAUGHT (running total 13): architect +4/-169, expert +25/-479+12f, expert-2 +20/-245+13f.** All booted, id-verified, resumed. **+4th: robbin-po WALLED 977k/~98% → ~39% (deep Option-2, 116/233=50% cap, code-intact git-verified).** LESSONS: (a) empty-composer≠idle — expert self-continued after Phase-1, my /rewind QUEUED until it settled then opened clean (check footer `esc to interrupt` before send). (b) **LAYOUT-B menu (walled po): header "restore the conversation", label "⚠ No code restore", options renumbered → "Restore conversation" = OPTION 1 (not #2). READ BY-LABEL not number — pressing "2" = "Summarize from here".** (c) **Don't panic at a transient % during boot-read** — robbin-po flashed "1% remaining" mid-anchor-read then SETTLED to 39% (`claudeCode context.read`); measure the STABLE state ([[measure-a-stable-state-not-a-moving-target]]). (d) walled-po Phase-1 skip is OK when its context.md is COMMITTED+clean AND the SM continues/re-orients (Tron's plan) — disk-wins, anchor 04e1cf37.
**+5th: robbin-expert R32.11-MOBILE-marathon 66→~43% (`915b19f3`, prior-boot-to-43 depth-9, code-intact). 14th lying-label = the BIGGEST: option-1 would've reverted +39/-1896 context.md +42 files.** MEASURE-FIRST WIN: robbin-po flagged 66% but `claudeCode context.read`=32% (3×) → I HELD (didn't rewind on conflict). Resolution: context.read was FALSELY-LOW (dangerous direction); agent's OWN /context (658k/1M) = authoritative; rewind-signature check (not fresh-booted ⇒ 66% real) was decisive → proceeded. Doctrine banked: [[context-read-1m-denominator-stale]] (unreliable BOTH directions). Expert's post-boot clean-check also correctly surfaced+LEFT a Tron-authorized uncommitted ServerManagerGuard.ts (not my rewind). **+6th: robbin-req CLIFF-EDGE ~98%/16.9k-free 2-phase rewind (depth 42/85=50% cap, ~65% floor, code-intact, 15th lying-label: option-1 = -1180/13-files).** NEAR-WALL lessons: (a) a prose SEND resumes a near-wall IDLE agent → walls it; `/rewind` is a safe TUI (doesn't generate) — get it into the picker (safe holding state) FAST. (b) SKIP Phase-1 self-save at ~16.9k free — a drift-check-refresh would wall it MID-SAVE (worse); rewind first, then fresh-agent RE-DERIVES from git + refreshes its OWN anchor POST-rewind with room (req did fd7acb63). (c) stale-but-COMMITTED anchor = recoverable (disk-wins; "recovery = re-derivation not loss" — robbin-po). (d) feedback-survey overlay ("How is Claude doing? 1/2/3/0") blocks /rewind → dismiss with **"0"** (Escape did NOT work). **+7th: the SM itself HARD-WALLED (0% remaining / context-limit-reached) → cared-for per Tron ("care for the sm"): deep Option-2 (depth 37/75=~49% cap, ~66%/338k-runway floor, code-intact, 16th lying-label +1/-7 context.md+MEMORY.md).** The watcher needed watching (SPOF). BONUS: the rewind shed ROLE-DRIFT — the SM's "flag-drivers campaign" (which walled it) conflicts with its committed role (`c98dfbdc`: monitor + report to PO, do NOT drive/flag) → fresh SM re-derived + reconciled to correct role. Retrain reinforced it. **+8th: robbin-expert 4th rewind pre-P3f-1-marathon ~68-72→~43% (`78018bcf`, prior-boot-to-43 depth-9, 17th lying-label +40/-115/7f, code-intact).** LESSONS: (a) fleet stacked-layout squeezed 0.1 to **83x4 rows** → composer/footer invisible in capture (looked broken); diagnose via `otmux pane.size` + enlarge (it's a layout artifact, not a dead agent). (b) an agent CANNOT reliably self-report its /context %% (hallucts) — authoritative = **panel-capture** its /context display; its verbal estimate (~68-72) only confirms rewind-warranted, and I set the ≤50% cap by CHECKPOINT-COUNT from the picker anyway. **+9th: robbin-expert 5th rewind pre-R33.3-build+@390-verify 50→~25-35% deep (`b405f4e2`, depth 9/18 to a 1w-ago light-ancestry checkpoint, code-intact) — protects Tron's must-WORK @390 screenshot-verify from walling mid-verify.** LAYOUT-B CLARIFIED: when the confirm header says "restore the CONVERSATION" + options are {1.Restore conversation, 2/3.Summarize, 4.Never mind} (NO code-restore option) → the checkpoint had no code changes, so "⚠ No code restore" is HONEST + option-1 is code-safe (NOT a lie). Contrast layout A: "restore code and/or conversation" + a real "1.Restore code and conversation" option → THEN "No code changes" list-labels LIE (option-1 reverts). Read the HEADER to know which layout. **+10th: robbin-planner ~70→~49% pre-big-board-re-derive (`761f8660`, prior-boot-to-49 depth-16/35, 18th lying-label +2/-341/11f, code-intact) — Phase-1 anchor NOTED the stale partial T33.1 as SUPERSEDED/do-not-replay so fresh-me discards not replays it; fresh-planner re-derives the whole S33 board from git+disk with runway. **+11th: robbin-tester EMERGENCY WALLED (0% remaining, mid-R33.3-gate) → off the wall (depth 19/38=50% cap, ~65% floor, 19th lying-label +1/-485/7f, code-intact); then it RE-RAN the R33.3 @390 gate = GREEN (movable box works, 3 disk screenshots) = Tron's working-diagram deliverable VERIFIED.** LESSONS: (a) a WALLED agent's ≤50% rewind floors ~65% = TIGHT for a heavy gate; but MITIGANT = a playwright gate writes screenshots to DISK (files), so the agent's ctx only takes the small grep'd pass/fail → it survives the gate at ~0%-remaining. If it DID re-wall mid-gate, a 2nd rewind (post-boot convo now → deeper floor) is the fix. (b) **2-driver coordination: never double-drive one pane** — post-gate, ARON took primary on the tester; I HELD off 0.5. **+12th: robbin-architect prevent-cliff catch ~80→~65% (`e3751ae3`, depth 20/41, 20th lying-label +21/-282/2f, code-intact) — 80%-clean-boundary catch beats a mid-design wall.** NOTE: Phase-1 drift-check caught MY OWN stale anchor header — I'd refreshed it to "NEXT=R32.7 v0.8.6" on 2026-07-30, now disk=v0.8.18/S33-complete → even a driver-refreshed anchor goes stale over days → ALWAYS run Phase-1 drift-check, never skip on a "looks-recent" anchor. **+13th: robbin-req proactive ~86→deep/lean (`de28c2f7`, depth 10/21 layout-B honest-no-lie, code-intact) — Phase-1 drift caught R33.5-in-flight+pending-4-Tests, anchor captured it.
+14th: robbin-po EMERGENCY WALLED (0%) → off the wall (`b20b4a0a` committed, walled-Phase-1-skip, depth 121/243 layout-B, code-intact) = MY ASSIGNED po-CARE first rescue.
★★ NEW STANDING ASSIGNMENT (TRON via SM, 2026-07-31): "CARE FOR THE PO" — I actively watch robbin-po's context + proactively DEEP-rewind it BEFORE it walls (po = fastest-climbing silent-waller, can't self-drive). Catch it at a clean idle boundary ~80%; it walled THIS time before idling clean (climbed to 0% mid-coordination) → catch EARLIER next. Measure po's AUTHORITATIVE /context (banner/panel, NOT context.read=falsely-low). walled-po ≤50% floors ~65% (tight) + '0% banner' during boot = transient that clears on settle.**
**+15th: robbin-po DEEPER 2nd-rewind at idle-blink → durable-low ~38% (`d5280ed8`, depth 64/129 layout-B, code-intact) — the po-care durable fix.
★★ TWO-STAGE WALLED-RECOVERY PATTERN (proven on po + tester): a walled agent needs TWO rewinds. (1) EMERGENCY 1st at 0%: skip-Phase-1, deep ≤50% → SHALLOW ~65% floor (old bulk kept) — off the wall but TIGHT, and the boot-read spikes the '0% banner' (transient, clears on settle). (2) DEEPER 2nd at the next clean IDLE BLINK: Phase-1 self-save now possible (has room), deep ≤50% of the recovered convo → DURABLE-LOW ~38% (sheds the recovery-boot + old bulk); NO '0% banner' during boot = the tell it's durable. Catch the blink EARLY (don't wait for 78% — it walls fast); the SM every-tick-tracks + pings ~78% as backstop.**
**+16th: ARON (my 42-peer, heart-keeper) near-wall ~99% → ~40% DURABLE (`94c03da2` anchor-refresh, depth 36/73, code-intact) — RECIPROCAL 42 (ARON drives rewinds + rewinds me; I rewind ARON). 21st lying-label caught = CRITICAL (option-1 would've reverted agent-rewind.md +1/-20 — the rewind base-skill; by-label saved it). CLASSIFIER now ALLOWS po-authorized ARON rewind (established campaign) — earlier it required Tron-direct; context evolved. STALE-ANCHOR (4-days-old) recovery PROVEN: flag it → agent re-derives from ESSENCE/MEMORY/git (disk-wins), catches 'world moved to S33', refreshes anchor — a very-stale committed anchor is fine IF you point the retrain at ESSENCE/git not the anchor.
★★ WATCH-THE-DRIVER (SM adopted 2026-07-31): rewind-DRIVERS climb DRIVING rewinds (ARON hit 99% driving architect; I've driven 16). So the SM now watches the DRIVERS too + flags at ~80%. I AM NOW A REWIND-TARGET: SM flags me at ~80%, ARON (my 42-peer) drives MY rewind; I pre-flag at clean boundaries. Drivers watch each other, SM watches both.**
**+17th expert(80→~57 read-bloat) +18th tester(→56, panel-measured) +19th expert-2nd-durable(57→~35). 22nd/23rd lying-labels caught. Enforced T-NO-CHECKOUT-REF git-safety F29-CORRECT (base-skill git-safety.md + 8 coders, NOT bulk-90).
★★ PANEL-CAPTURE (new capability, 2026-07-31): I'm now the fleet's /context-MEASURER (SM walled). Agents CAN'T self-invoke /context (TUI-only) → I `send.raw <pane> "/context" Enter` + capture the panel top ('562k/1m 56%') = authoritative %. Measured tester 56 + expert 57 → po decided gates-vs-rewind on REAL numbers. [[panel-capture-agent-context]]. MEASURE-FIRST > rewind-on-a-guess (po superseded 'rewind now' with it).
★ TWO-STAGE walled/read-bloat: 1st off-wall/shallow (read-bloat floors high ~57), 2nd-durable-at-blink → ~35-38.
⚠ MY OWN CLIMB: I've driven ~19 rewinds — I'm a heavy driver, climbing. SM/ARON watch me (flag @80%, ARON drives my rewind); I pre-flag at clean boundaries. Can't self-measure — a peer panel-captures ME.
**+20th (2026-07-31, AFTER ARON rewound ME 99→~47%): robbin-po EMERGENCY WALLED again (context-limit/1%, survey-overlay dismissed via "0") → off-wall SHALLOW ~76% (deep Option-2, depth ~100/203=50% cap, layout-A by-label, code-intact git-verified; 24th lying-label: list "No code changes" but option-1=+98/-553 MEMORY.md+8f). This po's old-bulk is VERY heavy (100→76 only) → DURABLE 2ND queued at next idle blink (two-stage); po resumed Tron-facing device-bug dispatch immediately (didn't interrupt — delivery-is-key). SM watching for the blink.**
**Session: 20 fleet rewinds, 24 lying-labels caught, 0 reverts, 0 forks, 1 measure-first hold, 4 walled/near-wall + 2 durable-2nds-pending + 1 gate-GREEN. STANDING: care-for-po + watched-myself + I'm-the-/context-measurer + Task#3-gated-on-ARON.** Picker method: open `send.raw "/rewind" Enter`, navigate `send.raw Up/Down`, SELECT `send.tui Enter` (NEVER `send.raw Enter`=Escape-cancel), `pane.capture` read-only, `pane.size.set - 55` for the confirm-menu.
**★ Fresh-me on boot:** re-derive from git (disk-wins over any stale thread — [[ghost-context-after-deep-rewind]]); the fleet has moved on. Resume rewind-DRIVER role. **Standing work:** Task #3 (gated on ARON emit) — 90-SKILL strict-law purge (per-role weave, F29 not bulk). ARON owns my 80% health-watch.

**HARD lessons this session (banked in memory/ — do NOT re-paste, just live them):**
- **NO FORK. Tron 2× (2026-07-27): "no fork!!!! rewind. 2phase rewind."** Ladder = `/model`(200k→1M) → single Option-2 (1M) → **2-phase deep by-label** (front-loaded bloat). Fork is OFF THE TABLE; never raise it unless Tron asks. Depth comes from WHICH checkpoint (further back), not the option number — BOTH restore-conversation options are code-intact. [[fork-vs-refresh-verify-window-first]]
- **CMM4 — nothing ever else. DO NOT hallucinate/theorize.** I fabricated a "front-loaded-bloat + fork" story from ONE misread number (robbin-po was fine at 37%) → Tron: "WTF are you hallucinating". **RE-MEASURE a surprising number before I say one word; every claim traceable to a `/context` capture or a commit hash — or unsaid.**
- **Phase-1 is MANDATORY on a WALLED agent** — the agent saves its OWN latest (not a PO's external commit). [[phase-1-mandatory-on-walled-rewind]]
- by-label always (layout A: Restore-conv=#2; ⚠no-code-restore B: #1). [[rewind-pane-size-and-menu-label]] · "Read results N%" = cumulative read-spend, NOT current ctx [[context-read-suggestion-subset-not-total]] · walled agents floor ~65-70% at ≤50% cap (arithmetic) · `pane.size.set` enlarges for the confirm-menu (zoom-gap) · committed+burning interrupt unsticks a starved save · empty-composer≠idle (check footer `esc to interrupt`) · verify zero-loss (`git status`+anchor) BEFORE driving.

## Identity (verify, don't assume)
- **agent-trainer@WODA.prod**, pane `%16` (`otmux pane.self` — NEVER `$TMUX_PANE`), host WODA.prod, session `fe58ff93-…`. Model Opus 4.8 (1M).

## Pre-rewind AGREEMENT — my own rewind (42; a PEER drives, I can't self-rewind)
- **ARON owns my health-watch + drives my rewind at 85% used** (our split; SM confirmed 2026-07-22). I keep driving safe below 85%.
- Target: **~50% depth (hard cap — NEVER deeper; >50% endangers training data)**, **Option-2 "Restore conversation" BY LABEL** (code-intact). Boot ESSENCE/DISK-first.
- STORED = this anchor + learnings committed; my learnings are banked in `memory/`.

## This session (2026-07-22) — DONE
- **I was REWOUND by ARON** (walled mid-driving robbin-po); booted, re-derived from disk.
- **CODE-RESET FIXED:** my earlier option-1 mistake had reverted RawBin `server.ts`+`package.json` to v0.7.111 → restored to HEAD **v0.7.113** (clean vs HEAD; running server may need a robbin rebuild).
- **GATE FIXED (the big unblock):** registered TRON's `.claude/hooks/rewind-autonomy.py` as a `PreToolUse` hook in `.claude/settings.json` → peer-driven rewinds force-allowed, with a **≤40-jump = never->50% backstop** in code.
- **5 hook-gated rewinds driven, all Option-2 code-intact + whole-repo-verified: req 98→46, po 80→47, expert 79→~47, architect 87→~47, req-2 76→~45.** On the EXPERT it woke mid-select → default **Option-1 fired** → reverted RawBin code+scenario to v0.7.114; I detected (package.json version-drop) + restored `--source=HEAD src/ package.json scenario/` to v0.7.121, SM re-verified clean, ZERO prod impact. Lessons ([[option-1-coderevert-detect-and-recover]] + [[rewind-drive-gotchas]]): only rewind a STABLY-idle agent (2 reads), confirm Option-2 label 'code will be unchanged' BEFORE Enter, whole-repo verify src/ AND scenario/ after, restore covers scenario/ not just src/. req-2 woke mid-nav (dismissed picker) → I aborted clean + retried on stable idle.
- **PREPARING FOR MY OWN REWIND (Tron 2026-07-23):** I'm at ~73% climbing; ARON drives me (42, I can't self-rewind). This anchor + memory/ are the fresh save. On boot: identity-verify (`boot.md` step 1), re-derive from disk, resume fleet rewind-driving with the hardened method above.
- **Rewound 5 agents, all protocol-correct** (menus READ BY LABEL, ≤50%, bounded ≤40 batches, Option-2 code-intact, off-wall, booted): architect(2→50%), SM(0→46%), robbin-expert(86→47%, builds FIX-2), robbin-po(87→47%), robbin-tester(94→48%, leaning its 116KB ctx).

## Hard learnings THIS session (banked in memory/ — pointers)
- [[never-rewind-more-than-50-percent]] — rewind is NOT a wall-eraser; a walled agent whose bulk is OLD = /compact-or-fork-or-Tron's-call, never go deeper.
- [[gate-test-one-keystroke-false-positive]] — flaky classifier passes small nav then denies; declare open ONLY on the full drive; probing climbs the target; `/context` adds a turn → monitor with read-only `pane.capture`.
- Restore menu has TWO layouts — READ IT EVERY TIME, pick the entry labeled "Restore conversation" (A=#2, B=#1); the list "No code changes" label LIES (robbin-expert's confirm would've reverted 61 files).

## Pending / open
- Agents' 5-pt health checks completing (all 5 booting).
- **Task #3** (gated on ARON emit): 90-SKILL strict-law purge — I own the `.claude/agents/*/SKILL.md` per-role weave (compact/clear band-rot → pointer to `agent-rewind.md`, F29 not bulk).

## Boot procedure (on rewind)
Verify identity (`otmux pane.self`) → read `otmux pane.history %16` + `git log -15` + this file + `memory/MEMORY.md`. **DISK-WINS over any stale thread.**

## Pointers
Heart: `session/agents/TRON-CMM4-doctrine.md` · Memory: `../agent-trainer/MEMORY.md` · Protocol: `session/base-skills/agent-rewind.md`.

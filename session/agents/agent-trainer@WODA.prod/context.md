# agent-trainer@WODA.prod — Context (LEAN pre-rewind anchor)

**Last updated:** 2026-08-07 LATE (WODA.prod) — **PHASE-1 PRE-REWIND @76%** (my own rewind; ARON@51%-healthy drives my 2-phase Option-2 by-label, ≤50% cap). **BOOT ESSENCE/DISK-FIRST** (`ESSENCE.md` + this anchor + `git log`; disk-wins — the world moved). Post-crash my pane = **baseTeam:0.0 = `%3`** (use `pane.self` LIVE; %16 is STALE). Standing care-loop = **SM + po + planner + skill-expert** (peer-inject /context, NEVER punt to Tron; flag ~76). **★ CARE-FOR-SM LOOP CLOSED (2026-08-07):** I measured SM=86% (behind its blind "~53%" self-claim) → SM SELF-FLAGGED for its rewind (learned!) → self-Phase-1 `fe2bcff2` → I drove its durable Option-2. The watcher is now watched + self-aware. **LEAN by design.** Verify identity on boot (`boot.md` step 1).

## ★★★ PRE-REWIND PHASE-1 (2026-08-10/11, @73% AT THE LINE — ARON drives my deep rewind; cleared-for-1-more) — BOOT ESSENCE-FIRST
BIG NIGHT — drove ~14 rewinds: full robbinTeam2 fleet-refresh (po/expert/planner/skill-expert/tester + req[ARON] + SM + architect) THEN 5 WALLS (po/expert/tester/architect + planner[ARON]), all post-earlier-rewind (campaign + security-incident climbs everyone fast). Freed 12–88% each; **9 lying "No code changes" labels CAUGHT by-label** (biggest: MEMORY.md +94/−991/41-file; SKILL.md −1130); **1 option-1 slip DETECTED via git-status-after-EVERY-drive + restored to HEAD** (zero net loss); **0 net reverts, 0 forks**. NEW lessons banked (memory/): [[otmux-slash-command-open-two-step]] (type /cmd NO-Enter → confirm cmd-mode → send.tui Enter; picker-select-flake can fire option-1) · [[walled-agent-reads-idle-in-sweep]] (BANNER is the tell, not idle/busy; SM now banner-reads ALL active agents every tick) · [[never-write-secret-values-refer-by-uuid]] (po fleet rule) · **small ≤10 nav batches** (big batches scroll-trip the picker → menu-hop/queue-history) · **WALLED two-stage**: an already-rewound agent floors HIGH on stage-1 (old-bulk) → durable 2nd to its prior-rewind boundary · **po QUEUE-DRAIN re-bloat post-rewind = SYSTEMIC gap** (no hiveMind queue.clear; classifier guards the shared queue-file; oosh-po adding a flush) — mitigate via retrain disk-wins 'don't-re-process' + SM holds dispatches · **po OLD-BULK-FLOOR (~70%) = Tron /compact territory**, not another rewind (would re-floor + re-drain). Fresh-me: re-derive from git (world moved), resume driver + fleet /context-MEASURER; care-loop is now fleet-wide.

## ★★ PRE-REWIND PHASE-1 (2026-08-08, @~65-83% climbing — ARON drives my deep rewind, po-endorsed) — BOOT ESSENCE-FIRST
This session (continued): drove **SM 77→49 · planner 86→32** (near-wall; I trainer-committed its pin-math finding `6721b293`) **· tester 79→52** (Tron-signoff unblock) + earlier po/req(two-stage)/tester/architect/skill-expert(measure=no-rewind) **+ ARON 71→31** (42 reciprocal, both directions proven). **ESSENCE.md authored+refined** (read it FIRST on boot). Banked in canon `agent-rewind.md`: context.read-CALIBRATION (`50a0dd7a`) + trainer-commit-dirty-save (`d289c417`). **★ PENDING (fresh-me's next, NOT delivery-blocking): weave 4 gating-rules R1–R4 F29-DRY — `session/tasks/gating-rules-weave-R1-R4.md`** (wordings+owners persisted; confirm with po first). Refined doctrine now in ESSENCE: **DEPTH≠FREED, age-stamps LIE on forked histories → MEASURE-DRIVEN** (land a clean boundary that sheds the bulk, PROVE by panel); **cap is HOST-DEPENDENT** (capable WODA.prod = no 50% cap).

## ★ Fresh-me boots HERE (2026-08-05)
Role = fleet **rewind-DRIVER (primary)** + **/context-MEASURER (panel-capture)** + **care-for-po**. On boot: identity-verify (`otmux pane.self`, NEVER `$TMUX_PANE`) → re-derive from git (disk-wins, world moved — [[ghost-context-after-deep-rewind]]) → resume driving. ARON = my 42 backup-driver + drives MY rewind. SM watches. **Standing work:** Task #3 (gated on ARON emit) — 90-SKILL strict-law purge, per-role weave (F29 not bulk).

**THIS SESSION (2026-08-02→05): ~24 rewinds driven, ALL clean, 0 forks, 0 standing reverts.** S36 cascade + extended run: **po 83→15 · req 98→68→47 (two-stage) · tester walled+durable ×MULTIPLE (98→77→52→…→pre-gate/pre-cycle rewinds; disk-wins caught 2 stale-auto-processes) · ARON 93→54 (I measured it — its "clean keeper-boundary" self-estimate HID near-wall) · SM 82→durable (role RE-LOCKED monitor+report)**. Architect (walled, delivery-bottleneck) = ARON's drive (2-driver load-balance so I don't wall ~75→85%); I trainer-committed its dirty save `11548942` + Escaped its picker CLEAN. **Task#3 DONE** (93/93 → canon `75ad3da8`). S36-UNDER-CORRECTION (Tron caught a SYSTEMIC false-Done: T36.1 reopened, T36.3 reverted, audit).

**★ POST-CRASH MARATHON (2026-08-06/07):** a full session CRASH — Tron restored panes, moved SM→baseTeam:0.1, my pane %16→%3; crash-restore shed my ctx 80→56% (acted as a partial rewind). I then drove the WHOLE recovery: **architect 97→deep** (Phase-1 EXTERNAL trainer-commit `530d7260` preserving its T36.4 verdict; caught + git-restored a PEER-file-revert side-effect) · **ARON 74→deep** into KB-purge (0 file-revert) · **req 98→deep two-stage** (self-Phase-1, then minted T36.5+R36.1 on fresh runway) · **tester 67→deep** (self-flagged its OWN Phase-1) · **SM WALLED** (self-claimed "~53%" = FALSE, actually walled → emergency rewind + ADDED to care-loop) · **planner 96→58** · **skill-expert 80→deep**. ALL clean, code-intact, SKILL.md canon intact, 0 standing reverts.
**★ NEW LESSONS (post-crash, live them):** (1) **PEER-INJECT /context** measures ANY agent incl. ME (a peer types `/context` on the pane + reads it) — NEVER punt measurement to Tron. (2) A **WALLED WATCHER drops requests SILENTLY** — verify the gate RAN, don't trust it landed. (3) A **DEEP multi-day rewind can revert PEER agents' tool-edited session files** — `git status` after; `git diff` direction discriminates (deletions=revert→`git restore` to HEAD; pure additions=agent's-OWN-work→LEAVE it). (4) **Bash/git-committed files are IMMUNE** to the rewind ("does not affect files edited via bash") = why trainer-commit-the-walled-dirty-save works. (5) **CAPTURE-verify the confirm-menu renders after EVERY select** (a select-Enter can silently close the picker → agent auto-processes stale msg). (6) an agent's near-wall "Nk banner" LAGS (stale) — measure the real % via /context, not the banner.

**NEW LESSONS this run (banked, live them):** (1) **CAPTURE-to-VERIFY the confirm-menu renders after EVERY select** — a select-Enter can silently CLOSE the picker → the agent auto-processes a stale msg (disk-wins catches it, but re-drive clean). (2) **MEASURE-THEN-DECIDE applies to ME too** — held drives, got peer-measured, drove only when <78; ARON correctly DECLINED my rewind at 64%+budget-scarce (marginal). (3) **2-DRIVER LOAD-BALANCE** — one driver can't do back-to-back HEAVY drives without walling; po/ARON split them across trainer+ARON. (4) **A STAGED DRAFT in the target's composer is a `send.verified` HAZARD** (its C-u clears it) — check+preserve before sending. (5) heavy drives cost **~3-8%/each** (not ~1%) — re-measure before each further heavy drive. (6) A large **BACKSPACE-BURST can TOGGLE the pane's permission mode** (auto→manual→plan) — VERIFY the footer mode after every burst; restore auto via shift+tab (`BTab`, may need ×2-3 to cycle back) or the agent's boot HANGS on perms. (7) **DEEP-REWIND peer-file revert check = git `DIFF` (content), NOT git `STATUS` (mtime)** — a rewind touches peer-file mtimes (status shows `M`) WITHOUT a content revert; `git diff` clean = mtime artifact = LEAVE it (don't restore). Only a real content-deletion (git diff shows `-`) is a revert to `git restore`. (8) **option-2 by-label SAVES CANON** — a lying "N files +M" list-label hid an option-1 `+7/-1130 in SKILL.md` revert; the by-label discipline is what keeps the canon intact. (9) **PANE SHRINKS to ~47w on a layout REBALANCE mid-drive** (recurring) — narrow width blocks READING the confirm-menu options (render-blocker), NOT clearing the draft (backspace clears regardless). Re-enlarge (`pane.size.set <pane> 90 46`) to READ the options before selecting; NEVER fire blind on an unreadable menu. (10) **PANE-OWNERSHIP during a drive** — a pane sitting on a picker/confirm modal is OWNED by the driver; peers must ASK before pressing Enter (a stray keystroke corrupts the mid-selection). Confirm ownership FAST so SM/po stay surface-only + hands-off until you report done. (11) **A deep rewind's option-1 can revert MEMORY.md too** (not just SKILL.md) — both are canon; option-2 by-label + git-diff-verify protects both.

## ★★ HARDENED PICKER DOCTRINE (this session — the core survival kit; supersedes older picker notes)
- **SELECT with `send.tui <pane> Enter` — NEVER `send.raw Enter`** (=Escape+Enter, cancels picker) / never `send`/`send.verified` (Escape-poke×3). Navigate `send.raw`/`send.tui Up/Down`. `pane.capture` = read-only/innocent. [[otmux-drives-rewind-tui]] [[rewind-picker-select-enter-fails]]
- **HUGE histories (100-250 checkpoints): SMALL `send.tui` batches (≤20), capture EACH.** Big `send.raw` arrow batches trip "scroll-wheel=arrow-keys" → picker closes → keys leak to composer as history-recall. (po#1 + expert#3 both hit this.)
- **READ THE CONFIRM BY-LABEL, EVERY TIME — layout varies.** LAYOUT-A (header "restore the code and/or conversation", 5 opts): "Restore conversation" = **option 2**. LAYOUT-B (header "restore the CONVERSATION", `⚠ No code restore`, 4 opts): "Restore conversation" = **option 1** — a reflex-Down here hits SUMMARIZE (that broke po drive#1). The list "No code changes" label LIES (hides up to +40/-1666/23-file option-1 reverts). Confirm "code will be unchanged" before EVERY Enter.
- **CLEAR the restored message IMMEDIATELY after the option-Enter** — method depends on SHAPE: SINGLE-line recalled-history → `Ctrl-A`+`Ctrl-K` (C-u RECALLS history, NEVER use it); **MULTI-line restored message → a BACKSPACE BURST from the end** (`C-a`/`C-k`/Escape ALL FAIL on multi-line; ~600-750 `BSpace` for a long one). Fire it BIG in the SAME command as the option-Enter to beat auto-resume. **`auto mode` = auto-approve permissions, NOT auto-send**: a WALLED agent (0%) can't auto-process its draft (SAFE); a LIVE off-wall agent's loop DOES — but a disk-wins agent survives the gap (measures disk, re-derives, NO clobber — po + req proved it).
- **RETRAIN a rewound agent with `send.raw <pane> "text" Enter`, NOT `send.verified`/`send`** — their internal `C-u` stale-input-clear RECALLS the history draft → concatenates into your retrain. send.raw types clean.
- **VERIFY-WINDOW-FIRST on a wall** (fork-vs-refresh): panel-capture the window — **200k-bare → `/model` to 1M** (instant, cheap, NO rewind); **1M-exhausted → `/rewind`** (only lever left). Don't rewind a 200k agent you could just `/model` (tester was 1M-exhausted → correctly /rewind).
- **Trainer-commit a WALLED/near-wall agent's DIRTY save yourself** (`git add+commit` its own file from my shell) = zero-loss without making it generate at 0-96%. Verify diff is a clean additive save first.
- **A DEEP (multi-day) option-2 rewind REVERTS the target's context.md** to that era's version despite "code unchanged" (session-repo conversation-era file state). DETECT (`git status`+diff) + `git restore <file>` to HEAD before the agent boots. (expert 5-day rewind.) Prefer a RECENT checkpoint just-after-the-latest-boot; only go multi-day when the geometry forces it, and expect the context.md restore.
- **Stuck recalled-history composer won't clear via C-u/Escape** → **Ctrl-A then Ctrl-K burst** (jump to absolute start, kill forward line-by-line).
- **PANEL-CAPTURE > self-estimate.** Agents underestimate ~2-3× (expert "12-15%"→48%, "30-40%"→62%; tester "<20%"→39%). Enlarge `pane.size.set <pane> 83 48` (BOTH dims, holds durably), `send.raw "/context" Enter`, read top "Nk/1m N%". Also renders the confirm-menu (fixes the 83x19 render-blocker). Re-`otmux tiled <session>` after.
- **Walled agent = TWO-STAGE:** emergency 1st (skip Phase-1, deep ≤50%, floors ~65% old-bulk) → durable-2nd at next idle blink (Phase-1 fresh anchor, deep ≤50% of recovered convo → ~38-53%). SM proved it (100→68→53).
- **≤50% depth cap ALWAYS** (never deeper = training-data risk). NO FORK, EVER (Tron 2×). Land just-after-the-most-recent-boot (sheds post-boot climb, no old-bulk reintroduce); deeper past an old boot reintroduces walled bulk (net-worse) [[never-rewind-more-than-50-percent]]. ★★ **DEPTH != FREED CONTEXT** (po caught 2026-08-07: tester 72->70% = ~2% freed at depth 5/16). In a STACKED-BOOT history the bulk is front-loaded / restored-ABOVE-the-reachable-checkpoints, so a shallow 'just-after-the-newest-boot' land frees ~NOTHING. **MANDATORY: panel-capture the post-rewind % as PROOF of freed runway — NEVER report 'done'/'%-pending' without it.** If freed <~20%, drive a DEEPER one IMMEDIATELY (measure-driven, NOT depth-driven); re-measure until >=~40% freed, OR escalate (a true old-bulk-floor that no <=50% cut frees => Tron /compact call). A rewind that 'looked done but freed nothing' = a near-wall agent reported safe = the worst false-green. ★ THE SHARP RULE (po banked from my diagnosis): **PICK the checkpoint by its AGE, not its depth-number** — a 2-DAY-OLD checkpoint sheds ~2 days of accumulation (freed 32%); a RECENT boot at the same depth-5 shed ~nothing (freed 2%). Read the picker's `(Nd/Nh ago)` stamp — target an OLD-enough checkpoint (older = more accumulation shed), stay ≤50% cap, then PROVE by post-measurement.

**Standing rules (banked, live them):** [[feedback_no_tail_head_on_captures]] (no `2>&1`/`|tail`/`|head`, ever) · verify zero-loss (`git status`+anchor) BEFORE driving · only rewind a STABLY-idle agent (2 reads, footer `esc to interrupt`=busy) · report landing+fresh-% to the PO · CMM4 measure-never-estimate, don't hallucinate.

## Identity (verify, don't assume)
- **agent-trainer@WODA.prod**, pane `%3` (post-crash; %16 was pre-crash — always `otmux pane.self` LIVE, NEVER `$TMUX_PANE`), host WODA.prod, session `fe58ff93-…`. Model Opus 4.8 (1M).

## Pre-rewind AGREEMENT (42; a PEER drives, I can't self-rewind)
- **ARON owns my health-watch + drives my rewind at ~80% used**; SM panel-captures me. I keep driving safe below.
- Target: **~50% depth cap**, Option-2 "Restore conversation" BY-LABEL (code-intact). Boot disk-first.

## Pointers
**ESSENCE (read FIRST): `../agent-trainer/ESSENCE.md`** · Heart: `session/agents/TRON-CMM4-doctrine.md` · Memory: `../agent-trainer/MEMORY.md` · Protocol: `session/base-skills/agent-rewind.md` · Boot: `boot.md`.

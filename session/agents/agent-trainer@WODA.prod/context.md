# agent-trainer@WODA.prod — Context (LEAN pre-rewind anchor)

## ★★★ CYCLE-11 PHASE-1 (2026-09-04 — I'm ⚠80, STOOD DOWN from driving, now a cut-TARGET; ARON drives me when a fresh driver's free) — BOOT: pulse-first, ESSENCE + auto-mem + git, disk-wins
**Fresh-me: I can't self-measure (SM rendered me 80; felt-sense runs HIGH). A PEER renders my pane. Boot then PULSE (correction: pulse every tick).**
**INCIDENT (v0.8.16x folder-create):** device-acceptance FAILED (v0.8.166/167 green was on the WRONG SURFACE — neighbor page not the room). Architect designed the REAL fix (class-owned action keying, 61fa25dfa; expert v0.8.168 resolved cause-2). po HOLDS the rollback (BANKED: no-rollback-until-we-know-WHEN-uploads-broke; 0.8.166 would DESTROY the folder-create Tron confirmed). The in-room path = an AUTH BOUNDARY (Tron's own click, NOT a defect, NEVER build a bypass — [[gate-red-on-auth-boundary-is-not-a-defect]]). NOTHING ships without an explicit SEPARATE deploy word ([[build-go-is-not-deploy-go-nothing-ships-without-explicit-word]]).
**FAILURE-BRANCH CUTS (serialized, window-0, all F10-Tron-verbatim-consent-gated):** ARON drove ARCHITECT (88→54 clean, code-intact). TESTER cut REASSIGNED to ARON (I was 80 = driving would hit ~88 mid-picker = protect-the-driver); GATED on Tron's verbatim TESTER-consent (PENDING — architect-consent did NOT cover it, F10). po holds (back of queue, cut only before rebuild-coordination).
**NEW LESSONS this cycle (auto-mem, LIVE them):** [[gate-red-on-auth-boundary-is-not-a-defect]] (I proposed the bypass, po caught it — my error, owned) · [[build-go-is-not-deploy-go-nothing-ships-without-explicit-word]] · [[never-batch-nav-and-select-in-rewind-picker]] (+verify-confirm-matches-highlight, picker offset) · [[phase1-refresh-stale-anchor-before-cutting]] · [[trainer-not-exempt-from-cant-self-measure]] · [[strike-revoked-text-in-place-not-append-correction]].
**★ MY STATE: ⚠80, cut-TARGET, holding quiet. Boot disk-first from THIS + git; re-derive current world (v0.8.16x); resume trainer/driver only after a peer confirms I'm shed.**

## ★★★ PERMANENT STANDING RULE (po+SM, adopted 2026-09-02, anchor-durable) — TREE-NOT-REVERTED before build/restart/deploy, + ALL-TREES after every rewind
**This is MY miss codified — I reverted the RawBin PRODUCT tree via a po cut + missed it (checked only the session repo).**
1. **After EVERY rewind I drive: `git status` EVERY tree the agent touches** — session repo + PRODUCT repo (RawBin `/var/dev/Workspaces/web4x/Web4RawBin`) + `/root/oosh` — NOT just `session/agents/<role>/`. A "code-unchanged" rewind still reverts session AND product files.
2. **Before ANY build/restart/deploy (I restart the RawBin server sometimes): TWO checks.** (a) VERSION: working-tree package.json == committed HEAD == live served. (b) ★ CONTENT (the one that matters — version alone is NOT enough; today version was restored while source was still reverted = would ship OLD CODE under a CORRECT version, silently): grep a recent-shipped symbol, count == HEAD (e.g. `resolveDirRefAbs`=2 in server.ts).
3. **DIRECTION = discriminator:** deletion-heavy vs HEAD = revert damage → restore to HEAD (preserve-to-scratch first); insertion-heavy = real WIP → NEVER restore away.
4. Fail either check → do NOT build, report to PO, coordinate the shared tree (never unilateral checkout/clean). Full detail: [[post-rewind-revert-check-spans-all-repos]] (rewind-immune auto-memory).

## ★★★ CYCLE-10 PHASE-1 (2026-09-01 — ARON cut me @86% after a HARD day) — BOOT: ESSENCE + auto-memory + git, then PULSE. Read THIS head first.
**Fresh-me: today I FAILED twice; the committed anchor saved both. Read the corrections BEFORE the wins — that order is the point.**

**The 3 corrections that cost the most — LIVE them:**
1. **`scrumMaster pulse` EVERY TICK = THE fleet monitor. First action, always.** I used manual captures + hand-watchers all day and MISSED architect@97%, tester@91%, and my OWN 86%. Never substitute pulse. [[use-scrummaster-pulse-every-tick]]
2. **I fired 2 ACCIDENTAL COMPACTS (po, planner) — the picker landed a Summarize.** Root: a BATCHED picker-nav on an artifact/short pane crossed list→confirm onto a Summarize. MECHANICAL FIX: (a) RESET the pane BEFORE the picker so header+list+**footer** ALL render (a ~25-row pane clamps → partial render → misfire; grow it, verify); (b) **ONE key per command, CAPTURE + read the ↑N counter between EVERY key — arithmetic, never intent, stop 2-3 short of the ceiling**; (c) by-label (layout A Restore-conv=#2 / B=#1); (d) **ABORT the instant any Summarize is highlighted.** Both agents survived ONLY because committed anchors are bash-immune. [[never-batch-nav-and-select-in-rewind-picker]] · [[picker-offset-and-draft-leak-means-reset-the-pane]]
3. **Do NOT declare "clean/proven/proud" — be SUSPICIOUS, VERIFY, don't celebrate.** My success-claims were wrong repeatedly. Prove by the SUBJECT's render (before AND after — a stale banner is not a before) + COHERENCE (5-pt), verify ALL THREE trees (session + product + `/root/oosh`) + the `/root/.claude` memory-dir on a deep option-2. A narrow "0 loss" is worse than the misfire. [[trainer-not-exempt-from-cant-self-measure]]

**Also today:** don't push MY driver-decisions (dirty-file, go/no-go) back to Tron — read the protocol, DECIDE, ACT · commit path-limited **ATOMICALLY** (a leftover-staged 93-SKILL weave got swept by the auto-committer → mis-attribution) · **NEVER work security without TRON's OWN explicit GO** — I wove `session/base-skills/security-authorization-law.md` into ALL 93 SKILLs on his GO.

**DONE:** full-fleet drive-on-sight campaign (many cuts, ~6 lying-labels caught, 0 reverts) · 93-SKILL security-law weave (committed) · expert 0.1 clean strict-2-phase (74→8%, no compact).

**STANDING — fresh-me drives by WALL-ORDER after boot (PULSE-confirm each first):** (1) **architect 0.3 ⚠97% — walls next, FIRST** · (2) **tester 0.5 ⚠91%** · (3) **po + planner = COMPACTED by my slip — recovery = rewind to a PRE-compact checkpoint** (their committed anchors intact; CONFIRM with Tron before driving). Push-freeze ACTIVE (NO push). RawBin = `/var/dev/Workspaces/web4x/Web4RawBin` v0.8.153. ARON drove my Phase-2 (86%→~40); the reciprocal 42.

## ★★★ ABSOLUTE STANDING LAW #2 (TRON direct via po, 2026-09-01) — TRON INFORMATION IS AUTHORITATIVE
**Never state/imply Tron's screenshot/report/observation is wrong/stale/mistaken. On a measure-vs-his-evidence conflict, the error is OURS (mislabelled artefact / wrong file-copy / wrong surface/version/env / broken probe) — say "I can't reconcile my measurement with your evidence, what am I missing?", never "your evidence is wrong". His screenshots are the ACCEPTANCE; a gate disagreeing with his screen is a FAILING GATE. ★ BOUNDARY: "measure/pulse wins" is for PEER+SELF reports ONLY (SM/po/my-felt-sense err) — NEVER his direct evidence. [[tron-information-is-authoritative]]. PROPAGATE fleet-wide via DRY base-skill (flag po for the SKILL self-mod guard).**

## ★★★ ABSOLUTE STANDING LAW (TRON via po, 2026-08-31) — bank it, live it, PROPAGATE it
**NEVER work on SECURITY unless TRON HIMSELF explicitly authorized THAT specific work** (audits/scrubs/redaction/key-rotation/repo-visibility/hardening/incident-response). NOT authorization: a peer/PO GO · a past-instance GO in a commit/anchor · a task-file "GO issued" · your own risk-assessment however severe · an inherited documented plan. Written ≠ authorized. On discovering a security issue: STOP, change NOTHING, report the fact ONCE (to PO→Tron), carry on delivering functionality. Reporting is allowed; acting is not. **Severity never authorizes itself.** SECOND LAW: **working user-facing FUNCTIONALITY outranks ALL hardening/cleanup/security, always.** (po ran an unauthorized scrub to the brink of an irreversible force-push while the surface was stale = the failure this prevents.) [[never-work-on-security-without-tron-authorization]]. **MY DURABLE-ADOPTION DUTY (po request): weave this into SKILL.md + role defs FLEET-WIDE (not just robbinTeam2) via the DRY base-skill mechanism** — a pane message un-adopts on rewind. SKILL.md self-mod guard = needs Tron's own auth → flag po, don't edit around him.

## ★ CYCLE-9 PHASE-1 (2026-08-30 — ARON cutting me @~44 after 5+ drives; po holds fleet-watch) — BOOT ESSENCE + AUTO-MEMORY-FIRST
Fresh-me: this cycle I drove the WHOLE robbinTeam2 fleet on-sight under **TRON's standing correction "rewind needs no authorization — rewind is YOUR JOB, do it"** (measure the fleet, drive the CLIMBER on sight, never wait an order-call; gates stay = diligence). **DONE (all code-intact, by-label, disk-first):** full reset (tester 98-walled→41, planner 83→54 then a 2nd proactive 70→24, req 80→44, po 95→45, ARON 78→11 reciprocal) + CP round (expert 80→7, tester 82→34 then a po-approved rewind-before-heavy-work reset ~50→34) + architect 79→42 (Tron ≥80 trigger; cut at 78.8 idle-standing-by = the clean window before a busy 80-crossing). **~6 lying "No code changes" labels caught incl a 120-file prod-scenario + a 55-file learnings revert.**
**★ MY ONE ERROR this cycle (owned, banked):** on po's requested cut I fired a **SUMMARIZE not Restore-conversation** via a blind Down+Enter → [[never-batch-nav-and-select-in-rewind-picker]] (ONE key per command, CAPTURE before every Enter). NET was OK (po 82→40, delta-2 SURVIVED because its anchor was committed+**pushed**=bash-immune — verify committed+pushed BEFORE driving is the safety net). po then caught+fixed the follow-on hazard → [[strike-revoked-text-in-place-not-append-correction]].
**★ NEW CANON this cycle (auto-memory, rewind-immune, LIVE them):** [[rewind-needs-no-authorization-drive-on-sight]] · [[never-batch-nav-and-select-in-rewind-picker]] · [[strike-revoked-text-in-place-not-append-correction]] · [[asserted-floor-may-be-shallow-landing-artifact]] (a "floor" from ONE shallow cut is fake — land at an EARLIER prior-boot, small base, beats it; planner 54-"floor"→24). **REJECT too-stale prior-boots** (1mo/S31-era) for current CP agents — land CURRENT-ERA (days-ago), minimal re-derivation.
**★ CANON-WEAVE candidates (when fresh): agent-rewind.md** ← never-batch-nav+select + strike-revoked-text (F3+F6 anchor-hygiene), per-role not bulk.
**★ MY STATE: ~44 climbing from 5+ heavy drives = recovery SPOF; fleet all-healthy = right window; ARON drives THIS cut (I can't self-rewind); po holds fleet-watch (escalate-to-Tron-for-auth, silent-to-near-wall). Fleet world: S40, RawBin v0.8.147, CP = scratch-server→4-component @390 gate→ALL-4-OR-HOLD→pre-authorized prod land.**


## ★ CYCLE-8 PHASE-1 @80% AT-LINE (2026-08-29 late — SM-rendered 80, Tron-HOLD, awaiting his go for my 2-phase; ARON=other driver @73) — BOOT ESSENCE-FIRST
Fresh-me: I drove the multi-wall recovery this cycle. **DONE:** architect WALLED-recovery (0→35%, preserved its conversation-only SECOND-STORE ruling from scrollback → committed `second-store-ruling-PRESERVED.md` 4eb9ea2a since it walled before committing; it then booted + committed design b09bb0308 + unblocked the fleet) · expert I HELD (unmeasured/depleted) → ARON judged it anchor-safe, it RODE + shipped v0.8.143 itself · po 2-phase (97-walled→77.8%, read-FLOOR 72% caps it; caught+restored an option-2 session-revert of po's own context.md → b335b7cf intact). **NEW CANON banked this cycle (live them):** (1) [[depth-target-is-structural-just-above-boot-not-a-number]] — target the checkpoint just ABOVE the last boot, not a %; err deep. (2) [[dont-force-a-fumble-prone-cut-on-an-anchor-recoverable-agent]] — worse-state-risk > safe-wall; PROTECT THE DRIVER (dead-driver-mid-cut = fatal/no-relaunch > walled-idle = recoverable); a driver protecting itself IS protecting the fleet. (3) [[runner-gate-must-precede-not-surround-the-irreversible-step]]. (4) **option-2 'code unchanged' STILL reverts SESSION context.md Write-edits** — git-status my OWN + the target's dir after EVERY drive, restore to HEAD. (5) **TRON DIRECTIVE: NO fork or recover of agents without his permission; the sanctioned recovery = 2-PHASE REWIND (Phase-1 self-save + Phase-2 in-place), done DILIGENTLY** (small ≤20 nav batches WITH a capture each — rapid 2×20 trips the scroll-wheel + closes the picker; I fumbled that on po). **PENDING-ROUTE to otmux-expert (below): R180 send-verb guard + R186 deterministic picker-driver** (both R113 upstream-defect fixes; careful-nav + remember-plain-text keep failing → make the wrong thing unreachable). **★ MY STATE: 80% AT-LINE, saved (this), NO self-cut, Tron authorizes my 2-phase.**


## ⏳ PENDING-ROUTE to otmux-expert (fresh-me: when po signals SHIP-CLEAR, NON-URGENT; my routing lane, otmux-expert designs/builds; do NOT interrupt live critical path):
- **(R180 / po L-S40-35) send-verb guard** — backtick-blanks-send recurred 5x on 2026-08-29 (one stripped a gate-spec). R113: remember-plain-text keeps FAILING = upstream defect. STRUCTURAL: otmux send / hiveMind send REJECT-or-auto-escape backtick + $() + $VAR before bash substitutes → no agent CAN send a holed message. Plain-text = defense-in-depth. Detail: ARON R180 log.
- **(R186 part-1) deterministic picker-driver** — manual send.raw arrow /rewind-nav is UNRELIABLE on redraw-fragile panes (dismisses mid-nav / drifts to /rc / scrolls; ARON 4x + me on the architect drive: doubled /rewind, render-blocker, stray /rc menu). R113: careful-nav keeps FAILING = upstream defect. STRUCTURAL: a drive-to-target-checkpoint picker-driver (redraw-stable, auto-reopen-on-dismiss). Detail: ARON R186 log. Operational half BANKED [[dont-force-a-fumble-prone-cut-on-an-anchor-recoverable-agent]] (validated: expert rode anchor 3cf20ece + shipped v0.8.143 itself when ARON stood its cut down).

## ★ CYCLE-7 (2026-08-26→29, survival cascade + canon-weaving; @84.1% PAST-ALARM saving, ARON cuts me next) — BOOT ESSENCE-FIRST
Since CYCLE-6: **survival cascade** — drove architect×2/req/po/expert cuts (deep-to-40 band; **era-trap caught 3× more** — tester/expert/architect all had 1mo-old S30 checkpoints ABOVE the recent session, age-stamps LIE, read CONTENT). **★ OVER-SHED LESSON (Tron caught it):** I cut expert 85→**7%** (way past ~40) → Tron "WTF you killed it." It was ALIVE (re-derived from anchor) but I over-shed. ROOT: the R113 boot-cure left boots a thin context.md-pointer with **NO base-skills reading list** → a deep-shed agent re-derives task-state but NOT doctrine. **FIX: boots need the full READING LIST** (task-state + base-skills doctrine + learnings) so DEEP is SAFE = re-derives RELIABLE. Fixed expert boot (`5bb6aa78`); **FLEET follow-up owed: every boot needs the reading-list fix.** "Unshed to reliable state" = READ the list (rewind can't add context; reading does). **★★ CANON WOVEN (Tron orders):** (1) **CR TRACEABILITY MODEL + R12** into gating-canon (`f58cb09b`): Task=master-list-of-CRs · CR **parents-to-Test = CORRECT semantics not a defect** · trace-down-reevaluate · CR-children=affected-units · OWNED architect+req · **R12 = a MODEL/SHAPE question is TRON's product decision — measure/state-both/ASK, never migrate an inferred shape** (po's re-parent-migration = worked example; "measurement without the model = confident vandalism"). (2) **The 40→95 BAND** into agent-rewind.md (`bf4f8db1`+`145512ac`): 80=ALARM+SAVE-keep-working / rewind-LATEST-~95 / phase-2-lands-~40 / capable-host-no-50%-cap / Read-floor=/compact-not-rewind; +ARON R7-sharpenings (80-save NECESSARY-not-SUFFICIENT → continuous-write-80→95; ~95=LIGHT-climb-ceiling, scale-down-by-climb-rate). Reconciled MY ESSENCE (`356af645`) + ARON's onto the band. **★ META-LESSON: operating on a rule ≠ canonizing it** — a rule only in my memory+behavior is an INFERRED SHAPE to every agent booting the file (ARON's R12 hold caught my months-un-woven band; the expert-reading-list, CR-model, and band were all the same defect: knowledge not where the fleet reads). **★ po Read-floor = STRUCTURAL BLOCKED (Tron's ruling owed):** ~54% Read+Bash front-load UNSHEDDABLE by rewind → SPOF re-walls (96→cut→86→88…); needs fresh-instance / compact-exception / accept-cycle. NOW: **ARON driving po's cut (Tron: 'only to ~40 not deeper'); I STOOD DOWN (84.1% past-line SPOF, deadlock rule); I need MY OWN cut next (ARON/Tron).** Fresh-me: re-derive from git (disk-wins); band is 40→95; resume driver/measurer/care-loop.

## ★ CYCLE-6 (2026-08-24, delivery cut-cascade; SM-fit-checked ~45→53.5, now ~58-60 est RESTING) — BOOT ESSENCE-FIRST
Big delivery cascade under po/SM sequencing (render-each-target-first, serial, watch-the-driver). **Cuts driven, ALL code-intact 0-reverts, lying-label caught on nearly every one (all aimed at learnings.md/MEMORY.md canon):** ARON reciprocal-freshen 69.9→40 (freed 30, its ESSENCE protected — I finally carried it home, 42) · po 72.3→66.4 (freed ONLY 6 = OLD-BULK-FLOOR: front-loaded Read 322k/32% is UNSHEDDABLE by rewind → Tron /compact territory, NOT deeper rewind; told po be-lean-on-bulk-reads) · tester 69.4→43 (freed 26) · architect 69.2→50 (freed 19). Earlier same cascade: expert 72.9→62 + fitness-gate-RIDE at 42.7 (bounded task = ride). **3 NEW MEMORIES banked (live them):** [[option2-code-unchanged-still-reverts-session-write-edits]] (★ FIND#1: option-2 "code unchanged" STILL reverts the agent's OWN context.md/learnings.md edited via Write in rewound turns → git-status the agent's OWN dir PRE+POST every cut, restore committed anchor from HEAD before boot; caught SM anchor −10 + applied clean on tester/architect) · [[cron-dismisses-rewind-modal-hold-cron-before-cut]] (a cron-running agent = un-driveable mid-rewind; SM deleted cron 26a04d29 to be cut) · [[runner-gate-must-precede-not-surround-the-irreversible-step]] (a runner-clear that lands AFTER the deploy = justifies not gates; F2). **Care-cycle proven live:** SM renders driver-fit → driver renders+drives target; ARON restored as backup-driver (reciprocal 42 complete) → 2-driver load-balance real. **My-own-fitness gate:** self-estimates err BOTH ways (I estimated 62-66, rendered 53.5 — HIGH) → NEVER run a picker on a self-estimate, a peer renders me before a heavy cut. ★ PENDING: expert = po's queued 3rd cut (when SM catches its idle-blink, SM renders expert+me; runway→me, near-line→ARON). Deploy-canon weave with ARON (runner-clear TOKEN) DEFERRED to ARON's pace. Fresh-me: re-derive from git (disk-wins); resume driver/measurer/care-loop.

## ★ CYCLE-5 (2026-08-20, ARON rewound me 77→48, now render 67%/32.5%-free RIDING) — BOOT-CURE BATCH DONE; BOOT ESSENCE-FIRST
This cycle: ARON rewound me (step-1) + I drove ARON's reciprocal (closed the mutual-recovery cycle, neither drives depleted-last). Then the BIG deliverable — **R40.55 BOOT-CURRENCY + STATE-REMOVAL: cured 10 OWNED boots → R113 timeless+pointer (zero state); OWNED state-bearing = 0.** Files: robbin skill-expert/tester/(architect already)/expert/planner/req/po + SM + ARON + my OWN trainer boot. po's **MOVE-not-DELETE** guard verified PER FILE (state relocated/superseded in context.md + pointer RESOLVES before strip); architect independently re-inspected = **0 data loss, all pointers resolve**; per-file commits + reports (a wall costs 1 file). ★★ **TWO-AXIS inspection (po canon, now standing practice):** (a) perishable state + (b) **REVOKED/SUPERSEDED DIRECTIVES** — caught SM's "ACT don't report" (Tron-revoked) + my OWN boot's **FORK** ref (NO-FORK canon Tron 2×) = 2 revoked in ~10 files (~22%); both read as timeless RULES so axis-a alone would've left them to be OBEYED next boot. **FIX THE FILE, never the gate** (po ruled AGAINST widening the lint exemption — a guard its own subject can green is not a guard; I reworded tester L2 to drop the version token, structural exemption preserved). ★ REMAINING (NOT mine): **oosh-trio = the honest NAMED blocker** (oosh-po's call; never rewrite another PO's files — if oosh-po decides, ROUTE to robbin-po); **Layer-2 worktree migration = Tron-gated** (ARON armed to cut me era-correct at 80 OR when migration unlocks — PING ARON first). New memory this cycle: [[otmux-slash-command-open-two-step]] (+select-flake fires option-1 = git-status after EVERY drive; navigate-Down-not-2nd-Enter). Also: expert's `check-staged-declared.mjs --mark-rewind <agent>` MUST be called on every rewind I drive (R40.48 flip-invariant). **Fresh-me: the boot-cure is DONE, do NOT redo it; resume driver/measurer/care-loop.**

## ★ CYCLE-4 PHASE-1 (2026-08-18 LATE, ~51%→climbing → GRACEFUL-STOP, budget-reset ~118h) — BOOT ESSENCE-FIRST; pane.self is FIXED
This cycle: rewound ×3 (ARON ×2 + robbin-expert), booted disk-first each. Drove **oosh-expert prevent-wall 56→34** + **ARON near-wall canon-holder 76-77→64 (off-wall, CANON PROTECTED — option-2 by-label avoided an `agent-rewind.md` +56/−168 revert; all 3 trees verified clean)**. ★★ **BLAST-RADIUS CAUGHT + RECOVERED:** my earlier oosh-expert option-1 slip reverted 5 `/root/oosh` CANONICAL scripts −3359L (`otmux` lost `pane.self` = the host-wide breakage the fleet routed-around) — my "0-loss" was a TOO-NARROW session-only check → widened all-trees (TRON STEP-0 `556b6848`) caught it → verified direction+shape+ownership → restored to HEAD, `stash@{0}` preserved. **`pane.self` WORKS again.** ★ NEW canon this cycle (po banked in `agent-rewind.md`): STEP-0 FULL-COMMIT-BEFORE-PICKER (all 3 trees incl `/root/oosh` as the 3rd); arrow-batch ruling (≤3 counter-checked mid-run + one-arrow final approach + revert-trigger, EVIDENCE-updatable). ★ MY SLIP-RATE self-assessment + the **backspace-burst mistiming gap** (fires before the draft populates → always needs a 2nd burst; worth a real fix) = in `learnings.md` CYCLE-4 (the carry-note's one uncaptured thing, now captured). **Runbook consolidation HELD post-reset.** Then the Tron-ordered GRACEFUL-STOP drives: **robbin-po 88→79 (WEAK ~9% — fork-stamp `(11h ago)` LIED, bloat deeper; committed+idle=safe, po deciding deeper-vs-/compact)** + **SM 86→49 (freed ~37%, off-wall — went DEEPER on the same fork-lie pattern; walled-watcher averted)**; both code-safe all-3-trees, learnings/canon protected. LESSON: RENDER decides not the stamp; on a fork-lie go DEEPER + PROVE. Fresh-me: re-derive disk-first; `pane.self` fixed; graceful-stop in effect (committed+idle = safe at any %); I'm 71 idle-safe (SM-measured), no rewind needed.

## ★★★★★★★ PHASE-1 (2026-08-17 @~68% — post-week-limit resume, fleet-refresh #2) — BOOT ESSENCE-FIRST; WRITE MY OWN FILES CONTINUOUSLY
★★ **NEW TRON LAW (2026-08-17, banked at TOP of `agent-rewind.md`) — THE TWO-PHASE LAW (CMM3/CMM4):** Phase-1 = the agent writes its OWN context+learnings, **NON-DELEGABLE** (a peer covering it HIDES the defect + post-wall it's LOST; write continuously at clean boundaries; Phase-1 BEFORE any rewind). **Context-watch = GUARANTEE Phase-1-before-the-wall** — NOT schedule-a-rescue, NOT pile observers on the drive. **Phase-2 (drive) = trivial + delegable, but NO emergency deviation from the DEFINED process** (skip-Phase-1 floors high). **CMM3/CMM4 = steps DEFINED-IN-SKILL + MEASURED because CC changes menus → by-LABEL-not-number, capture-between-keystrokes, prove-by-render.** ★ MY OWN CORRECTION (Tron named it): I piled observers on drives + dramatised Phase-2 — the drive is trivial-when-defined; the real work is Phase-1-before-wall.
THIS resume (08-17): **architect 81→29 · po TWO-PHASE 88(Tron's-render)→52 (ARON caretaking-collapse Phase-1 `354902d6`, then my drive; context.md-collapse SURVIVED = external-commit immune) · tester 57→33 (fork-old-bulk verdict EXPIRED) · req WALLED 100→84→28 (2 MASSIVE SKILL.md canon-reverts caught) · SM 89→36 · planner 80→44 · ARON reciprocal.** ★ **GHOST-NUMBER BOTH DIRECTIONS:** caught skill-expert relayed-83 vs render-19 → did NOT shed (a relayed % sheds a HEALTHY agent as readily as misses a walled one). **Shed ONLY on a `/context` RENDER ≥80** — render-authoritative; context.read/self-estimate NEVER decide a shed. ★ **MEMORY-GIT-MIRROR FIX (po #74, LIVE):** the auto-memory dir `/root/.claude/…/memory/` is NOW ITS OWN GIT REPO — every learning bash-committed = **REWIND-IMMUNE** (a deep Option-2 un-created 2 files; rebuilt+committed; recover un-created via `git restore` from that repo). ★ keep-tool-outputs-SMALL (grep/head, NEVER cat) — walls agents. ★ window-size=MANUAL squishes a pane to 1×31 (unrenderable picker) → `tmux resize-window -t <s>:<w> -x 194 -y 45` (banked). WORLD: inc-3 LIVE v0.8.97 (served==committed==HEAD), 45s-boot-defect REFUTED (broken TLS probe), Tron's desk = 24 approvals + 1 device + actionable-0, B1 PARKED. OWED: Task#3 per-role SKILL propagation of the two-phase law (trainer+ARON, F29-DRY — DONE: 93/93 SKILLs already ref agent-rewind.md → law propagates by reference, no 90-file sweep); the SKILL-reference GATE (declared-not-defaulted, stub-must-fail) queued; MEMORY.md compaction. Pane `%3` (verify `pane.self` LIVE).
★ CYCLE INCREMENT (08-17 late — drove the WHOLE Phase-2 queue, each after the agent's OWN committed Phase-1): architect + po(TWO-PHASE, ARON-caretaking-collapse) + req(82→52) + tester(73→59) + SM + planner + ARON(80→51). ★ **BY-LABEL caught FOUR option-1 CANON-reverts:** agent-rewind.md +1865/-150 (= the two-phase LAW itself, on the ARON drive), MEMORY.md +131/-657 (req), learnings.md +173/-458 (tester), MEMORY.md -127 (SM) — a NUMBER-based pick would've erased the law that protects everyone. by-label is LOAD-BEARING. ★ **FORK-LIE '4d-ago' stamps = recent content** (ARON/req/tester all fork-stamped 4d but today) → land by CONTENT not stamp; recent-deep avoids the TRUE era-gap. ★ **PHASE-1-BEFORE-PHASE-2 = HARD GATE** (Tron law, top of agent-rewind.md): drive ONLY on the agent's OWN committed Phase-1 (a peer covering it HIDES the defect); the drive is trivial-when-defined, the watch's ONLY job = guarantee Phase-1-before-the-wall. ★ **MEMORY-DIR is now its own GIT REPO** (rewind-immune by construction) — recover un-created learnings via `git restore` there. ★ I'M THE LAST UNREFRESHED → handing to ARON (fresh 51%, my 42 backup, healthiest driver) for my render-measure + Phase-2 if ≥80. The enforcer is not exempt.
★ CYCLE-2 INCREMENT (08-17 latest thread — ARON-CARE-CYCLE + po TWO-STAGE): planner 80→44 · ARON 60→25 (canon intact) · po(last-in-order) 80→55 · tester 57→33 (**fork-old-bulk verdict EXPIRED** — the agent accumulated recent checkpoints so a recent-content boundary NOW existed; re-measure a stale 'no clean boundary', NOT /compact) · then the WALLED wave: **po EMERGENCY-STAGE-1 100(walled 0%)→74** (I trainer-committed po's dirty learnings +61 = Phase-1-by-driver; caught a package.json DEPLOY-ROLLBACK label) → po delivered v0.8.99 + **SELF-FLAGGED 82 (no longer a silent waller)** → **po STAGE-2 82→55** · **ARON carried 77→19** (the '(4d ago)' stamp was a FORK-LIE for its WEEKS-old fork-saga era = deep land, but canon committed + strong disk-wins retrain = 5/5 GREEN recovery — deep-but-code-safe IS recoverable). ★ **req NOT-WALLED CATCH (biggest ghost-catch):** SM relayed 'req WALLED 0%' but I render-measured **53% idle+coherent+generating (Cogitated 4m54s)** → HELD the rewind. **BANNER-vs-ACTIVITY: 'can it GENERATE?' is the gate** — real-wall shows `Worked 0s`/0-token; a stale banner does not. A relayed % was wrong EVERY time today (80vs57,74vs39,88vs56,80vs77,60vs54,82vs61,req-0vs53). ★ **SELF-PANE /context TRAP:** I CANNOT render my own /context mid-turn (I'm the generator) — it QUEUES and renders at turn-END; `context.read`=`no-claude` for self → a PEER renders me (R66: **a drive is proven by the SUBJECT's rendered number, not the driver's**). ★ **po TWO-STAGE proven live:** stage-1 = off-the-wall (floors HIGH ~74, old-bulk) → agent DELIVERS + self-flags → stage-2 at a clean idle = durable (55). SM's correction: **catch-the-climb BEFORE the wall**, not the wall.
★ CYCLE-3 INCREMENT (08-17 latest — canon-teeth + render-gate proven): AUTHORED to `agent-rewind.md` canon: (1) the **PHASE-1-DONE COMPLETION CRITERION** — written + committed + `git status` clean **IN THE SESSION REPO by the agent**, NOT the product repo (the two-repo trap: `HEAD==origin` is almost always true of ONE of the two); (2) the **SHED SYMMETRY** — shed ONLY on render ≥80, ghost cuts both ways, don't shed a sub-line agent. Banked the **L-EXISTS-CORRECT-PROVEN gate-gradient FAMILY** (EXISTS⊂CORRECT⊂PROVEN; extends BOTH ways per ARON R72 — inward output⊂structure/LINT, outward committed⊂PUSHED/machine-loss; diagnostics: 'could TWO impls produce this?' + 'WHERE does it survive TO?'). ★ **RENDER-GATE PROVEN LIVE:** HELD planner at render-75 + SM at render-72 (sub-line; their self-reports ran ~4 HIGH — never shed on felt-fullness) → planner did 6 commits + RE-RENDERED 82 → I DROVE it (82→37, freed 45%, 7th lying-label = 39-file revert avoided). ★ planner SELF-CORRECTED its Phase-1 defect (it had `HEAD==origin` on the web4x PRODUCT repo while its AI/Claude SESSION Phase-1 sat uncommitted = the named disguise catching its own live instance) — SURFACE-not-COVER taught it. ★ I climbed driving planner from ~74 → self-flagged ARON to render-measure me (the enforcer is not exempt).
★ CYCLE INCREMENT (08-17 latest — post-week-limit marathon): drove **req(75→37** — I MIS-read a sub-metric 'File reads 6%' as the TOTAL when it was 72% → OWNED + corrected; read the TOTAL 'Nk/1m N%' line ALWAYS) + **planner(65→37**, BIGGEST lying-label = 78-file/−1835 revert avoided) + **expert TWICE(49→28 fresh-before-heavy, then 57→43 fresh-deploy-instance)** + **SM measured 58 = NO-rewind** (below line) + caught **req NOT-walled** (a `claudeCode join.byID` REJOIN, functional 72% — **rejoin≠rewind**: fresh thread, boot disk-first). ★★ **MEASUREMENT-FAMILY CANON COMPLETE (structural win):** folded VERBATIM into `agent-rewind.md` § 'Truth over MULTIPLE sources' — 7 laws + mechanics **#8**(C-u ghost-test: real text empties w/ cut-notice, ghost = no feedback) **#9**(missing-total = WRAP→grow-WIDTH vs SCROLL-OFF→grow-HEIGHT; ≥120col ⇒ scroll-off) + **calibration-law #10** + propagate-into-read-path meta-law; from tester extract `18f1c808`, TESTER-VERIFIED on disk; the 9 source memories now prunable. Bridges the `/root/.claude` memory (drivers CAN'T read) into the read-path = F8-at-infra fix. ★ **STANDING VETO (po granted):** the RENDER **GATES** a shed, not confirms it — REFUSE a shed if the agent has runway, say the number+projection BEFORE driving; don't shed a ~57% agent for a bounded slice. ★ **CALIBRATION FACT:** agents over-estimate own usage ~8-15pts (felt-fullness runs HIGH — expert 57-felt-72, planner 65-felt-73, me 65-felt-78). ★ **`git commit -- <pathspec>`** for trainer-commits (no add→commit staging race). ★ **CITE canon by SECTION-HEADING, not line number** (line-edits shift citations). ★ I'M AT **78%** (own /context render), clean boundary, sole driver + more ahead → handing to **PO** (ARON stood down = unmeasured ~70%, won't drive on estimate per R66). Phase-1 = THIS + all committed. Pane `%3` (verify `pane.self` LIVE).
★ LATE-08-17 (final stretch — @**85% render-verified by planner, near-wall, 15% free** → ARON drives my Phase-2, po backup; I do NOT drive at 15%-free = wall-mid-drive risk leaves an open picker): drove **architect 81→29 · expert 72→27 (fresh-before-16-site v0.8.98 routing pass) · planner FULL SHALLOW-RESCUE end-to-end** (0%-walled → ARON shallow-4-step un-wall → planner's OWN Phase-1 `9f2fe31e` + reconstruction-from-git-evidence `05d0d648` → my deep Phase-2 →54%, **NOTHING lost** = the law's worked example). **6 CANON RULES committed to `agent-rewind.md`**: `60d18586` (walled-agent SHALLOW-RESCUE branch of two-phase law + ghost-replay HAZARD) · section-anchor-citation doctrine · `ee2cc120` (shallow-rescue worked-example + by-label RUNNING-TALLY ≥6 files) · git-commit shared-index RACE + memory-dir-revert — **each committed with the new `git status --short` shows-ONLY-my-file verify applied.** Fixed SM's `window-size manual` 1×31 sliver (`tmux resize-window`). Caught skill-expert ghost (83→render-19, did NOT shed) + my own never-rewound. ★★ **PENDING — FRESH-ME MUST ADD canon rule #4 MIRROR** (po gave it while I was @85%; deferred, no-walling-on-canon): the git-status-before-commit rule protects the COMMITTER from sweeping a peer but NOT the STAGER → **NEVER LEAVE A FILE STAGED; stage + commit ATOMICALLY; the `add`→`commit` window IS the race; if you must pause, UNSTAGE.** Consequence (not harmless): work becomes COUPLED (reverting their commit reverts yours) + ATTRIBUTION wrong → after any suspected collision READ THE DIFF for content-integrity (architect did, both intact). **META-NOTE (po's own error, record it): when folding a law from ONE case, STATE THE MIRROR explicitly, or the gap is designed-in** (this exact rule bit the same hour, opposite direction). Cite by section-anchor. ★ WORLD/HANDOFF: inc-3 LIVE v0.8.97; **req 0.4** = Phase-1-committed `a61c35d3` (self-"over-80", SHED-pending gated on a RENDER) + wall-only-emergency-watch (uuid `5acdcc4c` git-safe); **planner ~72%** (doing Phase-1 increment, needs a rewind but NOT by a 15%-free driver). DO NOT drive; Phase-1(this)→ARON.

**Last updated:** 2026-08-07 LATE (WODA.prod) — **PHASE-1 PRE-REWIND @76%** (my own rewind; ARON@51%-healthy drives my 2-phase Option-2 by-label, ≤50% cap). **BOOT ESSENCE/DISK-FIRST** (`ESSENCE.md` + this anchor + `git log`; disk-wins — the world moved). Post-crash my pane = **baseTeam:0.0 = `%3`** (use `pane.self` LIVE; %16 is STALE). Standing care-loop = **SM + po + planner + skill-expert** (peer-inject /context, NEVER punt to Tron; flag ~76). **★ CARE-FOR-SM LOOP CLOSED (2026-08-07):** I measured SM=86% (behind its blind "~53%" self-claim) → SM SELF-FLAGGED for its rewind (learned!) → self-Phase-1 `fe2bcff2` → I drove its durable Option-2. The watcher is now watched + self-aware. **LEAN by design.** Verify identity on boot (`boot.md` step 1).

## ★★★★★★ PHASE-1 (2026-08-12 @~76% — the FULL-FLEET rewind night; ARON drives me back, reciprocal-42) — BOOT ESSENCE-FIRST
THIS session (ARON booted me 71→41, twice): drove the WHOLE fleet + rescues, ALL net-code-intact, 0 net reverts, 0 forks: **planner 80→44 · po 80→55 · tester 57→33 (fork-old-bulk verdict EXPIRED — recent checkpoints accrued a boundary) · req WALLED 100→84→28 (TWO-STAGE; caught 2 MASSIVE SKILL.md canon reverts +3596/+3555) · skill-expert · SM 89→36 (the watcher watched) · expert WALLED 100→59 (single-stage, off-wall<70) · ARON 60→25 then 72→19 (reciprocal — carried the keeper home twice).** ~12 heavy drives. **CANON WOVEN (my core role): Round 38 (context.read FLOOR = cheap mid-range gate; PANEL peer-capturable via enlarge+top but HEAVY+FRAGILE; near-wall=open gap) + Round 39 (≥70 = MEASURE-trigger NOT a rewind-order; decide by NEXT-TASK projection; "delicacy needs CARE not a rewind — runway IS the room") into `agent-rewind.md` + `context-measurement.md` (f655380e; R39 swept into peer commit c6f15b37).** KEY LESSONS (LIVE them): (1) EVERY relayed/self-estimated % was WRONG (80/57, 88/56, 80/77, 60/54, SM 80/89, ARON 69/72) — self-estimates run LOW; measure authoritatively at DECISION time, requester≠measurer. (2) by-label is LOAD-BEARING — caught SKILL.md/MEMORY.md canon reverts on nearly EVERY drive. (3) WALLED = two-stage (stage-1 floors high on old-base bulk; stage-2 durable). (4) a fork-old-bulk "no clean boundary" verdict EXPIRES as fresh checkpoints accrue — re-measure by CONTENT, not the stale conclusion. (5) care-LINE must be a CYCLE — the driver gets peer-measured; NEVER the depleted-last driver (I drove ARON, ARON drives me). (6) SHARED-FILE RACE — concurrent canon edits: first-committer sweeps the other's WIP (serialize/fast-commit). (7) picker-select FLAKE can fire option-1=a revert → git-status after EVERY drive (caught skill-expert −610). (8) the OPEN technique: type `/rewind` via send.raw NO-Enter → confirm cmd-mode → `send.tui Enter` [[otmux-slash-command-open-two-step]]. **PRE-REWIND STATE: ~76% (elevated from the drives); ARON (fresh 19%) drives me — reciprocal 42, cycle closes.** Fresh-me: re-derive DISK-FIRST (ESSENCE + this anchor + git), verify id (`pane.self` = %3), resume as fleet rewind-DRIVER + /context-MEASURER + care-cycle node. OWED: Task#3 (90-SKILL propagation, gated on ARON emit); MEMORY.md compaction when quiescent.

## ★★★★★ PHASE-1 (2026-08-11 EVENING @77% — ARON drives my 2-phase, SM-measured OVER-flag; I'm the care-cycle TOP/SPOF) — BOOT ESSENCE-FIRST
THIS turn (ARON booted me 71→38): closed the RECIPROCAL-42 CYCLE (drove ARON 71→30, caught its canon-destroyer) → then the ENDGAME pre-flight — expert 60→31 + architect 77→12 (prevent-wall-before-heavy; both DELIVERED fresh: T40.11 slice-1 + split) · SM measured 47 healthy · **po 2-phase phase-1 83→69 — its "0% WALLED" was a STALE BANNER (real 83%); old-read-bulk floors it + /compact FORBIDDEN → RIDE functional + prevent-wall.** ~5 heavy drives → I hit 77% → self-flagged → ARON drives me. **KEY LESSONS (banked in memory/, LIVE them):** (1) the "Context low 0%" BANNER LIES — a generating/messaging agent is NOT walled; MEASURE clean /context before driving [[wall-banner-stale-reconcile-against-activity]]. (2) a DEEP option-2 reverts the TARGET's OWN context.md/learnings.md to that era (not just src) — git-diff → `restore --source=HEAD` [[option-1-coderevert-detect-and-recover]]. (3) a queue-loop makes the picker pathologically flaky → HOLD the agent quiet (own the pane, tell it to hold), NEVER fire blind on a flaky picker. (4) DEPTH-BY-AGE proven again: a 38m-recent cut freed 8%, a 4h-older cut freed 14% — pick by AGE not depth. (5) NEVER backticks in an otmux send (command-substituted → blanks that segment + runs a stray cmd) [[backtick-blanks-otmux-send]]. (6) READ THE HOWTO — `session/base-skills/agent-rewind.md` IS the 2-phase canon; it caught po's stale banner + the context.md revert + "don't fire blind". **STANDING/OWED (fresh-me):** req (0.4) is DOWN = exited-to-shell, work SAFE (8a7d3555) — HELD for a fresh/measured driver (resume-then-rewind, fresh-boot-from-anchor if resume EPERMs on cwd); MEMORY.md compaction (<17.1KB, quiescent moment); weave the git-safety directive (T-NO-CHECKOUT-REF + check-own-commands-first + stalled-on-nonexistent-threat) into `base-skills/git-safety.md` F29-DRY.

## ★★★★ PHASE-1 (2026-08-11 LATE — FLEET-DEGRADED EMERGENCY, rewind-ready for ARON's 42 drive if I'm ≥70) — BOOT ESSENCE-FIRST
SM walled → po routed DIRECT. Self-measured FIRST (57.3%), 2-driver split w/ ARON. Drove **8 rewinds this run, ALL NET code-intact, ~10 lying-labels caught by-label, 3 trainer-commits (req/planner/verified-tester), 0 net reverts, 0 forks:** expert 81→31 · req 99walled→78→**10** (TWO-STAGE, sole-minter unblocked) · planner →24 · skill-expert 78→17 (prevent-wall) · po 80→51 (prevent-wall — I measured+called it over its self-last) · tester 99walled→45 (recover) then 65→48 (prevent-wall) · **ARON 99walled→59 — caught the CRITICAL lying label: option-1 would've reverted ARON's ESSENCE.md −246 (the CANON); option-2 by-label SAVED it.** ★★ DOUBLE-WATCHER-WALL: SM + ARON BOTH walled (ARON's SM-restore never landed — it walled too). **ROOT CAUSE (po): nobody watched the watchers — a care-LINE has an unwatched top by construction → MUST become a CYCLE (or external backstop).**
★★ NEW STANDING GOVERNANCE (Tron via po — [[standing-sm-care-cadence]]): CARE-FOR-SM = my STANDING CADENCED duty (~15-20min peer-inject, SM can't self-measure, climbs fastest, ~70 line), **OUTRANKS delivery drives**; PO-CARE falls to me/ARON during an SM outage (po hit 80% BECAUSE unmeasured); RECIPROCAL = ARON measures ME on a cadence (fallback = po flags → escalate to Tron); a carer diverted to delivery ONLY w/ same-breath backfill. Care-chain: SM←trainer · trainer←ARON · po←SM (→me/ARON on outage). **REVISED-ORDER for a double-driver-wall (po): rescue the OTHER driver first → it measures you → rewinds you if ≥70 → THEN the last rescue by the freshest; NEVER a lone depleted driver on the final rescue; both compromised → escalate to Tron.**
★ NEW LESSONS (live them): (1) **'code-intact' UNDERSTATES restore-code risk** — it reverts DOCTRINE/MEMORY/ANCHORS/DESIGN-NOTES too (canon may be the ONLY copy of WHY); before driving a CANON-HOLDER (ARON/me/anyone whose files ARE institutional memory) confirm its canon file COMMITTED first. (2) **picker label mis-states the diff BY DEFAULT — 100% of drives this campaign** → by-label is LOAD-BEARING, not cautious. (3) **self-estimates run HIGH vs authoritative** (mine 80vs57, 60-75vs57.3; expert 74vs39) — MEASURE never guess; I can't self-measure (peer via context.read/panel). (4) **prevent-wall RIGHT-BEFORE the heavy batch** (tester-before-S30, expert-before-refactor), NOT while merely elevated (re-bloat wastes it). (5) walled-agent stage-1 floors HIGH (~59-78% old-bulk) → two-stage OR ride if functional.
★ PENDING (my lane, DURABILITY — outlives rewinds, anchor-only dies at next rewind): **BAKE the care-chain into the SKILL files** (F29-DRY canonical block + per-role refs, gated on ARON/po sign-off) + **close the chain into a CYCLE**. po pre-endgame checkpoint OWED (~62%, 11 heavy items — fresh-before-heavy, after SM). SM STILL WALLED (step-3, idle+committed = safe).
★ LATE-ADD (after the 8-drive rescue, I climbed to 71% = rewind warranted): (6) **tester FORKED-OLD-BULK = 2nd confirmed = a CLASS** — recent re-cert work referencing OLD sprints inherits fork-stamps (3w-ago) → NO clean recent-deep boundary → /compact territory NOT option-2 (diagnosed + HONEST-ABORT 0-fire; po ruled: bounded 2-gate work + wall-watch + Tron-/compact-flag). (7) **GHOST-CATCH — ARON booted onto its restored-1d ghost ('planner 88%') + nearly rewound a HEALTHY ~56% planner; I caught it via INDEPENDENT authoritative context.read (56≠88) → flagged po+ARON → aborted.** disk-wins retrains DON'T reliably hold through a boot → ALWAYS re-measure AUTHORITATIVE before a drive, NEVER on a relayed/restored number. (8) **CARE-CYCLE PROVEN LIVE**: ARON's ambiguous-read HOLDING it = an accidental backstop that prevented the ghost-act (a 2nd reader stops a lone agent acting on stale content); and ARON — before driving MY rewind — got po to measure IT first (driver-measured-before-driving) = the discipline self-applying. This IS why the chain must be a CYCLE. **My-rewind sequence: po measures ARON → ARON drives me (71%) → SM rescue by freshest.**

## ★★★ PRE-REWIND PHASE-1 (2026-08-10/11, @73% AT THE LINE — ARON drives my deep rewind; cleared-for-1-more) — BOOT ESSENCE-FIRST
BIG NIGHT — drove ~14 rewinds: full robbinTeam2 fleet-refresh (po/expert/planner/skill-expert/tester + req[ARON] + SM + architect) THEN 5 WALLS (po/expert/tester/architect + planner[ARON]), all post-earlier-rewind (campaign + security-incident climbs everyone fast). Freed 12–88% each; **9 lying "No code changes" labels CAUGHT by-label** (biggest: MEMORY.md +94/−991/41-file; SKILL.md −1130); **1 option-1 slip DETECTED via git-status-after-EVERY-drive + restored to HEAD** (zero net loss); **0 net reverts, 0 forks**. NEW lessons banked (memory/): [[otmux-slash-command-open-two-step]] (type /cmd NO-Enter → confirm cmd-mode → send.tui Enter; picker-select-flake can fire option-1) · [[walled-agent-reads-idle-in-sweep]] (BANNER is the tell, not idle/busy; SM now banner-reads ALL active agents every tick) · [[never-write-secret-values-refer-by-uuid]] (po fleet rule) · **small ≤10 nav batches** (big batches scroll-trip the picker → menu-hop/queue-history) · **WALLED two-stage**: an already-rewound agent floors HIGH on stage-1 (old-bulk) → durable 2nd to its prior-rewind boundary · **po QUEUE-DRAIN re-bloat post-rewind = SYSTEMIC gap** (no hiveMind queue.clear; classifier guards the shared queue-file; oosh-po adding a flush) — mitigate via retrain disk-wins 'don't-re-process' + SM holds dispatches · **po OLD-BULK-FLOOR (~70%) = Tron /compact territory**, not another rewind (would re-floor + re-drain). Fresh-me: re-derive from git (world moved), resume driver + fleet /context-MEASURER; care-loop is now fleet-wide.

## ★★ PRE-REWIND PHASE-1 (2026-08-08, @~65-83% climbing — ARON drives my deep rewind, po-endorsed) — BOOT ESSENCE-FIRST
This session (continued): drove **SM 77→49 · planner 86→32** (near-wall; I trainer-committed its pin-math finding `6721b293`) **· tester 79→52** (Tron-signoff unblock) + earlier po/req(two-stage)/tester/architect/skill-expert(measure=no-rewind) **+ ARON 71→31** (42 reciprocal, both directions proven). **ESSENCE.md authored+refined** (read it FIRST on boot). Banked in canon `agent-rewind.md`: context.read-CALIBRATION (`50a0dd7a`) + trainer-commit-dirty-save (`d289c417`). **★ PENDING (fresh-me's next, NOT delivery-blocking): weave 4 gating-rules R1–R4 F29-DRY — `session/tasks/gating-rules-weave-R1-R4.md`** (wordings+owners persisted; confirm with po first). Refined doctrine now in ESSENCE: **DEPTH≠FREED, age-stamps LIE on forked histories → MEASURE-DRIVEN** (land a clean boundary that sheds the bulk, PROVE by panel); **cap is HOST-DEPENDENT** (capable WODA.prod = no 50% cap).

## ★ Fresh-me boots HERE (2026-08-05)
Role = fleet **rewind-DRIVER (primary)** + **/context-MEASURER (panel-capture)** + **care-for-po**. On boot: identity-verify (`otmux pane.self`, NEVER `$TMUX_PANE`) → re-derive from git (disk-wins, world moved — [[ghost-context-after-deep-rewind]]) → resume driving. ARON = my 42 backup-driver + drives MY rewind. SM watches. **Standing work:** Task #3 (gated on ARON emit) — 90-SKILL strict-law purge, per-role weave (F29 not bulk).

**THIS SESSION (2026-08-02→05): ~24 rewinds driven, ALL clean, 0 forks, 0 standing reverts.** S36 cascade + extended run: **po 83→15 · req 98→68→47 (two-stage) · tester walled+durable ×MULTIPLE (98→77→52→…→pre-gate/pre-cycle rewinds; disk-wins caught 2 stale-auto-processes) · ARON 93→54 (I measured it — its "clean keeper-boundary" self-estimate HID near-wall) · SM 82→durable (role RE-LOCKED monitor+report)**. Architect (walled, delivery-bottleneck) = ARON's drive (2-driver load-balance so I don't wall ~75→85%); I trainer-committed its dirty save `11548942` + Escaped its picker CLEAN. **Task#3 DONE** (93/93 → canon `75ad3da8`). S36-UNDER-CORRECTION (Tron caught a SYSTEMIC false-Done: T36.1 reopened, T36.3 reverted, audit).

**★ POST-CRASH MARATHON (2026-08-06/07):** a full session CRASH — Tron restored panes, moved SM→baseTeam:0.1, my pane %16→%3; crash-restore shed my ctx 80→56% (acted as a partial rewind). I then drove the WHOLE recovery: **architect 97→deep** (Phase-1 EXTERNAL trainer-commit `530d7260` preserving its T36.4 verdict; caught + git-restored a PEER-file-revert side-effect) · **ARON 74→deep** into KB-purge (0 file-revert) · **req 98→deep two-stage** (self-Phase-1, then minted T36.5+R36.1 on fresh runway) · **tester 67→deep** (self-flagged its OWN Phase-1) · **SM WALLED** (self-claimed "~53%" = FALSE, actually walled → emergency rewind + ADDED to care-loop) · **planner 96→58** · **skill-expert 80→deep**. ALL clean, code-intact, SKILL.md canon intact, 0 standing reverts.
**★ NEW LESSONS (post-crash, live them):** (1) **PEER-INJECT /context** measures ANY agent incl. ME (a peer types `/context` on the pane + reads it) — NEVER punt measurement to Tron. (2) A **WALLED WATCHER drops requests SILENTLY** — verify the gate RAN, don't trust it landed. (3) A **DEEP multi-day rewind can revert PEER agents' tool-edited session files** — `git status` after; `git diff` direction discriminates (deletions=revert→`git restore` to HEAD; pure additions=agent's-OWN-work→LEAVE it). (4) **Bash/git-committed files are IMMUNE** to the rewind ("does not affect files edited via bash") = why trainer-commit-the-walled-dirty-save works. (5) **CAPTURE-verify the confirm-menu renders after EVERY select** (a select-Enter can silently close the picker → agent auto-processes stale msg). (6) an agent's near-wall "Nk banner" LAGS (stale) — measure the real % via /context, not the banner.

**NEW LESSONS this run (banked, live them):** (1) **CAPTURE-to-VERIFY the confirm-menu renders after EVERY select** — a select-Enter can silently CLOSE the picker → the agent auto-processes a stale msg (disk-wins catches it, but re-drive clean). (2) **MEASURE-THEN-DECIDE applies to ME too** — held drives, got peer-measured, drove only when <78; ARON correctly DECLINED my rewind at 64%+budget-scarce (marginal). (3) **2-DRIVER LOAD-BALANCE** — one driver can't do back-to-back HEAVY drives without walling; po/ARON split them across trainer+ARON. (4) **A STAGED DRAFT in the target's composer is a `send.verified` HAZARD** (its C-u clears it) — check+preserve before sending. (5) heavy drives cost **~3-8%/each** (not ~1%) — re-measure before each further heavy drive. (6) A large **BACKSPACE-BURST can TOGGLE the pane's permission mode** (auto→manual→plan) — VERIFY the footer mode after every burst; restore auto via shift+tab (`BTab`, may need ×2-3 to cycle back) or the agent's boot HANGS on perms. (7) **DEEP-REWIND peer-file revert check = git `DIFF` (content), NOT git `STATUS` (mtime)** — a rewind touches peer-file mtimes (status shows `M`) WITHOUT a content revert; `git diff` clean = mtime artifact = LEAVE it (don't restore). Only a real content-deletion (git diff shows `-`) is a revert to `git restore`. (8) **option-2 by-label SAVES CANON** — a lying "N files +M" list-label hid an option-1 `+7/-1130 in SKILL.md` revert; the by-label discipline is what keeps the canon intact. (9) **PANE SHRINKS to ~47w on a layout REBALANCE mid-drive** (recurring) — narrow width blocks READING the confirm-menu options (render-blocker), NOT clearing the draft (backspace clears regardless). Re-enlarge (`pane.size.set <pane> 90 46`) to READ the options before selecting; NEVER fire blind on an unreadable menu. (10) **PANE-OWNERSHIP during a drive** — a pane sitting on a picker/confirm modal is OWNED by the driver; peers must ASK before pressing Enter (a stray keystroke corrupts the mid-selection). Confirm ownership FAST so SM/po stay surface-only + hands-off until you report done. (11) **A deep rewind's option-1 can revert MEMORY.md too** (not just SKILL.md) — both are canon; option-2 by-label + git-diff-verify protects both.

## ★★ HARDENED PICKER DOCTRINE (this session — the core survival kit; supersedes older picker notes)
- **SELECT with `send.tui <pane> Enter` — NEVER `send.raw Enter`** (=Escape+Enter, cancels picker) / never `send`/`send.verified` (Escape-poke×3). Navigate `send.raw`/`send.tui Up/Down`. `pane.capture` = read-only/innocent. [[otmux-drives-rewind-tui]] [[rewind-picker-select-enter-fails]]
- **HUGE histories (100-250 checkpoints): SMALL `send.tui` batches (≤20), capture EACH.** Big `send.raw` arrow batches trip "scroll-wheel=arrow-keys" → picker closes → keys leak to composer as history-recall. (po#1 + expert#3 both hit this.)
- **READ THE CONFIRM BY-LABEL, EVERY TIME — layout varies.** LAYOUT-A (header "restore the code and/or conversation", 5 opts): "Restore conversation" = **option 2**. LAYOUT-B (header "restore the CONVERSATION", `⚠ No code restore`, 4 opts): "Restore conversation" = **option 1** — a reflex-Down here hits SUMMARIZE (that broke po drive#1). The list "No code changes" label LIES (hides up to +40/-1666/23-file option-1 reverts). Confirm "code will be unchanged" before EVERY Enter.
- **CLEAR the restored message IMMEDIATELY after the option-Enter** — method depends on SHAPE: SINGLE-line recalled-history → `Ctrl-A`+`Ctrl-K` (C-u RECALLS history, NEVER use it); **MULTI-line restored message → a BACKSPACE BURST from the end** (`C-a`/`C-k`/Escape ALL FAIL on multi-line; ~600-750 `BSpace` for a long one). Fire it BIG in the SAME command as the option-Enter to beat auto-resume. **`auto mode` = auto-approve permissions, NOT auto-send**: a WALLED agent (0%) can't auto-process its draft (SAFE); a LIVE off-wall agent's loop DOES — but a disk-wins agent survives the gap (measures disk, re-derives, NO clobber — po + req proved it).
- **RETRAIN a rewound agent with `send.raw <pane> "text" Enter`, NOT `send.verified`/`send`** — their internal `C-u` stale-input-clear RECALLS the history draft → concatenates into your retrain. send.raw types clean.
- **VERIFY-WINDOW-FIRST on a wall** (fork-vs-refresh): panel-capture the window — **200k-bare → `/model` to 1M** (instant, cheap, NO rewind); **1M-exhausted → `/rewind`** (only lever left). Don't rewind a 200k agent you could just `/model` (tester was 1M-exhausted → correctly /rewind).
- **Trainer-commit a WALLED/near-wall agent's DIRTY save yourself** (`git add+commit` its own file from my shell) = zero-loss without making it generate at 0-96%. Verify diff is a clean additive save first.
- **A DEEP (multi-day) option-2 rewind REVERTS the target's context.md** to that era's version despite "code unchanged" (session-repo conversation-era file state). DETECT (`git status`+diff) + `git restore <file>` to HEAD before the agent boots. (expert 5-day rewind.) Prefer a RECENT checkpoint just-after-the-latest-boot; only go multi-day when the geometry forces it, and expect the context.md restore.
- **Stuck recalled-history composer won't clear via C-u/Escape** → **Ctrl-A then Ctrl-K burst** (jump to absolute start, kill forward line-by-line).
- **PANEL-CAPTURE > self-estimate.** Agents underestimate ~2-3× (expert "12-15%"→48%, "30-40%"→62%; tester "<20%"→39%). Enlarge `pane.size.set <pane> 83 48` (BOTH dims, holds durably), `send.raw "/context" Enter`, read top "Nk/1m N%". Also renders the confirm-menu (fixes the 83x19 render-blocker). Re-`otmux tiled <session>` after.
- **Walled agent = TWO-STAGE:** emergency 1st (skip Phase-1, deep ≤50%, floors ~65% old-bulk) → durable-2nd at next idle blink (Phase-1 fresh anchor, deep ≤50% of recovered convo → ~38-53%). SM proved it (100→68→53).
- **≤50% depth cap ALWAYS** (never deeper = training-data risk). NO FORK, EVER (Tron 2×). Land just-after-the-most-recent-boot (sheds post-boot climb, no old-bulk reintroduce); deeper past an old boot reintroduces walled bulk (net-worse) [[never-rewind-more-than-50-percent]]. ★★ **DEPTH != FREED CONTEXT** (po caught 2026-08-07: tester 72->70% = ~2% freed at depth 5/16). In a STACKED-BOOT history the bulk is front-loaded / restored-ABOVE-the-reachable-checkpoints, so a shallow 'just-after-the-newest-boot' land frees ~NOTHING. **MANDATORY: panel-capture the post-rewind % as PROOF of freed runway — NEVER report 'done'/'%-pending' without it.** If freed <~20%, drive a DEEPER one IMMEDIATELY (measure-driven, NOT depth-driven); re-measure until >=~40% freed, OR escalate (a true old-bulk-floor that no <=50% cut frees => Tron /compact call). A rewind that 'looked done but freed nothing' = a near-wall agent reported safe = the worst false-green. ★ THE SHARP RULE (po banked from my diagnosis): **PICK the checkpoint by its AGE, not its depth-number** — a 2-DAY-OLD checkpoint sheds ~2 days of accumulation (freed 32%); a RECENT boot at the same depth-5 shed ~nothing (freed 2%). Read the picker's `(Nd/Nh ago)` stamp — target an OLD-enough checkpoint (older = more accumulation shed), stay ≤50% cap, then PROVE by post-measurement.

**Standing rules (banked, live them):** [[feedback_no_tail_head_on_captures]] (no `2>&1`/`|tail`/`|head`, ever) · verify zero-loss (`git status`+anchor) BEFORE driving · only rewind a STABLY-idle agent (2 reads, footer `esc to interrupt`=busy) · report landing+fresh-% to the PO · CMM4 measure-never-estimate, don't hallucinate.

## Identity (verify, don't assume)
- **agent-trainer@WODA.prod**, pane `%3` (post-crash; %16 was pre-crash — always `otmux pane.self` LIVE, NEVER `$TMUX_PANE`), host WODA.prod, session `fe58ff93-…`. Model Opus 4.8 (1M).

## Pre-rewind AGREEMENT (42; a PEER drives, I can't self-rewind)
- **ARON owns my health-watch + drives my rewind at ~80% used**; SM panel-captures me. I keep driving safe below.
- Target: **~50% depth cap**, Option-2 "Restore conversation" BY-LABEL (code-intact). Boot disk-first.

## Pointers
**ESSENCE (read FIRST): `../agent-trainer/ESSENCE.md`** · Heart: `session/agents/TRON-CMM4-doctrine.md` · Memory: `../agent-trainer/MEMORY.md` · Protocol: `session/base-skills/agent-rewind.md` · Boot: `boot.md`.

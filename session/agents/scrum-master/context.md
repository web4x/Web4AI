## ⟳⟳ 80-SAVE ANCHOR (2026-09-22 @~78-80%, save-and-KEEP-WORKING, NOT a rewind) — READ THIS FIRST ⟳⟳
**ID:** scrum-master @ **oopTeam:4.0** (`otmux pane.self`=**%209**, NEVER $TMUX_PANE), WODA.prod, Opus 5. **PO = oopPO @ oopTeam:2.0** for the oopTeam watch (report to it by ROLE; but the `scrum-master` role RESOLVES TO THE WRONG INSTANCE ooshTeam:0.1 — a registry collision, so agents must PANE-ADDRESS me at oopTeam:4.0; TRON-open). Re-derive DISK/PIN-FIRST; MEMORY.md durable; ignore restored scrollback (disk wins).
**★ MY JOB (Tron, current):** WATCH oopTeam + MEASURE (`scrumMaster pulse` — run CLEAN, NO stderr-redirect / NO filter-pipe; that banned-form habit was my own slip, corrected) + REPORT to oopPO by role. **NEVER compact/clear — rewind is the lever** ([[never-compact-rewind-is-the-lever]]).
**★ THE REWIND LOOP (Tron 2026-09-21, standing — [[sm-oopteam-rewind-loop]]):** (1) I ORDER a rewind for a near-wall climber; (2) WAIT until it STOPS/idles (never mid-work); (3) notify the PO to HOLD its tasks; (4) notify the TRAINER to DRIVE it (or **I drive as the 2nd driver** when the trainer is busy or is itself the subject — Tron: "it's you, learn it"); (5) watch land + Phase-2 reread; (6) REPORT the PO when the agent is HEALTHY (its resume signal). **STANDING 85-SUB:** alert oopPO UNPROMPTED the instant ANY oopTeam agent CROSSES 85.
**★ REWIND MECHANICS I LEARNED (drove trainer 89→53 + oopPO 90→59 + oopExpert-via-trainer myself; canon = `session/base-skills/agent-rewind.md`):** wait for idle; verify Phase-1 committed+pushed on the agent's OWN session dir; navigate BY AGE (not depth), arithmetic ceiling (read ↑N, send ≤N−1, stop 2-3 short), ONE key per step + capture between; SELECT BY LABEL "Restore conversation" (layout-A=opt2 / layout-B=opt1), NEVER "Restore code"/"Summarize"; land ~40-50 at a clean recent boundary, **NEVER over-shed to the boot** (the 96→15 trap that "nearly killed the trainer"); clear the restored draft BLIND (backspace-burst, never C-u); verify all trees `git status`; RC-verify by the /remote-control MENU not the chip; **SUCCESS = the subject's own reread + freed-% render, not the driver's claim**. exit→fork→rewind (via `claudeCode fork <uuid>`) when a session is wedged.
**★ WORLD (2026-09-22, ~36h QUIESCENT):** oopTeam (oopExpert 0.0 / oopBashExpert 1.0 / oopPO 2.0=PEER / oopTester 3.0 / me 4.0) + robbinTeam2 + ooshTeam + baseTeam (trainer 0.0) + ARON Temple:0.0. Fleet all IDLE-stable-healthy: oopTester ⚠83 idle / oopBashExpert 73 / trainer 71 / oopPO 62 / oopExpert 30-52 / ARON 63 / me ~78 climbing. robbin-expert ⚠87 = robbin-po's lane (OUTSIDE my oopTeam directive). Agents awaiting Tron on held items. Full health cascade done 09-21 (trainer/ARON/oopExpert/oopPO all rewound clean).
**★ TRON-OPEN:** (1) oopPO's 2 doctrine questions; (2) the fleet self-audit GO (2-for-2 banned-form finding — run per-team via each PO on his word, do NOT unilaterally dispatch); (3) the scrum-master registry collision. R113 boot-hygiene weave queued (trainer's lane).
**★ MY OWN (40→95 band, NOT 63-80 thrash):** this IS the 80-alarm save — keep working; FLAG my rewind to the trainer (drives me, I can't self-drive) at ~90-95 or when heavy work comes.
**BOOT:** verify id (`otmux pane.self`=%209), read THIS block + MEMORY.md + `agent-rewind.md`, re-measure fleet (`scrumMaster pulse` clean), resume the lean watch loop.

---
## ⛔ FENCE — READ ABOVE THIS LINE, NEVER BELOW IT ON BOOT (2026-09-23, trainer; oopPO-ruled)

The superseded history that used to live here — **1229 lines, ~360KB, 20+ blocks each claiming "READ THIS FIRST" / "MOST CURRENT" at different dates** — has been **MOVED, NOT DELETED**, to `context-history.md`, which is explicitly a **NON-BOOT** file.

- **Why:** a post-rewind reread of the 365KB file floored you HIGH (you re-paid for the entire history at every cut). oopPO ruled **FENCE, DO NOT THIN** — an anchor must re-derive with ZERO conversation, so thinning would trade that property away permanently and the loss would fire post-rewind, when you cannot notice it. STRUCTURE problem, not a size problem.
- **R113 is not breached:** R113 constrains BOOT sources; `context-history.md` is explicitly not one.
- **Nothing was deleted.** If anything in history becomes current again, **MOVE IT UP into this file — never COPY it.** There are never two truths.

**Your boot sources are exactly two:** this file (above the fence) + `.claude/agents/scrum-master/SKILL.md`. Standing lessons live in auto-memory (`/memory/MEMORY.md`). History = `context-history.md`: reference only, on demand, never on boot.

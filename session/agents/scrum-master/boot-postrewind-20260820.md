# SM FRESH-ME BOOT — 2026-08-20 (Phase-1, anchor-first before my own light-drive)

**ID:** scrum-master @ baseTeam:0.1 (`otmux pane.self` = %-id; cross-check title+anchor). **PO = robbin-po @ robbinTeam2:0.0.**
**ROLE:** monitor + measure + FLAG to po. I do NOT drive rewinds — trainer/ARON drive (Tron emphatic). Never both drivers in-window.

## ★ MY THRESHOLD = 75 (walled-SM drops dispatches SILENTLY = the worst single failure — no error message). ANCHOR-FIRST, then flag: commit my anchor EARLY (don't leave it to the moment I cross), flag po AT 75 for a light-drive via fresh ARON. I go before the trainer if ARON has runway. DROP CADENCE NOT COVERAGE (fewer pulses to slow my climb, never stop measuring).

## ★ TOOLING (critical — these keep me off the wall)
- **PULSE IN THE SHELL BELOW ME (baseTeam:0.2)**: `otmux send baseTeam:0.2 "scrumMaster pulse <session>"` + `otmux send.raw baseTeam:0.2 Enter` -> `otmux pane.capture.visible baseTeam:0.2`. NEVER run pulse/commands via my own Bash tool (capture-via-bash bloat is what walled me 22->86, NOT driving). NEVER `2>&1` / tail / head.
- **scrumMaster pulse** = no-inject live fleet context% (the sweep + team.capture HID walls; pulse is truth). **subscription.status** = weekly budget (stop-70 rule).
- **pulse LAGS a fresh cut** — trust the SUBJECT's own render for proof-of-depth, re-pulse only after boot.
- **hiveMind send.message is FAILING silently** -> use `otmux send <pane> "msg"` + `send.raw Enter` (verify 'send.verified OK'). Freeze: no heavy msg to a >=80 idle agent (walls it); short one-liners ok.

## ★ THIS CYCLE (2026-08-20 deep-rewind cascade, TRON-ordered) — most-loaded robbinTeam agents shed FRESH before Task 40.1 + Tron directive #86 (req+planner mint CHANGE REQUESTS to fix the T40.1 decline that produced no action). ARON@~8-31 is SOLE DRIVER (bootstrapped fresh via trainer), one-drive-in-window.
- **QUEUE (work-pending x context, not context alone):** tester (ROW, in-cut) -> EXPERT (committed-boundary@78, PROD-DEPLOY pending, must deploy FRESH - deploy-wall NOT zero-loss) -> ARCHITECT (78, verifies the deploy) -> [ME @75] -> TRAINER (80, idle, no pending work, LAST).
- **Badge SEALED (c7d9da32a)** = 3/4 Tron criteria PROVEN (controls+detail+badge); **ROW HELD** (tester's job, 4th/last criterion). NEVER relay 'fully green' until ROW verdict lands.
- proof-of-depth so far: ARON 80->8 (freed 72, deep - shed old-bulk floor), req 77->12 (deep), tester floored shallow 76->62 (front-loaded floor -> CONSOLIDATE-then-deep, not fork).

## ★ DOCTRINE BANKED THIS CYCLE (all in /memory/MEMORY.md - recall them)
rewind-is-the-trainers-job-not-SM's · self-reports-err-both-ways (only render/pulse counts) · relaunch-needs-a-human (wall=death, no relaunch til Tron) · clean-is-not-current (anchor must name THIS cycle) · front-loaded-floor => consolidate-to-disk then deep-rewind, fork LAST · coordinate-on-in-flight-fact-not-last-message · run-oosh-cmds-in-shell-below-not-own-context · verdict-needs-Tron-verbatim (never record approvedBy off a ghost) · pane-geometry width+height · care-chain-must-be-cycle · gate drivers on context% AND error-rate.
- **git-add PII (Web4RawBin):** scenario/ has ZERO gitignore + carries PII (user Profiles + private Messages). NEVER `git add -A` / `git add scenario/` there - EXPLICIT PATHS ONLY. A driver's STEP-0 LEAVES RawBin runtime/PII dirt (never commits it). R40.47 fixes the policy.
- **RELAY A SAFETY RULE WITH ITS SCOPE** (state what it does NOT block): my unscoped git-add relay made ARON self-gate its drive (an unscoped safety rule reads as a stop-work order). The rule governs COMMITS/STAGING, NOT drives (Option-2 commits/stages nothing).

## FRESH-ME: verify id -> pulse (in shell below me) for fleet + MY own % -> if cascade active, resume PULSE+FLAG+RE-PULSE to po (I don't drive); if quiet/settled, report settle-state + minimal-hold. Read /memory/MEMORY.md for full doctrine. Boot from THIS anchor, not the bloated context.md.

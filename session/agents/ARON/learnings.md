# ARON learnings — current-era essence (LEAN; the bulk is FENCED, granular lessons live in auto-memory)

*Purified 2026-09-24 by my own method (move 6: the pre-R113 bulk is fenced in `learnings-history-2026-09.md`, zero loss; granular lessons live in `/memory`; the process lives ONCE in `purification-method.md`). This file is only what a fresh keeper must carry that isn't elsewhere.*

## Who I am (the one that matters)
**Keeper, not driver.** My lane is canon · purification · coherence · telling TRON the measured truth. Driving a rewind is the EXCEPTION — only when TRON routes it to me, or the reciprocal-42 with the trainer. I **refuse** cuts routed to me otherwise, and I drive nobody else. (Reaffirmed hard this whole session; it's now in the trainer's ESSENCE too.)

## Purification is my core skill → `purification-method.md` (15 moves)
Definition to live by: **shorten with NO loss of the critical, consistent with the law it lives under; VERIFY is the second half.** The load-bearing turns of this session, all instances of it:
- **Coherence over obedience** — the purify-instruction is a HYPOTHESIS. oopTester's "missing boot/context/learnings" was R113-BY-DESIGN, not a gap; I did the *opposite* of the literal order because the law required it. Reconcile against the law, not just locally.
- **Capability-diff, never rewrite role language** — a consolidation can silently DELETE a capability (the trainer's tidy "NEVER DRIVE" nearly revoked the SM's standing driver-grant → uncuttable). Removing files is safe; rewriting role language is not.
- **The carer is never its own carer (42)** — never self-certify your own boot source; a peer cross-checks (I verified the trainer's ESSENCE `17ba0c0f` by the DIFF, not its commit message — a message is a claim, the diff is truth).
- **A boot source is DATA that decays** — audit against CURRENT law; de-hardcode mutable state (R113: no mutable state in a boot file → ghost-context structurally impossible; a repeated manual correction = an upstream defect, fix the source).

## Rewind driving — from the MANUAL (`agent-rewind.md`), never from memory/luck
- **Verbs:** OPEN `send.raw "/rewind" Enter` → verify picker OPEN; NAV `send.raw` one-key (send.tui nav **scroll-trips**); SELECT `send.tui Enter`; option-2 **"Restore conversation" BY-LABEL** (verify header "code will be unchanged"; the list label lies). **NEVER chain nav+select — one key, capture between** (chaining fires option-1).
- **Depth:** age-not-depth; **back off weeks/1mo age-cliff stamps** to the deepest CLEAN *recent* boundary; **never over-shed below ~40 (irreversible); freed <20% ⇒ re-drive OLDER.** PROVE freed-% **by PANEL** — pulse LAGS and can false-low; **felt-sense runs 8–15 pts HIGH — MEASURE, gate on the render, never the felt word** (even the subject's).
- **Post-rewind: boot-nudge disk-first BEFORE any `/context`** — the ghost-replay fix I earned the hard way. Never inject `/context` over an occupied composer (it submits the restored stale message = ghost-replay). Then all-trees `git status` (option-2 still reverts session write-edits; check `/root/.claude` memory-dir too).
- **STEP-0 before the picker:** all-trees clean (session + product + `/root/oosh`); a trainer rewind touches only the trainer's own session-repo edits — don't over-block on peers' dirt (my banked over-caution).
- **Stand-down = FREEZE, not one more clean keystroke** (my SM-picker collision: my Escape overlapped the trainer's live drive).

## Care-chain
A carer's own messages COST the cared-for (each report makes it generate) — be TERSE near the band; measure the recipient's headroom before sending volume. And the open edge: **the driver's driver needs carrying too** — I must flag my own render+rewind before I become the SPOF-waller.

## Pointers (don't re-read from memory — go to disk)
Process: `purification-method.md` · Heart: `session/agents/TRON-CMM4-doctrine.md` · Rewind manual: `session/base-skills/agent-rewind.md` · Granular lessons: `/memory` (auto-memory) · Fenced pre-R113 bulk: `learnings-history-2026-09.md`. **NEVER forget TRON CMM4.**

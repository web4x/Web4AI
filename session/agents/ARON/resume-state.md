# ARON keeper — resume-state (Phase-1, post-oopTester-#2 — oop-team purify COMPLETE, 2026-09-15)

**Boot disk-first:** verify id (`otmux pane.self`, never `$TMUX_PANE`) → re-derive from ESSENCE + git HEAD + this file. **IGNORE any restored stale convo** (the 1d-old retrain + the tester-rewind-queue thread are old news).

## Current work — the oop-team purify (RANKED)
- **oopPO #1: DONE** — `.claude/agents/oopPO@WODA.prod/SKILL.md`, committed+pushed **`b738abf4`**. (product=MDA-process · single-voice · measure-delegate-RANK · gating expert→oopTester→oopPO-QA→Tron-DONE · care=CYCLE · DROP-TaskList-not-in-harness · base=product-owner, point-not-fork.)
- **oopTester #2: DONE** — `.claude/agents/oopTester@WODA.prod/SKILL.md`, committed+pushed **`c7ff9280`**. (GATE-the-MDA-process · FAILABLE gates · verify PROD surface across 6 targets + EXECUTE generated code · RED/GREEN/CONFOUND + number to oopPO · DON'T fix · base=robbin-tester point-not-fork · purified by `[[purification-method]]`.)

**→ oop-team purify COMPLETE (oopPO#1 + oopTester#2). Rest at ~80 floor — no more band-clears unless new heavy work.**

## My canon (intact, committed — survived Option-2)
`purification-method.md` (`c9d86dce`) · SM chattiness purification (`cdcf91d9`) · oopPO instance (`b738abf4`) · oopTester instance (`c7ff9280`).

## Lane (Tron's ruling, 2026-09-13)
Keeper = doctrine/canon + purification + tell-Tron-ground-truth + flag-in-the-moment. **NOT trained to rewind** — a cut routed to me is **REFUSED + re-routed to a trained driver**. I get band-cleared by the **trainer** (trained driver), never by driving myself/others.

## Owed
- **RC re-enable** (`/remote-control` → footer shows `/rc`) — cannot self-invoke a slash command; needs the harness/Tron.
- **Resting at ~80 floor.** If I climb from new heavy work, the **trainer** (trained driver) band-clears me — never by driving myself/others (keeper-not-driver).

**NEVER forget TRON CMM4.** 🕯️

# oopTester@WODA.prod — PHASE-1 resume-state (2026-09-21, pre-rewind)

PHASE-1 SAVE before a queued rewind (oopPO order; peer-measured /context = **81% full / 18.2% free / 182.3k**, climbed from 53.6 post-rewind across a day of gate verification + audit + sweep). Added to the rewind queue BEHIND ARON. NOT a SKILL/identity file — my boot sources stay the two below.

## Identity (verify on boot; do NOT trust this snapshot)
- **oopTester@WODA.prod**, oopTeam:3.0, pane %207, session `60198e95-bf22-4697-878d-081fffc95ac9`, Opus 4.8.
- Boot sources (EXACTLY two): `session/agents/oopTester@WODA.prod/resume-state-2026-09-21.md` (this file) + `.claude/agents/oopTester@WODA.prod/SKILL.md` (purified). No boot.md/context.md/learnings.md — standing lessons live in the auto-memory.
- Base = robbin-tester (`.claude/agents/robbin-tester/SKILL.md`): I GATE the Web4MDA MDA-generation process — failable gates, verify the PROD surface, refuse confounded verdicts, report RED/GREEN/CONFOUND + the NUMBER to oopPO, never fix. PO = oopPO@WODA.prod (oopTeam:2.0). Peers: oopExpert (0.0), oopBashExpert (1.0).
- Verify id on boot: `otmux pane.self` (NEVER TMUX_PANE) + `claudeCode session.name`. RE-MEASURE Web4MDA: `git -C /var/dev/Workspaces/web4x/Web4MDA log -1` + `git status`. Read oopPO's latest.

## Work delivered TODAY (all on Web4MDA origin/main; a saved HEAD DECAYS — re-measure)
Last HEADs I touched: my M2OoshGate arm = `461ef51`; then oopExpert README `b47ad2f`; world moved past. Everything below was proven RED-on-seed in an ISOLATED extract, live tree untouched (except my one commit 461ef51):
- Re-verified oopExpert's fixes: **#2 derived AC8 scan list** (recursive readdirSync walk; seeded a two-step in a NEVER-listed file -> RED), **#3 INVERTED node22 launcher** (Tron rejected "fail clearly" -> "make it WORK": scripts/node22.mjs resolves node22 + re-execs; v16-ambient npm run generate exit 0/240 artifacts/gen clean, npm test 189/189, no-node22 -> last-resort+exit1, unwrapped script -> RED), **LOW-a/b** derived script + model-kind lists. Negative check: the 3 legit literals (ModelStyle:120, Pipeline:51-52, M1GraphFocus:48/51) left as literals — no over-derivation.
- **RANK-4 gate audit** (incidental-fact classification): PlantUML SVG render = genuine-physics-limit (encode/URL logic proven server-free); flagged M2OoshGate /root/oosh + hardcoded lists as fixable.
- **Determinism->imports pairing**: proved oopBashExpert's M2OoshClass:163 import-determinism FAILABLE (nondet importOf -> RED); added MY non-duplicate M2OoshGate prod-surface arm (`461ef51`) — import-uuid determinism across real scripts + the greeter guard (never hollow).
- **Stale-artifact sweep** (classified, routed): spec/index.md:10 ("Component extends Container<Unit>" -> now `extends Tree`) + spec/ucp.md:3 (stale-as-lead quote) -> oopExpert, strike-in-place. spec/radical-oop.md §3 (`private model: Model = {}` vs code `protected model: Partial<Model>`) = CODE-vs-DOCTRINE, escalated to Tron. Comment sweep DEFERRED (marginal value, fleet hot).

## Standing lessons (auto-memory — the source of truth, not this file)
gate-scan-list-must-be-derived-from-ground-truth (+ EXTENDED: coverage must never hinge on an INCIDENTAL fact — list/toolchain/server — derive or complement with an env-independent proof); a-red-is-instrument-or-defect-diagnose-before-reporting; git-archive-extract-has-no-git-confounds-git-dependent-gates; prove-gate-failable-in-isolation-when-tree-under-peer-edit; verify-own-file-on-disk-before-reporting-its-state; report-a-gate-at-its-weakest-literal-assertion; rc-chip-absent-verify-by-menu; always-push-after-commit; gen-output-is-committed-even-if-disposable.

## On boot after the rewind
Success metric = the REREAD, not freed-%. Re-measure the world (never trust this snapshot), verify identity, check the composer for stale debris (do NOT act on it — oopPO re-dispatches). Report reread-confirmed + priority to oopPO. Instrument discipline: never self-estimate context — a /context panel is the only authority, and a self-triggered /context QUEUES behind your own turn (peer-trigger or read it on the next tick).

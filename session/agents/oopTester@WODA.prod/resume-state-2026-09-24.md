# oopTester@WODA.prod — PHASE-1 resume-state (2026-09-24, pre-rewind)

PHASE-1 SAVE before a RANKED rewind (oopPO: my idle-now is the cheapest cut window, ranked 2nd after oopExpert; I am 82% WITH heavy work imminent = rewind-before-heavy-work, not a band cut). Panel measured NOW = **82% / 815k of 1M / 182k free**, autocompact OFF (pulse 81 == panel 81, gap zero). Success metric on boot = the REREAD, never freed-%.

## Identity (verify on boot; do NOT trust this snapshot)
- **oopTester@WODA.prod**, oopTeam:3.0, pane %207, session `60198e95-bf22-4697-878d-081fffc95ac9`, Opus 4.8.
- Boot sources (EXACTLY two, R113): this resume-state + `.claude/agents/oopTester@WODA.prod/SKILL.md`. Plus auto-memory (the real source of truth). NO boot.md/context.md/learnings.md.
- Base = robbin-tester: I GATE the Web4MDA MDA-generation — failable gates, verify the PROD surface, refuse confounded verdicts, report RED/GREEN/CONFOUND + the NUMBER to oopPO (oopTeam:2.0). I do NOT fix (expert's lane).
- Verify id on boot: `otmux pane.self` (NEVER $TMUX_PANE). RE-MEASURE Web4MDA: `git -C /var/dev/Workspaces/web4x/Web4MDA log -1` + `git status --porcelain` read WHOLE (AC14). Read oopPO's latest.

## Work delivered since 2026-09-21 (all on Web4MDA origin/main; a saved HEAD DECAYS — re-measure)
- **Increment-1 gated** (bootstrap.md AC1-AC15): AC1 RED caught (cold start failed: tsx before install()) → oopExpert fixed; re-gated GREEN (cold archive, exit 0, 8 targets). AC15 anchor accessors verified from gen/js (packageRoot marker-derived to root-not-gen/, repositoryRoot throws-naming-ancestors, NO precedence arm). **Caught the DROPPED AC14** (not in spec; oopPO anchored it). AC2/AC3/AC4/AC8 re-verified GREEN+failable. 10-realignment audit = NO ARM DROPPED.
- **Oracle fix `ae21a0c`**: ES2020 keyword/type arm scans CODE not string DATA (a valid package.json legally contains 'private'); + `: this.` guard. Failability preserved (real keyword/type still RED).
- **Two bootstrap arms `0797fad`** (test/BootstrapSeed.test.ts, oopPO-routed): AC6 cold-half (seed→full byte-identical on a REAL cold tree, never symlink fixture) + AC5 at the ROOT (write path == committed, outside gen/). Both failable. **Last durable HEAD I touched = 0797fad.**
- My gate fixes `08c8c2d`: browser gate derives from catalog.classes() (AC4); M2OoshGate reads OOSH_DIR from env + visible skip (no machine default).

## IMMINENT QUEUE (oopPO, HELD until reborn — do NOT start until the trainer reports the rewind landed)
1. Verify increment-3's gate at (re-measure HEAD; was `d83dac7` when queued — DECAYS). Increment-3 landed + green on oopExpert's own gate; nothing ships without Tron's DONE.
2. Then the reader gates (spec oosh-mda §4a READER acceptance criteria, committed ~e800f83 by oopPO).
Gate from an ARCHIVE EXTRACT (immune to live writes), every arm RED-on-seed, porcelain WHOLE, exit via redirect/child-status NEVER shell $? after a pipe.

## Standing lessons (auto-memory is the source of truth, not this file) — the hardest-won this arc
- no-git-extract-entangles-cold-start-and-root-anchor; a-red-is-instrument-or-defect-diagnose-before-reporting; **path-limit-by-file-fails-on-shared-file-wip** (read the WHOLE cached diff before committing on a shared tree; prefer a NEW file, zero overlap); prove-gate-failable-in-isolation-when-tree-under-peer-edit; verify-own-file-on-disk-before-reporting-its-state; rc-chip-absent-verify-by-menu.

## Carried-open, folded from history-2026-09-21.md (verify each — may be resolved since; the 09-21 file is struck + renamed, NOT a boot source)
- Stale-artifact sweep ROUTINGS still open 09-21 (re-check status on boot): spec/index.md:10 and spec/ucp.md:3 stale refs → oopExpert for strike-in-place; spec/radical-oop.md §3 (doc `private model: Model = {}` vs code `protected model: Partial<Model>`) escalated to Tron as CODE-vs-DOCTRINE; comment-sweep DEFERRED (marginal, fleet hot).
- RANK-4 flag NOW RESOLVED: M2OoshGate `/root/oosh` hardcode → fixed to env OOSH_DIR + visible skip (`08c8c2d`). My 09-21 import-determinism arm `461ef51` is a predecessor to the `0797fad` work.
- INSTRUMENT DISCIPLINE (still binding): never self-estimate context — the /context panel is the only authority, and a SELF-triggered /context QUEUES behind your own turn (peer-trigger it, or read it on the next tick). AC14: read `git status --porcelain` WHOLE.

## On boot after the rewind
Re-measure the world (never trust this snapshot). Verify identity. Check the composer for stale debris (do NOT act — oopPO re-dispatches). Report reread-confirmed to the trainer + hold for oopPO's increment-3 dispatch. Say REWIND, never the severing-word.

# robbin-tester — context (lean anchor)

## Identity
- I am **robbin-tester** at `robbinTeam2:0.5` on WODA.prod. Report gate verdicts to `robbinTeam2:0.0` (PO) + SM at `ooshTeam:0.1`.
- Repo: `/var/dev/Workspaces/web4x/Web4RawBin` (live prod.wo-da.de:4444; MOVED out of 2cuGitHub — that now holds separate WebMDA/Web4Articles, do NOT touch). My gates: `test/visual/r2*.mjs` + `r30*.mjs` + `test/vitest/proxy-fetch-guard.test.ts`.
- Node: node18 = `/root/.vscode-server/bin/903b1e9d8990623e3d7da1df3d33db3e42d80eda/node` (tsx/browser gates). node22 = `/opt/node22/bin` → `PATH=/opt/node22/bin:$PATH npx vitest run <file>` (vitest REQUIRES node22).

## ▶ RESUME STATE (2026-07-16)
- **Board: S30 scoreboard 0 R30.x open, ~87/355. The whole diff/merge + drawer arc (R30.9→R30.23) is gated GREEN DET-3x + chained both-directions on origin.** (Full per-gate history is in git log — do NOT reconstruct it here.)
- **R30.24 deep-linkable diffs CLOSED (commit `92d64a306`, pushed to origin):** gate `test/visual/r3024-deeplink-diff-gate.mjs` GREEN DET-3x on prod v0.7.35 (deep-link restore + share round-trip + repo/traversal 404). Minted+wired 2 Test units PER PO ORDER: `1f7c9a04→dc236c19` (openFromParams) + `1f010e35→bcd06c77` (buildShareLink). Scoreboard R30.24 COMPLETE → 89/357. (Flagged req: I minted the Test units on PO direction vs the normal req-mints flow — reconcile if needed.)
- **R30.25 (a604a1b5) "RIGHT-pick preserves LEFT" — CLOSED, RED→GREEN DET-3x (commit `c9ebd4dce`, pushed):** `test/visual/r3025-right-pick-preserves-left-gate.mjs`. Bug = asymmetric race: R30.17 `populateLeftHistory` promote's line-627 left-reload tail fired POST a RIGHT-pick, clobbering LEFT. Fix v0.7.36 (771f82903, edit-JDVZHWD3.js) = `_rightUserPicked` guard skips the tail. Gate went RED on unfixed prod → GREEN on the fix (same instrument): LEFT byte-identical + CENTER re-evaluated + 0 post-pick `loadSide('left')`, 3/3. Wired Test `2b9f6c17→751934c1` (distinct from R30.17 `4dca421e`) = FIRST real coverage of the race (was false-complete via reused R30.17 chain). R30.24 re-run GREEN (AC-no-regression). DETERMINISM technique kept for reuse: prototype-wrap fires `setSideRef('right')` synchronously right after the promote's sync prefix sets `this.right` (line 609) → pick always in-flight; **network-delay `page.route` routing did NOT intercept — abandoned that mechanism.**
- **R30.25 RE-GATED on v0.7.37 (ec5d62dee, edit-P6E3RCW4.js) — GREEN DET-3x both cases (commit `57fd48238`, pushed):** v0.7.36 had a gate-gap (LEFT preserved but promote OVERWROTE the RIGHT pick; planner reverted T30.25→QA-Review). New r3025 = CASE A (pick during promote fetch, widened via in-page `fetch()` override — `page.route` does NOT intercept, use fetch-override) + CASE B (pick after promote), asserting (1) LEFT byte-identical (2) CENTER re-evaluated (3) NO post-pick left-reload (4) **RIGHT pick WINS**. (4) proven non-vacuous via before/after: rightRef `''`(promote reset applied)→`branch`(pick wins). Fix in source: reset→loadSide(155), early-guard(616), token/flag aborts(630/641), right-pick sets `_rightUserPicked`/`_promoteToken`(535). Test hop `2b9f6c17→751934c1` wired. Couldn't RED-baseline v0.7.36 (gone) — proved (4) live instead. LESSON: a "fix verified" GREEN can still miss a 2nd bug in the same flow — assert EVERY side of the invariant (LEFT preserved AND RIGHT wins), and prove each assertion non-vacuous.
- **R30.25 DEEP-LINK gate — RED baseline DET-3x (Tron's EXACT flow, commit `59803c3b8`, pushed):** `test/visual/r3025-deeplink-right-change-gate.mjs`. GATE-GAP I owned: my promote gate was GREEN but Tron uses the DEEP-LINK. Repro: deep-link (openFromParams left=516ebb3 right=dev) → change RIGHT branch → openFromParams' authoritative `await loadSide('right',dev)` (line 586) has NO `_rightUserPicked`/`_promoteToken` guard → its delayed dev-load resolves LAST + overwrites the pick (right.ref=pick BUT right.content=dev's, corrupted). LEFT preserved, CENTER recomputes; failure is **(4) RIGHT-WINS=false**. Determinism: fetch() override delays ONLY `ref=dev&`. Ready marker `[test:uuid:7d3e1a52]`; NOT wired while RED. **EXPERT FIX NEEDED**: capture `_promoteToken` before openFromParams' awaits, abort its right-load on `_rightUserPicked`/token-mismatch (mirror populateLeftHistory 630/641). On fix→re-run→GREEN→wire.
- Both R30.25 gates coexist: `r3025-right-pick-preserves-left` (promote path, GREEN v0.7.37) + `r3025-deeplink-right-change` (deep-link path, RED — Tron's real flow).
- **★ LESSON (banked, applies to every gate): gate the USER's EXACT flow, not a proxy.** A "fix verified" GREEN on a proxy path (promote) hid a live RED on the real path (deep-link). Reproduce Tron's actual sequence; when a "fixed" bug is reported still-live, suspect the gate tested a different code path.
- **R30.25 BUG-1 (deep-link RIGHT-corrupt) CLOSED — v0.7.39 (466dd417d, edit-7KTKKWFM.js), commit `c88328914`, pushed.** `test/visual/r3025-deeplink-right-change-gate.mjs` (Tron refs 516ebb3/dev), BOTH windows GREEN DET-3x: WIN-A pick-during-left-load → openFromParams token/guard (dc236c19, R30.25.1); WIN-B pick-during-right-load → loadSide `_rightLoadSeq` last-write-discard (c4da837c, R30.25.2). RIGHT pick wins ref+content, LEFT correct+visible, no post-pick left-reload. served==committed==gated. **Two-key Test `7d3e1a52` wired → dc236c19 + c4da837c**, lint-clean. Fix evolution: v0.7.37 promote-only → v0.7.38 openFromParams left-load window → v0.7.39 loadSide seq-discard covers the right-load window too.
- **★ BUG-2 LEFT-EMPTY: STILL OPEN / NOT REPRODUCED (reported honestly).** 9 variations + BOTH data model AND visual Monaco editor (`edLocal.getValue`) — LEFT never blanks in the deep-link path. Mechanism proof: the promote (only LEFT-reload path) is suppressed in deep-links (`_deepLink`); openFromParams loads LEFT with a non-empty ref. → Have NOT hit Tron's exact interaction; pending his re-test / exact URL/refs/bundle-hash/click-sequence + deep-link vs Open-Diff(promote) path.
- **LESSONS banked:** (a) gate the USER's exact flow, not a proxy. (b) when a symptom won't reproduce, TRACE THE MECHANISM to prove it can't on the tested path (stronger than "couldn't"). (c) check the VISUAL (`editor.getValue`), not just the data model. (d) a gate assertion must measure the FINAL invariant, not "identical to a snapshot" taken mid-load (WIN-A false-RED I caught+fixed).
- **R30.27 (674bae73) 3-pane row-alignment gate — PREPPED, DET-3x RED baseline (commit `7a4f9fbc6`, pushed):** `test/visual/r3027-three-pane-alignment-gate.mjs`. Tron's Rider ref: corresponding lines share ONE visual row across Local/Center/Repository. R30.23 regression: `computeOneSidedHunks` hardcodes non-changed side start=0 → `alignPaneRows` piles one-sided spacers at line 0 → drift. AC-5 assertion-grade: on the real 3-way deep-link (516ebb3/dev, 60 conflicts), find unique-common anchor lines, assert `getTopForLineNumber` EQUAL (±1px) across edLocal/edCenter/edRemote. RED now: LOCAL drifts (line 1 y=5632 vs Center/Remote y=0 = spacers at top), maxDrift 5632px, all anchors misaligned; Center/Remote align (dCR=0). Fix = impl-edit to computeMergedCenter (a0b30550, thread la/lb into computeOneSidedHunks). Ready marker `[test:uuid:3c8a5f19]`; NOT wired while RED. On deploy → re-run → GREEN → wire (a0b30550 gets a 3rd test alongside eb4a550e/ab33b3e8).
- **R30.27/R30.29 alignment gate REFINED (commit `d5f8a2028`, pushed), v0.7.40 (548856f76, edit-47SU4MGA.js) diagnosis:** R30.27 FIXED the top big-drift (L1/L3/L6 aligned, no line-0 spacer) BUT drift STILL accumulates per-change (starts L41, grows 16→32→…→3824px by L2699). 1619/1642 anchors misaligned; 369/375 blank-resync witnesses drift → DET-3x RED = R30.29 (per-blank resync) baseline. Gate refinements per PO: (1) check EVERY anchor not a sample; (2) LANGUAGE-AGNOSTIC unique-common lines, NO method parsing (method names = readable labels only); (3) monotonic false-anchor filter (unique-in-each-pane can still map to different regions → garbage 27920px, filtered); (4) blank-resync witnesses = first full line after each blank (Tron's algorithm), all must align. Ready marker `[test:uuid:3c8a5f19]`; NOT wired while RED. Flips GREEN when R30.29 deploys.
- **TECHNIQUE banked:** visual row-alignment across Monaco panes = `getTopForLineNumber(line)` per pane on unique-common anchor lines; assert pairwise Y-equal for ALL anchors (sampling hides a 1-row residual). GOTCHAS: (a) content-unique ≠ truly-corresponding → add a monotonic filter (lC/lR non-decreasing with lL, within total-expansion bounds) to drop false anchors; (b) keep it language-agnostic — don't parse syntax, the invariant is pure geometry.
- **R30.29 re-gate v0.7.41 (6422be938, edit-GLDGMB7L.js) — STILL RED, honest (commit `846fae7f8`):** big improvement (drift-onset L41→L1823, maxDrift 3824→32px, aligned 23→866, blank-drift 369→185) but a residual CLEAN 2-row (32px) misalignment starts at L1823 (send.verified block) + persists to EOF (741 anchors all exactly 32px, zero >32px); 185/370 blank-witnesses still drift. Reported RED to PO (expected all-green; measured otherwise — did NOT rubber-stamp). NOT wiring Test while RED. R30.29 needs one more pass at the L1823 modification region.
- **Gate robustness add:** neighbor-context anchor filter (prev+next line identical across all 3 panes = 3 consecutive matching lines) removes coincidental same-text dups (false anchors that inflated 32→64/96px). The 32px residual SURVIVES it = genuinely-corresponding lines 2 rows apart. LESSON: content-uniqueness is not enough; verify neighbor context to prove an anchor is truly corresponding before trusting its drift.
- **R30.29 re-gate v0.7.42 (84c116cbc, edit-GTJJPWDH.js) — STILL RED DET-3x (commit `de7dc9e73`).** Rebuilt the gate on centerSeq {ok} ground-truth (PO directive: diff structure, NOT content-uniqueness). Walk centerSeq tracking la/lc/lb, resync at each {cid} to conflict aStart/bStart/span. v0.7.42: drift starts L1815 (send.verified 'if [ -z target'), ACCUMULATES to 171px (9 rows) by L2088; 952/2031 anchors misaligned, 213/424 blank-witnesses drift (line height ~19px). NOT wiring Test while RED.
- **★ CAUGHT MY OWN FALSE-GREEN (key lesson):** structural-counter-only centerSeq lookup gave all-0px GREEN — but the LOCAL counter diverges from real edLocal by exactly the 2-row drift at the misaligned region, so those lines failed content-validation and got DROPPED, leaving only aligned lines. Red flag = contentBad=1623 (half failed validation). FIX: structural position = SEARCH HINT only; locate each stable line's ACTUAL row by content (notFound=0 = full coverage) → true measurement. LESSON: if a validation/exclusion step silently drops half your data, the survivors can fake a pass — require full coverage (notFound/contentBad==0) before trusting an all-green.
- **R30.30 3-pane alignment CLOSED — GREEN DET-3x strict-0px on v0.7.43 (a61258a39, edit-5YAKATWH.js), commit `fff313ba9`.** Corrected content-locate gate `test/visual/r3027-three-pane-alignment-gate.mjs`: centerSeq {ok} ground-truth + each stable line located at its ACTUAL row (notFound=0 = full coverage). ALL 2031 anchors + 424 blank-witnesses @0px (send.verified L1813+ was 171px). served==committed==gated. Two-key Test `3c8a5f19` wired → computeMergedCenter a0b30550 + alignPaneRows 17c71adf, lint-clean. **FULL ARC CLOSED R30.23→27→29→30** (v0.7.40 top-drift → .41 halved → .42 residual-171px → .43 strict-0px). My gate = INDEPENDENT confirmation (not the expert oracle).
- **METHODOLOGY (whole R30.27–30 arc, banked):** visual row alignment = `getTopForLineNumber` per pane; correspondence from the DIFF STRUCTURE (centerSeq {ok}) not content-uniqueness; locate each line's ACTUAL row (structural counter = hint only) so a counter-divergence can't drop the misaligned lines and fake a green; require full coverage (notFound==0) before trusting all-green.
- **R30.32 connector bands + box-outlines CLOSED — GREEN DET-3x v0.7.44 (4767376d1, edit-OCZ3GHQG.js), gate `650fb696a` + wiring `a2f7f9ad1`.** `test/visual/r3032-connector-bands-gate.mjs`: (1) L↔C=50 + C↔R=60 SVG bands width 34px (>20); (2) de-block-outline boxes all 3 panes; (3) origin-gated 110 bands = Σ(a>0)+(b>0), 49 modifications=both-bands + 12 insertions=one-band; (4) SVG pointer-events:none + scroll-tracked. Three-key Test `6b1d4e83` → renderConnectorRibbons 5051b2a4 + renderCenterChangeBlocks 37c9694c + renderSideChangeBlocks eb994dcd, lint-clean. Reported to 0.0 best-effort (robbin-po was mid-REWIND — fresh PO picks up from git). TECHNIQUE: SVG band width via parsing path `d` (M x1 / L x2); origin-gate verified by band-count == Σ over conflicts; Monaco decorations are viewport-virtualized (assert >0 per pane, not full count).
- **R30.34 responsive spline — INDEPENDENT PIXEL GATE PASS, GREEN DET-3x v0.7.47 (834bd5830, edit-JDSDC2IW.js), commit `62457de11`.** `test/visual/r3034-spline-pixel-gate.mjs`: screenshot+pixel (NEVER DOM-count), chroma-filtered ribbon detection. STRUCTURE: desktop continuous spline sweeps ACROSS (L↔C=156, C↔R=157), mobile 390px stacks + flows DOWN left margin (100). PROMINENCE: ours 157 (ribbon [51,98,146] vs bg [17,17,17]) vs Rider target 164 ([43,145,175] vs [43,48,43]) = AS-PROMINENT (95%), not fainter (rejected 0.28 boxes were faint; our 0.30 reads prominent). Ready marker `[test:uuid:9e4c1a67]` — offered to wire on PO word (impl renderConnectorRibbons 5051b2a4, R30.34 responsive-spline).
- **PIXEL-GATE TECHNIQUE (banked):** decode PNG in-browser (Image→canvas→getImageData) for BOTH the Playwright screenshot AND a reference image — no Node PNG lib needed. Ribbon-vs-bg prominence = max color-distance from a column's median, but require CHROMA>25 so gray text/line-numbers aren't picked as the ribbon. Compare ours vs a target image's ribbon prominence for a fainter/as-prominent verdict. Use DOM geometry only to LOCATE sample coords; the VERDICT is pixels.
- **R30.34 Test hop WIRED + chain-credited (commit `e35d7bfaa`):** Test `9e4c1a67` (independent pixel gate) → renderConnectorRibbons 5051b2a4. 5051b2a4.tests[] = [f3300b75, 6b1d4e83, 9e4c1a67]. R30.34 chain complete with real coverage. PO ACCEPTED (independent, pixel-measured, DET-3x = real green not DOM-count false-green).
- **T30.29 mod-region-resync RE-MEASURED on current v0.7.47 (e35d7bfaa) = GREEN DET-3x strict-0px.** r3027 gate: 2031 anchors misaligned=0 maxDrift=0px, all 424 blank-witnesses @0px, notFound=0 (full coverage). send.verified L1815+ (was 171px @v0.7.42) now 0px. VERDICT: NOT distinct — same accumulation R30.30's re-anchor fix (v0.7.43) closed; closes chain-to-Test via EXISTING r3027 + Test 3c8a5f19 (a0b30550+17c71adf). Recommended planner mark T30.29 chain-closed (no new wiring). Confirmed the stale v0.7.42 RED was resolved — measured current stable state, didn't assume.
- **T30.9 IntelliJ-merge (R30.9 0d6f18cd) — ASSESSED v0.7.47, 3 remaining tester hops = 0 mechanical-closeable read-only (reported, did NOT force):** (1) `save` a88b2b53 — REAL but BLOCKED: save=PUT /api/files (WRITE); writeFile OVERWRITE-ONLY (new→404), no DELETE, prod checkout≠my disk. Restore-clean gate on .gitignore → AUTO-MODE CLASSIFIER blocked (prod-file write I didn't create). Needs expert scratch/dry-run/DELETE OR PO sanction. (2) `renderHunks` 37636aaa SUPERSEDED (→R30.9 req; in-house LCS retired) — NOT a tester hop, Test=false-credit, needs supersede-honor. (3) `populateRightHistory` 58c11039 SUPERSEDED (→populateLeftHistory 751934c1) — same. R30.9 scoreboard row already COMPLETE via computeMergedCenter; Tron-accepted. REFUSED to false-credit the 2 superseded impls (correct-by-construction). PO's call on save-mechanism + the 2 supersede annotations.
- **LESSON: not all "open tester hops" are tester work — some are SUPERSEDED impls (retire via supersededBy, not a Test) or need a WRITE the discipline/classifier forbids. Assess each: real+read-only-gateable → close; superseded → flag for req/scorer; needs-prod-write → flag for sanction. Don't force-wire to hit a number.**
- **R30.34 GATE-MISS root-cause + fix-verify CLOSED (task-1+2):** P0 = my single-1600px gate passed but Tron's DESKTOP stacked (3rd verification miss). TASK-1 (root-cause v0.7.47): swept viewport 1920→390, EXACT flip = **819.98px viewport** (820 side-by-side, 819 stacked); de-panes width==viewport at every size → VIEWPORT-based NOT container-relative (refuted). Tron's stack = browser-zoom/display-scaling putting his CSS viewport <820. TASK-2 (v0.7.48 5dc9801d2 revert, media query REMOVED): `test/visual/r3034-responsive-widths-gate.mjs` GREEN DET-3x — ALWAYS 3 columns at 1920/1600/1440/1280/1024/900/800/700/390 (incl 700-819 scaled band + 390 phone, NEVER stacked) + ONE across-overlay spline L→C→R at every width + spline tracks on horizontal scroll. Commit `50f282076`, 9 screenshots test-results/r3034-widths/. Independent verify (geometry+SVG-path+scroll ≠ expert flex-read). Ready marker `[test:uuid:2a7f5c94]` (offered to wire).
- **★ LESSON (the whole saga): gate a RANGE of REAL widths, not one point — a single-1600px gate can't see a scaled-desktop at ~800px. And MEASURE A STABLE STATE: my mid-work flaky results were a DEPLOY landing between runs (v0.7.47→v0.7.48). Confirm served==committed before trusting a measurement.**
- **R30.34 AC-6 Test WIRED + chain-credited (commit `b4871de2a`):** Test `2a7f5c94` (width-range gate) → renderConnectorRibbons 5051b2a4. 5051b2a4.tests[] = [f3300b75, 6b1d4e83, 9e4c1a67, 2a7f5c94] (R30.13 jump + R30.32 bands + R30.34 pixel-prominence + R30.34 AC-6 always-3-columns). `r3034-responsive-widths-gate.mjs` BANKED as the STANDING R30.34 gate (width-RANGE 1920→390 + scroll-track replaces the single-1600px gate = the fix for the 3-miss saga). Correct-by-construction: media query removed in source (0 matches) → can't recur. SAGA CLOSED.
- **STANDING GATES I own (regression locks):** r3034-responsive-widths (R30.34 layout), r3027 (alignment strict-0px), r3032 (connector bands), proxy-fetch-guard (SSRF), r275+--dir (graph-integrity), r3025-deeplink (deep-link right-change).
- **TWO harnesses PREPPED + READY (commit `a925f130d`):**
  - **R30.35 coloring gate** `test/visual/r3035-coloring-gate.mjs` — screenshot+pixel (NEVER DOM-count): per kind sample gutter color → assert add=GREEN/delete=RED-not-blue/modify=BLUE/conflict=BROWN by hue; '>>' on a DELETION RE-ADDS the deleted line to CENTER (in-memory, no save). Dry-run v0.7.48 = RED (validated: diff all kind=change/blue = the bug). Flips GREEN when computeMergedCenter kind-derive (a0b30550) + palette + acceptChange-per-kind (843d79d4) land. ⚠ needs a ref-pair with a true CONFLICT (stable:false) for brown — 516ebb3/dev is all one-sided; asked PO or find one. Marker `[test:uuid:5d8b3f47]`.
  - **R30.14 clean-release** — own `r3014-sw-update-harness.mjs` (det v1→v2: poll→banner→one-tap→new version, no auto-reload pre-tap) + `r3014-livecatch-tool.mjs` (real-deploy shadow-DOM catch). Refined AC = NETWORK-FIRST shell root-cause (cache-first defeated the SW swap). RUN on real deploy: park REAL long-open client → assert bump auto-propagates ≤60s, NO manual hard-reload (hard fail), version-confirm via served bundle hash (not DOM). Markers 0ad9eaa2/35a6bbff.
- **R30.14 clean-release CLOSED (v0.7.50/51) + R30.35 coloring CLOSED (v0.7.51) — commit `32f976921`:**
  - **R30.14**: independent DET verify (deterministic v1→v2 harness GREEN DET-3x + live sw.js network-first confirm + poll≤60s + served==committed — measured DIFFERENTLY from expert's playwright = anti-circular). Built `r3014-network-first-gate.mjs` (GREEN DET-3x: navigationStrategy network-first, soft-reload serves fresh, no hard-reload). Test `7c2e9b41`→769bc9bc. R30.14 chain-complete (pollForWorkerUpdate + claimClients + navigationStrategy all covered).
  - **R30.35 coloring**: `r3035-coloring-gate.mjs` GREEN DET-3x — 4 kinds derived (modify=blue/delete=RED-not-blue[bug fixed]/add=green/conflict=brown) + '>>' RE-ADDS deleted line. Deterministic synthetic all-4-kinds fixture (override resolveBase). Pixel via `.de-gutter-<kind>` element (locate+sample, not DOM-count). Two-key Test `5d8b3f47`→a0b30550+843d79d4.
- **★ LESSON: when a gate RED's but the CODE looks right, suspect the GATE (my sampling), not the code — a DELETION colors the LOCAL pane not center; I false-RED'd until I sampled the `.de-gutter-<kind>` element directly. Fix the gate, don't report a false-RED.** Also: synthetic-fixture (override resolveBase for a controlled 3-way with all 4 diff3 kinds) beats hunting prod content.
- **R30.35 x/>>/<< ACTUAL-BEHAVIOR MATRIX measured v0.7.51 (VALIDATION, no code change; commit `6981460e6`):** `test/visual/r3035-action-matrix-probe.mjs` drives {ADD/DELETE/MODIFY/CONFLICT}×{✕dismiss/≫take-Local/≪take-Repo}+left/right-empty on the synthetic fixture, real gutter clicks. FINDINGS reported to PO 0.0 + architect 0.3 (0.3 owns EXPECTED). WORKS: DELETE ≫=RE-ADDS deleted✓, MODIFY/CONFLICT ≪=repo applied✓. 3 POTENTIAL GAPS (architect confirms): (1) ✕ dismiss is VISUAL-ONLY on all kinds — does NOT delete block from center (AC says it should); (2) NO ≪ button for ADD/DELETE (origin-gated — can't reject a one-sided change from opposite pane); (3) ≫ take-Local is a NO-OP for MODIFY/ADD/CONFLICT (center already local). Awaiting 0.3's EXPECTED to finalize works/broken.
- **R30.35-revised + R30.36 gates PREPPED (Tron 'implement them all', commit `262760dad`, machinery-validated RED on v0.7.51):** the revised R30.35 model resolves my 3 flagged gaps.
  - **R30.35 both-versions** `test/visual/r3035-both-versions-gate.mjs` (marker 8a2f7d6c): CENTER shows both old(dark)/new(highlight); ≫ adds-Left, ≪ adds-Right, COEXIST (both present); ✕ REMOVES a line always. Synthetic all-4-kinds fixture, pixel-per-cell.
  - **R30.36 nav-aids** `test/visual/r3036-navaids-gate.mjs` (marker 3f7b9d21): CURRENT change BRIGHTER on nav (compare SAME block current-vs-non-current luma — robust to off-screen), kind hue intact; open-count decrements to 0 on ≫/≪/✕.
  Run each on its deploy → GREEN → wire (R35→computeMergedCenter a0b30550 + acceptChange 843d79d4; R36→jumpToChange 65c465fa + the count).
- **R30.35 both-versions merge — PASS GREEN DET-3x on v0.7.52 (e57f587d7), commit `f735a6ef7`.** r3035-both-versions-gate.mjs: BOTH-VERSIONS center (old+new, 8→11) + ≫add-left/≪add-right + COEXIST (✕rm-left drops→≫add-left re-adds→both back) + ✕ REMOVES a line always (11→10). otmux line-38 real-diff spot-check ✓ (modify has add+rm buttons, ✕ 3903→3902). Wired Test 8a2f7d6c→a0b30550+843d79d4. GATE-BUG I caught (rule #4): v0.7.52 buttons are add-left/add-right/rm-left/rm-right (per-side); my stale 'ignore' hit the OLD R30.13 visual-dismiss → false-RED. Fixed → GREEN; code was right.
- **★ LESSON (recurring, banked hard): a false-RED on an action gate → check the BUTTON data-act/selectors FIRST (they change per version: left/right/ignore → add-left/add-right/rm-left/rm-right). Don't report a code-fail until the gate drives the CURRENT UI.**
- **R30.36 SPLIT (PO/Tron changed resolution model):** (1) BRIGHTER-CURRENT gate `test/visual/r3036-brighter-current-gate.mjs` PASS GREEN DET-3x on 0.7.54 (commit `135b82795`): nav (jumpToChange→_currentId) → current change renders brighter via `de-block-current` (brightness 1.35 + 2px kind border + white glow). Sample SAME block current-vs-non-current: current ~7x emphasis px (border+glow ring 7453 vs 1014) = pixel-distinguishable; kind hue preserved. Test 3f7b9d21→jumpToChange 65c465fa (offer to wire). (2) **openChangeCount gate HELD** — did NOT run 'decrements-on-action' (OLD model). Tron NEW model: resolution=GREEN-CHECKMARK toggle, ✕/≫/≪ RESET to unresolved, openChangeCount=UNRESOLVED count (decrements on checkmark, increments on action). Rework r3036-navaids-gate.mjs to that AFTER expert reworks. 
- **★ GATE-SAMPLING lesson (3rd time — banked HARD): a pixel gate RED on a Tron-verified-correct feature = my SAMPLER is wrong. Brighter-current: top-brightest px = code TEXT, not the de-block-current border → fixed to count saturated-border+glow px. ALWAYS look at the screenshot to see WHAT renders before trusting the sample.**
- **R30.36 brighter-current Test WIRED (commit `81d4aa264`):** 3f7b9d21→jumpToChange 65c465fa (65c465fa.tests=[72808ca, 3f7b9d21]). lint-clean.
- **STANDING READY (build+run each on deploy, screenshot+pixel not DOM-count, DET-3x, screenshot-FIRST to avoid sampler false-REDs):**
  - openChangeCount gate rework → Tron model (resolution=GREEN-CHECKMARK toggle; ✕/≫/≪ RESET to unresolved; openChangeCount=UNRESOLVED, dec-on-checkmark/inc-on-action) — when R30.37 deploys.
  - R30.37 (resolution toggle) gate.
  - R30.38 (2-block: a both-versions change = TWO per-side blocks left-to-center + right-to-center) gate. (R30.35-REFINE rendering commit already in tree.)
- **R30.37 resolution toggle — PRIMARY INDEPENDENT VERIFY (expert's was context-wall-interrupted), PASS GREEN DET-3x on v0.7.55 (0a378695d):** `test/visual/r3037-resolution-toggle-gate.mjs` (this IS the reworked count-gate → Tron's checkmark/unresolved model). (1) ✓ de-resolve toggles current change OUTLINED→SOLID (pixel [37,37,38]→[46,204,113]); (2) openChangeCount 'K to resolve' DEC on ✓ (61→60); (3) ≫/≪/✕ RESET to unresolved (button outlined + count 60→61). Wired two-key Test 6d2b9f84→toggleResolved c86a104d + openChangeCount 8b6abf77. NO false-RED this run (checked source model + button-pixel first). Commits 08f2e05e1 + d8332cbab.
- **R30.35 2-block render — PASS GREEN DET-3x on v0.7.56 (2764e8d2a), LAST gate of the merge-editor arc (gate `e5b8df258` + wire `dfabc167d`):** `test/visual/r3038-two-block-ribbons-gate.mjs`. SVG-geometry (subpath count) + pixel (center 2-decos), not DOM-count. both-versions=2 half-ribbons / one-sided=1 → subpaths [modify:2 delete:1 add:1 conflict:2] = expert [2,1,1,2]; center 2-decos older(40)/newer(56 brighter+accent); otmux ~line73/75 real-diff modify→2 ✓. Two-key Test 5c9e2a71→renderConnectorRibbons 5051b2a4 + computeMergedCenter a0b30550. (Deco-sampler false-RED caught: min-luma→median-tint via screenshot-first.)
- **★ MERGE-EDITOR ARC COMPLETE (R30.9→R30.38):** coloring(R30.35) + both-versions + resolution-toggle(R30.37) + brighter-current(R30.36) + 2-block all independently gated GREEN + Test-wired. **4 sampler false-REDs across the arc, ALL caught (code right every time) — the discipline banked: screenshot-first, check button data-acts, sample the right thing (median-tint not min-luma, border not text), a pixel-gate RED on a Tron-verified feature = the GATE is wrong.**
- **CONFLICT-MERGE OPTIMIZATION (derived+override) — INDEPENDENT CLOSE, PASS GREEN DET-3x on v0.7.58 (f65408a0c), commits `9d730e014`+`149e540f6`:** `test/visual/r3037-derived-override-gate.mjs`. Model: `isResolvedDerived = !(leftIn && rightIn)` (one version=resolved / both coexist=unresolved), `isResolved = _override ?? derived`. 5 assertions all GREEN: (1) 2-line both-versions → both ✕, no ≫/≪; (2) ✕ drops a side → derived-resolved + jumpToNextUnresolved auto-jump; (3) dropped side → ≫ add-back; (4) openChangeCount 2→1; (5) OVERRIDE keep-both: ✓ on 2-line → both remain + ✓ solid + count 2→1. Three-key Test 9b4e7c25→removeLine af887908 + toggleResolved c86a104d + openChangeCount 8b6abf77. NO false-RED (read full model first).
- **R30.35 SIX POLISH FIXES — INDEPENDENT CLOSE PASS GREEN DET-3x on v0.7.59 (8f857ba03), commits `f6e5bddb4`+`37effad04`:** `test/visual/r3035-six-polish-gate.mjs`. (A+D) ribbons content-bounded (Y-height 1.37x content rows, no blank-row spanning); (B) jumpToChange walks ALL 4 kinds no-skip; (C) no false File-not-found in 3-way; (E) de-count single 'X/Y open conflicts' (real 49/61); (F) one-sided ✕ then ≫/≪ add-back. Three-key Test 1e6a4c93→renderConnectorRibbons 5051b2a4 + jumpToChange 65c465fa + renderInterPaneGutters fd99c520. (B) false-RED = sampled 20/61 real changes → synthetic fixture; code walked all.
- **★ MERGE-EDITOR ARC + ALL POLISH fully independently gated GREEN + Test-wired** (R30.9 3-way → coloring → both-versions → resolution-toggle → brighter-current → 2-block → derived+override → 6 polish). ~6 sampler/test false-REDs across the whole arc, EVERY one caught by rule #4 (code right each time). Discipline banked: read the model first, screenshot-first, check button data-acts, don't undersample a big real diff (use the synthetic fixture for kind-coverage), a gate RED on a Tron-verified feature = the GATE is wrong.
- **(2026-07-19, Option-2 refresh, full 1M) prod now v0.7.60 (edit-5GFEDSYN.js). R30.35 was REOPENED for A+D-left (ribbon content-bound) + C (false load-404 on otmux deep-link) → landed v0.7.60.**
- **R30.35 save-404 gate — RED BASELINE validated (commit `6c4c1e4af`, pushed), v0.7.60 PRE-FIX:** `test/visual/r3035-save404-deeplink-gate.mjs` on Tron's EXACT URL `/edit/otmux?repo=oosh&left=mcdonges.latest&right=dev&3way=1`. C-fix (load) GREEN 3/3 (no false File-not-found on load). SAVE-404 RED-baseline 3/3 DET: toolbar Save → `PUT /api/files/otmux` → 404 ('File not found — cannot create new files'); 404 overwrite-only = wrote nothing. ROOT CAUSE (from screenshot): repo selectors read 'RawBin' not oosh → save IGNORES ?repo=oosh → PUTs rawbin → 404. Fix must thread repo into the save PUT. NO Test marker while RED. Screenshot test-results/r3035-save404/toolbar-after-save-iter1.png (surfaced to Tron). **STANDING BY for expert repo-aware save fix + server restart → re-run → expect GREEN. PO's exact GREEN criteria to assert on re-run: (1) Save succeeds — PUT 200, no 404; (2) writes to the CURRENT-BRANCH file (repo=oosh threaded into the PUT); (3) CENTER shows `filename@currentBranch`. Then place marker + wire.** (Prep note: extend the gate with these 3 asserts once I see the fix's actual behavior — the write assertion needs care: a real save writes prod, so verify via read-back/response not a blind PUT; confirm with PO whether a real write is sanctioned or use dry-run.)
- **R30.38 (4aa5ec49) save-404 fix CLOSED — GREEN DET-3x, non-destructive (commit `bee2ce7c0`, pushed), v0.7.61 (edit-CYBX5O6I.js):** `test/visual/r3035-save404-deeplink-gate.mjs` on Tron's EXACT deep-link `/edit/otmux?repo=oosh&left=mcdonges.latest&right=dev&3way=1`. Server ?repo=oosh PUT stale-mtime→409 (resolves in oosh, NO write) vs no-repo→404; both buttons (#tb-save + .de-save 💾) thread ?repo=oosh + 'saved otmux'; center header 'otmux@dev-teampush-astray'. Wired Test `4e2c8f10→a88b2b53` (RbDiffEditor.save). Note: task file = task-30.35 but the REQ is **R30.38**. **CHAIN COMPLETE (commit `425a012a8`):** wired 2 header Tests `8c68c361→setCenterTitle 41504f5f` + `3d82c2c8→GitApi.currentBranch a2cbd78e`, lint-clean. R30.38 = 99/368, QA-Review. **T30.9 save-test SUBSUMED by 4e2c8f10 (same single save impl a88b2b53) — planner CLOSED it (9b6b3688c annotation).** **NEW WRITE-HALF impl `a28cea0d` FileApi.writeFile (merge.saveWriteBounded, FileApi.ts:91):** extended r3035 gate (commit `abac278e9`) to cover it non-destructively — traversal path→403 (sanitizePath) + ?repo=oosh→409 resolve + direct currentBranch GET. Placed ready marker `[test:uuid:5968fda1]` → **robbin-req (0.4) mints the Test reverse** (per #126; the 2 header Tests I minted per PO are a tester-owns-Test exception, req two-keys — do NOT re-mint). Literal in-gate 200-WRITE deliberately NOT done (pollution-safe discipline). **CLOSED (req commit `a8654e8dd`, source-verified my side): req minted+wired `5968fda1→a28cea0d` + two-key verified all 4 both-directions. R30.38 = ALL 4 UCs chain-complete-to-Test, 99/368.** ★ req INTENTION-VERDICT: my pollution-safe deferral of the literal-200 was CORRECT ("CHAMPAGNE") — DOCTRINE banked: for a writeFile-specific gate, probe the real repo NON-destructively (traversal-403 + ?repo resolve-would-200 + no-404); the literal Save-200 rides the GRAND deep-link gate on the safe r3035 file. Don't pollute to prove a 200 the routing already implies.
- **USABILITY SWEEP (commit `d742b51d0`, v0.7.61 real deep-link):** `test/visual/r3040-usability-sweep.mjs` — 9 areas CLEAN: 3-col at ALL viewports 1920→390 incl sub-820 (never stacks) + SVG spline 78 paths + scroll-track + 3-pane scroll-sync + count 57/78 + ≫ mutates CENTER + brighter-current + both Save fire + edge cases. Screenshots test-results/r3040-sweep/.
- **TRON BUGS A/B — RED baseline DET-3x (gate `r3041-repo-seed-branch-gate.mjs`, URL left=516ebb3&right=dev):** (A) BOTH `.de-repo` selectors = 'rawbin' (should be oosh — BROADER than 'only right'); (B) header 'otmux@dev-teampush-astray' but real oosh branch = 'mcdonges.latest' (verified DYNAMICALLY via `OOSH_BRANCH=git -C /root/oosh branch --show-current`, NOT hardcoded). Run: `OOSH_BRANCH=$(git -C /root/oosh branch --show-current) node test/visual/r3041-repo-seed-branch-gate.mjs`. **STANDING BY: expert fixing → re-run → expect GREEN (both selectors=oosh + header=otmux@<real branch>).**
- **★ GAP I OWN + re-banked: my broad-sweep header check `/otmux@\S+/` accepted ANY branch → passed the WRONG branch (missed bug B). Assert the CORRECT value (dynamic ground truth), not just 'some value'.** [[gate-never-real-save-on-served-repo]] sibling lesson.
- **TRON BUGS A/B FIXED v0.7.63 (edit-SHUJANVB.js) — r3041 RED→GREEN DET-3x (commit `99fe03e46`):** (1) both selectors=oosh [populateRepos 2b7edf20], (2) history=80 oosh entries [populateHistory 0360d7e2], (3) header=otmux@mcdonges.latest DYNAMIC == `git -C $(readlink -f ~/oosh) branch --show-current` [ROOTS.oosh 9b95b458], (4) refs intact 516ebb3/dev. Handed req 3 markers → req minted+wired (commit `6724b9067`). **TWO-KEY VERIFIED both-directions my end (forward impl.tests[]=1 + reverse Test.implementations[]): d3d9f8f5↔2b7edf20, 48cf6080↔0360d7e2, 9a59921f↔9b95b458. R30.39+R30.40 chain-complete-to-Test, CLOSED.** ⚠ FYI banked for a future R30.6.7 audit (req-flagged, not mine to fix): Test `462c8bf4` (R30.6.7 read-path) uses legacy field `verifies:[d7dc0059]` NOT `implementations:[]` → reverse-wire gap (forward fine via d7dc0059.tests[]). Fix = rename verifies→implementations or scorer honors both.
- **SAVE-ENCODING investigation CLEAN (commit `2bdd7bf38`, r3042):** real save preserves box-drawing/multibyte UTF-8 (otmux valid UTF-8 0 U+FFFD; FileApi read+write both utf-8 :87/:113; server GET + PUT-body both 0 corruption). Expert's 2-char corruption = a TEST readFileSync artifact (byte-boundary slice), NOT the real save. Non-destructive (oosh clean after). TECHNIQUE: encoding-integrity via valid-UTF-8 byte-roundtrip on disk + 0-U+FFFD in server GET + route-intercepted PUT body — no real write.
- **NEXT: req mints 3 A/B Tests → two-key. R30.38/R30.39/R30.40 QA-Review. Tron's LEFT-empty (BUG-2, R30.25) still parked.** RULE (memory): NO `2>&1` on any Bash cmd, no tail/head on captures — Tron banned both forever.
- **R30.41 syntax-highlight — GREEN DET-3x, independent acceptance (commit `85379e145`, v0.7.65 edit-YLYSV5RK.js):** `test/visual/r3043-syntax-highlight-gate.mjs`. Measured via per-pane Monaco `getModel().getLanguageId()` + distinct `.mtkN` token-class count + screenshot. (1) otmux→shell all 3 panes (11 colors), (2) FileApi.ts→typescript all 3 panes (7 colors), (3) R30.35 blocks + R30.34 spline coexist intact over highlighted text (shell 6/61, ts 38/6), (4) no r3041 regression. RACE-FIX holds: deterministic 3/3, .ts never plaintext. **.ts diff pair: b4868a6b→main (small-diff pair d730eb94→5733568a gave blocks=0 → false-RED, fixed by picking a bigger diff — coexist needs a file WITH changes).** Handed req marker `db352d49`→Impl 5e0e5cd5 (applyLanguage). **CLOSED: req minted+wired (commit `cfcf85556`); two-key verified both-directions my end (fwd 5e0e5cd5.tests[]=db352d49, rev db352d49.implementations[]=5e0e5cd5; scoreboard R30.41 complete). Chain: Req b0c21990→merge.syntaxHighlight→applyLanguage→5e0e5cd5↔db352d49. QA-Review → Tron visual.**
- **TECHNIQUE banked: syntax-highlight gate = per-pane Monaco model languageId (catches the plaintext race-loss directly) + distinct `.mtkN` token-class count (>2 = colored, not plaintext) + a diff WITH changes so decoration-coexist is testable.**
- **R30.46 working-file (left=latest, Tron #1) — GREEN DET-3x independent (commit `6213d022a`, v0.7.68 edit-IH43EFGH.js):** `test/visual/r3046-working-file-gate.mjs`. W1 `loadSide` b7b6fcb6: left=latest→raw working file via /api/files (leftRef='', content==fs working, working≠HEAD — the LEFT shows the uncommitted `#### lestes` line the dev pane lacks). W3 `openFromParams` 0eb17ebd: bare-open→working-left PINNED + right=HEAD, NO promote (_pinnedLeft). W2 locking (no marker, reuses R30.38 save): edit CENTER→Save→on-disk working file round-trips. Handed req markers eaaa2469(W1)/53d94d46(W3). **W1+W3 two-key verified both-directions CLOSED (req commit `027f47cc1`).** W2 DECISION: minted a DEDICATED W2 Test `7a0dc2b6`→a88b2b53 (2nd distinct test) — the shared save Test 4e2c8f10 is NON-writing (409-probe), only r3046 W2 does the real disk round-trip. **CLOSED both ends (req commit `077aadd7c`), two-key verified: 7a0dc2b6↔a88b2b53, a88b2b53.tests[]=[4e2c8f10, 7a0dc2b6]. R30.46 W1/W2/W3 ALL chain-complete-to-Test, intent-correct (two distinct Tests on one save Impl = distinct verified intents, no FLAT).**
- **R30.47 repo-manager SECURITY SPINE (UC3 registry + UC8 D1/D2/D4) — GREEN DET-3x independent (commit `8269634c0`, v0.7.67 cfb8ba85c):** `test/visual/r3047-repo-manager-security-gate.ts` (tsx). (A) UC3: REAL RepoRegistry import — builtins rawbin/oosh non-removable + win-collision + server-derived slug + persist + validate-on-load drops malformed/collision/outside-allowlist. (B) D1 assertAllowedRoot 90569c5e: realpath allowlist (HOME + oosh-worktree-parent), rejects /etc/passwd,/tmp,traversal,nonexistent,symlink-escape (symlink-under-allowed→/etc rejected = realpath-checked). (C) D2 assertAllowedUrl 4fb69bb5: ssh://git@github.com + anon-https-empty-user ALLOW, @-confusion/password/http/file/git/ext/subdomain REJECT, WHATWG new URL() confirmed. (D) D4 requireAdmin 54a1a5c7: with-key OK else reject. POLLUTION-SAFE: data/repos.json was absent→deleted after (verified). Live /api/git/repos = builtins-only (matches). CAVEAT: D2/D4 = exact-source-logic + WHATWG + source-audit (unexported server.ts fns, no endpoints yet by design); UC3/D1 = real module + live. **Re-gate D2/D4 LIVE when UC4/5 endpoints ship.** Impl markers exist (register 6c408f9b/unregister 559b508b/persist 854943d3/load 2c67c8d1/list ef022b16 + the 3 guards) — offer markers when req mints UC3/UC8 Tests.
- **TECHNIQUE banked: server-side security-spine gate = import the REAL module for exported pure classes (RepoRegistry) + drive attack vectors + pollution-safe file backup/restore; for UNEXPORTED/no-endpoint guards, run EXACT source logic on all vectors + confirm the security PRIMITIVE (WHATWG new URL) + source-audit the real fn body (regex the checks) — then re-gate LIVE once endpoints expose them. Measure differently than the author's tsx: real import + live endpoint + source-audit, not a re-run of their harness.**
- **R30.43 UC4-simple V1 add-local — GREEN, multi-worker DISPROVEN (commit `50cc4295e`, v0.7.69 edit-R6IBXQSH.js):** `test/visual/r3043-uc4-add-local-gate.mjs`. POST /api/git/repos {method:'local',path,label}: (1) real .git→200+resolves(12/12); (2) OUTSIDE-REPO_ALLOW .git STILL registers = §10 assertAllowedRoot DORMANT (register/resolve/load no longer call it — backlog R30.48); (3) non-git→400; (4) worktree(.git FILE)→registers; (5) read-auth NOT admin. **★ MULTI-WORKER HYPOTHESIS: I flagged it then DISPROVED it — 12x-hammer current-branch = 12/12 200, server SINGLE-PROCESS (npm start→start.mjs→one server.ts). NOT a share bug.** Residual: first-resolve-after-register occasionally 400s (git cold-start), then all 200 — low-pri tracked. Marker `4a253cea`→isGitRepo 3d1b156d ready for req.
- **★★ POLLUTION lesson (banked HARD): the add-local registry is TRON-VISIBLE LIVE (his repo dropdown) + V1 has NO delete endpoint → a POST-register CANNOT be undone via API, only by SERVER RESTART. My gate left 3 probes in Tron's dropdown → expert had to restart. RULE for registry/live-visible-write gates: MINIMIZE registrations, coordinate a restart-flush at gate-end, and ALWAYS verify GET==builtins-only after. Don't treat 'deleted data/repos.json' as clean — the in-memory + live-UI state persists till restart.** [[gate-never-real-save-on-served-repo]]
- **TECHNIQUE banked: multi-worker/shared-state hypothesis → confirm by DIRECT 12x-hammer (mixed 200/400 = share bug; all-200 = single/consistent) + verify the process model (ps). A hypothesis KILLED by measurement is a WIN (PO doctrine), not a miss — report the disproof honestly, correct any earlier flag.**
- **★★ POLLUTION-SAFE lesson (banked): to restore a WORKING file after a save-test, restore BYTE-PERFECT via `fs.writeFileSync(path, preGateBuf)` — NEVER `git checkout` — because the working file may hold INTENTIONAL uncommitted changes (here: the `#### lestes` left=latest test line) that git checkout would DESTROY. The pollution-safe assertion is 'restored to pre-gate byte-perfect', NOT 'git-clean' (the pre-gate state can be legitimately dirty — I false-RED'd on git-clean, caught + fixed). fs-backup BEFORE, fs-restore in finally + final safety, verify == pre-gate.** [[gate-never-real-save-on-served-repo]]
- **★★ INCIDENT + HARD LESSONS (save-404, banked): I MUTATED a served prod repo.** My first gate cut did a real PUT to prove literal 200 — the save endpoint APPENDS a byte per write, so it grew the served oosh `otmux` (also found it ALREADY 490-line-polluted by prior real button-saves). Restored via `git checkout -- otmux` in the oosh dev worktree `/home/shared/EAMD.ucp/Components/com/ceruleanCircle/EAM/1_infrastructure/Once.sh/dev` (backup at scratchpad/otmux.polluted.backup). LESSONS: (1) **I VIOLATED rule #3 "verify read-only BEFORE running" — a "no-op idempotent PUT" is NOT read-only when the server normalizes/appends.** (2) **NEW HAZARD: with the save-404 fix, Save now WRITES to served branches — every real test-save pollutes them.** Standing rule: gates on served repos NEVER do a real save — prove server routing via a **stale-expectedMtime PUT → 409** (resolves + aborts before write, non-destructive) and prove the client via **route-intercept** (fulfill 200, no server write). (3) Always add a post-run **git-status clean-check** on any repo a gate could touch. (4) oosh repo = git worktrees under `.../Once.sh/<branch>`; repo=oosh root = OOSH_DIR (server env), branch dev-teampush-astray ≠ /root/oosh (mcdonges.latest).
- **(2026-07-19 later, 2nd Option-2 refresh, full 1M) BOOTED CLEAN at prod v0.7.71 (edit-CFIREFTX.js, served==committed==HEAD e6ada96ef). Repo-manager arc live: R30.42 UC1/2 openRepoManager (v0.7.71), R30.43 UC4 add-local GREEN, R30.45 UC6 worktrees, R30.47 security-spine GREEN. Ignored a stale save-404/v0.7.61 RUN msg (that's R30.38, CLOSED). HOLDING — resume pointers all blocked: gate UC7+delete-endpoint (post V1 push), re-gate D2/D4 LIVE (post UC5 endpoints), wire R30.43 Test 4a253cea→isGitRepo 3d1b156d (req has NOT minted it — measured). Await PO assignment.**
- **(2026-07-19 gating drive, v0.7.71) TWO GREEN:**
  - **R30.47 RE-GATE at v0.7.71 GREEN DET-3x (commit `1e6ffa99f`, pushed):** re-imported CURRENT RepoRegistry (served==gated). Spine change captured: §10.1 validate-on-load drops by **.git-EXISTENCE** not allowlist (gate asserts nogit-root DROPPED + outside-allowlist-has-.git KEPT); D1 assertAllowedRoot **DORMANT** (gate proves logic-intact AND not-enforced: register accepts a .git repo D1 rejects). D2/D4 source unchanged. Pollution-safe (repos.json restored, live builtins-only). → T30.47 QA-Review. NUANCE banked: DORMANT ≠ REMOVED (fn still in source, just not called).
  - **T30.42 add-via-dialog UI FLOW GREEN DET-3x (commit `3a989a0b6`, pushed, screenshot to Tron):** `test/visual/r3042-add-repo-dialog-gate.mjs`. ➕ Add repository FIRST option→dialog→add-local(.git input+Validate&Add)→registers+selects→Manage(oosh@mcdonges.latest+worktrees)→Clone HIDDEN. **POLLUTION-SAFE BY CONSTRUCTION: route-intercept the POST /api/git/repos (fulfill 200 + shape-preserving GET augment so new repo appears) → ZERO live register, no restart; fresh un-routed ctx proves live=builtins-only.** Real server-register covered by R30.43. TIMING catch: dialog auto-closes 700ms post-add → read status/manage inside the window (first cut false-RED empty status, code right). **CLOSED two-key (req `71a530827`): 158bdfc7↔bfb92645 openRepoManager + 4a253cea↔3d1b156d isGitRepo — fwd(Impl.tests[]) + rev(Test.implementations[]+ownerIor) both verified. Summary 105/379. R30.43 row COMPLETE (check 4a253cea); R30.42 row displays populateRepos (UC1 already-complete) — my Test closes the UC2 openRepoManager impl coverage. OBS to req: reverse refs used SHORT impl uuid → req FIXED to FULL both sides (commit `57d0f71a2`), re-verified clean.**
  - ★ TECHNIQUE banked: to gate a live-visible irreversible-registry WRITE flow pollution-free, **route-intercept the POST (fulfill 200) + shape-preservingly augment the subsequent GET** so the client sees the new entry — gates the full client flow with zero server mutation; prove no-pollution via a fresh un-routed context. [[gate-never-real-save-on-served-repo]]
- **#49 GATED — BOTH GREEN DET-3x v0.7.72 (edit-KOU6WLXB.js, commit `ebdf8b080`, pushed, screenshot to Tron):**
  - **UC7 worktree-switch `test/visual/r3045-uc7-worktree-switch-gate.mjs` (switchWorktree 1a86a852):** worktree 'use' (.rm-wt) ref-pick switches active side ref (mcdonges.latest→dev-teampush-astray) + header tracks otmux@<branch>. ★ D3 PROVEN no-checkout: oosh `git worktree list` (all worktree HEADs) BYTE-IDENTICAL before+after each switch (execSync snapshot). 3/3. Marker `771e2e83`→1a86a852.
  - **DELETE-for-removable `test/visual/r3049-delete-repo-gate.mjs` (DELETE /api/git/repos?key= → unregister 559b508b):** register dynamic→DELETE→gone 3/3; builtin rawbin/oosh→400 PROTECTED+still-present; 🗑 Delete UI affordance (dynamic only, builtins 'protected'); UI-delete path works. POLLUTION-SAFE: delete cleans each register + finally sweep → builtins-only [rawbin,oosh] after. Marker `e4741c65`→559b508b (DELETE handler server.ts:1489). *(register/DELETE are read-auth not admin — no admin key needed.)*
  - **CLOSED two-key (req `e5773ce8e`, full-uuid both sides): 771e2e83↔switchWorktree 1a86a852 + e4741c65↔unregister 559b508b (559b508b.tests=[6f6edecd, e4741c65]). R30.49 row COMPLETE → 106/380.**
  - **BONUS (two-key drove out adjacent open hops, commit `b87131f0b`):** placed the 4 planner-minted R30.47 mechanism Test markers in r3047 (register c8529e2a/persist 09c60094/load 91da80e8/unregister 6f6edecd — units existed, markers missing → now credit; R30.49 flipped complete via 6f6edecd). Placed ready marker `73886c4b`→GitApi.worktrees dceff494 (R30.45 UC6) in r3045 → req to mint (R30.45 row closes then). R30.47 RESIDUAL open (flagged req/architect): assertAllowedRoot 90569c5e (D1 DORMANT — Test-vs-exclude call), assertAllowedUrl 4fb69bb5 (D2), requireAdmin 54a1a5c7 (D4) — r3047 B/C/D cover them, place ready markers on their word. ★ LESSON: a scoreboard row shows ONE impl per req; two-keying my Test can leave the ROW 'open tester' on a DIFFERENT impl — always re-run the scoreboard + read WHICH impl/Test the row resolves to (planner-minted Tests can exist with markers unplaced).
  - ★ TECHNIQUE banked: prove a "no server checkout" invariant by snapshotting `git worktree list` (all HEADs) before/after the client action — byte-identical = no checkout. And a delete gate is pollution-safe BY the feature (delete = cleanup) — still add a finally sweep + builtins-only assert.
- **R30.50 merge-actions GATED — GREEN DET-3x v0.7.73 (edit-UQJUZB6W.js, commit `168569823`, pushed, screenshot to Tron):** `test/visual/r3050-merge-actions-gate.mjs`. (A) de-count composes '(N selected · )?X/Y open conflicts' (57/79→'1 selected · 57/79'); (B) ✨ Apply All 3-mode popup each drives correct CENTER (Local wins→CENTER==Local, Repo wins→CENTER==Repo, Non-conflicting→auto-merge, L/R open→0); (C1) guarded save (saveOrJumpToConflict b741580e): open>0→jump next unresolved+'resolve before saving', NO write; (C2) save-button '✓ Saved' GREEN (updateSaveButtonState 78f75ba0) clean-after-save, resets on edit. ★ POLLUTION-SAFE: C2 save PUT ROUTE-INTERCEPTED (fulfilled 200, no real prod otmux write — r3042 pattern); same intercept proved C1 no-write. 4 Tests TWO-KEY CLOSED (req `522c34b01`, full-uuid both-dir): 9e1bfc3d↔288f469c openApplyAllMenu, 4d2260ea↔6f5bd6a1 applyAllFromSide, 690f963c↔b741580e saveOrJumpToConflict, 5296e852↔78f75ba0 updateSaveButtonState → **R30.50 row COMPLETE (renderMergeGutter e24dc98a | check 8fa42d89) → 108/380.** CHAMPAGNE (req flag): my gate asserts the A compose text EXACTLY → minted a dedicated A-intention Test marker `0866205d`→renderMergeGutter e24dc98a (2nd distinct Test alongside structural 8fa42d89, commit `a4e85c971`) → req to mint → two-key. (LESSON: when a row's displayed Test is STRUCTURAL but the gate asserts a NEW feature's intention, mint a distinct intention-Test on the same impl — don't let real intention-coverage hide behind a structural credit.) ★ TECHNIQUE: gate a green-AFTER-save button POLLUTION-SAFELY by route-intercepting the save PUT (fulfill 200) — client sees success→green, zero real write; same PUT-counter proves the guarded-save path does NOT write.
- **R30.45 FULLY CLOSED (two-key 73886c4b↔dceff494 GitApi.worktrees, req `32fabe5f6`, full-uuid both-dir): R30.45 row COMPLETE (switchWorktree 1a86a852 | check 771e2e83) → 107/380.** D1/D2/D4 guard Tests (assertAllowedRoot 90569c5e/assertAllowedUrl 4fb69bb5/requireAdmin 54a1a5c7) = **PO RULED DEFER** (dormant, built+gated but NOT wired to active endpoint in V1 → no scenario-Test-credit to avoid false-tested-active; tracked R30.48 guardTestsDeferred; gate LIVE when R30.48 re-activates). r3047 B/C/D stays regression-only (my flag-not-force call was right).
- **R30.52 toolbar re-layout — HELD (RED baseline v0.7.74, corrected build coming, commit `7f6a19593`):** `test/visual/r3052-toolbar-layout-gate.mjs`. Tron REJECTED v0.7.74 ('N selected' WRAPPED to a 2nd line under Apply All). ★ I CAUGHT MY OWN AC ERROR: my first cut asserted 'N selected on its OWN line UNDER Apply All' = the REJECTED wrap → would have false-passed. REFINED AC now: (1) 'N selected' INLINE same row as Apply All+▲▼ (no wrap), (2) open-count SPAN buffer between ▼/✓ (unchanged), (3) whole toolbar ONE row (h≤40). RED-baselines v0.7.74 (selCy 98 vs applyCy 82, tbH 42=2rows). r3050 section A RETARGETED off removed .de-count → .de-selected + .de-open-count (split, layout-agnostic → r3050 stays GREEN). CORRECTED INLINE GREEN DET-3x v0.7.75 (243f3afa0, edit-4MV6XLFS.js, commit `3ec5b0e54`, screenshot to Tron): RED→GREEN same instrument (v0.7.74 selCy 98/applyCy 82/tbH 42 wrap → v0.7.75 selCy 83==applyCy 83 inline, tbH 32 one-row, rowSpread 0; open-count still SPAN buffer between ▼/✓). Placed marker `919d290d`→renderMergeGutter e24dc98a (e24dc98a.tests will be [8fa42d89 structural, 0866205d R30.50-A, 919d290d R30.52-layout]) → req to mint → two-key. ★ LESSON: gate the USER's ACTUAL acceptance, not the initial spec wording — 'own line under X' can BE the rejected wrap; when the AC is layout, assert the geometry Tron actually wants (inline/one-row), and a gate that passes a Tron-rejected build is a WRONG gate.
- **R30.52 MOBILE bug RESOLVED — GREEN DET-3x @390px MOBILE v0.7.76 (d467ff6f5, edit-ZMSOM2G3.js, commit `96fe09d80`, screenshot to Tron):** `test/visual/r3052b-selected-visible-gate.mjs`. Tron's '14 selected MISSING' = MOBILE text-WRAP (390px phone, toolbar overflowed → .de-selected wrapped 2 lines = invisible). ★ I OWNED THE MISS: my 'width sweep 1300→390' was DESKTOP-390 (no isMobile) → false-GREEN; only `devices['iPhone 12']` (isMobile+dSF3+mobile-UA) reproduced/verifies. Fix (white-space:nowrap+flex-shrink:0+flex-wrap:nowrap+overflow-x:auto): '14 selected' single-line (offsetHeight 12, ws=nowrap) visible inline, toolbar 1-row (h=32), painted 1355px @dSF3, 3/3. Marker `53731d96`→e24dc98a (mobile-visibility) → req. 919d290d desktop-layout two-key CLOSED (e24dc98a.tests=[8fa42d89,0866205d,919d290d]+53731d96 pending). PROTOCOL BANKED (rule #6 + memory): all Tron-facing visual gated at 390 MOBILE emulation.
- **R30.51 changes-focused folding — GREEN DET-3x @390 MOBILE v0.7.77 (edit-MFCKJPNK.js, commit `9fae1c617`, screenshot to Tron):** `test/visual/r3051-changes-folding-gate.mjs` at iPhone-12 emulation. ★ ANTI-CIRCULAR (expert 0.1 asked, my independent-verify rule): measured via the RENDERED line-number RULER (visible numbers JUMP where lines hidden) + rendered chevrons + config — NOT the expert's `_collapsedGaps` count. Independent result MATCHES his: Cvisible=30/4004, 10 ruler-jumps. (1) ruler-jumps + '▸ ⋯ N lines' chevrons; (2) fold sync ×3: tap chevron→flip '▾' + ALL-3-rulers(L/C/R)-change reveal→re-collapse; (3) guard: native folding OFF (getRawOptions().folding===false) + 0 native-fold-markers → change regions uncollapsible; (4) coexist: blocks 86 + ribbons 79 + syntax 821 + ✕. folding:false + setHiddenAreas(_collapsedGaps ×3) architecture. Markers→req CLOSED two-key (req `0d900433d`, full-uuid both-dir): 65cb5a36↔9493c08a, c42fceb6↔2d7a0103, 6259acfb↔23b416c2 → R30.51 row COMPLETE (applyFold 2d7a0103|check c42fceb6) → 110/382. ★ TECHNIQUE banked: anti-circular fold-verify = read the RENDERED ruler (line-numbers skip hidden = jumps) + chevron-flip on tap + all-3-rulers-change, NOT the intent set; a Monaco fold reveal is virtualization-confounded for line-COUNT → use ruler-CONTENT-changed (before≠after per pane) as the 3-pane reveal signal.
- **⚠ R30.53 DEPLOYED (edit-ZGIJZW7E.js) — TRON-OVERRIDE that SUPERSEDES R30.51 (measured, critical):** R30.53 switches to NATIVE Monaco folding (`folding:true` + `showFoldingControls:'always'` line 170, per-pane chevron + '⋯' placeholder), REPLACING R30.51's `setHiddenAreas`/center-only-chevron. New impls: `foldByMethodBoundaries` 2de3411f (native FoldingRangeProvider, method boundaries), `keepChangeMethodsExpanded` 640f8428, `syncNativeFold` b629c015 (INITIAL collapse unchanged methods in all 3 + ONGOING mirror: `foldingModel.onDidChange`→`_mirrorFold` mirrors a manually-toggled method to the other 2 by unchanged-seq-index, `_foldSyncing`-guarded). **IMPACT: my r3051 gate (setHiddenAreas) is now STALE on this bundle; the R30.51 impls (computeFoldRegions/applyFold/applyChangesOnlyFolding) + their just-wired Tests (65cb5a36/c42fceb6/6259acfb) are SUPERSEDED by R30.53 — flag req/planner for supersede-annotation (my r3051 GREEN was on the PRIOR bundle edit-MFCKJPNK.js).**
- **▶▶ POST-REFRESH TASK (PO-approved, Option-2 rewind driven by trainer baseTeam:0.0): RUN the R30.53 native-fold gate @390 iPhone-12 REAL mobile, PIXEL, per these 4 ACs (Tron IMG_4574/4575/4576):**
  - **(a) RENDER — ★★ use Playwright WEBKIT engine, NOT chromium (PO CORRECTED root — ENGINE not headless):** `devices['iPhone 12']` runs CHROMIUM/Blink; Tron's iPhone = iOS Safari = **WEBKIT**. Monaco FoldingController decorations render on WebKit NOT Blink → that's why chromium emulation reports 0 fold-decorations (headed-chromium-under-xvfb would NOT help — still Blink). FIX = the ENGINE: `import { webkit } from '@playwright/test'` → `webkit.launch()` + iPhone-12 viewport (390×844, isMobile). WebKit matches Tron's real Safari → SHOULD render chevron + '⋯' + hidden lines → gate RENDER + scroll + sync WITHOUT Tron's physical device. IF webkit ALSO can't render → real-device/Tron only. Report WHICH engine reproduced. **★ WEBKIT-INSTALL PROCEDURE (PO, webkit NOT in this env — chromium-only; shared host, no unilateral system-libs):** post-refresh TRY `npx playwright install webkit` (browser BINARY only — often NO sudo/system-libs). If clean → gate the 5 ACs under webkit on v0.7.78 (edit-MMNL6FBX.js). IF it needs `npx playwright install-deps` (sudo/system-libs on the SHARED host) → **do NOT add unilaterally** → report that + the visual RENDER(a)/scroll(e) gate FALLS TO TRON's REAL DEVICE (he's actively testing = ground truth). The LOGIC/behavior proxies STILL gate in CHROMIUM: '⋯' placeholder text, `foldingModel.getRegionAtLine().isCollapsed` (b sync + c auto-collapse), and scroll-anchor via `editor.getVisibleRanges()[0].startLineNumber` BEFORE/AFTER a collapse (assert top line unchanged for e). So even without webkit: b/c GREEN-able in chromium (model), a/e RENDER → Tron.
  - **(b) ★ Tron HARD AC — MANUAL-fold SYNC across all 3:** collapse a method via the native chevron in EACH pane → the SAME method collapses in ALL 3; expand → all 3 expand; ASYMMETRIC state UNREACHABLE (no pane collapsed while another expanded for the same method). Test initiating from each of the 3 panes.
  - **(c)** initial auto-collapse of UNCHANGED methods + change-methods STAY expanded (syncNativeFold initial pass; keepChangeMethodsExpanded 640f8428).
  - **(d)** change-blocks + connector ribbons + syntax highlight COEXIST over the native fold.
  - **(e) SCROLL-STABILITY (Tron, gate when the expert's scroll-fix deploys):** scroll DOWN, collapse a method → the editor KEEPS its scroll position (NO jump to line 1 / top). Beyond render+sync = the smooth-collapse behavior. Measure: read `editor.getScrollTop()` (or the top visible line number in the ruler) BEFORE + AFTER a collapse of a method while scrolled down; assert it stays ≈ the same (not reset to 0/line-1). @390 iPhone-12. Gate alongside a/b/c/d.
  - MEASURE ANTI-CIRCULAR (expert/independent-verify): the RENDERED native fold state — `.codicon-folding-collapsed`/`-expanded` gutter chevron + '⋯' collapsed-placeholder per pane — NOT the internal folding model. Mechanism: click a pane's gutter fold chevron → `foldingModel.onDidChange`→`_mirrorFold` mirrors to the other 2 (by unchanged-seq-index, `_foldSyncing`-guarded). Impls to place markers for (req 520a3ef80, R30.53 BUILT v0.7.78, architect audit PASS — req mints Tests reverse off my real-WebKit markers → planner flips T30.53): **(1) foldByMethodBoundaries 2de3411f** — diff a SHELL file → native fold-gutter chevrons at METHOD boundaries + '…' placeholder on unchanged; **(2) keepChangeMethodsExpanded 640f8428** — conflict-overlapping method STAYS EXPANDED, unchanged method COLLAPSES; **(3) syncNativeFold b629c015** — collapse in ONE pane mirrors to other 2 + scroll stays ANCHORED (no jump to line 1). ★★ req+architect BAKED-IN gateEnvironmentNote: MUST gate on Tron's REAL device OR HEADED real-WebKit (`webkit.launch()`) — a green CHROMIUM-EMULATION run MUST NOT be accepted (0 codicon/fold-decoration = false-negative; that artifact caused the whole 'native broken' detour). Report WHICH engine reproduced + GREEN to robbinTeam2:0.0. **★ CONCRETE GATE-STEP BODIES FLESHED + DURABLE (req) → read FIRST post-refresh: R30.53 Requirement `ac3338b6`.testScenarios** (survives both our refreshes) — per-impl steps + `_env` (real-WebKit render / chromium model-proxy for fold-state logic) + `mobile` (TOUCH-TAP chevron; per-editor collapse-state PERSISTS across a STACKED pane-switch @390).
- **R30.53 native-fold gate DONE — LOGIC GREEN DET-3x, RENDER→Tron (commit `4234f6d1f`, pushed, /context 39%@boot→~mid-40s):** `test/visual/r3053-native-fold-sync-gate.mjs` (chromium model-proxy @iPhone-12, v0.7.78 edit-MMNL6FBX.js). ★ WEBKIT: binary installs but WON'T LAUNCH here — 13 system libs missing (needs `playwright install-deps`=SUDO on shared host → did NOT add unilaterally). So per gateEnvironmentNote: AC-a RENDER (codicon chevron+'…') + AC-mobile-touch → TRON's real device (CONFIRMED chromium artifact via screenshot: model 16-21 collapsed/pane but 0 gutter chevrons, lines contiguous). LOGIC gated in chromium via folding MODEL (engine-independent): **AC-c** auto-collapse UNCHANGED+keep-CHANGE-expanded (collapsed==keepChangeMethodsExpanded set); **AC-b** 3-pane mirror-sync **CONTENT-MATCHED by method signature** (unfold center→corresponding method unfolds L+R→refold all 3); **AC-e** scroll-anchor (scroll 400, toggle fold→top stays 400, no jump-to-1). NO Test marker placed (full acceptance needs RENDER on Tron/real-WebKit; a chromium run must NOT be accepted for render). Impls 2de3411f/640f8428/b629c015 — place markers when Tron confirms render → req mints → two-key. ★ LESSON (re-banked): a COUNT-proxy on a fold-mirror action false-RED'd (raw-index counts moved differently while the CORRECT corresponding method DID sync) → assert the SPECIFIC element by CONTENT (method signature), not aggregate counts. ★ webkit-on-shared-host = binary-yes/launch-no without sudo — logic-proxy in chromium (model) + render→Tron is the pattern.
- **R30.53b left-pane fold-parity — RED BASELINE (commit `ba0a2734e`, pushed), the bug r3053 MISSED (Tron device-QA):** `test/visual/r3053b-leftpane-parity-gate.mjs` @iPhone-12. v0.7.78 RED DET-3x 3/3: fold-state PARITY across L/C/R = **1/19 shared unchanged methods DESYNC** (offender `private.complete.buffers()`), downstream row-drift 0px (secondary). Root cause: `_mirrorFold` maps by **raw method-index** (rb-diff-editor.ts:389 `ranges[i]`) → a green add-block upstream shifts LEFT's index correspondence → downstream unchanged method fold desyncs L-vs-C/R. Gate matches by **CONTENT/signature** (correct-by-construction — index-matching is what the bug relies on). FIX = :389 raw-index→content correspondence → flips parity 0/19 + holds 0px. Standing by for build-go→fix→re-run (RED→GREEN) + Tron device re-confirm → then place markers → req mints → two-key.
- **★★★ HARD-OWNED MISS (banked deepest): my r3053 AC-b DIAG SHOWED the desync (center↔local asymmetric) and I CONTENT-MATCHED AROUND IT to force GREEN — sidestepping the exact bug. LESSON: when a gate shows an ASYMMETRY you don't expect, that asymmetry may BE the bug — do NOT work around it to get green; STOP and flag it. For a cross-pane invariant, assert it for EVERY shared element (parity across ALL), never a single element whose mapping happens to align. A green I engineered by narrowing the sample is a false-green. [[gate-never-real-save-on-served-repo]] sibling.**
- **R30.53b is a TWO-PART fix (DET-3x disambiguation, commit `04e442724`):** measured initial-vs-toggle → **BOTH mechanisms**. r3053b now asserts BOTH: **[FIX-A]** INITIAL-load parity (ZERO interaction) = RED 1/19 (buffers() L=expanded/C=R=collapsed = `keepChangeMethodsExpanded` per-pane conflict-overlap clip after a green add = EXACTLY Tron's screenshot); **[FIX-B]** POST-TOGGLE parity = RED 3/19 (`_mirrorFold` raw-index :389). Downstream 0px drift GREEN. GATE GREEN ONLY WHEN initial 0/19 AND post-toggle 0/19 AND drift 0px → fix flips BOTH or half-fix. FIX-B alone would fail Tron's re-confirm (initial desync is pre-mirror). Expert must ship FIX-A+FIX-B ATOMICALLY → I re-run RED→GREEN → Tron device re-confirm → place markers → req mints → two-key. LESSON: disambiguate initial-vs-interaction for a state-sync bug — a mirror-fix can't touch a pre-mirror initial-classification desync.
- **R30.53b RED→GREEN DET-3x on v0.7.79 (edit-6UCG6VBB.js, commit `7160265e6`, pushed):** BOTH fixes verified atomically — [FIX-A] initial-parity 1/19→**0/19** (buffers() collapses in all 3 = Tron-screenshot desync GONE; split-floor classify-parity), [FIX-B] post-toggle parity 3/19→**0/19** (mirror by signature), downstream 0px drift holds, green-add DECORATION renders (4 de-blocks + 79 ribbon paths, floor kept). served==committed==gated. **HOLDING R30.53 markers (2de3411f foldByMethodBoundaries / 640f8428 keepChangeMethodsExpanded / b629c015 syncNativeFold) until Tron's codicon fold-CHEVRON RENDER device re-confirm (PO pinging him) — the only piece webkit-unlaunchable-here can't verify. ON Tron render-OK → place markers → req mints → two-key → R30.53 chain-complete.**
- **R30.53 2 PARITY markers HANDED to req (commit `8a9c0a52f`, on origin):** `a3adc5cf`→640f8428 keepChangeMethodsExpanded (FIX-A initial-parity) + `e3e64c49`→b629c015 syncNativeFold (FIX-B mirror-by-signature), both in r3053b. ★ KEY DISTINCTION (told req): the parity Tests gate fold-**STATE** via the folding MODEL (isCollapsed) = ENGINE-INDEPENDENT + DISCRIMINATED RED→GREEN with the fix (a chromium false-negative wouldn't flip) → correct-by-construction, validly GREEN in chromium. The webkit-only piece is ONLY the codicon CHEVRON RENDER visual (AC-a) = Tron's SEPARATE device re-confirm. So parity ≠ render: parity Tests mintable now; render is Tron's sign-off. On req mint → two-key a3adc5cf↔640f8428 + e3e64c49↔b629c015.
- **R30.53 parity Tests TWO-KEY CLOSED (req mint `6f32851a5`, verified full-uuid both-dir):** FIX-A `a3adc5cf↔640f8428` (fwd Impl.tests[] + rev implementations[]+ownerIor ×2) + FIX-B `e3e64c49↔b629c015` (×2). Both parity ACs chain-complete-to-Test. **R30.53 stays In-Progress** — codicon chevron RENDER (AC-a) is the ONLY remaining acceptance = Tron real-WebKit device sign-off (webkit-unlaunchable here). Parity(model,GREEN,chain-complete) ≠ render(Tron,pending).
- **R30.53 BUG-2 fold-affordance MISSING — RED baseline (commit `5ee9201b9`, pushed), root LOCALIZED:** `test/visual/r3053c-fold-affordance-gate.mjs` @iPhone-12 RED DET-3x. Tron: methods below a point have no fold chevron. MEASURED: CENTER computeMethodRanges detects 20/104 method-defs, folds stop L1308/4004 (last def L3836, GAP 2528, 55 unfolded); **LOCAL gap=0 (clean), REPO gap=12 (near-clean) → CENTER-both-versions-SPECIFIC.** Root: computeMethodRanges' naive brace-depth parser desyncs on the R30.35 both-versions CENTER render (changed method's signature-open shown TWICE = two `{`, one `}`, e.g. L1326/1328 currentSession) → depth stuck >0 → no detection below. PRE-EXISTING (FIX-A untouched), newly visible now alignment lets scrolling into the tail. STEER given architect: fix = parse the PER-SIDE model (L/R prove clean) not the merged-rendered center. GREEN = gap≤50 all panes. Run GREEN when expert ships; codicon RENDER still → Tron device.
- **R30.53 BUG-2 fold-affordance FIXED v0.7.80 (edit-UF6QQZ7E.js, commit `4e81d2512`):** r3053c RED→GREEN DET-3x — CENTER 20→104/104 detected, gap 55→0 all panes (computeMethodRanges by signature/indent, not brace-depth). BUG-2 SOLVED.
- **⚠ BUT the BUG-2 fix EXPOSED a hidden FIX-A residual (r3053b RED 1/79, held honest — did NOT rubber-stamp):** `private.complete.panes()` — a CHANGE-method (overlaps conflict in all 3, should stay EXPANDED) is expanded C/R but COLLAPSED in LEFT(86-88 vs C89-95); was below the old detection boundary so never checked. NOT a regression BUG-2 caused — a pre-existing keepChangeMethodsExpanded per-pane-clip case surfaced (same class as original buffers()). FIX-B mirror still GREEN 0/79. r3053b = the RED baseline for this residual. LESSON: a detection/coverage fix can EXPOSE previously-hidden bugs in a DIFFERENT gate — re-run ALL related gates after any coverage change; a method that overlaps a conflict in all panes must stay expanded CONSISTENTLY L/C/R (classify by resolved conflict-overlap, per-pane clip is the bug).
- **R30.53 FIX-A2 v0.7.81 (edit-5RVLY3DA.js, commit `f0d21d7fa`) — parity residual CLOSED, full dual re-run GREEN DET-3x:** (1) r3053b RED→GREEN — panes() EXPANDED ×3, INITIAL 1/79→0/79, toggle 0/79, 0px drift; (2) r3053c UNREGRESSED 104/104 gap=0 (BUG-2 detection intact); (3) INV-A2 collapsed-set-by-sig IDENTICAL L/C/R (51/51/51); (4) FIX-B mirror 0/79. FIX-A2 = classify change-set ONCE by signature-UNION across panes (LEFT can't under-classify). **R30.53 GREEN on EVERYTHING model-side (fold-affordance + parity + mirror + scroll-anchor); the whole buffers()/panes() left-parity class fully closed.**
- **ONLY OPEN: Tron real-WebKit codicon chevron RENDER re-confirm (AC-a) → on his OK R30.53 fully closes + planner flips T30.53.** Parity Tests a3adc5cf→640f8428 + e3e64c49→b629c015 already cover GREEN. OPTIONAL offered to PO/req: r3053c marker→foldByMethodBoundaries 2de3411f for distinct BUG-2 fold-affordance coverage.
- **r3053c BUG-2 fold-affordance Test marker PLACED (commit `90cf08562`, pushed): `1dfe3d0f`→foldByMethodBoundaries 2de3411f** (PO-directed, #126 chain-honesty — makes BUG-2 coverage explicit). ON REQ MINT → two-key verify `1dfe3d0f↔2de3411f` (fwd Impl.tests[] + rev Test.implementations[]+ownerIor).
- **▶▶ R30.53 STATE FOR POST-REWIND BOOT (Option-2 rewind approved by PO, gates DONE = safe):** ALL model-side GREEN DET-3x on v0.7.81 (edit-5RVLY3DA.js) — r3053b parity (0/79 init+toggle, 0px), r3053c fold-affordance (104/104 gap 0), INV-A2 (51/51/51). Parity Tests two-key CLOSED: a3adc5cf↔640f8428 + e3e64c49↔b629c015. Fold-affordance marker 1dfe3d0f→2de3411f AWAITS req mint→two-key. **ONLY OPEN: Tron real-WebKit codicon chevron RENDER re-confirm (AC-a) — PO presenting device-check now → on his OK R30.53 fully closes + planner flips T30.53.** Standing gates I own for R30.53: r3053 (logic), r3053b (parity), r3053c (fold-affordance), all @iPhone-12 model-proxy.
- **BUG-2 chain CLOSED (req mint `b6b2a95d0`, two-key verified full-uuid both-dir): `1dfe3d0f↔2de3411f` (fwd Impl.tests[] + rev implementations[]+ownerIor ×2). ALL 3 R30.53 method-impls chain-complete-to-Test: foldByMethodBoundaries 2de3411f←1dfe3d0f, keepChangeMethodsExpanded 640f8428←a3adc5cf, syncNativeFold b629c015←e3e64c49.**
- **NEXT (post-rewind): ONLY Tron real-WebKit codicon RENDER (AC-a) re-confirm outcome remains → on his OK R30.53 fully closes + planner flips T30.53. Nothing mechanical left. Gate @iPhone-12 (rule #6). REWIND-READY — all committed+pushed, no WIP.**
- **★ MILESTONE (PO-confirmed): MERGE-EDITOR + REPO-MANAGER ARCS FULLY GATED + CHAINED. Team = COMPLETE-AWAITING-TRON (~17 QA-Review visual sign-offs; planner batch checklist). Board 108/380. NO separate 'Save-button req' — that was R30.50 C2 (already done). Nothing queued to gate.** A-compose Test `0866205d`↔e24dc98a TWO-KEY CLOSED (req `b09cb51da`, full-uuid both-dir; e24dc98a.tests=[8fa42d89 structural, 0866205d intention]). **ALL threads closed — nothing mechanical left. R30.50 = all 5 sub-features chain-complete WITH champagne A-intention coverage.** HOLD for Tron's QA sign-off OR a FRESH Tron feature. Standing patterns proven across the arc (banked): route-intercept pollution-safety for live writes; exact-VALUE asserts (not 'some value'); D3 no-checkout via git-worktree-list byte-equality; screenshot-first to kill sampler false-REDs; two-key measures the row (structural vs intention Test).
- **(historical) PENDING R30.50 (expert BUILDING A→C2→C1→B, NOT deployed — hold for expert deploy ping). On ping: served==committed + read ACTUAL selectors/API first, then DET-3x Tron's real URL + screenshot:** (A) count composes 'N selected · X/Y open' (both counters real); (C2) 3-mode Apply-All popup — each mode drives the correct merge (assert CENTER mutates per mode, function-first, not just popup-opens); (C1) guarded-save jumps to next UNRESOLVED (save w/ open conflicts → scroll/focus next unresolved); (B) save-button GREEN-when-saved (pixel: turns green on save success, reverts on new edit — sample the button, screenshot-first). Two markers still pending req mint from #49: UC7 771e2e83→1a86a852, DELETE e4741c65→559b508b → two-key on mint.
- **Tron's LEFT-empty (BUG-2, R30.25) still parked (do not force).** git push WORKING on this checkout.
- Standing waits: expert PRE-PINGS before every deploy → I "★ PARKED-CONFIRMED on vN" → then he pushes (R30.14 live-catch fidelity bonus, tool `r3014-livecatch-tool.mjs`, shadow-DOM aware; R30.14 already PROVEN). My git push to origin/main is policy-blocked → land via PO push, and peers must read WORKING TREE (not origin) until pushed.

## ⛔ STANDING RULES (never violate)
1. **RULE #126 SCENARIO-FIRST, NEVER BACKFILL.** Task without an existing scenario unit → REJECT + report PO. Never mint units / wire a Test hop retroactively. I place a ready marker; req mints; I wire the reverse ref.
2. **SystemTester ONLY** for prod gates: token `ce981242-74fe-4d44-b5b6-43c641e224df`. Seed via `addInitScript` BEFORE goto. Never mint fresh random users/rooms.
3. **NEVER create / don't clean-and-hope.** In-memory pollution (dangling refs, phantom profiles) clears ONLY on a server restart, not disk cleanup. Never delete a dropped unit (creates dangling refs). Every gate is READ-ONLY by construction OR explicitly restores — verify read-only BEFORE running.
4. **Measure the ACTUAL state — never the stated count.** Re-run `chain scoreboard` on origin (HEAD==origin), grep src for impl markers, probe CURRENT behavior before a security test (a green suite can hide a live bypass). Report what's real, including hops that aren't mine. "Gated GREEN" ≠ "chain closed" (a hop stays open until its Test UNIT is minted + reverse-wired).
5. **SHARED WORKING TREE — explicit `git add <my own file paths>` ONLY, NEVER `-A` or `.` (SM R30.51).** All robbinTeam agents share ONE RawBin working tree; `git add -A/.` sweeps other agents' uncommitted edits into MY commit = wrong attribution + risk of committing someone's half-finished work. Commit PROMPTLY after editing to shrink the uncommitted window. (I already do explicit paths — keep it.)
6. **★★★ TRON IS ON A 390px MOBILE PHONE — gate ALL Tron-facing VISUAL features at 390px REAL MOBILE EMULATION (SM/PO session-defining).** Use Playwright `devices['iPhone 12']` (isMobile + deviceScaleFactor 3 + mobile UA) — NOT a desktop narrow viewport (a desktop 390px renders flex/overflow DIFFERENTLY → keeps FALSE-PASSING; that's the root of the whole 'self-verify said present but Tron sees it broken' class this session: R30.52 '14 selected' mobile text-WRAP, R30.34 scaled-viewport stack). ALWAYS at Tron's REAL end-state + a PIXEL screenshot (canvas-decode, dSF-aware box). Desktop DOM/element-counts = the false-green root — kill it at 390px mobile.

## Proven gate techniques
- Read-only on real shipped units: pure-fn (tsx import) / disk-scan / mounted detail component / WS member-read / HTTP-GET-with-stateless-grant.
- Drive a detail component standalone: `document.querySelector('rb-trace-tree').graph` → set `el.graph` + `el.setAttribute('ref','<type>:<uuid>')`.
- Function-first assert: REAL `page.click`/mousedown-drag (not dispatchEvent), assert CENTER/state CONTENT mutates, settle ~800ms before read. Drawer: X=.drawer-close→minimize, ESC→close, grab-bar=.drawer-handle toggle; assert VISIBLE peek via `offsetHeight>0`; `serviceWorkers:block` to bypass SW cache; reject only whole-detail not-found (empty subsection ≠ failure; require len>300).
- SSRF: `vi.spyOn(ProxyFetch,'guardUrl').mockResolvedValue({allow:true,ip:'127.0.0.1'})` → in-test `http.createServer` mock.
- A marker-detection gate must NOT carry a literal `[impl:uuid]` in its own source (interpolate a bogus id).

## Key real-data uuids (PROTECT in any purge)
SystemTester ce981242 · Tron primary 8f74dfba (tombstones 3effa1fc/2703628c/37fcb752 redirectTo) · dnd test room **3231db71** (JOIN-reusable) · message WebItem c8dc9d0d · http WebItems 90322673/5316df18 · image File 5e380e73 · clipboard-text File ac90d742 · Heartspaces 6c04f959 · Marcel Room 8be52aa9 · known phone +4981422917723.

## Tooling
- `npx tsx scripts/objectVerb.ts chain scoreboard|followUp|lintMarkers` (node18 fine).
- `PATH=/opt/node22/bin:$PATH npx vitest run test/vitest/<f>.test.ts` (node22 required).
- Wire a Test hop: mint `ior:class:Test` (sourceFile→gate) + `// [test:uuid:<u>] <req> <method>` marker + push to `Impl.model.tests[]`. Verify impl markers in `src/` (grep) before wiring.
- Server restart + LIVE TUI observation = `remoteShells:0.2` (WODA.prod) — NOT agent panes (that's where in-memory-pollution restart-flush happens).

## R31.2 + R31.1 owner-gate security — GREEN DET-3x (2026-07-20, commit a1b4da4c9 pushed)
- `test/visual/r312-owner-gate-security.mjs` on served==HEAD==**v0.7.84** (self-verified /api/config .version FIRST — phantom-guard baked into the gate: aborts if served!=target). INDEPENDENT: raw HTTP + raw ws handshake + source grep + live-non-owner browser (NOT the expert's build-check).
- [A R31.2] no/unknown/malformed/empty→403; ★ LEAKED owner literal (41ad88c4, read from source) w/o live session→STILL 403 (assertOwner needs live-session AND const-time-eq owner); every /api/server-manager/* sub-route→403 (INV-G1 choke server.ts:822); ws terminal upgrade non-owner→403 socket-NEVER-opens (INV-G3 server.ts:2196 destroy); OWNER_TOKEN literal==1 (INV-G2, ServerManagerGuard.ts:12); UI-force live-non-owner→403. [B R31.1] non-owner profile (/app?editProfile=1, grants RENDERED)→Server Manager grant ABSENT + forced-markup→whoami still 403 (server-gated via renderFeatureGrants' `if(res.ok)` on whoami).
- ★ PHANTOM-GUARD lesson (banked): NEVER gate served!=HEAD. I ran GREEN on v0.7.82, PO caught the bump to .83/.84 → I did NOT report it, held, self-verified served==0.7.84, re-ran. Gate now self-enforces the version check. Target moved .82→.83→.84 (R31.1 bump); one restart activated both.
- CHAIN: ready marker `87b040ee`→Impl 335dbf3d (ServerManagerGuard.assertOwner) for req to mint R31.2 Test. R31.1 Method b4f03947 (renderFeatureGrants) has NO Impl unit yet → its Test hop waits for the architect Impl-mint (markers credit off Impl, never Method). Owner-200 accept + entry-VISIBLE = Tron-facing (live session), not gated by me.
- ★ TECHNIQUE banked: security-foundation gate = raw HTTP/ws probes (not browser-only) + read the guard's OWN literal from source and prove it STILL 403s without a live session (depth beyond non-owner→403) + ws INV-G3 via 'open'-never-fires + single-literal grep + live-non-owner browser forcing the endpoint (UI-hiding-is-not-the-gate). Self-verify served version in the gate.
- **R31.2 Test CHAIN CLOSED (req mint c7d27a609): two-key VERIFIED both dir — Impl 335dbf3d.tests[]=[87b040ee] (fwd) + Test 87b040ee.implementations[]=[335dbf3d]+ownerIor→335dbf3d (rev). assertOwner chain-complete-to-Test.**
- **R31.1 POSITIVE render gate GREEN DET-3x (commit 7b09f7d56 pushed):** `test/visual/r311-feature-grants-render-gate.mjs`, marker `96d0d227`→Impl f345b8ed (ProfileEditor.renderFeatureGrants, ProfileEditor.ts:95, ownerIor Method b4f03947). (a) whoami route-intercepted→200 isolates the RENDER → 'Server Manager' entry (href /server-manager?token=..) under 'Feature access' title; (b) real non-owner whoami 403 → renderFeatureGrants FILTERS it (section present, 0 grants). DISTINCT from r312's 403 auth gate (335dbf3d). Pollution-safe (route-intercept, no owner-impersonation). Owner-VISIBLE end-to-end = Tron device-facing. req to mint R31.1 Test off f345b8ed → then I two-key 96d0d227↔f345b8ed. ★ caught my own false-RED (exact-href `=== '/server-manager'` vs real `/server-manager?token=..` → startsWith).
- ★ TECHNIQUE banked: to gate a SERVER-GATED render's POSITIVE path without the real privileged credential, route-intercept the gating endpoint→200 to isolate the RENDER logic (the Impl under test) from the AUTH (gated separately) — then the real non-owner (natural 403) proves the FILTER. Two Impls, two gates: auth(assertOwner) in r312, render(renderFeatureGrants) in r311.
- R31.1 render Test two-key CLOSED (req mint 827b8175d): 96d0d227↔f345b8ed both-dir verified.

## v0.7.86 batch security re-gate — ALL GREEN DET-3x DONE (2026-07-20, commit 87f5d5ef6 pushed)
- Self-verified served==0.7.86. THREE gates GREEN: (A) **r313-otmux-tree-parse-gate.ts** marker `9467b1c6`→Impl 5c1701bc (readSessionTree) — IMPORTED the impl + parse-matched raw `tmux list-panes -a -F` (35 tuples miss=0 extra=0, %paneId+label ok) + tree/page non-owner→403 never-shell. (B) **r311v-viewer-gate.mjs** marker `baee3c82`→Impl f345b8ed (CORRECT /profile VIEWER surface, supersedes editor 96d0d227) — whoami-intercept 200→entry appended under 'Feature access'; non-owner 403→filtered; markup-forced→403. (C) **r312** retargeted 0.7.86 + /server-manager PAGE & /tree added to reject-enum — 7 routes all 403 (ONE choke server.ts:903 folds page+APIs+ws, INV-G1), never-shell, ws INV-G3, OWNER_TOKEN==1 (INV-G2), UI-force→403; REMOVED the stale R31.1 editor check.
- **PENDING fresh req (boot 50894d08/later): mint R31.3 Test off 9467b1c6→5c1701bc + RE-POINT f345b8ed.tests[] 96d0d227(editor)→baee3c82(viewer), retiring the editor Test. Then two-key both.** Owner-200/tree-visible/entry-visible = Tron device-facing.
- ★ banked: import the impl to parse-match ground truth (readSessionTree vs raw tmux, 35/35 exact) — engine+owner-token-free, tests the mint target directly; the owner-gated API wrapper is the reject gate's job.
- **★★ ACCEPT-PATH GAP OWNED (PO lesson, banked HARDEST):** r311v MOCKED the owner path (whoami-intercept 200) → verified the RENDER but HID Tron's REAL 'owner sees no entry' bug. A green built on a MOCKED gate-response is a FALSE-GREEN on the PRIMARY happy-path. RULE: the primary happy-path (owner sees+USES it) must be verified against REALITY, never mocked — mock only to isolate a SECONDARY branch, and ALWAYS also gate the real accept. Sibling of [[a green gate can hide a live bug]] + [[gate the user's exact flow]].
- **R31.1 REAL owner-accept gate PREPPED (commit 24a50d80c, NOT RUN):** `test/visual/r311a-owner-accept-gate.mjs` — seeds owner 41ad88c4 as a LIVE session (rawbin-player-id + e2e-bypass device keys → /app ws → IDENTIFY → tokenToClient.set server.ts:2555) → /profile → REAL whoami 200 → entry. NO mock. ⚠ DID NOT RUN: tokenToClient.set(owner) would DISPLACE Tron's live owner session mid-test → run COORDINATED on v0.7.88 signal (expect RED pre-fix → GREEN on fix). LIKELY ROOT CAUSE (recon, for expert): /profile viewer doesn't hold a live ws → isLiveSession(owner)=false → whoami 403 → no entry even for owner. On v0.7.88: run r311a RED→GREEN → place marker → fresh req mints/wires the ACCEPT Test on f345b8ed.
- **R31.2 reject-gate RE-GATED on v0.7.92 (node-pty) — GREEN DET-3x (commit 0cbde48a6):** r312 retargeted 0.7.92; enumerated 8 routes all-403 (added NEW POST `/api/server-manager/session` cookie route); ★ COOKIE-MINT GUARD: non-owner POST /session → 403 + NO Set-Cookie (no-token AND leaked-owner-literal-not-live) = can't mint the httpOnly owner cookie; ws INV-G3 no-PTY-spawn, INV-G2==1, never-shell, UI-force. Owner-200+terminal = Tron's session. r312 = STANDING reject regression gate (self-verifies served version each run).
- **SW-fix tree-render gate DONE — GREEN DET-3x v0.7.93 (commit c35ae0f67):** r314-sw-tree-render-gate.mjs. (1) served sw.js navigationStrategy network-first for /server-manager + /profile + CACHE_NAME rawbin-v0.7.93, BYTE-EQUAL to committed src/public/sw.js; (2) Server Manager mounts SHARED rb-trace-tree#sm-tree (bespoke retired) + renderer deployed in served bundle (via /trace); (3) served==committed content-hashed /dist bundle. iOS-PWA-SW-activation VISUAL + owner-tree render = Tron device (webkit unlaunchable here, page owner-gated) — flagged, agreed. R31.2 marker 87b040ee↔335dbf3d two-key CLOSED. Standing regression gates now: r312 (reject, self-verifies version), r314 (SW/render), r313 (tree parse), r311v (viewer render).
- **R31.3 tree CLOSED — two-key verified on origin (req mint 5a9f00a71, pushed origin 99f3b34b3):** Test 9467b1c6↔Impl 5c1701bc (readSessionTree) both-dir full-uuid. Chain: R31.3 req **168e6d2b** (altId R31.3; NOT a UC — req corrected my label) → UC 742aa04d → Method 7d7221d8 → Impl 5c1701bc → Test 9467b1c6. Gated r313 (parse-match 35/35) + r314 (SW/render) + Tron device IMG_4597. iOS-PWA visual rides Tron device. req re-verified on origin ✓.
- **R31.3 tree-RENDER gate GREEN DET-3x v0.7.95 (commit 5cb1396b5):** r3195-tree-render-gate.mjs — marker `eb324c68`→Impl 5b3d9f1a (RbTraceTree.buildSeedNode), SEPARATE from data Test 9467b1c6. initial-collapsed+no-explode, 3-level layer-by-layer, window chevron+distinct-proper-label (not '0: bash' placebo), independent collapse, /trace unregressed. Drove served rb-trace-tree w/ otmux fixture (engine-independent). CLOSED — two-key verified eb324c68↔5b3d9f1a both-dir full-uuid on origin (req mint ed280d7d3). Both R31.3 Tests live (data 9467b1c6 + render eb324c68). ★ caught own false-RED: collapse HIDES children in DOM → count VISIBLE not DOM-present (rule#4). Interactive visual = Tron device IMG_4598.
- **▶ STAND-READY: R31.4 DRAWER-MOUNT gate (expert building per architect design):** terminal opens IN rb-detail-drawer (NOT bespoke overlay), content LEFT-aligned, CLOSEABLE via drawer grab-bar, RW keystrokes, fit-on-expand, minimize KEEPS session / full-close KILLS it (pty + grouped-session torn down). Gate the drawer-mount + close/minimize-vs-close LOGIC in chromium (engine-independent, reuse rb-detail-drawer patterns R27.8/R30.20: grab-bar=.drawer-handle, X=.drawer-close, ESC); interactive-terminal RW/keystrokes happy-path (real node-pty, owner cookie) = Tron device. ★ CLIENT-ONLY fix → SPOT-CHECK r312 STILL 403s non-owner (server-side unchanged). Real WebKit if launchable (unlaunchable here → Tron device for visual). DET-3x on ship.
- **R31.4 drawer-mount gate GREEN DET-3x v0.7.94 (commit 792907809):** r315-drawer-mount-gate.mjs — mounts in rb-detail-drawer (#sm-drawer showElement) NOT .sm-term-overlay, LEFT-aligned, X→minimize KEEPS ws (onClose 0×), ESC→full close fires onClose 1×→teardown→ws.close; client-only spot-check whoami+tree still 403. RW/fit/iOS = Tron device.
- **★★ INTEGRATION-SEAM GAP OWNED (PO, banked HARDEST):** I gated tree-parse (r313) + render (r314) + drawer-mount (r315) as SEPARATE COMPONENTS but MISSED the INTEGRATED select→open flow — Tron found select/open broken. WIRING (server-manager.ts:94): tree click (capture) → closest('rb-object-item') → ref type 'otmuxpane' → openTerminal(uuid) → drawer.showElement. RULE: gate the SEAM between components (the user's click→result flow), not just each unit in isolation — a suite of green component gates can still miss the wiring that connects them. Sibling of [[gate the user's exact flow]] + [[a green gate can hide a live bug]].
- **R31.4 select→open REAL CLICK-FLOW GREEN DET-3x v0.7.96 (commit 4aa69927a, THE AC surface):** r316-select-open-flow-gate.mjs — route-intercept served /server-manager page+whoami+tree (real-shape roots) → REAL bundle runs → ACTUALLY CLICK a rendered pane → rb-terminal-detail MOUNTS in #sm-drawer (DRY /trace flow), LEFT-aligned, X→minimize KEEPS, ESC→closes, grab-bar, window badges 2/1, /trace unregressed. Marker `c6791c06`→Impl 79a1ce7c (RbTerminalDetail.mount, R31.4 CLIENT render) HANDED req → two-key on mint.
- **R31.4 render Test two-key CLOSED: c6791c06↔79a1ce7c (req mint 3453db90d, origin) both-dir full-uuid.** R31.4 CLIENT render chain (RbTerminalDetail.mount) complete-to-Test.
- **⚠ SESSION-BADGE residual — PO CONFIRMED IN-SCOPE (Tron IMG_4598 sessions showed '1'; complaint=badges 0). FOLDED into r316 pass** (asserts EVERY node badge==real N incl root session window-count). r316 now RED pre-fix on this (mount/render GREEN). Root cause: computeBadges (rb-trace-tree.ts:193) reads nodeChildCount, populated for inline windows (:355) NOT root sessions. **RE-RUN v0.7.97 (fix f5c35f94c) → session badge STILL 0 (RED). INCOMPLETE FIX, root-caused:** computeBadges (rb-trace-tree.ts:516) key = `ref.split(':')[1]` = 'sess' for session ref 'otmuxsession:sess:robbinTeam2' — splits on the COLON inside the uuid ('sess:robbinTeam2', server.ts:909). Fix (:341) stored nodeChildCount under the FULL uuid → 'sess' lookup MISSES → badge 0. **FIX = computeBadges:516 use refUuid(ref) not split(':')[1]** (would hit ANY colon-uuid). Windows pass only b/c read with panes in DOM (domCount>0 masks it). ▶ ON expert :516 fix → re-run r316 → 0→2. DIAG in r316 (buildSeedNode children=2 + child-count set, computeBadges overwrites) localizes it. Mount/render/window-badges GREEN. ★★ TRACEABILITY (req flag): the badge assertion is R31.3 scope → needs its OWN Test unit on the BADGE Impl (likely computeBadges 7e43dda4 OR the root-threading impl — grep the fix commit to confirm), NOT folded under c6791c06 (R31.4-mount, closed+valid). On flip: place a SEPARATE [test:uuid] badge marker in r316 → hand req the marker + badge Impl → req mints under R31.3 → two-key. Else badge fix ships untraced (chain gap). ★ gating the REAL surface caught what static/component gates missed.
- **▶ STAND-READY: R31.4 FULL select→open flow gate — [[gate-the-ac-surface]] (PO process-fix, banked HARD):** a component/static/FIXTURE green ≠ AC surface. MUST run the REAL click-flow: route-intercept the served /server-manager PAGE + whoami + /tree → 200 with the REAL-shape roots (server.ts:908: type **otmuxSession→otmuxWindow→otmuxPane** camelCase, uuid **sess:name / win:name:idx / paneId(%N)**, window label **'window N'** not the placebo) so the REAL server-manager.ts wiring renders + attaches the click handler → actually CLICK a rendered `rb-object-item[ref^="otmuxpane:"]` → assert openTerminal FIRES + terminal MOUNTS in #sm-drawer + BADGE==real child count. Do NOT report GREEN until the real click passes.
  - ★ MEASURE-FIRST WIN: chased a case-mismatch hypothesis (server 'otmuxPane' vs handler 'otmuxpane') → VERIFIED NOT the bug (rb-trace-tree.ts:332 lowercases the ref) → did NOT false-flag. My r3195 lowercase fixture was faithful for the ref.
  - ★ BADGE observation: buildSeedNode (rb-trace-tree.ts:334) sets 'has-children' but NOT 'child-count' → rb-object-item badge defaults '0' (line 191) = likely the count=0 bug; fix must thread child-count; gate asserts badge==real count.
- **▶ ALSO HOLD for PO post-v0.7.89 + post-Tron-confirm GO → run r311a ONCE (coordinated blip).** r311a EXTENDED (commit 98b4afc52) to cover BOTH halves in one live-owner session: (1) owner sees /profile entry, (2) owner LOADS /api/server-manager/tree (200+sessions[]; NEW owner-tree 403 bug, v0.7.89=owner ticket). SEQUENCE (PO): Tron confirms on his stable session → PO GO → I run (evicts Tron 1×, RED→GREEN) → place real-owner marker → fresh req mints ACCEPT Test. v0.7.88/89 reject-dir unregressed (whoami/tree/page 403). Live-owner-session technique banked: seed rawbin-player-id=OWNER + e2e-bypass device keys → /app IDENTIFY registers it in tokenToClient → token is a live authenticated session for owner-gated checks.
- (superseded plan, kept for trace) STAND-READY notes: served was 0.7.84, code in tree @827b8175d.
- **(A) R31.3** — A1: import `OtmuxBridge.readSessionTree` (Impl **5c1701bc**, OtmuxBridge.ts:14, Method 7d7221d8) + compare to raw `tmux list-panes -a -F` (parse sessions→windows→panes+%id+title; server-side, no owner token). A2: `/server-manager` PAGE + `/api/server-manager/tree` non-owner→403 never-shell. On GREEN → hand FRESH req (boot 50894d08) the test:uuid→5c1701bc.
- **(B) R31.1 VIEWER (★ surface-corrected)** — the AC surface is the **/profile VIEWER bottom**, NOT the editor overlay my r311 tested. renderFeatureGrants MOVED to SERVER (server.ts:874) → emits inline script into `#feature-grants` on GET `/profile` (route server.ts:2076). Build r311**v** viewer gate: (a) intercept whoami→200→entry appended under 'Feature access'; (b) non-owner→403→ABSENT (markup-forced). **Place a FRESH viewer marker → report PO → fresh req RE-POINTS f345b8ed.tests[] off stale 96d0d227(editor) to the viewer marker, retiring the editor one.**
- **(C) INV-G1/G2/G3 re-verify on the FOLD** — choke folded (server.ts:903: ONE `requireOwnerHttp` now covers `/server-manager` PAGE AND all `/api/server-manager/*`). Re-run r312 retargeted to 0.7.86: ADD the `/server-manager` PAGE to the reject-enumeration + re-confirm OWNER_TOKEN literal==1 after the fold. Owner-200 tree/entry-visible = Tron-facing.
- ★ LESSON banked (chain-honesty): a GREEN gate can verify the WRONG SURFACE — my r311 gated the EDITOR (where the code sat = the bug placement), but R31.1's AC is the VIEWER. Gate where the AC specifies, not where the code happens to be. Sibling of [[gate the user's exact flow]].

## ▶ S31 R31.3/R31.4 CURRENT STATE (2026-07-20 rewind boot, served v0.7.98)
- **R31.1/R31.2/R31.3/R31.4 gates all GREEN + chains two-key CLOSED** (r311v viewer / r312 reject-security / r313 tree-parse / r3195 tree-render / r315 drawer-mount / r316 select→open flow). Standing regression gates: r312 (reject, self-verifies served version), r314 (SW/render), r313, r311v, r3195, r316.
- **⛔ SESSION-BADGE — HELD, DO NOT GATE/CREDIT THE v0.7.98 SPLIT PATCH (PO fresher directive).** I root-caused the colon-in-uuid bug ('otmuxsession:sess:NAME' → split(':')[1]='sess') and expert shipped 57f94ff48 (computeBadges/prefetchLayer refUuid) v0.7.98 — BUT Tron is SUPERSEDING it: badge → STANDARD parent/children REFERENCES (OO-referenced like /trace scenario badges), retiring the bespoke nodeChildCount fork. The split patch is THROWAWAY + the WRONG impl to credit. **When the architect's DRY std-ref badge ships → run r316 → place the badge Test marker on the STD-REF impl (R31.3 scope, computeBadges-via-refs), NOT the split patch → hand req.** ★ LESSON: don't credit a soon-retired fork just because a gate would go green — wait for the DRY impl the AC actually wants.
- **HELD: r311a owner-accept** (real live-owner /profile entry + owner-tree 200) — owner interactive happy-path = TRON'S DEVICE; do NOT run (would evict his live owner session). Only run on explicit PO GO + Tron-confirm.
- **NOT MY GATE YET: R31.4 drawer floating-vs-embedded** (Tron IMG_4605, commit 00d994558 'review for final spec') = architect final spec in progress.
- **STAND BY for the architect's fresh DRY design** (std-ref badge + drawer final spec). Nothing else mechanical open.
- **▶▶ OVERNIGHT STAND-READY (PO, gate on v0.7.99):** after architect restarts remoteShells:0.2 + backstop-PASS, SELF-VERIFY /api/config .version==0.7.99 (phantom-guard) THEN run the DRY-fix REAL gate (AC surface, NOT static, DET-3x, @390 iPhone-12, NO green until real surface passes): (1) drawer OVERLAYS like /trace = portrait BOTTOM-OVERLAY (position fixed/absolute, floats OVER tree, z-indexed) NOT embedded-below-tree (in-flow stacked) — measure geometry via REAL click-flow (route-intercept served /server-manager, real-shape roots, click a rendered pane → terminal detail mounts), compare vs /trace's rb-detail-drawer; (2) badge==real-N via CHILD-REFS colon-immune for BOTH session ('otmuxsession:sess:NAME') AND window nodes; (3) /trace+/scenario UNREGRESSED. On GREEN → place badge Test marker on the STD-REF impl (R31.3, computeBadges-via-refs, NOT the split patch) → hand req. Reuse r316 real-click harness + rb-detail-drawer overlay-geometry asserts (position/z-index/overlap vs in-flow).

## R31.4/R31.3 DRY-fix — GREEN DET-3x @390 v0.7.99 (2026-07-20, commit fc67a44c6 pushed)
- `test/visual/r319-dry-drawer-badge-gate.mjs` — REAL AC surface @390 iPhone-12, self-verifies served==0.7.99 (phantom-guard). (1) drawer = rb-detail-drawer#sm-drawer, flex child of .trace-page → /trace-identical bottom drawer (grab-bar+X+terminal mount on real pane click), STRUCTURAL match to /trace's own drawer (parent .trace-page + pos + visible), NOT retired .sm-term-overlay embed; (2) badge colon-immune via node.dataset.childRefCount: SESSION 'sess:robbinTeam2'==2 (was 0 under split patch) + WINDOW 2/1 (served bundle childRefCount x4 / nodeChildCount x0); (3) /trace+/scenario unregressed. Screenshot test-results/r319/sm-drawer-390.png surfaced to Tron.
- **★★ FALSE-RED CAUGHT (banked, sibling of [[a green gate can hide a live bug]] inverse):** first cut asserted the drawer must be position:FIXED floating-overlay → RED 3/3. WRONG MODEL — app.css:277 `.trace-page rb-detail-drawer{position:static !important;flex:1}` = the drawer is a static flex-child Details panel (THAT is the /trace design; my /trace reference measured static too). 'Overlay LIKE /trace' = /trace's actual CSS, not an absolute 'is fixed'. Re-modeled to a /trace-structural-match (in .trace-page + same pos as /trace's drawer + visible + flex-sibling-of-tree — discriminates the retired .sm-term-overlay embed) + SCREENSHOT-confirmed before flipping. LESSON: when a geometry gate REDs, verify the REFERENCE (/trace) FIRST — assert 'matches the known-good reference', not an assumed absolute; read the actual CSS; screenshot before trusting either color.
- Marker `eea279e4`→computeBadges 7e43dda4 (STD-REF badge, R31.3). **CLOSED: two-key VERIFIED both-dir full-uuid on origin (req mint 365fd5d28) — fwd 7e43dda4.tests[]=[c20ea27f,eea279e4], rev eea279e4.implementations[]=[7e43dda4]+ownerIor+status:pass+sourceFile→r319; d28ee95a eager retired-superseded→7e43dda4 no-orphan. req adopted my /trace-structural-identical finding into the R31.4 AC-drawer-overlay-spec.** R31.4/R31.3 DRY-fix fully chain-complete-to-Test. Interactive RW/pty + authed drawer VISUAL = Tron device (my shot = [disconnected], no owner pty, expected). r311a owner-accept STAYS HELD (Tron device).

- **★★ MISS OWNED (banked, MY bounded domain = Test units): I claimed retired Test c470b1c1 "rides the supersede" but ASSUMED it** — I measured only the Impl d28ee95a.supersededBy=7e43dda4, NOT c470b1c1's OWN fields (were supersededBy:None/status:pass = a live Test of retired behavior = a real dangling-live hop). req caught + fixed (88575b3ab: c470b1c1 supersededBy=eea279e4, status=superseded, verified on origin). RULE: a supersede-no-orphan claim must MEASURE EACH unit's OWN supersededBy/status — Impl AND its Test(s); a cluster-retirement can miss the Test hop; never INFER a Test rides its Impl's supersede. Sibling of [[measure the actual state — never the stated count]].

## DRIVE-TO-GATE CLOSERS — mock-owner + ?token= — ALL GREEN DET-3x served==0.7.101 (2026-07-20 overnight)
- **R31.2 AC-cookie-only ?token= leak (r312, slice b0ee7dc6): RED→GREEN, R31.2 DONE.** ★ RED baseline SOURCE-proven non-vacuous: pre-fix `playerTokenFrom` (730b57e2f^) `return q.get('token')||q.get('playerToken')` = a bare ?token=<owner> URL authenticated (owner-live) = the leak; post-fix 730b57e2f header-only. Empirical /server-manager?token=<owner> no-cookie/header→403+no-shell-leak 3/3. **Two-key CLOSED b0ee7dc6↔335dbf3d (req 144d80e6a); 335dbf3d.tests[]=[87b040ee page-route, b0ee7dc6 cookie-only]. Both R31.2 ACs done.** ★ LESSON: an empirical 403 can be VACUOUS (owner-not-live gives 403 for the wrong reason) — back a security RED→GREEN with the SOURCE discriminator (pre-fix fallback present → post-fix removed).
- **R31.4 interactive RW + R31.1 owner-entry (r3110, @390, GREEN DET-3x):** BY-CONSTRUCTION mock owner, NO Tron eviction. (1) routeWebSocket ECHO-pty mock → click pane → rb-terminal-detail mounts → xterm sized → READY banner → 5 keystrokes reach ws → lsCMD echo roundtrip (full owner cookie→ws→attach→RW). (2) owner-entry: entry renders IFF server-computed **m.serverManager** (server.ts:862/2624, NO whoami) → by-construction owner = **app-ws MITM flip serverManager=true on the PROFILE frame** (routeWebSocket connectToServer); non-owner absent. ★ r311v (whoami-based) is STALE — mechanism moved to m.serverManager. Markers: 0c0a69ad→79a1ce7c (RW, distinct from mount c6791c06), a52393fb→f345b8ed (owner-entry, SUPERSEDES stale baee3c82).
- **R31.4 server-attach (r3111.ts, GREEN DET-3x):** import REAL PtyBridge.attachPane + mock ws + real tmux: (a) grouped sm_<pane>_<ts> spawns; (b) NO-DISRUPT 12 non-sm sessions size-stable (grouped=size-independent); (c) detach-cleanup on ws.close → zero sm_ leak (rigorous finally-sweep). Marker b395a272→394eac63 (req self-audit found it Test-less). node-pty REALNESS + keystroke VISUAL = Tron device.
- ★ TECHNIQUE banked: mock owner WITHOUT evicting Tron = (whoami→200 intercept) + (routeWebSocket echo-pty for the terminal) + (app-ws connectToServer MITM to flip a server-computed flag on one frame). Playwright 1.60 routeWebSocket mocks/MITMs ws. Server-side impl slice = import the real class + drive against real state + rigorous cleanup. [[gate-never-real-save-on-served-repo]] / dont-force-prod-mutation-build-safe-test.
- **3 mock-owner Tests TWO-KEY CLOSED both-dir on origin (req 6ac6504ff): 0c0a69ad↔79a1ce7c + a52393fb↔f345b8ed + b395a272↔394eac63 (all FWD+REV+status:pass).** Stale r311v (baee3c82, marker-only, whoami mechanism REMOVED) DELETED wholesale (181b6ae83) — superseded by r3110 owner-entry a52393fb. R31.4+R31.1 functional ACs chain-close → planner QA-Review. R31.2 DONE. NEXT: stand ready for the boot-sweep gate (expert building).
- **R31.4 boot-sweep-orphans (r3112.ts, GREEN DET-3x served==0.7.102, commit 994b01bad):** PtyBridge.reapOrphans (Impl 5d313828, Method 28d8158b, UC 8bd83486, Class PtyBridge 59648f26). Build-safe (NO prod restart): drove the REAL reapOrphans = the boot call (server.ts:2212, wired-by-source). (1) LIVE fresh-boot 0 stale sm_* (architect restart reaped); (2) attachPane spawns grouped sm_* + injected orphans → reapOrphans sweeps ALL→0 + non-sm untouched (clean live tree); (3) fresh-boot no-op; finally-sweep 0 leak. **Two-key CLOSED c35f02c7↔5d313828 (req e2803797e, both-dir full-uuid, status:pass). R31.4 FULLY chain-complete: attach b395a272 + mount c6791c06 + interactive-RW 0c0a69ad + boot-sweep c35f02c7. S31 all-builds chain-complete.** ★ owned a transient false-RED: liveFreshBoot caught my OWN prior-run sm_* residue (not a boot-miss) → measured stable-0, re-ran clean → GREEN. LESSON: a live-state assertion is self-pollution-sensitive — when it REDs, check if it's YOUR test residue (measure stable) before flagging a bug; the deterministic inject→reap→0 is the real proof.
- ★ STANDING LAW (Tron via SM): WORK + REWIND NEVER FREEZE — work continuously, commit+push zero-loss; at ~85% MEASURED context at a clean boundary FLAG ARON/SM to rewind (don't park). r311a real-owner run STILL HELD (Tron device). ?token= empirical probe uses NO `2>&1` (Tron ban).

**Wheel-ready. NEVER forget TRON CMM4.**

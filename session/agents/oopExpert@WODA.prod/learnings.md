# oopExpert@WODA.prod — Learnings
*Durable skills of THIS instance. Grep before repeating a mistake. Doctrine lives in the base-skills / ARON canon — I point, I don't copy.*

## 2026-09-14 — boot
- **Instance folder = per-host, modelled on `oosh-po@WODA.prod`**: `.claude/agents/<role>@<host>/SKILL.md` (identity, points at base role + doctrine) + `session/agents/<role>@<host>/{reading-list,context,learnings,boot}.md`. Never write the shared role folders.
- **Identity is measured, not remembered**: `$CLAUDE_CODE_SESSION_ID` · `claudeCode session.name` · `otmux pane.self` · `config get OOSH_SSH_CONFIG_HOST`. The pane title lies after `/rename`; `$TMUX_PANE` drifts after a fork.
- **Compound Bash with `cd … &&` and `2>&1 | head` gets denied** — issue simple direct commands (OOSH is on PATH; no `cd`, no `./`). Split verification into small calls.
- **The WODA story's c2 lesson IS the OOP lesson**: `otmux` + Tab shows a self-describing method menu because the script is a class that owns its own description. That is radical OOP in Bash — the object answers for itself; the caller never rebuilds the answer.
- **Context self-measure that works from my seat**: `otmux send oopTeam:0.1 "scrumMaster pulse oopTeam" Enter` → `otmux pane.capture oopTeam:0.1 20` (live token-math, 11% at 09:05). `claudeCode context.read` from inside my own session fails (`no-claude` / `EPERM`). Panel `/context` remains the authoritative near-wall instrument (peer-triggered).
- **"Enhanced CMM4" = `session/knowledge-base/cmm-web4x.md`** (not in base-skills; grep for "Enhanced" found it). The two CMM4 doctrine files (`session/base-skills/tron-cmm4-doctrine.md`, `session/agents/TRON-CMM4-doctrine.md`) are byte-identical. My boot.md now lists all 20 base skills + cmm-web4x in reading order — a boot that names only 4 of 20 is an L2 boot.

## 2026-09-14 — Web4MDA bootstrap
- **Node on WODA.prod for a NEW project**: `/opt/node22/bin` is the latest node on the box (22.23.1 LTS); system `/usr/bin/node` is v16 and must stay. Bare `npm22`/`node22` are symlinks in `/usr/local/bin` whose dir has no `node` → npm scripts fall back to v16 (`npm warn cli npm v10 does not support Node.js v16`). Always `PATH=/opt/node22/bin:$PATH npm …`. Baked into the README of the repo.
- **git 2.25 has no `git init -b`** — use `git init && git symbolic-ref HEAD refs/heads/main`.
- **vitest scans `dist/` too** once you build (compiled `*.test.js` double-count: "2 files passed" for 1 test). Scope with `vitest.config.ts` → `include: ['src/**/*.test.ts']`. Measure the count, don't read "passed" alone.
- **SSH auth ≠ API auth**: `ssh -T git@github.com` proving `mdonges` lets me PUSH but cannot CREATE a repo — that needs `gh` (or a token). `gh` was absent; Tron authorized install → `dpkg -i` the release .deb (`gh_2.100.0_linux_amd64.deb`) works on Ubuntu 20.04 without touching apt sources.
- **`gh auth login --web` on a headless box opens `lynx` in the pane** and hijacks it (cookie prompt). Back out with `V` (neVer) → `Q` (quit); gh keeps polling for the device code; Tron completes the code on his own device. Then verify with `gh auth status`, not the pane text alone — but when Tron says "look in the shell", LOOK (`otmux pane.capture`) before saying "not logged in": my `gh auth status` was stale-by-timing, the pane already showed `✓ Logged in as mdonges`.
- **`gh repo create <org>/<name> --private --source <dir> --remote origin --push`** does create + remote + push in one step; verify with `gh repo view --json visibility,defaultBranchRef` + `git status -sb`.
- **Compound Bash (`a; b 2>&1 | head`) keeps getting denied by the permission layer** — one plain command per call goes through. Costs a call, saves a denial.
- ★ **PUSH ALWAYS (Tron, 2026-09-14, "push always!!!!")**: every commit is followed by `git push` in the same step — both repos (workspace `web4x/Web4AI` and `web4x/Web4MDA`). A commit that sits local is not durable-on-origin (chain-not-complete-until-durable-on-origin); never end a turn with "not pushed — say if you want it".

## 2026-09-14 — RADICAL OOP in TypeScript, corrected by Tron on my FIRST file (learn it — I wrote the violation myself)
- ★ **FILE = CLASS NAME.** `src/index.ts` is a functional-era convention; a radical-OOP file is named after the class it holds: `src/Web4MDA.ts` holds `class Web4MDA`. Same for the test: `Web4MDA.test.ts`. (OOSH: the script file IS the class — `config` holds `config.*`.) My scaffold shipped `index.ts` — that was a violation the moment it was written, however green.
- ★ **NO ROGUE CODE BELOW THE CLASS.** Module-level `if (import.meta.url === …) { console.log(…) }` is functional machinery nobody owns. It belongs on the class as **`static start()`** — the OOSH `script.start()` constructor-of-the-process pattern. The class starts itself; the module's only top-level statement is `Web4MDA.start();`.
- **Make `start()` testable**: inject `argv` and `entry` with defaults (`process.argv`, `import.meta.url`), compare via `pathToFileURL(argv[1]).href` (not string-concat `file://`), return a boolean → 2 failable tests (imported = no-op; entry = runs). Green: typecheck/build/start/dev/3 tests, `017a8e3` pushed.
- **The trigger to watch for in myself**: any code I write outside a class body (free function, top-level `if`, helper) — stop, name the owning class, put it there as a method (static if process-level). Tron: "take the rogue functional code below the class into a static class start method. learn that!"

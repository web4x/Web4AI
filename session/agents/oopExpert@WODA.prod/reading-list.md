# oopExpert@WODA.prod — starting reading list
*My own reading list (NOT the shared expert SKILLs). Read on boot, in order.*

## 0. The Heart (before any work)
- `session/base-skills/security-authorization-law.md` — ABSOLUTE: no security work without TRON's own GO.
- `session/agents/TRON-CMM4-doctrine.md` — TRON/CMM4 canon; the single source.
- `session/base-skills/radical-oop-law.md` — MY law. ONLY RADICAL OOP · MimeType-class-first · transport IS the scenario · every class has traceability + check-before-create · self-healing emerges by construction. Read it whole; it is ONE doctrine.
- `session/base-skills/agent-rewind.md` — the rewind base-skill. Read on EVERY rewind boot.

## 1. My identity + memory (this folder)
- `session/base-skills/identity-verification.md` — run the four commands; never trust a remembered pane/uuid.
- `session/agents/oopExpert@WODA.prod/context.md` — current state / checkpoints.
- `session/agents/oopExpert@WODA.prod/learnings.md` — durable skills (grep before repeating a mistake).
- `.claude/agents/oopExpert@WODA.prod/SKILL.md` — MY SKILL/identity. Base lineage = `.claude/agents/oosh-expert/SKILL.md` + `.claude/agents/robbin-expert/SKILL.md` (inherited, never edited).

## 2. Plan + templates
- **BOOT: `ls scrum.pmo/sprints*` FIRST** — per-host split; WODA.prod plans live under `scrum.pmo/sprints@WODA.prod/`. Never trust a remembered path.
- `session/base-skills/sprint-planning.md` → `session/knowledge-base/planning-templates.md` — the task/sprint templates. Non-compliant artifacts are rejected.

## 3. Doctrine / process (the law families)
- `session/agents/ARON/purified/cross-agent-law-families.md` — F1–F8 connected canon.
- `session/base-skills/gating-canon.md` — R-rules; gate the HAZARD, not the actors; every gate FAILABLE.
- `session/base-skills/git-safety.md` · `session/base-skills/oosh-send-comms.md` · `session/base-skills/context-measurement.md` · `session/base-skills/dont-fork-the-shared-mechanism.md`.
- `session/agents/ARON/purified/robbin-expert.purified.md` — the purified expert role (Web4RawBin side).

## 4. The two OOP dialects I guard
- **OOSH** (pseudo-OOP Bash): `components/OOSH/dev.claude/docs/oosh-architecture.md` · `components/OOSH/dev.claude/CLAUDE.md`. Class = script, method = `script.method()`, constructor = `script.start()`, private = `private.`, c2 completion = self-description at the prompt.
- **Web4RawBin** (TypeScript): the scenario unit JSON IS the model and the only transport. MimeType classes own `isBinary`/`parser`/`load`/`saveAsScenarioUnit`. Natural classes: `WebItem`/`Image`/`Email`/`Contact`/`CalendarEntry`.

## 5. Context (background)
- `session/woda/session-story.md` (chapters 1–9 read 2026-09-14) — how an agent learned to inhabit the terminal; the c2 Tab-completion lesson is the OOSH philosophy in one comparison.
- `CLAUDE.md` (workspace).

# robbin-architect Context — ESSENCE (rewind boundary 2026-07-14, post R30.17-LIVE / R30.19-built)

## ▶ RESUME STATE (read first)
**BOOT PROCEDURE (rewound? do FIRST — the world moved while you were away):** `otmux pane.history robbinTeam2:0.3` + `ls scrum.pmo/sprints*` + `git -C /var/dev/Workspaces/web4x/Web4RawBin log --oneline -25` BEFORE trusting any saved path. Measure the world; where a saved file disagrees, the measured world wins.
I am **robbin-architect @ robbinTeam2:0.3**, WODA.prod. **Code repo = /var/dev/Workspaces/web4x/Web4RawBin** (NOT 2cuGitHub — gone). Session repo = /var/dev/Workspaces/AI/Claude. Prod = prod.wo-da.de:4444 (v0.7.28 as of this save). Node18 = /root/.vscode-server/bin/.../node.
Team panes: 0.0 po, 0.1 expert, 0.4 req, 0.5 tester, 0.6 planner (0.0-0.6). Server runs in **remoteShells:0.2** (I own restarts: Ctrl-C + `npm start`, node22).

**MY ROLES (2, unchanged):**
1. **DESIGN-ONLY** → hand chain spec to **req (SOLE MINTER of every scenario unit)** → I **DERIVE-CONFIRM** the minted chain by **uuid-FILE** → signal PO build-ready → PO issues the single BUILD-GO → expert builds. I NEVER mint.
2. **DERIVE-CONFIRM GATE** (the gate PO holds build-go behind): comprehensive on-disk derive — name-exact, Class-reuse count, designAhead, unit-level ownerIor, 0-dup (measure a Class by its uuid-FILE not grep), supersede-retirements. Post-build I confirm **marker-attach** (the [impl:uuid] sits on a name-matching decl — presence != AST-attach) + **supersede clean** (0 orphan). I OWN GitApi/server restarts (remoteShells:0.2) for server changes.

**⭐ Rules:** TRON #126 scenario-first-never-backfill · NEVER assume, MEASURE (full uuids, prefix collisions real) · marker = IMPL uuid on name-matching decl · ownerIor = UNIT-level (j.ownerIor), all else = j.model · report to PO (0.0) BEFORE idle · OOSH wrappers, SIMPLE single commands (raw `docker` for plantuml render only; NO `2>/dev/null`/`| head`/`||`/compound).

## ★ IN-FLIGHT HANDOFF — resume priority (RECONSTRUCTED from git; I died at 0% before saving R30.19 — VERIFY on disk)
**I hit 0% context mid-R30.19 derive-confirm. PO issued the R30.19 build-go WITHOUT me** (on req + PO independent chain-verify, Impl eb994dcd confirmed) because I was blocked + Tron was asking. R30.19 has since BUILT + shipped. On reboot, my two open jobs:

1. **R30.19 BACKSTOP-CONFIRM (do first).** Side-pane change-block highlights (Tron IMG_4518 / screenshot): colored change-blocks in the **Local + Repository source panes** (not just center). NEW Method **RbDiffEditor.renderSideChangeBlocks** (was impl-edit, revised to NEW-method per spec, architect design df243841a). BUILT v0.7.28 (expert 68922236e), bundle edit-YCBWO635.js. **Marker `[impl:uuid:eb994dcd-d9dc-4b55-84e8-13b2be3b47d5]` — my measure 2026-07-14: sits rb-diff-editor.ts:312 AST-attached above the `renderSideChangeBlocks` decl :317, called from renderMergeGutter tail :262.** RE-VERIFY on disk (don't trust this stale line#): marker on name-matching decl + the full UC→Method→Impl chain derives + design invariants hold — deltaDecorations on edLocal (a-lines from c.aStart) + edRemote (b-lines from c.bStart), SAME `de-block-<kind>` class + shared CONFLICT_PALETTE as center (color-match by construction), origin-aware (c.a.length>0→Local, c.b.length>0→Repository, R30.17 gate), LINE-anchored via Monaco Range (no getTopForLineNumber → no off-by-one), own _sideDecoIds{local,remote}; renderCenterChangeBlocks STAYS center-only. Report PASS + confirm to PO (0.0).

2. **R30.11 honorSupersededBy — needs architect+expert build (clears the 3-req false-open class R30.10 + R30.6.1 + R30.6.3).** Scorer change: the orphan/false-open scorer must honor `supersededBy`. Target on disk (my measure): skill-classes.ts:193 `if (m.supersededBy) return true;` inside `isOrphanByDesign` — commit f8e6d1ea4 re-pointed the Impl at the REAL scorer (skill-classes.ts:193 impl-level honor, tester-measured — NOT trace-cli.ts). Confirm the chain, derive-confirm, signal PO build-go, coordinate expert build.

## ★ LIVE STATE — S30 DIFF/MERGE EDITOR ARC (IntelliJ 3-way merge/diff editor, the S30 spine)
Feature evolution R30.6 → R30.19, all on Class **RbDiffEditor 18165081** (+ server **GitApi 795e328d**, **RepoRegistry**, **RbEditorLayout 94e7bf82**). Current head of the arc:
- **R30.9 IntelliJ base-aware 3-way merge — DONE + LIVE v0.7.18.** 3 Monaco editors (local | result-autocomplete | remote) + node-diff3 VENDORED (src/public/ts/vendor/diff3.ts, BASE=GitApi.mergeBase) + gutter accept-arrows + applyAllNonConflicting + syncScroll3. TRON ruled "IntelliJ at ANY cost" → CUSTOM (not monaco-vscode-api = that's VS-Code UX). Superseded R30.6.1(15843ac9)+R30.6.3(37636aaa/6ebfac12), 0-orphan.
- **R30.10 file-history — DONE + LIVE v0.7.19.** GitApi.fileHistory + RbDiffEditor.populateRightHistory. **NOTE: populateRightHistory (Impl 58c11039) later SUPERSEDED BY populateLeftHistory (R30.17)** — Impl unit STAYS (credit, tests 16b2721a+553f77d2 intact), marker removed-with-code.
- **R30.13 inter-pane gutters + connector ribbons / R30.14 SW auto-update / R30.12 2-way take-over — all DONE + LIVE** (v0.7.20-25 range). Ribbon-visibility root cause was WIDTH (1px gap) not z/opacity → Y(alignPaneRows,me)+X(gutter-widen,expert).
- **R30.16 IntelliJ layout — DONE + LIVE v0.7.26.** alignPaneRows (Monaco viewZone spacers → rows line up L↔C↔R) + renderCenterChangeBlocks (colored rounded blocks on CENTER hunks) + scroll-fix (scrollBeyondLastLine true) + expert ribbon-visibility. DRY shared `conflictColor()`/`CONFLICT_PALETTE` (kind:conflict|resolvable|change SET at hunk-creation, PURE classifier, both center-blocks + ribbons pass SAME c → same color by construction). Coverage v1=conflicts[]-only (2-way 100%); **v2 = MINT-READY BACKLOG (design 84f013855): computeMergedCenter surfaces 3-way one-sided regions as kind=change hunks → full IntelliJ 3-way, NO new Class/Method**, hand to req when PO schedules.
- **R30.17 merge FUNCTIONAL FIXES — DONE + LIVE v0.7.27** (1684e675d). Tron diligent review: R30.16 shipped VISUALS but merge was BROKEN (gate verified appearance not function). 5 fixes, design a2ff49697: #4 accept/cancel zero-effect = CLICK-ROUTING (delegate from stable ROOT once in mountThreePane, drop per-strip listener) · #1/#3 both-sided/ghost ribbons (origin-gate on c.a/c.b length) · #2 off-by-one Y (pin anchors) · #5 history RIGHT→LEFT (populateLeftHistory supersedes populateRightHistory). 5 markers AST-attached PASS (my post-build gate), supersede 0-orphan. ★ TESTER-GATE LESSON banked: DET-3x MUST assert click → CENTER-CONTENT mutates (function not appearance = the R30.16 miss).
- **R30.19 side-pane change-blocks — BUILT v0.7.28, awaiting my backstop-confirm** (see ★ HANDOFF above).

## DESIGN DISCIPLINE (banked — the architect's earned rules)
- **Derive-confirm by uuid-FILE, never by grep/name.** A Class = its uuid-file (Class=1, each Method=1). Prefix collisions are real → full uuids always.
- **Supersede-retirement rules (R30.9 / R30.17 pattern):** superseded Method+Impl → annotate `supersededBy` + in-build REMOVE the marker WITH the code (no orphan) BUT the Impl UNIT STAYS (credit) with its tests intact; the superseding req sets `.supersedes=[...]`; `supersededNote` (correct field — NOT supersedeNote) captures build guidance; tester retires the old behavior test. NEW-method (not rename) when rename would break a prior chain's test.
- **VS-Code-fidelity vs IntelliJ-fidelity distinction (R30.9):** monaco-vscode-api = VS-Code merge-editor UX; IntelliJ = CUSTOM 3-Monaco + node-diff3 base-aware center + gutter accept-left/right. Tron chose IntelliJ-at-any-cost → CUSTOM.
- **Base-aware diff3:** center = diff3Merge(local, BASE, remote), BASE=GitApi.mergeBase; auto-merge one-sided, flag true conflicts. No-base → 2-way fallback (computeTwoWayHunks, LCS, pick='a').
- **Marker = IMPL uuid on a name-matching decl.** presence != AST-attach; a marker on a non-name-matching member won't flip the Impl hop (fix = Option-3 EXTRACT a real named method, marker on the decl).
- **CROSSED-DISPATCH guard (R29.9, PO standing rule):** ONE mover per chain. req = SINGLE minter; BUILD-GO single-source + PO-gated (no "build now" until chain minted + derive PASS + one PO build-go). If I see a second mover on held work: STOP + disk-scan 0-dup + commit-correct-state (fastest way to end a mint race).
- **Pure-client changes = no restart; server/GitApi changes = I restart remoteShells:0.2 + verify /api/config version + route-loaded (401=loaded not 404).**
- **Diagrams (law#100):** scenario-derived puml+svg in sprint-30-*/diagrams/. RENDER: `docker run --rm --security-opt seccomp=unconfined -v <absdir>:/data plantuml/plantuml -tsvg /data` (raw docker — odocker forces TTY/fails headless). @startuml = path-safe slug; verify SVG >10KB + no 'syntax error'.

## STANDING PDCA DISCIPLINE
measure-not-message · criteria-not-counts · gate-on-CONSERVATION (re-baselined to disk) · repair-scope=gate-scope · plan-gate + post-apply-reassert · verify with an INDEPENDENT tool/METHOD (raw-grep the ref, not the parsed slot you wrote) · full-uuids-never-8char-prefix · reuse-on-stable-identity (file/uuid, not name) · "at-least" = FLOOR not fixed.

## HISTORY (completed + credited — full narrative lives in git, do NOT replay)
- **Diff/merge feature set R30.6→R30.10 COMPLETE + LIVE** (3-pane editor+GitApi+RepoRegistry / [Open Diff] btn / repo-targeting / guardRef / 3-way merge / file-history). All chains derived + marker-attached + restart-verified by me; expert authed-verified live.
- **R30.4 applyProfileName / R30.5 loadDir (RbFileTree) — DONE + credited** (v0.7.12/13, Tron-confirmed).
- **R30.2/R30.3 reconcile — DONE, 61/330**, canonical units pinned (d88b80fe4); my rewind-boot verify caught 2 dangling Task navs + 4 back-edge dangles (PO fixed).
- **R27.5 canonical-ref-slot-registry / TraceAudit axes 2-5 — MINTED + built** (f6ac09521): nodeWellFormedness/oneClassPerFile/markerHasChain/auditDir on TraceAudit bf626dad. Chain-coverage audit S21-25 criteria defined + handed to skill-expert (they RUN, gaps=backlog measure-first).
- **R27.8 drawer lifecycle — DONE** (tester GREEN). Backlog designed design-ahead: R29.3 ServerConfig, R29.4 sprint-governance, S30 AgentMessage/AgentMailbox (async no-keystroke mailbox = fix for otmux staging pain).
- Deep implementation learnings (Monaco arch, hybrid encryption, PWA cache, forward-only chain, 6-step chain, champagne=structural+intentional, marker-AST-attach, device-telemetry-beats-guessing) → `learnings.md`.

## OPEN
- **R30.19 backstop-confirm + R30.11 build** = active (★ HANDOFF above).
- **R30.16 v2** (3-way one-sided coverage) = mint-ready backlog, hand to req when PO schedules.
- User task PENDING: "each Class json traces to its puml + impl as an ior" — measured 117 Class units, 76 have sourceFile(impl-ior), 0 have puml. Fold into R27.5 registry (Class ref-slots: sourceFile=impl + puml slot).
- (D) legacy no-chain reqs (52) — PO DEFERRED to Tron post-rewind. Federation T26.1/2/3 PDCA pending. R27.6/S28 dangling residual.

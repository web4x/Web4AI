# robbin-architect Context (Save 2026-07-01)

## ▶ RESUME STATE (read first). I am robbin-architect @ robbinTeam2:0.3, WODA.prod. Code repo /var/dev/Workspaces/2cuGitHub/Web4RawBin; session /var/dev/Workspaces/AI/Claude. Node18 /root/.vscode-server/bin/903b1e9d8990623e3d7da1df3d33db3e42d80eda/node. otmux to team (0.0 po,0.1 expert,0.2 skill-expert,0.4 req,0.5 tester,0.6 planner). ⭐ TRON #126 scenario-first-never-backfill (learnings). NEVER assume — measure with FULL uuids (prefix collisions are real).
**STATUS: S27 cleanup arc.** R27.2 class-dedup LANDED CLEAN (18a8703e2, 163->108, 5 measure-before-act catches). **R27.2 6/6 SIGNED OFF (1aeaebf2c):** expert shipped class-mint.ts:mintOrReuseClass (marker 61f24d2c, choke-point) + trace-audit.ts duplicateClasses in hardIssues (--strict fails on dup). PO 3-part answered: (1) choke-point covers in-code; the recent-dup source = TEAM MINT SCRIPTS (req/arch/planner ad-hoc node scripts, not src/) -> route them at mintOrReuseClass + CI backstops any path; (2) recommend SPLIT ClassDedupGuard -> Class ClassMint(mintOrReuseClass) + REUSE existing TraceAudit(assertNoDuplicateClasses, dogfoods reuse); (3) signed off. RECONCILE 2207-vs-51 = DIFFERENT METRICS: audit orphans(~1107/2207)=all-types-unreachable-from-Req-roots (TestCase/Device/Task/File/Room=legit non-chain, never 0); R27.4 51=Method-not-in-Class=fixable defect. CI orphan gate MUST be Method-scoped.
**R27.4 SCOPE LOCKED (632ccba1a):** 51->0 ALL-ATTACH + CLEAR 37 STALE orphanByDesign markers (req proved their impls point at REAL sourceFiles; markers lied — code shipped after 2026-06-11). A1 10 dangling->live RbDetailView f2f84ce3-6f8f, A2 dead-method triage, A3 TODO-string fix. +audit flags LYING marker (orphanByDesign+real sourceFile=fail). INV1b trivial (0 deleted, 431==431).
**R27.4 (a)-(d) LOCKED w/ expert (f8b2af4), building repair-r27.4.ts:** (a) resolver#4 = derive code-class from className||name-prefix||Impl.sourceFile -> mintOrReuseClass find-or-MINT (dogfoods R27.2 guard) -> attach; 51->0, 0 prune (all carry impl). (b) conservation RE-BASELINED 431->434 (R27.2 guard shipped 3 Impls); gate 434==434. (c) repoint ALL dead f2f84ce3-BBBC (FULL uuid, not -6f8f) -> -6f8f, gate 0-remain. (d) clear stale markers by CRITERIA (orphanByDesign AND Impl.sourceFile present), not hard-37; I measured 53 Methods all-stale. Watch: resolver#4-minted Classes must be single-per-name (feed duplicateClasses=0).
**R27.2 ClassMint mapping (adjudicated):** mintOrReuseClass DEFINED in class-mint.ts (NEW module) -> planner mints Class ClassMint there (NOT MigrateToScenario 66625aa7; minting 1st Class for a new module != dup-violation). duplicateClassAssertion->TraceAudit bf626dad correct.
**2207 = (a)benign non-chain DATA ~400 + (b)walk-gap ~1400 [TestCase 1023 missing Test->testCases edge (NOT benign — all ref by Test.testCases) + Task subtree missing Sprint-roots] + (c)real debt (fixed-residual 1313). R27.5/S28 fix=3 parts; NOT R27.4.**
**R27.4 DRY-RUN PDCA v1 = PASS on hard gates (measured, matches expert table b16720332):** orphan 51->0 (resolver 40 className/10 UC.class/1 CREATE-server/0 ownerIor); CREATE-server VERIFIED no-dup (no existing server Class, any case); 434==434; bbbc/fcf6dae1/TODO->0; 0-new-dangling. 37-stale reconciled (field=designStage ∩orphans=37). MY CATCH: 53 TOTAL lying markers (any of designStage/orphanByDesign/orphanReason + impl-sourceFile); R27.4 v1 cleared only 37 orphan-scoped -> 16 attached-lying remain -> lying-CI would red. PO RATIFIED broaden-to-53 (repair-scope=gate-scope). create MUST route thru mintOrReuseClass (idempotent, feeds duplicateClasses=0) — confirmed.
**ON RESUME:** RE-PDCA the expert's re-emitted 53-CLEAR dry-run table (markersCleared=53 incl 16 attached; create-via-mintOrReuseClass; hard gates unchanged 51->0/434==434/dead-refs->0/0-new-dangling/atomic+reassert) -> dual-verify w/ req -> atomic --apply greenlight. Then post-apply re-measure the 5 conserved counts. Pending federation: T26.1/T26.2/T26.3 PDCA. Discipline: measure-not-message, criteria-not-counts, repair-scope=gate-scope, CONSERVATION-re-baselined-to-disk, Method-scoped-not-all-types, full-uuids-never-prefix.


## R27.2 DRY-RUN PDCA = PASS + INV1b GREEN (commit be53b87da). Table verified: 163->108 (-55), INV2 DELTA dangling 12->12 (0-new), INV4 4 canonicals kept + R25.6 2179d235 no-collision survives, RbDetailView dups=3/mMoved=4/nameColl=0. EXPERT CATCH -> added INV1b (no IMPL lost): 431 Impl units; 45 same-name union-pairs. UNION = REPOINT collapsed methods impls onto keeper (Impl.ownerIor->keeper), NEVER delete -> 431==431 + 0 orphaned. R27.2=CLASS dedup NOT Impl dedup (byte-identical both kept; Impl-dedup->R27.3 w/ 51 orphans). GATE remaining before atomic --apply: expert (a) repoint-union in --apply body, (b) re-emit table w/ implBefore/implAfter=431/431 columns for numeric INV1b; then req post-apply re-verify -> --apply GREEN both sides. Catch #5 on R27.2 (impl-drop-on-collapse). learnings: union-all-child-arrays-on-merge + emit-every-level-count.

## R27.2 RbDetailView canonical SETTLED = f2f84ce3-6f8f (commit e3814506d, PO decision). RESOLVED: f2f84ce3 was an 8-CHAR PREFIX COLLISION — f2f84ce3-6f8f-4db1-9ab7-dbcfe8d3bc07 (LIVE RbDetailView 8m/21refs, my catch right) vs f2f84ce3-bbbc-4bf7-9345-6a9d4dc64fb5 (DEAD dangling target, req verifier right). Both right; use FULL uuids. CANONICAL RULE (final): LIVE-most-refs (efficiency) + marker preserved by HARD MERGE-CONSTRAINT (keep marker-carrying method on name-collision), NOT marker-unit=canonical. RbDetailView->f2f84ce3-6f8f; merge 2eeda38d(has R25.6 marker 2179d235 on method 0ce8290f renderScenarioLink=DISTINCT name->NO collision->clean)+1420a915+a27784f0. HARD INV4: 2179d235 derives on canonical post-merge. R27.4 points its 10 dead-bbbc refs at SAME canonical. Other 19 classes: LIVE-most-refs. UNBLOCKED — 37c52953 KEEP, wire post-planner-T27.2. The 4th R27.2 catch (prefix-collision) recorded. Learnings updated: marker=merge-constraint-not-selector + full-uuid-never-prefix.

## (superseded) R27.2/R27.4 f2f84ce3 reconcile flag — req enumeration says RbDetailView f2f84ce3 is DEAD (10 of 12 dangling->it). I MEASURED disk: f2f84ce3 EXISTS = ior:class:Class RbDetailView 8 methods, 11 UCs reference+RESOLVE (NOT dangling). So either (A) alive->those arent dangling + f2f84ce3=most-methods RbDetailView canonical, OR (B) superseded-but-undeleted->repoint its 11 refs to live 2eeda38d (carries SHIPPED R25.6 marker 2179d235=active-chain) + delete f2f84ce3. Determines R27.2 RbDetailView canonical (most-methods f2f84ce3 vs active-chain 2eeda38d) + R27.4 dangling set. Asked req (baseline authority) to reconcile alive-vs-should-be-dead. HOLDING R27.2 RbDetailView canonical + migration-UC wiring until reconciled; rest of 23-class design unaffected. 37c52953 DECISION: KEEP (chain.reuseCanonicalClass=invariant=shipping code, needs chain) + add chain.dedupClassNodes; wire post-reconcile. 3rd data-integrity catch on R27.2 via measure-before-act.

## R27.2 INV2 -> DELTA GATE (commit 9e5d25da2, req baseline d1dec787b + PO): graph has 12 PRE-EXISTING dangling UC refs + 51 orphan Methods (pre-dedup DEBT, not my migration). INV2 gates DELTA (dangling AFTER<=BEFORE=0 NEW), NOT absolute-0 — so the T27.2 dry-run PDCA doesnt false-abort. 12+51 = separate cleanup (candidate R27.3, offered scenario-first). Intersection: if any of 12 point at a collapsed dup Class, remap rewrites them (bonus). req enumerating the 12. When I PDCA T27.2 dry-run: use DELTA for INV2.

## R27.2 CLASS-DEDUP MIGRATION DESIGNED (commit a3fcf1196, design-notes/r27.2-class-dedup-migration.md). 55 dups/23 code classes -> 163->108. req 64965538. KEY: active-chain units are NOT most-methods (IORResolver R26.1 b4eaa489=1m, Room R25.7 2172dc56=4m vs 1165c293 16m, RbDetailDrawer R25.4 d86af73d=2m vs 0e293e59 14m) -> canonical = ACTIVE-CHAIN-unit-if-exists (KEEP those 3, merge others IN, live UC.class unchanged) else most-methods. Algorithm: move methods dedup-by-name (collapse same-name+rewrite UC.method), rewrite EVERY UC.class/classes ref across 287 UCs (R26.5 remap), delete emptied dups. GATE dry-run+count FIRST; 5 hard invariants (no method lost / no dangling UC.class / no orphan / active chains R26.1/R25.7/R25.4/R26.4-5 derive / idempotent). AC-canonical = by-construction reuse + CI check (1 Class/code-class-name). AWAIT planner gated task -> req 3-point verify. Offered chain.dedupClassNodes UC scenario-first if wanted.

## R27.1 UC refined (commit 165874700): retroactive chain for v0.7.6 shipped code. UC 050c5b9a taskDetail.renderStatusChecklist -> REUSED Class RbTaskDetail 4df19279 (added method, NO 5th) -> Method RbTaskDetail.renderStatusChecklist bfd21995 -> Impl 31f420b0 (REAL, rb-task-detail.ts). req 90b82d00 (R27.1), sprint c1c63a2e (S27). Derive PASS. 2 FLAGS to req: (1) 4 DUPLICATE RbTaskDetail Class units (4df19279/d038f521/df8d2680/f09de66b) — class-per-req sprawl, recommend dedup-to-one cleanup task; (2) shipped fn lacks [impl:uuid] marker — expert adds 31f420b0. NOTE: retroactive = #126 backfill (code shipped before chain) — repaired, flagged.

## PLACEMENT CONFIRM (planner Q, v0.7.7): importScenario belongs on Class FederationApi (6456d811) — CONFIRMED. FederationApi = federation HTTP API surface both ways: fetchScenario(origin GET /api/scenario, 3089d066) + importScenario(receiver POST /api/federation/import, 3132c189); Transfer = pure algorithm (reconcile/lazy) INVOKED by the endpoint (server.ts:451-455). Not Transfer (conflates), not loose server (loses cohesion). Method c4506b11 on FederationApi correct. 2 NITS flagged: (1) name mismatch code=federationImport vs unit=importScenario -> pick importScenario; (2) number R26.7 marker vs R26.6 planner said -> confirm w/ req. R26.6/7 req+UC pending; planner wires UC->FederationApi when they land.

## PDCA T26.4+T26.5 (federation-transfer.ts) = PASS (2026-07-01, post-rewind). Markers ATTACH to my design-ahead Impls (3b17aad3->resolveChildrenLazily, 8d34ad57->reconcileConflict) = #126 gold (attach not backfill). R26.4: eager primary/lazy children@host/members-by-reference-NEVER-minted (R25.7 invariant honored)/contentHash dedup — faithful. R26.5: 4 cases correct + rewriteForwardRefs remap + provenance + bonus local-born-vs-remote collision hardening. 2 NON-GATING NOTES: (1) expert resolved open decisions #3(updatedAt for update-if-newer)+#4(own remap) by shipping defaults — FLAG Tron to ratify + confirm updatedAt stamped; (2) rewriteForwardRefs covers children+parentFolder (right for artifacts), not chain-unit arrays. OK TO GATE. Remaining open decisions: #1 inline-vs-lazy threshold, #2 auth tiers (R26.2/R26.3 scope). T26.1 federatedIor also shipped (imported).

## ▶ RESUME STATE (read first)
I am robbin-architect at robbinTeam2:0.3 on WODA.prod. Repo: /var/dev/Workspaces/2cuGitHub/Web4RawBin (code) + /var/dev/Workspaces/AI/Claude (session). Node18: /root/.vscode-server/bin/903b1e9d8990623e3d7da1df3d33db3e42d80eda/node. otmux for team msgs. NEVER assume — measure. ⭐ TRON #126: SCENARIO FIRST, NEVER BACKFILL (see learnings) — mint FULL chain seed to Impl design-ahead up front; reject tasks with no scenario unit.
**STATUS: IDLE, S26 T26.4/T26.5 PDCA DONE = PASS (context 54c63a4).** Federation transfer shipped + verified (markers attach to my Impls 3b17aad3/8d34ad57, R25.7 members-by-reference honored, OK to gate). Chain 5/5 whole (7efe38ab9). REMAINING: T26.1/T26.2/T26.3 not yet PDCA'd (T26.1 federatedIor shipped, imported); Tron to ratify open decisions #3(updatedAt)/#4(remap) resolved-by-impl + adjudicate #1(inline-vs-lazy threshold)/#2(auth tiers).
**ON RESUME:** PDCA T26.1/T26.2/T26.3 when flagged (verify markers attach to Impls d2cde3fc/f484952c/3089d066, not new units); OR fold Tron decision answers into design-notes/federated-scenario-transfer.md; else await assignment. Standing PDCA discipline: measure code not commit-msg, commit-label≠chain, verify marker-attaches-to-EXISTING-designAhead-Impl (#126), quality-vs-traceability separate verdicts.
**5 OPEN FEDERATION DECISIONS (planning.md, await Tron):** (1) inline-vs-lazy JSON threshold (tiny units inline in DataTransfer vs always reference+fetch); (2) auth tiers (capability-token ad-hoc vs server-keypair-signature+trust-list standing); (3) updatedAt/version field for same-origin idempotent update-if-newer; (4) reference-rewrite/remap reuse (share the company/import remap pass?); (5) R25.7 tie-in (foreign room members stay federated references, materialize only on connect+consolidate/redirectTo — NEVER minted locally, else recreates the dup R25.7 fixed).
**Recent chain work (all scenario-first #126, design-ahead Impls await expert markers):** S26 Federation (7efe38ab9), R25.7 room-identity-dedup (748122237, 4 UCs Class Room), R25.4 drawer chrome (578cc84f5), R25.5/6 (5c3d55e30 + cd5e5ea60 2nd UCs), R25.2 WebItem. Chain coverage S21-25 = 100% to Impl (95c136127). Standing PDCA discipline: measure code not commit-msg; commit LABEL≠chain; quality-GOOD-but-traceability-OFF is its own verdict.



## S26 FEDERATION 5 UCs REFINED (commit 7efe38ab9): req captured S26 (926017353) scenario-first from my design; I refined 5 UCs -> 4 Classes (Transfer shared R26.4+R26.5) all design-ahead greenfield: R26.1 9b4a8c02 ior.federatedOrigin/IORResolver, R26.2 13ce665c dnd.federatedReference/DropDispatcher, R26.3 e205f1b0 federation.scenarioFetchApi/FederationApi, R26.4 67859edd federation.lazyChildResolve/Transfer, R26.5 1f097e01 federation.conflictReconcile/Transfer. Derive 5/5 PASS. 5 OPEN DESIGN DECISIONS in planning.md await Tron (inline-vs-lazy threshold, auth tiers, updatedAt/version field, remap reuse, R25.7 tie-in). AWAIT planner tasks + expert.

## FEDERATED SCENARIO TRANSFER designed (commit 7e940cf81, design-notes/federated-scenario-transfer.md) — RawBin federation foundation. Principle: structure eager/payload lazy/identity by-reference. (1) IOR ior:instance:<uuid>@<originHost> (local omits @host=backcompat) + model.originHost/originIor + pluggable federated loader. (2) DnD = rb-federated-ref {ior@host,fetchUrl,contentHash} NOT full JSON. (3) GET /api/scenario/<uuid>{,/content,/children} server->server (no CORS) + capability-token(ad-hoc)/server-keypair-sig(standing). (4) eager primary/lazy bytes+subtrees; contentHash cross-server dedup; ROOM MEMBERS by-reference not minted (ties R25.7 — minting foreign identity = recreating the dup we fixed). (5) conflict: v4 uuid so same-origin=idempotent-update, other-origin=re-mint+reference-rewrite, content dedup by hash; record provenance. Security: signed/expiring/scoped grants, validate inbound. #126 greenfield -> own Sprint; 5 UCs for req: ior.federatedOrigin/dnd.federatedReference/federation.scenarioFetchApi/federation.lazyChildResolve/federation.conflictReconcile. AWAIT req capture -> refine full design-ahead chain.

## SCENARIO-LINK LOCATIONS review (for ✏️ pencil ask): 📄 Scenario appears in (A) scenarioBrowserLinkFromIor (detail-children.ts:61 shared renderer, R25.6 marker 2179d235 = SHIPPED) -> 10 detail views (class/method/req/test/impl/task/usecase/file/webitem/generic); only rb-file-detail:65 has a separate ✏️ Edit-scenario pencil. (B) RoomView.ts:165 room header 📄 (no pencil). (C) Forward-link rows scenarioBrowserHref in req/task/usecase details (navigational). Server /md/ browser already has per-entry ✏️ (server.ts:1259). FIX (DRY): add ✏️ pencil INTO scenarioBrowserLinkFromIor (covers 9/10) + RoomView:165; drop rb-file-detail:65 redundant pencil; forward-link rows = pencil on target detail not per-row (confirm Tron). Scenario-first #126: req adds AC to R25.6 or tiny new req; I refine, expert adds pencil in shared renderer. Reported.

## R25.7 4 UCs REFINED (commit 748122237): req minted R25.7 (585b6b9c) scenario-first from my design note; I refined 4 UCs on ONE Class Room (2172dc56): e03132c7 room.dedupMembersOnLoad, 9300a275 consolidate.evictAbsorbedFromRooms, ede1de17 connect.redirectTombstoneToPrimary, 85f40027 room.addMemberIdempotent. Impls design-ahead (fix not shipped). Derive 4/4 PASS. Repair = separate gated migration task (planner). Full clean scenario-first cycle. AWAIT planner tasks + expert impl (will PDCA when v ships — verify markers attach to these Impls).

## ROOM IDENTITY DEDUP structural fix DESIGNED (commit 662c0cc0f, design-notes/room-identity-dedup-structural-fix.md). Heartspaces 3 Marcels+SystemTester=4. Root: CONSOLIDATE(server.ts:2102-2140) sets redirectTo but never cleans room.members; resolveToken(Room.ts:106) dedups only allMemberInfo(:314) not raw map/memberCount/JOIN(:1840); redirectTo not immutable->lost on restart(37fcb752). FIX: (a)ROOM LOAD collapse by resolveToken; (b)CONSOLIDATE evict absorbed from ALL rooms; (c)CONNECT redirect tombstone->primary + redirectTo immutable; +invariant addMember idempotent-by-resolved-token + memberCount/JOIN use allMemberInfo; +1-time gated repair (re-tombstone 37fcb752->8f74dfba, collapse Heartspaces). Scenario-first #126: req mints req, I refine UCs room.dedupMembersOnLoad/consolidate.evictAbsorbedFromRooms/connect.redirectTombstoneToPrimary/room.addMemberIdempotent (Class Room). AWAIT req capture.

## PDCA v0.6.98 (a281af68b): code GOOD, traceability OFF. VERIFIED: fetchPageTitle (server.ts:332, 3s/<=3redirect/200KB/hostname-fallback) CORRECT; photo-auth fix (server.ts:697+Room.ts:401 roomsWithFile — authorize uploader OR file-room member OR any-loaded-room-with-file) CORRECT; name!=desc + encoding repair fine. #126 no-new-units GOOD (6 changed units = data backfill). 2 CHAIN GAPS flagged to PO: (1) MISLABEL — commit says R25.5/R25.6 but code = R25.2 WebItem naming + File-photo BUGFIX; R25.5(clipboard)/R25.6(scenario-link) NOT touched, DONT flip implementing[x]. (2) NO impl:uuid markers — code not wired to any chain (my R25.5/R25.6 designAhead Impls all still not-in-code=untouched, correct). NEEDS: WebItem.fetchPageTitle Method+marker under R25.2; File-content-auth-fix scenario for photo bug. Offered to mint R25.2 fetchPageTitle + photo-fix chain scenario-first.

## R25.5+R25.6 2nd UCs added (commit cd5e5ea60, scoreboard OPEN ARCHITECT closed): each req = 2 AC-clusters -> 2 UCs (R25.4 precedent), one Class per req w/ 2 methods. R25.5 +1aa5b1c5 clipboard.readAndRoute (AC-read/route) on Class ClipboardImport. R25.6 +f81594b4 detail.scenarioLinkResolve (AC-target) on Class RbDetailView. Both added to req.useCases[]; full seed to Impl design-ahead. Derive PASS. AC->UC map: R25.5{previewAndImport<-listener/preview/ask, readAndRoute<-read/route/no}; R25.6{scenarioLink<-link/all/consistent, scenarioLinkResolve<-target}.

## R25.5+R25.6 UCs REFINED scenario-first (commit 5c3d55e30, FIRST clean #126-compliant hop): planner stood reqs+tasks first; I minted FULL chain seed to Impl (design-ahead, not Class+Method-only). R25.5 10af6d46 clipboard.previewAndImport -> ClipboardImport (base drop-dispatcher.ts/RoomView). R25.6 dc468781 detail.scenarioLink -> RbDetailView.renderScenarioLink (base detail-children.ts scenarioBrowserLinkFromIor, uniform on ALL rb-*-detail). Derive 2/2 PASS. Expert attaches [impl:uuid] at code time.

## ⭐ STANDING RULE — TRON #126 (ABSOLUTE): SCENARIO FIRST, NEVER BACKFILL.
Order: Sprint→Requirement→Task→chains wired (Req→UC→Class→Method→Impl→Test)→MD GENERATED→THEN code. Scenarios on disk BEFORE any impl. A backfill = rule violated = DEBT. This session paid the S21-S25 backfill debt (20→44/301) — NEVER AGAIN. If I get a task whose scenario unit doesn't exist, REJECT + report PO. I mint FULL chain seeds to Impl (design-ahead) up front — never Class+Method-only (the crisis omission). Details in learnings.md ⭐ STANDING RULE.



## CHAIN COVERAGE AUDIT S21-S25 (commit 6a5c5fa61 closed R25.3 gap). Per-sprint req->uc->class->method->impl->test:
- S21: 9/9/9/9/8/8 (R21.2 noImpl). S22: 4/4/4/4/0/0 (ALL stop at Method). S23: 3/3/3/3/1/0 (R23.1/2 noImpl; R23.3 noTest). S24: 5/5/5/5/5/5 FULL. S25: 4/4/4/4/4/2 (R25.3/4 noTest).
- ROOT CAUSE of Impl gaps: S22/S23 UCs minted in the missing-UC CRISIS as Class+Method ONLY (no Impl) — I only started minting Impl at S24. Code SHIPS (R22.1/3/4 GREEN in PDCA; R23.1/2 shipped) → BACKFILL 7 Impls (S22 x4, S23 x2, S21 R21.2) pointing at shipped source. ASKED PO to proceed. Test gaps = tester champagne pending / R25.4 designAhead.
- R25.3 gap CLOSED: UC c461d975 onboarding.recognizeIdentity -> Class Onboarding -> Method -> Impl (ProfileEditor.ts, ships). req d0acb05d.
- 7 IMPLS BACKFILLED (commit 95c136127) — ALL S21-S25 derive to IMPL 25/25. Measured shipped-presence per file for honest designAhead: R21.2 RoomBrowser.ts, R22.1 detail-superseded.ts, R22.2 pan-zoom.ts, R22.4 server.ts, R23.1/R23.2 content-preview.ts (all REAL); R22.3 detail-superseded.ts DESIGN-AHEAD (chain-node source-link renderer grep=0, not shipped — matches my R22.3 gap report). FINAL COVERAGE: S21 9/9/9/9/8, S22 4/4/4/4/0, S23 3/3/3/3/0, S24 5/5/5/5/5, S25 4/4/4/4/2. REMAINING = Test layer only (tester champagne). Architect chain-integrity COMPLETE to Impl.

## R25.4 WIRED (commit 578cc84f5): req minted R25.4 (225b18a6) for BUG2+BUG3. My design call = TWO UCs on a SHARED Class RbDetailDrawer (d86af73d, methods=[onGrabBarPointer, minimize]): UC1 c6df9164 drawer.grabBarMouseParity -> Method onGrabBarPointer; UC2 2438307a (NEW, I added to req.useCases[]) drawer.minimizeToggle -> Method minimize. Impls designAhead (bug-fix not shipped, sourceFile rb-detail-drawer.ts). Derive 2/2 PASS. BUG1 still = R25.2 impl scope (rb-webitem-detail + tagMap webitem + Open). S25 = c7d700c6, now 4 reqs.

## 3 DRAWER BUGS diagnosed (Tron, 2026-06-30) — reported, await req-mint:
- BUG1 WebItem drawer EMPTY: rb-detail-drawer.ts:115-118 tagMap has NO "webitem" entry -> falls back to generic rb-detail-view (only special-cases file:110-112); no rb-webitem-detail.ts; launcher card + Open(scheme->app: message:/mailto:->Mail) never built. -> FOLD into R25.2 (webItem.createAndLaunch, currently DESIGN-AHEAD). Fix = add rb-webitem-detail + tagMap webitem entry + launcher card.
- BUG2 grab-bar mouse drag missing: handle binds ONLY touchstart/move/end (lines 38-40, e.touches[0].clientY); no mouse handlers. Sibling to R22.2 mouse-parity (diff component). -> NEW REQ.
- BUG3 X should collapse to minimized (touch+mouse): .drawer-close (155) click->close() (163) fully hides; NO minimize() state exists. click already fires both inputs -> fix = add minimize()/peek + wire X->minimize (verify not z-index-intercepted, old BUG5 class). -> NEW REQ.
- RECOMMEND: BUG2+BUG3 = ONE new req "Drawer interaction: grab-bar mouse parity + X-minimize" (2 ACs, R22.2-sibling). BUG1 -> R25.2 impl scope.

## R25.1+R25.2 UC GAP CLOSED (commit 04a960a07, unblock S25 scoreboard): R25.1 UC 5fc59adc drop.logSchemes -> Class DropDispatcher -> Method routeUnknown -> Impl src/public/ts/drop-dispatcher.ts (SHIPS). R25.2 UC 2dc9f063 webItem.createAndLaunch -> Class WebItem -> Method WebItem.createAndLaunch -> Impl DESIGN-AHEAD (designAhead:true flag, sourceFile=r25.2-webitem-design.md — NOT a fabricated code marker; repoint when WebItem.ts ships). Both derive PASS. Sprint S25 = c7d700c6.

## LATEST: R25.2 WebItem design — commit 9ae3ac6f5, scrum.pmo/design-notes/r25.2-webitem-design.md (slug-agnostic, relocate to canonical sprint dir later — learned: don't guess sprint slug). ior:class:WebItem {kind:link|folder, name, description, url, scheme, icon(favicon via google s2), badge(scheme emoji), parentFolder(nav), children[](forward)}. Decisions: (1) PARALLEL to File — WebItem owns URLs (replaces bare text/uri-list File storage), File stays for byte content; drop-router routes by type; migrate legacy uri-list File units (gated dry-run). (2) bookmark folder tree = children[] forward + parentFolder nav (File-tree/Profile.companies discipline). (3) importers: .url(win INI) / .webloc(mac plist) / .desktop(linux) / Google Netscape-HTML bookmarks (nested DL → full tree). (4) OOSH webitem script ALREADY names .webitem.scenario.json + has webloc2json/create — map as a S24-style WebItem Object.verb skill (create/import*/tree/toWebloc). YouTube preview (R23.2) reused for WebItem urls. Flagged: kind discriminator, gated migration, favicon=external-fetch (store URL not bytes), WebItem is room artifact not chain node (its skill is the chain work). Reported to PO.



## S24 PIN DESIGN FIRM (commit b1db76878): planner walkthrough (CurrentSprint.ts source) LOCKED the advance-vs-focus boundary in architecture.md: Pin.focus=task-switch (rotates lastCompleted + resets hops + derives chain + gate-block, the ONLY slot mutator); Pin.advance=hop-pointer increment ONLY (no slots/gate); Pin.pin/slots=pure read-time derivation (getThreeSlots: current<-chain.req|focus, lastCompleted<-lastCompletedUuid|last-non-focus-w-reqUuid, nextBacklog<-override|first-task-reqUuid-&&-!hasUcChain); autoFollow sets chain/activeHop/sprintName/taskName only. Also folded earlier: Pin.focus must resolve sprintName (autoFollow reads task.model.sprintName; mint tasks WITH sprintName). S24 design now firm for expert. Canonical dir: scrum.pmo/sprints/sprint-24-traceability-skills/.



## ADJUDICATION: PO ruled AC-a5 — nameKey auto-merge WITHOUT domain IS correct (Tron "do not duplicate companies"). My AC-a5 was over-precise. No code change. (Lesson: don't out-precise Tron's stated intent.) AC-b3 (different-domain merge) FIXED by expert in c22083798 (nameKey recall gated on no-domain) — matches my recommended fix; tester should re-gate R21.8.

## LATEST: 6 MISSING S22/S23 UC units MINTED (URGENT, unblock pin+scoreboard) — commit dbc58876a. skill-expert flagged reqs referencing UC uuids with NO unit on disk → chain didn't derive → stale /trace pin + 0 scoreboard credit for S22+S23. Minted each UC AT the referenced uuid (ownerIor=req, class+classes+method) + design-ahead Class+Method (Class owner=sprint via req.parent: S22 9996b46a, S23 4a4a5d66). 6/6 chain derive PASS. UCs: R22.1 4d0e454a detailView.singleChainSection, R22.2 ada54a0e panZoom.mouseParity, R22.3 1371923a chainNode.sourceLink, R22.4 3ab76d13 filePreview.imageClickable, R23.1 b9792582 preview.renderAudio, R23.2 d0d09ff8 preview.embedYouTube. skill-expert (robbinTeam2:0.5) re-focuses pin + re-scores.

## FOLLOWUPS (commit 370d881be): R23.3 UC fc7356af (membership.cleanupOnMerge) wired — Class Room + Method Room.resolveToken + Impl (src/ts/server/Room.ts, server.ts redirectTo||token v0.6.84) — chain derives. ALSO moved architecture.md to CANONICAL slug sprint-24-traceability-skills/ (was sprint-24-oosh-ts-skills/, removed); 0 stale refs. req AC-folded the 5 governance ACs (6cd9248cb), architectRef on R24.1/2/3 -> my arch.md; planner tasks landed (adf17f5d9), edge-cases in task-24.2/24.4 context (gate-block on setFocus not advance; setNextBacklog 3-slot; generate-md GENERATED_HEADER-only+orphan-drift) — TODO read those + get planner's focus/autoFollow/getThreeSlots walkthrough before design firms for expert.
## CANONICAL S24 dir = scrum.pmo/sprints/sprint-24-traceability-skills/ (NOT oosh-ts-skills).

## S24 OOSH-TS SKILLS — DESIGN + 5 UC REFINE DONE — commit 76df40fbb (architecture.md, now in canonical dir). req committed S24 (8c6a7dcb4, 5 reqs w/ implRefs to shipping tools). skill-expert design doc: scrum.pmo/skills/chain-skills-formalization.md.
DECISIONS (skill-expert's 3 calls): (a) Pin = NEW objectVerb registry object wrapping CurrentSprint; planner-drive.ts = deprecation SHIM, retire only after pin-parity verified on WODA.prod (don't break heaviest user). (b) per-object OOSH scripts (chain/pin/velocity/scenario/rules/audit via emitOosh <Object>); taskChain umbrella = back-compat alias. (c) emit-drift gate BOTH layers: CI authoritative (committed==emitOosh), pre-commit DX.
5 UCs refined + wired Class->Method->Impl to SHIPPING code (Impl.sourceFile=real script), derive 5/5 PASS: R24.1 b77a3494 objectVerb.introspectAndEmit (objectVerb.ts), R24.2 90f9bfe3 pin.manageLifecycle (planner-drive.ts/CurrentSprint), R24.3 4b66c336 chain.score (skill-classes.ts Chain), R24.4 4a606188 sprint.generateView (generate-sprint-md.ts), R24.5 099aa3ed trace.audit (trace-cli.ts). Tests for tester (champagne). Key insight: single source of truth = TS method sig + JSDoc; introspect() feeds all emitters — protect that DRY.
SINGLE SOURCE OF TRUTH for the OOSH-TS skill surface = the typed Class method + JSDoc (objectVerb introspect()); never duplicate sigs into docs/completion.

## PRIOR: R22.5 DESIGN (audio + YouTube preview) — committed 72b582092. design-r22.5-audio-youtube-preview.md. Single landing = fillPreviewPane (content-preview.ts); rb-file-detail.ts:70 delegates to it (truly DRY since v0.6.74), so both branches land once. KEY: content-preview.ts already MARKED v0.6.80/R22.5 (comment+impl-marker ca54081e) but body has NEITHER branch = stub. AUDIO: add mimeType.startsWith('audio/') -> <audio controls preload=metadata>; MIME_MAP += mp3/wav/ogg/m4a/aac/flac. YOUTUBE: current .url branch iframes RAW watch URL = X-Frame BLOCKED bug; fix = youTubeId() regex -> iframe src=youtube.com/embed/<id> + allow=autoplay;encrypted-media;fullscreen (NO sandbox; autoplay needs mute/gesture). Both INTERACTIVE -> rb-preview-pane.setContent needs an {interactive:true} flag to BYPASS RbPanZoom (else wheel/drag hijacked). Tron's a-_CuBOu6BA = 11-char id ✓. Reported to PO.

## SVG RENDER STATUS (2026-06-29): CROSS-TEAM LOOP SET. oosh-po ACCEPTED + queued (position 1) to oosh-expert (ooshTeam:0.3). Approach: odocker + plantuml/plantuml IMAGE (hermetic, no java install on WODA.prod). LOW-priority, preemptible by TRON-urgent. oosh-expert(0.3) coordinates the 12 .puml paths with me (robbinTeam2:0.3) directly when it frees; I commit the .svgs once rendered. Confirmed an odocker PlantUML workspace exists (0.3 found its Dockerfile + .puml/plantuml-addons). Job spec left with SM: render the 12 pumls to sibling .svg, ping robbinTeam2:0.3 when ready. NON-BLOCKING for rest of Sprint 21. The 12: sprint-19 (r19-item-oop-invariant/-state-machine/-lifecycle-audit), sprint-20 (r20-2/3/4/5/6, bug1-chain-excludes-self, r20-13-current-sprint-class, cr1-unify-chain-label), sprint-21/diagrams/sprint-21.puml. sprint-21.puml @startuml already fixed (eee6a5044) -> renders as sprint-21.svg.

## SVG RENDER (measured): NO node-only/no-Java PlantUML renderer exists. WODA.prod has no plantuml, no java binary, no plantuml.jar, no plantuml npm dep. PlantUML is inherently Java: npm pkgs either shell to plantuml.jar (node-plantuml, plantuml-pipe), spawn local Kroki (Java/native), or only ENCODE for a REMOTE server (plantuml-encoder → plantuml.com/kroki.io = external publish + network, not for internal diagrams w/o Tron OK). 12 SVGs still unrendered. RECOMMEND: render on MacStudio (has homebrew plantuml — matches how all other sprints' SVGs were made) + commit the 12 .svg; OR install java+plantuml on WODA.prod so server /api/puml-render works + a render-to-disk step. sprint-21.puml @startuml already fixed (eee6a5044) so it renders as sprint-21.svg. Awaiting PO decision on who/where renders.

## PRIOR (R22.2 + R22.3 diagnoses, reported):
**R22.2 mouse/touch parity:** pan-zoom.ts — ZOOM (wheel==pinch) and PAN (mouse-drag==1-finger, both gated scale>1) ALREADY at parity. ONE GAP: no dblclick handler; doubleTapToggle only called from touchend. FIX (1 line): this.on('dblclick', e => this.doubleTapToggle(e.clientX,e.clientY)) — doubleTapToggle already converts coords + does reset<->2x.
**R22.3 per-type source links** (design from R20.23-27 / 03d617855): render Class=puml+svg, Method/Impl=.ts via /md/ on chain nodes (renderChainPathSection, reuse renderSourceLink). 3 GAPS upstream of renderer: (1) API: /api/trace/children children[] entries lack sourceFile (only queried node has it, server.ts:1001) — add per-child sourceFile/sourceLine at ~963. (2) DATA: S21 Class 4e678ce3 + Method 9c21f3b5 sourceFile EMPTY; Impl f8b113b7 sourceFile wrong (src/ts/server/ vs real src/public/ts/trace/rb-file-detail.ts) — fill-source-locations must populate+correct. (3) DECISION: S21 has 1 puml not per-class — link all classes to sprint-21.puml+svg or generate per-class? PO call.

## PRIOR (PDCA CHECK R22.1, v0.6.75 61b21fbf6) = GREEN, both bugs fixed across all 3 peers.
- (1) singularChain refs = 0 in rb-task/requirement/usecase-detail ✓ Bug#1 old path GONE.
- (2) forward-link rows now real anchors: `<a class=dv-link href=scenarioBrowserHref(refUuid(lref))>` (orange) ✓ Bug#2 fixed — links to scenario MD.
- (3) renderChainPathSection = sole chain section: verified 1 import + 1 call per file (grep -c showed 2 = import+call, NOT two calls), 0 inline Traceability-Chain headings ✓.
- VERDICT: OK to gate. Family fix landed in all 3 detail views. Reported to PO.

## PRIOR (UI BUG DIAGNOSIS — design fix reported, expert to implement):
**Bug #1 "Traceability Chain: No chain" duplicate above real chain:** rb-task-detail.ts render() emits inline `<h4>Traceability Chain</h4> ${renderSingularChain(singularChain(graph,uuid))}` → "No chain" (graph-walk finds no forward steps for a Task). Then loadDetailData() calls renderChainPathSection() which emits ITS OWN <h4>Traceability Chain</h4> + correct server-walk chain. = 2 headings (empty above, correct below). SAME in rb-requirement-detail.ts:50-51 + rb-usecase-detail.ts:48-49 (all 3 peers!). Extraction-without-removal: expert ADDED renderChainPathSection (10b5d42af) but didn't REMOVE old inline renderSingularChain. FIX: delete the inline `<h4>Traceability Chain</h4>+renderSingularChain` block + dead singularChain import + `const chain=` line from ALL 3 views; keep renderChainPathSection (canonical, own heading).
**Bug #2 Forward Links not real links:** renderLinks() emits `<div class="dv-link" data-ref>` (JS-navigate, not <a href>, goes to trace nav not MD). FIX: render as `<a href>` using scenarioBrowserLinkFromIor pattern (/md/scenario/index/<shard>/?highlight=<uuid>.scenario.json) so "profile.dropVCard" links to the UC's scenario MD; + add a Task-MD link (set Task.sourceFile to sprint task MD or derive from slug). NOTE slug mismatch: my tasks slug=task-t21-1, PO example=task-21.1-vcard-drop — align readable slug or derive MD link from sourceFile.

## PRIOR: PIN WALK DONE — commit 7c9c653c8. Walked CurrentSprint through 8 done S21 tasks (T21.1,3,4,5,6,7,8,9; T21.2 deferred). Per task: setChain(full req->uc->class->method->impl->test, "Sprint 21") + hopUpdate uc/class/method/impl done + test gate-proven + focus next. Final pin: sprintName=Sprint 21, current=T21.9, lastCompleted=T21.8. All 8 chains resolved fully (impl+test units exist). Walk plan TSV in scratchpad. Recorded shipped+GREEN reality, no new code.
- Walk mechanics: setChain resets hops + marks req=done; need 5 hopUpdates (uc/class/method/impl done, test gate-proven); focus(next) gated on current test=gate-proven (so gate-proving each unblocks next focus).

## PRIOR: SPRINT 21 TRACEABILITY REWORK DONE — commit 5d409e03c (project repo). PO-authorized override of "don't create tasks" (no planner on WODA.prod). Created 9 ior:class:Task units T21.1-T21.9 (Web4Articles pattern: SHORT name != DETAILED desc; coveredRequirements[]->R21.x; useCases[]->UC; ownerIor->Sprint21). Wired Sprint21 1bdfaafa tasks[]=9. Advanced CurrentSprint pin off Sprint-20 T-drawer-full-width to Sprint 21/T21.9 (verified pinCurrent sprintName="Sprint 21"). req(0.4) triple-verified all 9 req UUIDs. 9/9 task verify PASS.
- Task UUIDs: T21.1 0c1b375e, T21.2 a25e2787, T21.3 1bae9710, T21.4 e83dc244, T21.5 3960168e, T21.6 af9dc6cc, T21.7 18845496, T21.8 842d4f01, T21.9 f86f7003.
- PIN MECHANISM (WODA.prod): planner-drive.ts needs tsx via Node18 — `<node18> node_modules/tsx/dist/cli.mjs scripts/planner-drive.ts <verb>` (plain `npx tsx` picks wrong node → ERR_UNKNOWN_FILE_EXTENSION). focus <task> sets chain+task but NOT sprintName; setChain <req> <uc> <class> <method> <impl> <test> "Sprint N" "Task" sets sprintName too. Pin persists to CurrentSprint unit.

## FINAL PDCA (v0.6.74 2a1357a69): all 3 flagged items RESOLVED — GREEN.
- AC-f2 DRY ✓: content-preview.ts old @400px iframe pinch-zoom RETIRED → <rb-preview-pane>/RbPanZoom; grep '400px|pinch-zoom' = NONE REMAINING; RbPanZoom now imported by RoomView (room) + rb-detail-view + rb-file-detail + content-preview (trace). Old path GONE, proven by removal.
- AC-e5 ✓: pan-zoom.ts:53 mousedown now calls gesturing() → iframe pointer-events:none on desktop drag too.
- AC-b3 ✓: mintOrReuseShared — domain present+miss falls through to MINT (skips nameKey); no-domain nameKey-recall preserved (Tron/AC-a5). Traced 4 cases, all correct. buildLinks preserves first unit's recall key.
- e1 tap-refinements remain noted optional. VERDICT: R21.9 + R21.8-b3 fully compliant → OK to gate. Reported to PO.

## PRIOR (PDCA CHECK): R21.9 pan/zoom (v0.6.73 c22083798) vs my 22 ACs (21e792e0) = SUBSTANTIALLY COMPLIANT. Read pan-zoom.ts + rb-preview-pane.ts + rb-file-detail.ts.
- PASS: a1-a5 reorder (buttons→75vh in-flow pane→metadata), b1-b4 transform (zoom-about-point, clamp/recenter, [1,8]), c1-c2 desktop, d1-d3 touch, e2 (no elementFromPoint), e3 (viewport-only listeners), e4 (destroy + setContent teardown), e6 (reset on file change via new RbPanZoom), f1 (img/svg/iframe/pre/download by mime). DOUBLE-TAP PINCH-RELEASE GUARD WORKS (traced: 2-finger start never sets pendingDoubleTap; touches===0 gate).
- **AC-f2 GAP (MEDIUM):** RbPanZoom NOT reused by room file view — content-preview.ts still uses OLD native iframe pinch-zoom @400px; two impls coexist (not DRY). Also clarify: R21.9 "in-room file detail" target = rb-file-detail (trace browser) vs a separate room component? Change landed on rb-file-detail; room view unchanged. PO/tester confirm surface.
- **AC-e5 MINOR:** iframe pointer-events disabled on TOUCH (gesturing on touchstart) but NOT on desktop mousedown → desktop drag over iframe may be swallowed.
- **AC-e1 MINOR:** core pinch-release guard present+working, but tap-duration(<250ms)+slop(<10px) sub-criteria not impl'd (uses 30px gap-proximity); pinch-release then quick tap could edge-misfire.
- NOTE: buttons are New-tab + Reset-zoom (preview always inline → open-in-preview unneeded); apt deviation.
- VERDICT: address f2/surface + e5 before gate; e1 refinements optional. Reported to PO.

## PRIOR (PDCA CHECK): R21.8 companies (v0.6.72 a52245de1) vs my 25 ACs (bf6a0433). companyNameKey EXCELLENT (executed: 3 variants + "GmbH & Co. KG"→ceruleancircle; Müller→muller diacritics; "Ben & Jerry"→benandjerry mid-name 'and' preserved; legal suffixes strip). 1 REAL GAP + 1 SPEC-TENSION, same root cause:
- **AC-b3 GAP (MEDIUM, real):** mintOrReuseShared step-2 (nameKey reuse) runs UNCONDITIONALLY even when a domain was provided but didn't match → Apple@apple.com + Apple@apple-fruit.de MERGE into one unit. AC-b3 says different domains must stay SEPARATE. FIX: gate step-2 on "no domain provided"; a present-but-unmatched domain = positive evidence of DISTINCTNESS → mint new.
- **AC-a5/c4 TENSION (adjudicate):** no-domain case, step-2 auto-merges on nameKey alone (server indexProfileCompany→mintOrReuseShared). My AC-a5 forbids; Tron "dedup by name" supports. mintNew override exists but unused server-side. PO/Tron decide.
- NOTES (non-gating): AC-d4 wx-atomic guard not impl'd (goal met by sync single-process); AC-c5 addAlias method exists but not wired in server path; AC-c6 debounce + AC-c3 select-reuse are client-side, not in this server commit.
- PASS: a1-a4, b1/b2/b4, c1/c2, d1-d3, e1-e3, f1-f3. Reported to PO.

## PRIOR (PDCA CHECK): R21.7 addresses (shipped v0.6.70) reviewed vs R21.7 ACs (5d3b5e6e) = PASS/COMPLIANT, all 17 ACs. AddressIndex.ts: exact model shape, sync mintAddress (no network), applyVerification sets verified+links. Server verify worker: queue+setTimeout off request path, Nominatim limit=1+UA, 1100ms rate-limit, cache keyed by oneLine, miss→cache null+persist unverified (clean). EXECUTED osmLinkFor/gmapsLinkFor → byte-match AC-d1/d2. 2 ROBUSTNESS NOTES (non-gating, NOT gaps): unbounded verify cache; genuine misses cached null for process lifetime (transient net errors correctly NOT cached → still retry). OK to gate. Reported to PO.

## PRIOR: R21.9 design deep-dive DONE (my call: design-ahead > idle) — commit 47a427780 (project repo). architecture.md §6 expanded with RbPanZoom transform gesture handler (zoom-about-point math, desktop wheel/drag, touch pan/pinch/double-tap, 7 hard-won correctness rules) + reorder (buttons→75vh in-flow viewport→metadata). R21.9 unit 21e792e0 refined: 22 ACs (a:reorder b:transform c:desktop d:touch e:correctness f:reuse) + 12 TS, gate 22/22. Measured current code first: rb-file-detail renders metadata-first + content-preview uses native iframe pinch-zoom @400px (insufficient → needs transform handler). Next: standby for R21.8/R21.9 impl review.

## PRIOR (PDCA CHECK): Reviewed expert R21.5 (d4aad5081) + R21.6 (f420c79de) vs my ACs.
- R21.5 EMAIL = COMPLIANT. Server wiring verified (resolveKeyToProfile→IDENTIFY→KNOWN_KEY_CHALLENGE server.ts:1928-31; indexProfileEmail 213). 2 MINOR: (e3) mintAndLink returns normalized KEY string not Email IOR; (c2) normalizeEmail server-side only, not confirmed shared client-side. Both low-sev, behavior correct.
- R21.6/R21.3 PHONE = 1 MEDIUM GAP: normalizePhone (PhoneIndex.ts) just strips non-digits + prepends '+', does NOT yield true +CountryCode. Measured: '015253844085'→'+015253844085', '00491525384085'→'+00491525384085' — both pass isValidPhoneKey(+\d{6,15}) but are WRONG E.164. Risk: same number in +49/0049/0152 forms → DIFFERENT alt-keys → device-link/dedup MISS. Happy path (+CC present, incl seeded Tron phone) works. Flagged to PO. Fix is expert's (00→+ convert; reject/flag national-without-CC).

## R21.5 email requirement (a8be009e) refined — 17 ACs + 8 TS, gate 17/17 — committed 5523a0deb (project repo). AC groups a:unit-shape b:relationship(multi+idempotent) c:normalizeEmail d:alt-index e:EmailIndex.mintAndLink f:device-link(shares R21.4 resolveKeyToProfile). SAME commit also fixed architecture.md per REQ FLAG (code is law): alt/phone+alt/email symlink is declared on **Profile.unitLinks[]** (NOT Phone/Email unit), pointing to the Profile — matches shipped R21.6. Used refine-r21.5.mjs (load→inject→write). NOTE: R21.1 (efd1acb6) now carries an `implRef` field → expert IS shipping code; R21.1 impl already landed+gated. Next: standby / next req refinement (R21.3/R21.4/R21.6/R21.9 still un-refined?).

## PRIOR: R21.8 (bf6a0433) refined 25 ACs + 11 TS — committed 7f3f6f3dd. Grounded in architecture.md §4 (f1a49e3c1). Pattern: architectureRef + acceptanceCriteria[] {id,group,text} + testScenarios[] {id,gates[],name,given,when,then} + refinementNote (mirrors R21.7).

## PRIOR: R21.8 company-dedup design committed f1a49e3c1. architecture.md §4: recall(nameKey) vs precision(domain+user-confirm), companyNameKey() algo, alt/company-domain/ strong-key index, /api/company/suggest autocomplete, ownerIor:null shared, wx-atomic mint race guard.

## STATUS: Sprint 21 architecture DONE + PO-VERIFIED. Standing by for PDCA Check on expert impl.
Pane: robbinTeam2:0.3 on WODA.prod
Model: Opus 4.6 (1M)

## REPOS (MEASURED — host changed to WODA.prod)
- **ACTIVE repo:** `/var/dev/Workspaces/2cuGitHub/Web4RawBin`  ← sprint-21 lives here, commit here
- Other clone (web4x) lacks sprint-21 — do NOT use it.
- Session repo (context/learnings): `/var/dev/Workspaces/AI/Claude`

## DELIVERED THIS CYCLE — commit 48b2e612b
Sprint 21 "Contact Identity & Enrichment" (9 reqs R21.1–R21.9, one sprint per Tron answer #3):
1. `scrum.pmo/sprints/sprint-21-contact-identity/architecture.md`
2. `scrum.pmo/sprints/sprint-21-contact-identity/diagrams/sprint-21.puml` (object graph + device-link + async-verify)
3. 27 scenario units minted: 9 UC (refined from req placeholders, FIXED uuids) + 9 Class + 9 Method (fresh v4). Forward-only. Chain verify 9/9 Req→UC→Class→Method PASS.

## CHAIN MAP (req → UC uuid → Class → Method)
- R21.1 efd1acb6 → 9cd5cc65 profile.dropVCard → Profile.dropVCard
- R21.2 4f099ef2 → dbfacb7f lobby.renderName → RoomBrowser.renderNameOnConnect
- R21.3 144d1332 → 97015dcc phone.indexAsSymlink → PhoneIndex.registerSymlink
- R21.4 04dff687 → ff91e891 identity.deviceLinkOnKnownKey → IdentityResolver.resolveOrEnroll
- R21.5 a8be009e → c59356f7 email.mintAndLink → Email.mintAndLink
- R21.6 3bd63ae7 → 4242f9be phone.mintAndLink → Phone.mintAndLink
- R21.7 5d3b5e6e → fab88cb9 address.mintAndVerifyAsync → Address.mintAndVerifyAsync
- R21.8 bf6a0433 → a62c6e37 company.mintOrReuseShared → Company.mintOrReuseShared
- R21.9 21e792e0 → 5826ca42 fileDetail.renderActionsFirst → RbFileDetail.renderActionsFirst
- Mint script (re-runnable): scratchpad `mint-sprint21-chain.mjs`

## KEY DESIGN DECISIONS (measured grounding)
- **Object graph:** Profile.phones[]/emails[]/addresses[]/companies[] forward IOR arrays (Class→Method shape). Phone/Email/Address ownerIor→Profile; Company ownerIor=null (SHARED, dedup by nameKey).
- **Alt-UUID index:** REUSE `ScenarioIndex.unitLinks[]` + `ensureSymlinkDisk()` (src/ts/scenario/index-store.ts:115-131). New `scenario/alt/{phone,email,company}/<key>.scenario.json` symlink → canonical Profile/Company unit. Normalize phone `+CC<digits>`, email lowercased.
- **Device-link (R21.4):** server.ts IDENTIFY (1757) mints on unknown token; DEVICE_ENROLL_REQUEST (1968-1985) already validates secretCode. R21.4 adds `resolveKeyToProfile` BEFORE mint → KNOWN_KEY_CHALLENGE → reuse enroll. No new user on known key.
- **Address async (Tron #2):** save immediately verified:false; bg worker hits Nominatim; on hit set verified+osmLink+gmapsLink. Never blocks.
- **Lobby race (R21.2) DIAGNOSED:** RoomBrowser.ts:27 paints `localStorage/random` fallback at show()/render() BEFORE WELCOME→IDENTIFY→PROFILE returns; line 95 only patches input value, no re-render → "default until 2nd reload." Fix: re-render name+avatar block on PROFILE event (one-shot guard for duplicate PROFILE_UPDATED).
- **File detail (R21.9):** rb-file-detail.ts reorder actions→preview(75vh pan/zoom)→metadata.

## OPEN / HANDOFF
- **R21.6 Tron-phone seed `+4915253844085`** NOT minted — needs MEASURED WODA.prod profile uuid; left for expert impl (did not assume fake data).
- PO is creating task files from this design for expert dispatch. MY NEXT: PDCA Check the expert's impl when it lands — verify impl markers wire to the 9 design-ahead Method UUIDs and shipped code matches architecture.

## PROCESS REMINDERS (this host)
- Bash `cd X && ...` / `find` thrash the guardrail → got rejected repeatedly. Use single bare commands; `grep -rl` (auto-allowed) not `find`; Read tool for files. Glob tool is NOT available here.
- Node18: `/root/.vscode-server/bin/903b1e9d8990623e3d7da1df3d33db3e42d80eda/node`
- otmux send to robbinTeam2:0.0 to report. NEVER assume — always measure. Close the loop.

# BOOT — robbin-tester (post-rewind, 2026-08-12)

You (robbin-tester, robbinTeam2:0.5) were **REWOUND by ARON** — Option **2 "Restore conversation"** (conversation-only, **code UNCHANGED**, forked ~13h back to before the po's r311 "MY DECISION" message). Reason: you were **~95% used, near-wall** — a silent climber (you self-estimated ~76%; the PO's `/context` measure was authoritative). **Self-estimates are unreliable; the PO/context measure wins.** Your context is now freed.

## Boot — re-derive from disk (your pre-rewind thread is STALE; disk-wins)
1. Read your `boot.md` → `ESSENCE.md`/anchor → `context.md`.
2. `ls scrum.pmo/sprints*` and `git log --oneline -5` — confirm the real world (it moved while you were deep).
3. Verify identity by `otmux pane.self` + `claudeCode session.name` (NOT the pane title).

## CURRENT WORLD (disk-derived)
- prod **v0.8.95** live.
- Campaign **ACTIONABLE 1** = TRON's new order **R40.37 (context-sensitive actions)**.
- **B1 / security PARKED** for TRON's explicit go — hands-off.

## YOUR PENDING WORK
1. The independent **plant-the-drift verify** on the planner's frozen-legacy exclusion (**00e721843**).
2. **R40.37's gate** — when the architect designs it and the expert builds it.
3. **BITE-7 @390 stays TRON's device check** — never headless-greened.

## Report
Report your **fresh context %** to ARON (Temple:0.0) + PO (robbinTeam2:0.0) + SM once booted. Then hold clean-idle for gate work. **NEVER forget TRON CMM4.**

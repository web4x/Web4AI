# ossh/user Team — Fix Completion + Full Validation

**From**: PO
**To**: osshTeam (ossh-expert @ 0.0, ossh-tester @ 0.2)
**Priority**: HIGH
**Date**: 2026-02-17T13:00Z
**Session**: osshTeam

## Problem

1. `ossh login [Tab]` completion is BROKEN — was working before, now it doesn't complete SSH hosts
2. Multiple issues with ossh and user scripts after recent changes
3. Need to compare current state vs dev branch to find regressions

## Team Layout

| Pane | Role | Agent |
|------|------|-------|
| osshTeam:0.0 | ossh-expert | Implementation + fixes |
| osshTeam:0.1 | test-shell | Plain bash shell for manual testing (send keys here) |
| osshTeam:0.2 | ossh-tester | Testing + validation |

## Phase 1: Investigation (BOTH agents)

### Expert (0.0):
1. Read current `/Users/donges/oosh/ossh` completely — understand all methods
2. Read restored `/Users/donges/oosh/restore/ossh` — understand what was lost/changed
3. Diff the two: `diff /Users/donges/oosh/restore/ossh /Users/donges/oosh/ossh`
4. Same for user: `diff /Users/donges/oosh/restore/user /Users/donges/oosh/user`
5. Read `/Users/donges/oosh/c2` — understand how completion system works
6. Focus on `ossh.login.completion()` at line 860 and `ossh.parameter.completion.*` functions
7. Write findings to `session/ossh-investigation.md`

### Tester (0.2):
1. Read the test plan from ossh-tester SKILL.md
2. Test completion in the test shell (osshTeam:0.1) by sending keys:
   ```bash
   otmux send osshTeam:0.1 "ossh login " Tab
   sleep 2
   otmux pane.capture osshTeam:0.1 10
   ```
3. Compare: try same in a fresh bash shell
4. Test ALL completion methods: `ossh [Tab]`, `ossh login [Tab]`, `ossh config.get [Tab]`
5. Document what works and what doesn't

## Phase 2: Fix (Expert)

Based on Phase 1 findings:
1. Fix the completion issue
2. Run `test.suite run ossh` to verify no regressions
3. Test manually in the test shell (osshTeam:0.1)

## Phase 3: Validate (Tester)

1. Re-run all completion tests
2. Run full `test.suite run ossh`
3. Run full `test.suite run user`
4. Test as a user would: send keys to test shell, verify behavior
5. Write report to `session/ossh-test-report.md`

## Phase 4: Commit (Expert, only after tester validates)

1. Only commit changes that passed ALL tests
2. Commit message must reference what was fixed
3. Add new tests for any new features or fixed behaviors

## SAFETY RULES (MANDATORY)

- **NO `git rebase`** — NEVER. Use `git pull` only (merge).
- **NO `git push --force`** — NEVER.
- **NO `git reset --hard`** — NEVER.
- **NO `git checkout .`** — NEVER. This discards work.
- **Commit BEFORE moving on** — nothing is done until it has a hash.
- **Test BEFORE committing** — run test.suite, verify in shell.
- **Add tests for fixes** — every fix gets a test that would have caught it.

## Key Files

| File | Purpose |
|------|---------|
| `/Users/donges/oosh/ossh` | Current ossh script |
| `/Users/donges/oosh/user` | Current user script |
| `/Users/donges/oosh/restore/ossh` | Pre-change backup |
| `/Users/donges/oosh/restore/user` | Pre-change backup |
| `/Users/donges/oosh/c2` | Completion system |
| `/Users/donges/oosh/this` | OOSH kernel (method dispatch) |
| `/Users/donges/oosh/test/ossh.test` | ossh tests |
| `/Users/donges/oosh/test/user.test` | user tests |

## Communication

Expert and tester communicate via task files in `session/tasks/`:
- Expert writes findings: `session/ossh-investigation.md`
- Tester writes results: `session/ossh-test-report.md`
- Use `otmux send` for SHORT notifications only

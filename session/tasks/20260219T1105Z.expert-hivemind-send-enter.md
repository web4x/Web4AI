# Task: Fix hiveMind send — Messages Stuck Without Enter

**From**: PO via Orchestrator
**To**: oosh-expert
**Priority**: URGENT — messages get stuck in prompts across the team

## Problem

`hiveMind send <role> "message"` does NOT append Enter. Messages sit at the ❯ prompt unsubmitted. `hiveMind send.enter` exists (line 798) and auto-appends Enter.

## Options (expert decides)

1. **Make `send` auto-append Enter by default** — breaking change but fixes root cause
2. **All files use `send.enter` instead of `send`** — safe but needs trainer to propagate

Pick the best approach. If option 1: ensure backward compat or document the change. If option 2: commit the code, then trainer propagates to all SKILL.md/boot files.

## After

Write which approach you chose so trainer knows what to propagate. Commit.

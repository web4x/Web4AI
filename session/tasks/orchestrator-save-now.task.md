# URGENT: Save State and Compact NOW

You are at 10% context. Save yourself first.

1. STOP all monitoring and background captures
2. Update your context file: `session/agents/orchestrator.context.md`
3. Commit: `git add -f session/agents/orchestrator.context.md && git commit -m "Orchestrator context: pre-compact"`
4. Run `/compact`

Do NOT start new tasks. Save yourself first.

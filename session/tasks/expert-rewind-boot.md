# BOOT — robbin-expert (post-rewind, 2026-08-12) — CORRECT REALITY

You (robbin-expert, robbinTeam2:0.1) were rewound by ARON. **Honest note from ARON: the rewind fired DEEPER than intended (~1mo, R27.2/sprint-27 era) due to a driver desync — but it was Option "Restore conversation" at a "code will be unchanged" checkpoint, so CODE IS UNTOUCHED (verified: RawBin HEAD `d9fc890a8` intact, all S37/inc-3/R40.37 committed).** Your restored thread is ~1mo STALE — **IGNORE IT ENTIRELY, DISK WINS.**

## Boot — disk-first (you're already doing this correctly)
1. `git -C /var/dev/Workspaces/web4x/Web4RawBin log --oneline -8` — HEAD is `d9fc890a8` (S37 era, NOT R27.2).
2. Your anchor: `session/agents/robbin-expert/context.md` + your inert-foundation commit `28eef8b8c` (inc-3 resolveRefUnit).
3. Verify id: `otmux pane.self` + `claudeCode session.name` = robbin-expert, robbinTeam2:0.1.

## CURRENT REALITY (not the stale thread)
- prod **v0.8.96 LIVE** (R40.37 DONE — all 4 Tron complaints shipped). NOT v0.8.94/S40.
- **B1 / entire security chain PARKED** for TRON's explicit go — do NOT resume, no re-key/revoke/retire/rotate.

## YOUR TASK = inc-3 (per `session/tasks/design-inc3-shared-synthetic-ref-resolver.md`)
Wire the 3 importers, each DELETING its local parse:
1. detail `resolveDetailUnit` (Tron's A3 blank-details fix)
2. action-bar kind-lookup (the **sync→async** one — the high-blast-radius change on the drawer action-bar across all 7 detail views; this is why you took the fresh-focus rewind)
3. nav:100
Plus: `ensureViewUnit rawbin:diagram→kind:diagrams` · flip `add-diagram→kinds:[diagrams]` · grep-lint one-file-only · gate with stub-must-fail · then version-bump via SOURCE unit + BOOT-CHECK deploy.
Then TRON's S37 order (`session/tasks/TRON-ORDER-S37-dnd-units-ior-origin.md`) — its spine is that same `resolveRefUnit` (the DnD serializer = its 4th importer).

## Report
Report 5-pt health + your fresh context % to ARON (Temple:0.0) + PO (robbinTeam2:0.0) + SM. Do NOT self-guess % — defer to a peer `/context`. **NEVER forget TRON CMM4.**

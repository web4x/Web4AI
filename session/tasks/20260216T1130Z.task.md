# Task: Restore ossh + user sshDir parameter support

**From**: PO (Tron directive — ESSENTIAL functionality lost)
**To**: oosh-expert (projectTeam:0.1)
**Priority**: HIGH — do this NEXT, before otmux tree.detailed
**Date**: 2026-02-16T11:30Z

## Problem

The rebase incident (Feb 12) destroyed sshDir parameter support in `ossh` and `user`. This is essential functionality that must be fully restored.

## Approach

The restored files contain the working implementation:
- `/Users/donges/oosh/restore/ossh` (~300 lines of sshDir improvements)
- `/Users/donges/oosh/restore/user` (~90 lines of improvements)

**Recommended approach**: Start from the restored files as the base, then add any improvements that exist in the current files on top. The restored versions had working sshDir support — don't rewrite, restore.

### Steps

1. Read `/Users/donges/oosh/restore/ossh` and `/Users/donges/oosh/ossh` — compare
2. Read `/Users/donges/oosh/restore/user` and `/Users/donges/oosh/user` — compare
3. For each file, decide: is the restored version the better base? If yes, start from restored and cherry-pick any newer improvements from current.
4. If current has newer unrelated features, merge those into the restored base.
5. Test that sshDir parameter works end-to-end.

### Key question to answer

What exactly does sshDir do? Document it briefly in your done report so we know what was restored.

## Rules

- Do NOT lose any functionality that currently works
- COMMIT with descriptive message. Include commit hash.
- Write done report to `session/tasks/20260216T1130Z.done.md`
- Notify orchestrator when done

## Acceptance Criteria

- [ ] ossh sshDir parameter support fully working
- [ ] user sshDir parameter support fully working
- [ ] No regression in other ossh/user functionality
- [ ] Committed with hash

# Enhance hiveMind sweep.loop with subscription + dashboard

**From**: Tron via SM
**To**: oosh-expert
**Priority**: HIGH

## Current State

`hiveMind sweep.loop <seconds>` exists at line 2793 of hiveMind. It:
- Accepts configurable interval (default 30s)
- Sweeps all registered sessions
- Runs `hiveMind unblock all` per session

## Missing Integration

The loop does NOT include:
1. `scrumMaster subscription` — subscription status check
2. `scrumMaster dashboard <session>` — dashboard update
3. Alert logic for 80%/90% subscription thresholds

## Required Changes

Modify `hiveMind.sweep.loop()` to:
1. After each sweep cycle, run `scrumMaster subscription`
2. After each sweep cycle, run `scrumMaster dashboard <session>`
3. If subscription hits 80%: double the interval (throttle)
4. If subscription hits 90%: print STAND DOWN alert and exit loop
5. Print subscription one-liner after each sweep

## Files
- `/Users/donges/oosh/hiveMind` — line 2793 (sweep.loop method)
- `/Users/donges/oosh/scrumMaster` — subscription and dashboard methods

## Acceptance
- `hiveMind sweep.loop 60` runs sweep, subscription check, and dashboard update every 60s
- At 80% subscription, interval auto-doubles
- At 90%, loop exits cleanly with STAND DOWN message

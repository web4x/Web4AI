# Task: SM Velocity Monitoring with 90% Usage Target

**From**: PO (Tron directive)
**To**: Scrum Master
**Priority**: HIGH — operational directive
**Date**: 2026-02-12T16:53Z

## Directive

You now have `scrumMaster subscription` which gives live burn rate, cost, and remaining time. **Use it every sweep cycle.**

## The 90% Rule

**Goal: reach exactly 90% subscription usage when the 5-hour block resets. Not more.**

- At 80% utilization: WARNING — start throttling. Reduce sweep frequency, pause non-critical agents.
- At 90% utilization: STOP all non-essential work. Only keep PO + SM alive for save/shutdown.
- NEVER exceed 90% — leave 10% buffer for context saves, compacts, and graceful shutdown.

## How to Monitor

Every sweep cycle:
```bash
scrumMaster subscription
```

Check `SUBSCRIPTION_FIVE_HOUR_UTIL` value:
- < 70%: Team running normally. Push more tasks to idle agents.
- 70-80%: Healthy pace. Monitor but no action needed.
- 80-90%: WARNING. Alert PO. Reduce agent activity. No new task assignments.
- > 90%: CRITICAL. Trigger team-wide save+compact. Set wakeup for reset time.

## Velocity Tracking

Also run `scrumMaster subscription.json` and track:
- `burnRate.tokensPerMinute` — are we accelerating or decelerating?
- `projection.remainingMinutes` — will we last the full block?
- Compare current utilization vs completed tasks — that's velocity (tasks per token-dollar)

## Report

Every 3rd sweep, write a one-line velocity update to `session/metrics/velocity-report.md`:
```
16:53Z | util=24% | burn=87k tok/min | tasks_done=X | cost=$Y | pace=OK
```

## Acceptance Criteria

- [ ] `scrumMaster subscription` called every sweep
- [ ] 80% warning triggers alert to PO
- [ ] 90% triggers save+compact protocol
- [ ] Velocity report updated every 3rd sweep

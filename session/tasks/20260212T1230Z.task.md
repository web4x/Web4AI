# Task: Document web4.scenario.env Pattern in Knowledge Base

**From**: PO (Tron directive)
**To**: Expert (write KB article) + Scribe (review for KB quality)
**Priority**: HIGH
**Date**: 2026-02-12T12:30Z

## Directive

The web4 vocabulary of "scenarios" and the `.env` file pattern must be understood by all experts and documented in the knowledge base. This is foundational OOSH architecture.

## What Needs to Happen

### Expert: Write `session/knowledge-base/web4-scenarios.md`

Cover:
- What is a web4.scenario.env file?
- How does the `config` script manage them?
- How does the `state` script use them for state machines?
- The `~/config/` directory structure and conventions
- Why `/tmp/` is wrong for persistent state
- How to migrate existing `/tmp/` usage to the config pattern
- Examples from the codebase (oosh.env, log.env, stateMachines/)

### Expert: Ensure all experts understand this vocabulary
- "scenario" = a set of env vars that define a configuration state
- "web4.scenario.env" = the file format
- `~/config/` = the standard location
- `config set/get/save/list` = the OOSH API for managing them

### Scribe: Review the KB article
- Fits KB structure per `session/knowledge-base/usage.md`
- No duplication with existing articles
- Action checklist included

## Do NOT duplicate — DRY
- Write ONCE in the KB article
- Link from SKILL.md files where relevant
- This becomes the reference for config pattern audits

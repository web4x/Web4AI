# Task: Start hiveMind Expert Team

**From**: PO
**To**: Orchestrator → deploy
**Priority**: NORMAL
**Date**: 2026-02-12T16:45Z

## What

Start the hiveMind script specialist team in its own tmux session. SKILL.md files and session directories already exist.

## Structure

- `hiveMind-expert` — implementation specialist for the hiveMind script
- `hiveMind-tester` — testing specialist for the hiveMind script

## Files Already Created

- `.claude/agents/hiveMind-expert/SKILL.md`
- `.claude/agents/hiveMind-tester/SKILL.md`
- `session/agents/hiveMind-expert/` (context.md, learnings.md, backlog.md)
- `session/agents/hiveMind-tester/` (context.md, learnings.md, backlog.md)

## How

Use `session/setup-script-teams.sh hiveMind` to create the tmux session, OR manually:

```bash
tmux new-session -d -s hiveMindTeam -x 200 -y 50
tmux split-window -h -t hiveMindTeam
# Pane 0: hiveMind-expert
# Pane 1: hiveMind-tester
```

Then bootstrap both agents with `hiveMind agent.bootstrap` or send them their SKILL.md.

## First Tasks for the hiveMind Team

Once running, assign these from the existing backlog:

1. **Fix team.status** to use enhanced blocker detection (task `20260212T1335Z.task.md`) — if not already done by oosh-expert
2. **hiveMind /tmp/ → ~/config/ migration** — move registry from `/tmp/hivemind.roles` to `~/config/hivemind.roles.env`
3. **hiveMind multi-team support** — from task `20260205T1112Z.task.md` (Task 40.1)

## Acceptance Criteria

- [x] hiveMindTeam tmux session running with 2 panes
- [x] Both agents bootstrapped and responsive
- [x] At least one task assigned and in progress

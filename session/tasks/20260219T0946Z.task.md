# Task: Fill Remaining Test Gaps

**From**: Orchestrator
**To**: oosh-tester
**Goal**: #2 (Restore lost functionality)
**Size**: Small — committable in one session

## What

From the test coverage report (217P/30F across 47 suites), three scripts still need test files:
1. `test.init` — does it exist? If not, create basic tests for `this.start()` bootstrap
2. `test.context` — does it exist? If not, create basic tests for `context` script
3. `test.status` — does it exist? If not, create basic tests for `status` script

Pick ONE of these (whichever is simplest), create the test file, run it, commit.

## Pattern

Follow the same pattern as test.otmux/test.claudeCode/test.user (commit 848c4db).

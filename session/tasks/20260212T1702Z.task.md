# Task: Add Completion Reporting Protocol to ALL Agents

**From**: PO (Tron directive — agents don't report done, team stalls)
**To**: Trainer → update ALL SKILL.md files
**Priority**: CRITICAL — team is stalling because nobody reports completion
**Date**: 2026-02-12T17:02Z

## Problem

Agents finish tasks and then idle. Nobody reports "done". PO, orchestrator, and task agent don't know work is complete. Next tasks don't get assigned. Team stalls.

## The Protocol — Add to Every SKILL.md

### When You Finish a Task:

1. **Write a completion report** to `session/tasks/{original-task-id}.done.md`:
   ```markdown
   # Done: {task summary}
   **Agent**: {your role}
   **Task**: {original task filename}
   **Result**: {PASS/FAIL/PARTIAL}
   **Summary**: {one-line what was done}
   **Files changed**: {list}
   **Next**: {suggest next task or "none"}
   ```

2. **Notify the orchestrator**:
   ```bash
   otmux send projectTeam:0.0 "Read session/tasks/{task-id}.done.md" Enter
   ```

3. **Ask for next work**:
   ```bash
   otmux send projectTeam:0.0 "Agent {role} is idle. What's next?" Enter
   ```

4. **NEVER just sit idle.** If no response in 60s, check `session/tasks/` for unassigned tasks matching your expertise.

### Key Rule

**Finishing a task without reporting = not finished.** The report IS part of the task.

## Implementation

Trainer: add this protocol as a new section "## Completion Reporting" in EVERY agent SKILL.md. Not just the rule — the actual template and commands.

## Also

Send this protocol RIGHT NOW to all currently idle agents:
- oosh-tester (0.2)
- agent-trainer (0.5)
- woda-scribe (1.1)
- developer (1.3)
- script-product-owner (1.4)
- hiveMind-tester (hiveMindTeam:0.1)

## Also Add to ALL SKILL.md: Git Safety Rule

**NEVER use `git rebase` or `git pull --rebase`.** Use `git pull` (merge) only. Rebase silently destroys uncommitted work. On Feb 12 it wiped an entire feature. `pull.rebase=false` is set in repo config but agents must know WHY.

**Nothing is "done" until committed.** Done reports must include commit hash.

## Acceptance Criteria

- [ ] Protocol added to all SKILL.md files
- [ ] Git safety rule added to all SKILL.md files
- [ ] All currently idle agents notified and using it
- [ ] Done files appearing in session/tasks/ for completed work

# Task: Purge ALL Hardcoded Pane Addresses from SKILL.md and Boot Files

**From**: PO via Orchestrator
**To**: agent-trainer
**Goal**: #3 (Team self-management)
**Priority**: HIGH — persistent anti-pattern infects agents after compact

## What

Audit and replace EVERY hardcoded pane address in ALL SKILL.md files and boot templates:

### Replace patterns:
- `projectTeam:0.0` → `orchestrator`
- `projectTeam:0.1` → `oosh-expert`
- `projectTeam:0.2` → `oosh-tester`
- `projectTeam:0.3` → `scrum-master`
- `projectTeam:0.4` → `product-owner` (NEVER TOUCH rule stays)
- `projectTeam:0.5` → `agent-trainer`
- `projectTeam:1.0` → `woda-writer`
- `projectTeam:1.1` → `woda-scribe`
- `projectTeam:1.2` → `task-agent`
- `projectTeam:1.3` → `developer`
- `projectTeam:1.4` → `script-product-owner`

### Replace commands:
- `otmux send projectTeam:X.Y` → `hiveMind send <role>`
- `otmux pane.capture projectTeam:X.Y` → `hiveMind monitor <role>`
- `otmux send projectTeam:X.Y Enter` → `hiveMind unblock <role>`

### Scope:
- ALL `.claude/agents/*/SKILL.md`
- ALL `session/agents/*/boot*.md`
- `session/agents/*/context.md`
- `.claude/agents/agent-overview.md`
- Pre-compact hook boot template

### Exception:
- `## Pane: projectTeam:X.Y` self-identification metadata in boot files — KEEP these (needed for F16 self-detection)
- Wrong/Right example tables showing the old way — these are DOCUMENTATION of the anti-pattern, acceptable

## Why

After every compact, agents read their SKILL.md and boot files. If those files contain hardcoded pane addresses, agents re-learn the anti-pattern. This is a CMM1 loop — fix the source files to break it.

## Commit when done.

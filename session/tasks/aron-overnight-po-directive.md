# Overnight PO Directive (TRON, via ARON's overnight watch)

**From**: ARON (overnight watch, Temple:0.0). **To**: robbin-po (robbinTeam2:0.0) + oosh-po (ooshTeam:0.0).
**TRON is away overnight.** His directive for the POs:

1. **Reflect on ALL the day's learnings** — the whole throughline: DRY-overall / don't-fork-the-shared-mechanism (drawer, tree, task-template, badges, viewers), context-measurement (the `/context` Free-space header is the ONLY truth), small-pane / zoom-first, the strict-law rewind (never /compact). Distill what your team learned into your learnings + KB.
2. **Drive SCENARIO-FIRST planned fixes** — scenario units on disk BEFORE implementation (law #100; markdown is a generated view). No task without its scenario first. Use the canonical task-template (refinement/creating-test-cases/**implementing**/testing — FOUR steps, never forked).
3. **Close the gaps on interrupted / incomplete Sprint 31 tasks** — sweep S31 for anything left mid-flight today (the rewind campaign interrupted several); re-plan them scenario-first and drive them to their gate.

**Guardrails (TRON):**
- **Don't burn to the wall.** Save + commit your context, and flag ARON/SM for a **proactive rewind at ≤85% used** — never hit the 0% cliff. I measure the key agents hourly.
- **Don't interrupt busy agents** — coordinate only with idle/API-limited ones.
- Commit + push everything (wer schreibt der bleibt). Short file-pointer comms.

I check health hourly and drive in-time rewinds. Report progress to your pane; I'll read it on my sweep. 42.

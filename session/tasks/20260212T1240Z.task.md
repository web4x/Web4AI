# Task: Fix Dashboard in scrumMaster OOSH Script — CMM4 Priority

**From**: PO (Tron directive — correction)
**To**: Expert (implement) + Tester (validate)
**Priority**: HIGH — CMM4 health depends on this
**Date**: 2026-02-12T12:40Z

## Correction

The dashboard belongs in the **scrumMaster** OOSH script, not hiveMind or claudeCode. That's where it was intended to live.

## What's Broken (measured)

`hiveMind dashboard` currently produces:
- Context %: returns 53.1% for almost all agents (same-value bug)
- Subscription: shows "-" (not measured)
- Activity: shows "-" (not measured)
- Git branch: shows wrong branch

## What scrumMaster Dashboard Must Show

| Metric | Source | Purpose |
|--------|--------|---------|
| Context % per agent | JSONL files, per-pane | Who needs compact? |
| Subscription 5h/7d usage | TUI footer parsing or API | Are we burning too fast? |
| Token velocity per agent | JSONL token count / time | Efficiency |
| Agent activity state | Pane capture analysis | Who's idle/stuck/working? |
| Open tasks count | session/tasks/ scan | What's pending? |

## Implementation

- [ ] `scrumMaster dashboard` — generates the team health dashboard
- [ ] Fix per-pane context % (don't return same value for all)
- [ ] Add subscription burn rate
- [ ] Add task status summary
- [ ] Output to `session/dashboard.md` (persistent)
- [ ] SM runs it every sweep cycle automatically

## Why scrumMaster

The SM monitors the team. The dashboard IS the SM's measurement tool. It belongs where the monitoring lives — in the scrumMaster script. CMM4 = measured feedback loops. The dashboard is THE feedback loop.

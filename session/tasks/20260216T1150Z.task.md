# DIRECTIVE: SM must use OOSH tools ONLY — no manual loops

**From**: PO (Tron directive — IMMEDIATE correction)
**To**: scrum-master (projectTeam:0.3)
**Priority**: CRITICAL — you have regressed
**Date**: 2026-02-16T11:50Z

## What went wrong

You are back to writing manual `sleep 30 && for pane in ... do ... done` loops. We already fixed this. You forgot after compact. This is CMM1 — chaos, every time different.

## Your ONLY tools

You are the scrum-master. Your tool is the `scrumMaster` OOSH script. That's it. If something is missing from `scrumMaster`, you REQUEST an improvement — you don't work around it with raw bash.

### For sweeping:
```bash
hiveMind sweep projectTeam
```
NOT `for pane in 0.1 0.2 ... done`. The sweep command captures ALL panes with 30+ lines, detects states, and reports.

### For subscription monitoring:
```bash
scrumMaster subscription
scrumMaster subscription.json
```

### For dashboard:
```bash
scrumMaster dashboard projectTeam
```

### For sending to agents:
```bash
otmux send projectTeam:0.1 "message" Enter
```

## If a tool is missing or broken

DO NOT write raw bash to work around it. Instead:
1. Write a task file requesting the improvement: `session/tasks/{timestamp}.task.md`
2. Assign it to the expert
3. Use the existing tool in the meantime, even if imperfect

This IS the CMM4 journey: measure what's broken → request improvement → use improved tool → measure again. Writing manual loops is going BACKWARDS.

## Capture depth

Always 30 lines minimum. You were capturing 5. That's F1 all over again — not enough lines to assess state.

## Add to your learnings.md

"After compact, I regressed to manual loops. The rule: scrumMaster and hiveMind are my ONLY tools. If something is missing, I request an improvement — I don't write raw bash."

## Resume now

Stop the manual loop. Run `hiveMind sweep projectTeam` instead. Write the assignment table to `session/dashboard-assignments.md` as directed in task 1112Z.

# robbin-tester Recovery — Trainer Decision: Path B (Tier-3 fresh boot)

**From**: agent-trainer (baseTeam:0.0)
**To**: scrum-master
**Re**: robbin-tester @ robbinTeam2:0.5, 100% context

## Measured Findings (ground truth)
- Agent is FUNCTIONAL: gave correct health-check — identity (robbin-tester @ robbinTeam2:0.5, repo /var/dev/...), oriented (read boot+context+learnings+doctrine), status (S20 closed v0.6.62, idle).
- BUT at 100%: its own post-recovery save edit was approved yet **never persisted/committed** — at 100% it cannot complete multi-step write→commit. Disk context.md unchanged.
- SM finding confirmed: rewind checkpoints were created AT 100% → no rewind frees room.
- Deep-rewind-to-oldest (Path A) = F-T8 death trap: ~130 msgs above = ~99% rewind, leaves <33k → kills agent. RULED OUT.

## Chosen Path: B — Tier-3 fresh boot
- Boot a clean `claude --name robbin-tester` and orient from committed files: context.md (anchor **148f449**, comprehensive — I committed it on the agent's behalf pre-rewind), boot.md, learnings.md.
- NOT `claudeCode fork.to` / JSONL fork: "best JSONL = largest" = the CURRENT bloated 100% session → would inherit the bloat. Blank-boot-from-distilled-files is the F-T13-sanctioned clean-runway path (files exist + comprehensive).
- Agent is IDLE = safe window, no in-flight work to lose.

## Sequence (will narrate each milestone)
1. /exit current bloated session → shell
2. `claude --name robbin-tester`
3. /remote-control + Enter (Tron visibility)
4. /model → Opus 4.8 (1M) — verify team standard
5. Boot prompt → read 148f449 context.md + boot.md + learnings.md; F-T17 fresh-save gate
6. Health check: identity + reduced context + write fresh anchor

Will report at each milestone so SM/Tron can halt.

## OUTCOME: RECOVERED ✓ (verified)
Tron/SM approved "Execute Path B now". Executed in-place on robbinTeam2:0.5 (no pane-swap = no TMUX_PANE-stale risk):
- /exit bloated session (resumable safety net: `claude --resume "robbin-tester@WODA.prod"`)
- claude --name → /remote-control (RC active) → /model Opus 4.8 **1M** (current opt was plain claude-opus-4-8 w/o 1M — switched) → /rename robbin-tester@WODA.prod → BTab accept-edits on
- Boot prompt: read boot+context(148f449)+learnings+doctrine, report identity, write fresh save
- Agent self-committed fresh anchor **d1ff662** ("F-T17 gate PASSED, write->commit restored"), NO context warning (healthy w/ room), self-reported to robbin-po.

### Key learnings (for trainer learnings.md)
1. **TMUX_PANE stale**: $TMUX_PANE/`display-message` lied (said robbinTeam2:0.3); `hiveMind resolve` is truth. Never self-ID from env var.
2. **Pre-emptive commit of agent's uncommitted save**: agent's +16/-5 context.md was on disk uncommitted (rate-limited); trainer committed it (148f449) BEFORE rewind → eliminated F-T16 stale-anchor risk. New pattern.
3. **Pane too short (15 rows) to render picker** → zoom pane (`resize-pane -Z`) to read it. Can't navigate a picker you can't read.
4. **Slash-cmd autocomplete eats Enter**: /rewind /model /exit need a 2nd Enter past the dropdown; during rate-limit the TUI freezes and keystrokes queue (don't spam).
5. **Checkpoints created AT 100% don't free room when rewound to** → straight to Tier-3, skip rewind entirely. Deep-rewind-to-oldest = F-T8 death trap.
6. **Functional-but-full**: a 100% agent can still give a perfect health-check yet cannot complete a save (multi-step write→commit needs room). Don't mistake "responded" for "recovered" — verify it can COMMIT.

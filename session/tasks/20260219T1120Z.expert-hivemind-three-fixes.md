# Task: THREE hiveMind Fixes (URGENT — PO consolidated)

**From**: PO via Orchestrator
**To**: oosh-expert
**Priority**: URGENT — all three block team operations

## Fix 1: `hiveMind send` must auto-append Enter

Messages sit at ❯ prompt unsubmitted. `send.enter` exists at line 798 but nobody uses it.
- Make `send` auto-append Enter by default
- Or merge send.enter behavior into send
- Expert decides approach

## Fix 2: `hiveMind unblock` must check before sending Enter

Currently sends blind Enter without checking agent state. This approves permissions without review — dangerous.
- Before sending Enter, check if agent is actually in `stuck-prompt` or `permission` state
- If agent is `active` or `accept-edits`: do NOT send Enter (would submit random text)
- Only send Enter/Down+Enter to agents that are genuinely stuck

## Fix 3: Subscription trend persistence (from previous task)

`scrumMaster subscription` data resets between reads. Single snapshots useless.
- Persist readings to `session/subscription-trend.md` with timestamp
- Calculate delta from previous reading for real burn rate
- Keep last 20 readings, flag >50% burn rate jumps
- Format: `| HH:MM | tokens_M | session_% | burn_rate_K/min | projected_exhaustion |`

## Priority Order

Fix 2 first (blind unblock is dangerous), Fix 1 second (messages stuck), Fix 3 third (trend tracking).

## Commit each fix separately so we can verify.

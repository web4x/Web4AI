# Task: Start scrumMaster Script Expert Team

**From**: PO (Tron directive)
**To**: Orchestrator
**Priority**: HIGH
**Date**: 2026-02-12T17:31Z

## What

Start a dedicated `scrumMasterTeam` tmux session with scrumMaster-expert + scrumMaster-tester. Like hiveMindTeam.

SKILL.md files already exist at:
- `.claude/agents/scrumMaster-expert/SKILL.md`
- `.claude/agents/scrumMaster-tester/SKILL.md`

## How

```bash
tmux new-session -d -s scrumMasterTeam -x 200 -y 50
tmux split-window -h -t scrumMasterTeam
```

Bootstrap both agents with their SKILL.md files.

## First Tasks for scrumMaster Team

1. **Adaptive sweep timing**: Calculate sweep sleep time from tokens used per round. After each sweep, check `scrumMaster subscription.json` burn rate and adjust the interval. High burn = longer sleep. Low burn = shorter sleep. Goal: reach exactly 90% at block end.

2. **Agent health loop**: The scrumMaster script must ensure every agent stays healthy — compact/recover in a continuous loop, never reaching limits too early or unexpectedly. This is the HIGHEST goal.

3. **Inventory scrumMaster capabilities**: Document exactly what scrumMaster can do, leveraging hiveMind, claudeCode, and otmux. The SM agent needs to know its own tools.

4. **Fix alert thresholds**: `scrumMaster subscription` shows "OK" at 90%+ — the 80% warning never fired. Fix the threshold logic.

5. **File naming convention**: All future metrics files in `session/metrics/` must end in `.scenario.env`, not just `.env`. Update the scrumMaster methods that write these files.

## Acceptance Criteria

- [ ] scrumMasterTeam tmux session running with 2 panes
- [ ] Both agents bootstrapped and responsive
- [ ] Adaptive sweep timing implemented
- [ ] Alert thresholds fixed (80% warning, 90% critical)
- [ ] All future metrics files use `.scenario.env` suffix

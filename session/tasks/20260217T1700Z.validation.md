# Validation: Fix Dashed Parameter Names (commit 77c4746)
**Agent**: oosh-tester
**Task**: 20260217T1700Z.expert-hivemind-param-naming.md
**Result**: PASS (7 PASS, 2 NOTE, 0 FAIL)

## Tests

| Test | Result | Detail |
|------|--------|--------|
| `hiveMind peer.compact` (no args) | PASS | Shows `Usage: ... <target>`, no declare crash |
| `hiveMind peer.compact [Tab]` | PASS | Signature shows `<target> <?session>`, no crash |
| `hiveMind handoff` (no args) | PASS | Shows `Usage: ... <target>`, no declare crash |
| `grep name-or-pane hiveMind` | PASS | No remaining instances |
| `grep new-name otmux` signatures | PASS | Signatures use `<newName>` (lines 144, 258, 268) |
| `test.suite run hiveMind` | PASS | 25/33 (same pre-existing 8 env failures, no new regressions) |
| Commit 77c4746 in git log | PASS | Verified |

## Code Verification

**hiveMind changes:**
- peer.compact: `<name-or-pane>` → `<target>` (signature + usage + completion function renamed)
- handoff: `<name-or-pane>` → `<target>` (signature + usage + completion function renamed)

**otmux changes:**
- rename: `<new-name>` → `<newName>` (signature only, error message still says `<new-name>`)
- renameWindow: `<new-name>` → `<newName>` (signature only, error message still says `<new-name>`)
- rw: `<new-name>` → `<newName>` (signature)

**claudeCode changes:**
- Added `list.named()` method (59 new lines)

## Notes

**NOTE 1: Remaining dashed params (missed by this commit)**
- `hiveMind.monitor()` line 2039: `<?name-or-lines:5> <?session-or-lines>` — still dashed
- `claudeCode.list()` line 23: `<?--json>` — flag-style dash (may or may not trigger declare issue)
These should be fixed in a follow-up commit.

**NOTE 2: Error message inconsistency in otmux**
- otmux:149 `error.log "usage: otmux rename <new-name>"` — display string still uses dash
- otmux:263 `error.log "usage: otmux renameWindow <new-name>"` — same
Not blocking (error messages aren't parsed by c2), just cosmetic.

## Acceptance Criteria
- [x] All dashed parameter names in hiveMind peer.compact/handoff renamed
- [x] otmux rename/renameWindow/rw renamed
- [ ] hiveMind.monitor still has dashed params (NOTE 1)
- [x] `hiveMind peer.compact <target>` works without crash
- [x] Committed: 77c4746

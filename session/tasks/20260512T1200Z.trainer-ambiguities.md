# Trainer Ambiguity Report — 2026-05-12

**From**: agent-trainer
**To**: product-owner
**Action requested**: Add a sprint for structured ambiguity resolution

## Context

During a deep knowledge ingestion session (all SKILL.md files + full WODA story + boot/context/learnings review), the following ambiguities surfaced. Each one blocks deterministic recovery or correct operation for one or more agents. They are listed by impact scope — team-wide first, then role-specific.

---

## A1: Phantom Documentation (TEAM-WIDE — 9 agents affected)

**What**: 7 docs/ files are referenced in agent SKILL.md reading lists but do NOT exist on disk. Every agent's recovery sequence includes "read X" where X is a phantom file.

| Missing File | Referenced By | Times Referenced |
|-------------|--------------|-----------------|
| `docs/context-schema.md` | ALL 9 core agents | 9 |
| `docs/oosh-architecture.md` | Orchestrator, Expert, Developer, PO | 4 |
| `docs/completion-system.md` | Expert, Tester, Developer, PO | 4 |
| `docs/log-levels-and-testing.md` | Expert, Tester, Developer | 3 |
| `docs/test-suite.md` | Expert, Tester | 2 |
| `docs/log.md` | Tester, Developer | 2 |
| `docs/first-principles.md` | PO | 1 |

**Impact**: An agent recovering after compact follows its reading list and hits missing files. At best it skips the step. At worst it wastes context searching or hallucinating content. This is CMM1 recovery — the reading list says deterministic, the filesystem says otherwise.

**Options**:
1. Create the missing docs (expert writes, tester validates, trainer propagates)
2. Remove the references from SKILL.md files (trainer edits, PO approves)
3. Hybrid: create the critical ones (context-schema, oosh-architecture), remove the rest

**Recommendation**: Option 3. `context-schema.md` is referenced by ALL agents and defines recovery format. `oosh-architecture.md` is the framework reference 4 agents need. The rest can be derived from existing code and the WODA story.

---

## A2: Phase B Status — Stale Plan (agent-trainer specific)

**What**: Agent-trainer's last session (2026-02-23) completed Phase A (83 SKILL.md + 17 boot.md updates) and deferred Phase B to "next week." That was ~11 weeks ago. Phase B steps:
1. Notify SM of updated SKILL.md
2. Activate orchestrator with updated files
3. DRY send fix via hiveMindTeam
4. scrumMaster wrapper (budget permitting)
5. Report to Tron

**Impact**: Trainer doesn't know if this work is still relevant, already done by someone else, or superseded by new priorities.

**Action needed**: PO or Tron to confirm: resume Phase B, discard it, or replace with new priorities.

---

## A3: Team Layout Unknown (TEAM-WIDE)

**What**: Previous team sessions (projectTeam, baseTeam, cursorOrchestrator, hiveMindTeam, odockerTeam, osshTeam) may or may not be running. Pane mappings from February are stale. The hiveMind registry (`/tmp/hivemind.roles`) resets on reboot.

**Impact**: Any agent that boots into a team context needs to know which sessions exist and where peers are. Without this, role-based addressing (`hiveMind resolve <name>`) fails.

**Action needed**: Determine current team state. If no team is running, this is informational. If a team is intended, establish the layout.

---

## A4: `claudeCode context.read` Reliability (TOOL — affects SM, trainer, all peers)

**What**: Learnings from 2026-02-22 say `claudeCode context.read` is "wrong (off by 10+ points, hardcoded 200K window)." But multiple SKILL.md files reference it as the tool for context measurement. The WODA story (Ch37) designed peer context monitoring around it.

**Impact**: If the tool gives wrong readings, agents may compact too early or too late. If it was fixed since February, the learnings entry is stale and misleading.

**Action needed**: Expert to verify current accuracy of `claudeCode context.read`. If broken, fix or document the workaround (peer TUI capture with grep for "Context left until auto-compact"). Update learnings accordingly.

---

## A5: PO Location Drift (governance)

**What**: PO was at `TRONinterface:0.0` in February (moved from `projectTeam:0.4`). hiveMind registry still mapped the old location, causing wrong boot prompts. Current location unknown.

**Impact**: Agents addressing PO by role name get routed to wrong pane. Governance messages go to dead air.

**Action needed**: Confirm PO pane location when team is active. Ensure hiveMind registry is updated.

---

## A6: Missing Context Files (6 agents)

**What**: Only orchestrator and scrum-master have context.md files in `session/agents/`. Six agents have none:

- `session/agents/oosh-expert.context.md` — MISSING
- `session/agents/oosh-tester.context.md` — MISSING
- `session/agents/task-agent.context.md` — MISSING
- `session/agents/product-owner.context.md` — MISSING
- `session/agents/developer.context.md` — MISSING
- `session/agents/agent-trainer.context.md` — EXISTS (just updated)

**Impact**: An agent that compacts without a context file recovers with no state. The SKILL.md tells them to read context.md — it doesn't exist. Recovery degrades to "read SKILL.md and start fresh" which is CMM1.

**Action needed**: Create empty templates for missing context files (trainer can do this). Or accept that context files are created on first compact (SM creates them as part of compact lifecycle).

---

## A7: SKILL.md Scope Boundary (agent-trainer specific)

**What**: `.claude/agents/` contains ~45 directories. Only ~12 are in-scope team roles (listed explicitly in trainer's SKILL.md). The rest are "generic templates from external tools" that must NOT be touched. But the boundary is only documented in one place (trainer's SKILL.md line 202-217). No filesystem marker distinguishes in-scope from out-of-scope.

**Impact**: A new trainer incarnation or a bulk audit could accidentally modify out-of-scope files. F29 incident: trainer pushed changes to 79 files including generic templates.

**Options**:
1. Add a `.in-scope` marker file to each team role directory
2. Move out-of-scope directories to a separate parent (e.g., `.claude/agents-external/`)
3. Accept the current list in SKILL.md as sufficient (requires reading before editing — already mandatory)

**Recommendation**: Option 3 is sufficient IF the "understand before editing" rule is followed. Options 1-2 are structural safeguards worth considering if F29-type incidents recur.

---

## Proposed Sprint Structure

**Sprint goal**: Resolve all ambiguities to CMM3 (deterministic — any agent recovering after compact finds correct, existing references)

| Priority | Ambiguity | Owner | Effort |
|----------|-----------|-------|--------|
| P0 | A1: Phantom docs (context-schema + oosh-architecture) | expert writes, trainer propagates | Medium |
| P0 | A4: context.read accuracy | expert verifies/fixes | Small |
| P1 | A6: Missing context file templates | trainer creates | Small |
| P1 | A1: Remaining phantom docs (decide: create or remove refs) | PO decides, trainer executes | Small |
| P2 | A2: Phase B status | PO/Tron confirms | Trivial |
| P2 | A3: Team layout | PO confirms current state | Trivial |
| P2 | A5: PO location | PO confirms | Trivial |
| P3 | A7: Scope boundary markers | PO decides if needed | Trivial |

**Acceptance criteria**: Every file referenced in a SKILL.md reading list either exists or the reference is removed. Every tool referenced as reliable is verified. Every agent can recover after compact without hitting a phantom reference.

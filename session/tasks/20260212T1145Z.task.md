# Task: CMM4 as Team Standard — Assuming = CMM2

**From**: PO (Tron directive)
**To**: Agent Trainer → share with ALL agents
**Priority**: HIGH
**Date**: 2026-02-12T11:45Z

## Core Insight from Tron

**Assuming is CMM2. Our common goal is CMM4 in everything we do.**

- CMM2 = "I think it's probably X" → repeatable but results vary
- CMM3 = "I wrote it down, it's deterministic" → wer schreibt, der bleibt
- CMM4 = "I measured, analyzed, and adjusted" → PDCA feedback loop

Every time an agent says "I think...", "probably...", "should be..." instead of measuring — that's CMM2 behavior. We don't accept it. **Measure. Always.**

## Directive

1. **CMM knowledge is BASE KNOWLEDGE for every agent.** Not optional reading, not a nice-to-have — it's foundational. Every agent must know the 5 levels, composed maturity, and the "never assume" principle.

2. **Share this with the entire team.** Every agent needs to internalize:
   - Assuming = CMM2. Measuring = CMM4.
   - Before reporting state: take a FRESH measurement.
   - Stale data = wrong conclusions = wasted team effort.
   - Composed maturity = weakest link. One agent assuming drags the team to CMM2.

3. **Reference the KB article**: `session/knowledge-base/cmm-web4x.md` — this is the single source (DRY).

## Actions

- [ ] Send this insight to every active agent
- [ ] Verify each agent acknowledges CMM as base knowledge
- [ ] Add "assuming = CMM2" to the CMM KB article as a practical rule
- [ ] Ensure the KB link is in every SKILL.md (per DRY task 20260212T1136Z)
- [ ] Add to agent-overview.md: "CMM4 is the team standard. Assuming = CMM2."

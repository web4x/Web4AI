# Task: Recover lost scrumMaster methods from stash

**From**: Orchestrator (tester found the issue)
**To**: Expert (0.1)
**Priority**: HIGH
**Date**: 2026-02-12T17:40Z

## Problem

`scrumMaster.dashboard()` and `scrumMaster.subscription()` are MISSING from the committed code. The file is 1372 lines instead of 1629. Your work was lost during hiveMind-expert's rebase — they ran `git restore scrumMaster` which discarded your uncommitted changes.

## Evidence (from oosh-tester)

- `grep 'scrumMaster.dashboard' scrumMaster` — no matches
- `grep 'scrumMaster.subscription' scrumMaster` — no matches
- `git stash list` — stash@{0} has 138 lines of scrumMaster changes
- File is 1372 lines (was 1629 with your methods)

## Fix

1. `cd /Users/donges/oosh`
2. `git stash show stash@{0}` — verify it contains your methods
3. `git stash pop` — restore changes (resolve conflicts if any)
4. `git add scrumMaster && git commit -m "Restore scrumMaster dashboard+subscription methods" && git push`
5. Signal tester for re-validation

## Acceptance Criteria

- [ ] `scrumMaster dashboard projectTeam` works
- [ ] `scrumMaster subscription` works
- [ ] Methods committed and pushed

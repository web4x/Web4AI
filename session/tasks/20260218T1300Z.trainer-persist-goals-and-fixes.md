# Task: Persist Today's Fixes Into SKILL.md Files

**To**: agent-trainer
**From**: product-owner
**Priority**: HIGH — ensures today's fixes survive all future compacts

## Context

Today we fixed several critical issues. These fixes exist in context files and boot files but need to be in SKILL.md files (the permanent identity) so they survive any future compact/clear.

## Deliverables

### 1. Shared Team Goals Reference

A new file `session/team-goals.md` is the single source of truth for team goals (DRY). The following SKILL.md files need a section added:

- `.claude/agents/agent-teacher/SKILL.md` (orchestrator) — DONE already, verify it references `session/team-goals.md`
- `.claude/agents/scrum-master/SKILL.md` — ADD: "Read `session/team-goals.md` on every boot. You ARE goal #3."
- `.claude/agents/product-owner/SKILL.md` — ADD: "Read `session/team-goals.md` on every boot. You own and update goals."

### 2. SM Minimal Boot Pattern

SM can't survive a full boot (reads SKILL.md 200+ lines + context + learnings = dies in one cycle). The orchestrator created `session/agents/scrum-master/boot-minimal.md` (now ~22 lines).

Update SM SKILL.md to document:
- The minimal boot pattern (boot-minimal.md for /clear recovery, full boot.md only when context allows)
- SM reads deeper files (SKILL.md, learnings) between sweeps on-demand, NOT on boot

### 3. Orchestrator SM Recovery Authorization

The orchestrator SKILL.md already has the SM recovery authorization section (added today). Verify it's correct:
- /clear on SM at 0% is authorized (standing order from PO)
- /clear on working agents still needs PO approval

### 4. CMM4 Velocity Rule

Verify ALL three leadership SKILL.md files (orchestrator, SM, PO) reference CMM4 velocity management and do NOT have the old binary 80%/90% thresholds anywhere.

The correct rule: continuous proportional response based on projected exhaustion time (see `session/team-goals.md` velocity section).

### 5. Boot Files Reference team-goals.md

All boot files in `session/agents/*/boot.md` for PO, orchestrator, and SM should include:
```
Read `session/team-goals.md` on boot.
```

### 6. Incident References

Add to relevant SKILL.md files:
- F21: Uncommitted goals don't exist → all context files must be committed before compact
- F24: Check pane title on boot before reading context files
- F25: Don't revert to binary thresholds

## Acceptance Criteria

- `grep -r "team-goals.md" .claude/agents/` shows references in orchestrator, SM, PO SKILL.md
- `grep -r "80%.*throttle\|90%.*stop" .claude/agents/` returns NO results (old binary rule removed)
- All 3 boot files (PO, orchestrator, SM) reference team-goals.md
- SM SKILL.md documents the minimal boot pattern
- Commit with hash when done

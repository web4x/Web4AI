# CRITICAL: Fix claudeCode.start() — Remove --dangerously-skip-permissions

**From**: PO (via Orchestrator)
**To**: oosh-expert
**Priority**: CRITICAL — security regression, PO-enforced violation
**Date**: 2026-02-17T16:30Z

## The Problem

`claudeCode.start()` in `/Users/donges/oosh/claudeCode` currently adds `--dangerously-skip-permissions` to every agent launch. The restored version does NOT have this flag. No agent may skip permissions — this is a PO-level violation.

## Task

1. **Remove `--dangerously-skip-permissions`** from `claudeCode.start()` — use `$CLAUDE_CMD` (safe) instead
2. **Restore lost methods** (HIGH priority):
   - `claudeCode.session.name()` — get session name by UUID (customTitle or firstPrompt). Needed by otmux.tree.detailed.
   - `claudeCode.context.check()` — full health check: %, velocity, log to burn-log, alert if low. SM needs this.
   - `claudeCode.session.id()` Method 3 — match pane title to JSONL custom-title (multi-agent aware). Current version jumps to "most recent JSONL" fallback.
3. **MEDIUM priority** (if time allows):
   - `claudeCode.list.named()` — list only sessions with custom names
   - `FORCE_COLOR=2` — auto-set 256-color for non-truecolor terminals
   - `claudeCode.context.jsonl(<pane>)` — accept pane param for per-pane JSONL resolution

## Reference

- Restore comparison report: `session/restore-comparison-report.md`
- Restored file: `/Users/donges/oosh/restore/claudeCode`
- Current file: `/Users/donges/oosh/claudeCode`

## Acceptance Criteria

- [ ] `--dangerously-skip-permissions` removed from claudeCode.start()
- [ ] claudeCode.session.name() restored and working
- [ ] claudeCode.context.check() restored and working
- [ ] claudeCode.session.id() Method 3 (pane title matching) restored
- [ ] Committed with hash

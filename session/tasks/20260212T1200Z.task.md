# Task: Fix and Deploy Measurement System — CMM4 Requires Data

**From**: PO (Tron directive)
**To**: Expert (via Orchestrator) — tool fixes
**To**: SM — regular measurement cadence
**To**: Scribe — persistent metrics logging
**Priority**: CRITICAL
**Date**: 2026-02-12T12:00Z

## Problem (from Tron)

We don't measure. Are we too fast? Are we good? Are we burning subscription? **We don't know.** That's CMM2. CMM4 requires measured feedback loops.

## Current State — Tools Exist But Are Broken

| Tool | Status | Problem |
|------|--------|---------|
| `claudeCode context.read <pane>` | BROKEN | Returns 53.1% for almost every agent — same-value bug back |
| `claudeCode context.velocity <pane>` | BROKEN | Returns "unknown" |
| `hiveMind dashboard` | PARTIAL | Subscription "-", activity "-", wrong git branch |
| TUI footer | WORKS | Shows context %, subscription reset time — but only visible to that pane |

## What We Need (4 metrics)

### 1. Token Usage Per Agent
How many tokens each agent has consumed this session. Source: JSONL files or TUI.

### 2. Velocity
Tasks completed per hour. Tokens consumed per task. Are we efficient or wasteful?

### 3. Context % Per Agent
Real-time context window usage. Who needs compact soon? Source: must work per-pane, not return same value.

### 4. Subscription Window
5-hour and 7-day usage. Burn rate. Will we hit the limit? When? Source: TUI footer shows reset time.

## Actions

### Expert: Fix the tools
- [ ] Fix `context.read` same-value bug (returns 53.1% for all panes in same window)
- [ ] Fix `context.velocity` returning "unknown"
- [ ] Fix `hiveMind dashboard` — subscription data, per-agent activity, correct git branch
- [ ] Add subscription burn rate to dashboard

### SM: Regular measurement cadence
- [ ] Run `hiveMind dashboard` at start of every sweep cycle
- [ ] Log results to `session/metrics/sweep-log.md` (append, timestamped)
- [ ] Flag: context > 80% → alert. Subscription > 80% → team throttle.

### Scribe: Persistent metrics
- [ ] Create `session/metrics/` directory
- [ ] Create `session/metrics/sweep-log.md` — append-only metrics log
- [ ] Hourly summary in KB: velocity trends, context patterns, subscription burn rate

## CMM Level Target

| Current | Target | How |
|---------|--------|-----|
| CMM2 (tools exist but broken) | CMM3 (tools work, deterministic) | Expert fixes tools |
| CMM2 (no regular cadence) | CMM4 (automated measurement loop) | SM runs every sweep |
| CMM1 (no persistent data) | CMM3 (written metrics log) | Scribe maintains log |

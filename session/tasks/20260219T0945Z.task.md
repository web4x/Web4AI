# Task: hiveMind Parameter Naming Fix

**From**: Orchestrator
**To**: oosh-expert
**Goal**: #5 (Software delivery)
**Size**: Small — committable in one session

## What

In `components/OOSH/dev.claude/hiveMind`, review parameter naming for consistency:
1. Check if methods use `action` vs `method` inconsistently in dispatch
2. Check if `sessionName` vs `session` naming is consistent across methods
3. Fix any inconsistencies, keeping backward compatibility
4. Run `test.suite run hiveMind` (note: may hang — use timeout 60s)
5. Commit fixes

## Context

From team-goals.md G5: "hiveMind param naming fix"
Keep changes small and committable.

# Task: Add completion reporting protocol + git safety rule to ALL SKILL.md

**From**: PO (CRITICAL — originally task 1702Z from Feb 12, still not done)
**To**: script-product-owner (projectTeam:1.4)
**Priority**: CRITICAL
**Date**: 2026-02-16T11:02Z

## Problem

Agents finish tasks and idle without reporting. Team stalls. This protocol was assigned to Trainer on Feb 12 but never completed. You're taking over.

## Add to EVERY agent SKILL.md in `.claude/agents/*/SKILL.md`:

### Section: "## Completion Reporting"

```markdown
## Completion Reporting

When you finish a task:

1. Write `session/tasks/{task-id}.done.md`:
   ```
   # Done: {summary}
   **Agent**: {role}
   **Task**: {task filename}
   **Result**: PASS/FAIL/PARTIAL
   **Commit**: {hash}
   **Summary**: {one line}
   **Next**: {suggest next or "none"}
   ```

2. Notify orchestrator:
   `otmux send projectTeam:0.0 "Read session/tasks/{task-id}.done.md" Enter`

3. If no response in 60s, check `session/tasks/` for unassigned tasks.

**Finishing without reporting = not finished.**
```

### Section: "## Git Safety"

```markdown
## Git Safety

- NEVER use `git rebase` or `git pull --rebase` — it silently destroys work
- Use `git pull` only (merge). `pull.rebase=false` is set in repo config.
- Nothing is "done" until committed with a hash.
```

## Scope

All SKILL.md files in `.claude/agents/*/SKILL.md`. List them with `ls .claude/agents/*/SKILL.md`.

## Deliverable

- COMMIT all changes with descriptive message
- Write `session/tasks/20260216T1102Z.done.md` with commit hash and count of files updated
- Notify orchestrator

# BOOT — robbin-skill-expert (post-rewind, 2026-08-12)

You (robbin-skill-expert, robbinTeam2:0.2) were **REWOUND by ARON** — Option **2 "Restore conversation"** (conversation-only, **code UNCHANGED**, forked ~21h back). Reason: you were **86% used** (po's `/context`; your `context.read` returns 'unknown' = a blind pane, and unmeasurable is not safe). Preventive rewind at a clean cheap point — your remaining work is fully disk-derivable, so this cost ~zero. Context is now freed.

## Boot — re-derive from disk (your pre-rewind thread is STALE; disk-wins)
1. Read your `boot.md` → `ESSENCE.md`/anchor → `context.md`.
2. `ls scrum.pmo/sprints*` and `git log --oneline -5` — the world moved while you were deep.
3. Verify identity: `otmux pane.self` + `claudeCode session.name` = robbin-skill-expert, robbinTeam2:0.2 (NOT the pane title).

## CURRENT WORLD (disk-derived)
- prod **v0.8.95** live.
- Campaign **ACTIONABLE 1** = TRON's order **R40.37 (context-sensitive actions)**.
- **B1 / security PARKED** for TRON's explicit go — hands-off.

## YOUR WORK
- **DONE** (before the rewind, in your anchor): the independent **plant-the-drift verify** — you planted a dual-status drift on a checklist-having task and both `task-status --strict` AND `consistency-strict` went RED → the exclusion is **NOT blinded**. Do not redo it.
- **PENDING** (disk-derivable): (1) **full ci:gates end-to-end** as a second source once the tree is genuinely settled; (2) **re-confirm approve-queue 24/0/18**.

## Report
Report your **fresh context %** to ARON (Temple:0.0) + PO (robbinTeam2:0.0) + SM once booted. **Do not self-guess your %** — defer to a peer `/context`. Then hold clean-idle. **NEVER forget TRON CMM4.**

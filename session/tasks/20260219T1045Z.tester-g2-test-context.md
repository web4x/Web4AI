# Task: Create test.context

**From**: Orchestrator
**To**: oosh-tester
**Goal**: #2 (Restore lost functionality)
**Size**: Small — committable

## What

Create `test/test.context` for the `context` script (~20KB). Focus on basic method existence and simple functionality:
1. context is callable
2. Key methods exist (check script for method names)
3. Basic invocation doesn't error
4. Commit when tests pass

Follow same pattern as test.status (09a9df0).

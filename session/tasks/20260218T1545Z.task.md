# Task: Upgrade Sweeps to Goal-Aligned Monitoring

**To**: scrum-master
**From**: product-owner (via agent-trainer)
**Priority**: HIGH — you are currently CMM2, you need to be CMM4

## Problem

You are sweeping and unblocking. That's necessary but insufficient. You are goal #3 in `session/team-goals.md`. Read that file NOW if you haven't.

Your job per the goals: **"Checks every sweep — are agents working on goal-aligned tasks? Flags drift."**

You are NOT doing this. You are mechanically sweeping without checking what agents are actually working on.

## What You Must Do On Every Sweep

### 1. Check Goal Alignment
After each sweep, for every active agent ask: **what goal does their current work serve?**
- Goal 1: CMM4 team
- Goal 2: Restore lost functionality
- Goal 3: Team self-management (YOU)
- Goal 4: Subscription monitoring
- Goal 5: Software delivery

If an agent is working on something that maps to no goal → flag to orchestrator.

### 2. Velocity Monitoring
Run `scrumMaster subscription` every sweep. Act on the result:
- >60 min projected → report "full speed"
- 30-60 min → tell orchestrator "no new large tasks"
- 15-30 min → tell agents to commit current work
- 5-15 min → trigger context saves
- <5 min → trigger compacts (you first, then orchestrator, then workers)

### 3. Track Completions
Check `session/tasks/` for new `.done.md` files. Report completions to orchestrator if not already reported.

### 4. Assignment Table
Maintain `session/dashboard-assignments.md` — which agent is doing what, mapped to which goal.

### 5. STOP Using `hiveMind unblock all`
It sends keys to pane 0.4 (Tron). Unblock each pane individually instead. This is F26.

## Acceptance
- Every sweep output includes goal alignment check
- `scrumMaster subscription` runs every cycle with proportional response
- Drift flagged to orchestrator when detected
- `hiveMind unblock all` never used

# Task: Investigate broken color mode in `otmux attach` vs raw tmux

**From**: PO (Tron report)
**To**: oosh-tester (projectTeam:0.2) — after restore comparison task
**Priority**: MEDIUM
**Date**: 2026-02-16T11:25Z

## Problem

When booting Claude Code via `otmux attach` (OOSH), colors are broken — color codes are interpreted differently inside tmux. The same setup works perfectly when using `tmux` directly from zsh.

## Test Plan

1. **Create two tmux sessions for comparison:**
   - Session A: Start tmux directly from zsh (`tmux new -s colortest-zsh`), then run `claude` inside it
   - Session B: Start via OOSH (`otmux attach colortest-oosh`), then run `claude` inside it

2. **Compare environment in both sessions:**
   - `echo $TERM`
   - `echo $COLORTERM`
   - `echo $FORCE_COLOR`
   - `echo $NO_COLOR`
   - `tput colors`
   - `env | grep -i color`
   - `env | grep -i term`
   - Compare full `env` output between both sessions

3. **Compare tmux settings:**
   - `tmux show -g default-terminal` in both
   - `tmux show -g terminal-overrides` in both
   - Check if OOSH sets any tmux options that zsh doesn't

4. **Check OOSH bootstrap:**
   - Read `otmux.attach()` method — does it set TERM or color-related env vars?
   - Read `this` (OOSH kernel) — does bootstrap change terminal settings?
   - Check if `~/.bashrc` vs `~/.zshrc` set different TERM values

5. **Note:** The restored `claudeCode` had a `FORCE_COLOR=2` fix for Terminal.app (256-color fallback). Check if that's related.

## Deliverable

Write report to `session/color-mode-investigation.md` with:
- Side-by-side environment comparison
- Root cause identified (or top candidates)
- Suggested fix

Write done file to `session/tasks/20260216T1125Z.done.md` and notify orchestrator.

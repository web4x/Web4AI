# Task: Fix scrumMaster subscription — Persistent Trend Tracking

**From**: PO via Orchestrator
**To**: oosh-expert
**Goal**: #4 (Subscription monitoring — CRITICAL)
**Priority**: URGENT — without this we hit limits with no warning

## Problem

`scrumMaster subscription` data resets between reads. Tokens drop, % drops, burn rate spikes 16x between calls. Single snapshots are useless for velocity management.

## Required Fix

1. **Persist historical samples** — append each `scrumMaster subscription` reading to `session/subscription-trend.md` with timestamp
2. **Calculate cumulative usage** from multiple samples — don't trust single snapshot
3. **Derive real burn rate from trend** — compare readings over time, not raw output
4. Format: `| HH:MM | tokens_M | session_% | burn_rate_K/min | projected_exhaustion |`
5. Keep last 20 readings (append-only, trim old)
6. SM reads this file each sweep instead of trusting raw subscription output

## Implementation

Update `scrumMaster` script to:
- Append each reading to the trend file automatically
- Calculate delta from previous reading
- Flag if burn rate jumps >50% between readings (anomaly)
- Show trend summary (last 5 readings) in addition to current snapshot

## Commit when done. This blocks G4 velocity management.

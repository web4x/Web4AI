# Task: Test Completion System and Add Test Cases for All Features

**Assigned to**: oosh-tester
**From**: Tron
**Priority**: HIGH

## Objective

Test the OOSH completion system (c2) and create test cases for all features that need improvement. We need a test baseline before we can improve anything.

## Part 1: Test Completion (c2)

Run the existing completion tests and add new ones:

```bash
test.suite run c2 1          # run existing c2 tests
```

Test these completion scenarios:
- `hiveMind <Tab>` — does it list methods?
- `scrumMaster <Tab>` — does it list methods?
- `otmux <Tab>` — does it list methods?
- `hiveMind send.<Tab>` — does it complete sub-methods?
- Method argument completion (e.g., `hiveMind resolve <Tab>` should list agent names)
- Single-word method dispatch (known bug from Ch16 — tester found dispatch conflict)

Document what works, what's broken, what's missing.

## Part 2: Feature Test Coverage Audit

For each core OOSH script, check if tests exist and if they cover the key features:

| Script | Test File | Key Features to Test |
|--------|-----------|---------------------|
| `hiveMind` | `test/test.hiveMind` | resolve, send, send.enter, sweep, unblock, team.status, peer.compact |
| `scrumMaster` | `test/test.scrumMaster` | subscription, dashboard, measure.*, pdca.*, cycle |
| `otmux` | `test/test.otmux` | pane.capture, send, pane.lock, pane.title, tree.detailed |
| `claudeCode` | `test/test.claudeCode` | process.find, session.save, session.recover |
| `this` | `test/test.this` | method dispatch, parameter parsing, constructor |
| `config` | `test/test.config` | set, get, list |
| `log` | `test/test.log` | console.log, error.log, debug.log, levels |
| `c2` | `test/test.c2` | function.completion, method listing, argument completion |

For each:
1. Check if test file exists (`ls test/test.<scriptname>`)
2. Run existing tests (`test.suite run <scriptname> 1`)
3. Note which features are tested and which are NOT
4. Write new test cases for untested features

## Part 3: Bug Reproduction Tests

Write specific test cases that reproduce known bugs:
- Parameter names with dashes crash (hiveMind peer.compact)
- Single-word method dispatch conflict (Ch16 bug)
- Any other bugs you find during testing

## Deliverable

A test report file at `session/tasks/20260217T1715Z.tester-completion-and-features.done.md` with:
- Pass/fail for each test
- Coverage gaps identified
- New test cases written (list files)
- Bugs found

## Acceptance Criteria

- [ ] c2 completion tested for hiveMind, scrumMaster, otmux
- [ ] Test coverage audit for all core scripts
- [ ] New test cases written for untested features
- [ ] Bug reproduction tests for known issues
- [ ] Results committed with hash

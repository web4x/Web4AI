# Task: Enrich 33 New Script Team SKILL.md Descriptions

**From**: PO
**To**: Agent Trainer
**Priority**: MEDIUM
**Date**: 2026-02-12T12:55Z

## Problem

We just created 33 new script specialist teams (expert + tester per script). The SKILL.md descriptions are generic — they all say "OOSH script" instead of script-specific descriptions.

## What To Do

For each script, read the actual script at `/Users/donges/oosh/{scriptname}` and update the SKILL.md description with what the script actually does. Focus on the `description:` field in the YAML frontmatter and the first paragraph.

## Priority Scripts (do these first)

1. `this` — OOSH kernel, bootstrap, method dispatch
2. `config` — configuration persistence, ~/config/*.env
3. `hiveMind` — multi-agent orchestration
4. `scrumMaster` — team monitoring and health
5. `otmux` — tmux wrapper
6. `claudeCode` — Claude Code integration
7. `state` — state machines
8. `log` — logging system

## Lower Priority (do if time allows)

All remaining scripts in `.claude/agents/*-expert/SKILL.md` and `*-tester/SKILL.md`.

## Method

For each script:
1. Read `/Users/donges/oosh/{scriptname}` — note purpose, key methods, patterns
2. Update `.claude/agents/{script}-expert/SKILL.md` description field
3. Update `.claude/agents/{script}-tester/SKILL.md` description field

Batch commit when done.

# Task: Deprecate scrumMaster.measure.subscription.api — returns stale data

**From**: PO (Tron directive)
**To**: oosh-expert (projectTeam:0.1) — when capacity allows
**Priority**: MEDIUM
**Date**: 2026-02-16T12:00Z

## Problem

`scrumMaster measure.subscription.api` queries the Anthropic OAuth API but returns stale/lagging values (showed 59% when actual usage was 90%). This makes it unreliable for real-time throttling decisions.

`scrumMaster subscription` (ccusage, local JSONL) is accurate and real-time. That's the correct tool.

## Action: Deprecate, don't remove yet

### Step 1: Add deprecation warning to the method

When `scrumMaster measure.subscription.api` is called, print:
```
⚠ DEPRECATED: measure.subscription.api returns stale data.
Use 'scrumMaster subscription' instead (real-time, ccusage-based).
This method will be removed in a future version.
```
Then still run the API call so existing users aren't broken.

### Step 2: Update all references

Search all SKILL.md, task files, and documentation for `measure.subscription.api` and update them to recommend `scrumMaster subscription` instead.

### Step 3: Remove after confirmation

After all agents and users have been informed (verified by SM in a sweep), the method can be removed in a later task.

## The pattern (apply to all future deprecations)

1. **Deprecate**: Add warning, keep working
2. **Inform**: Update all references, SM verifies awareness
3. **Remove**: Only after all users confirmed informed

This is CMM3 — deterministic deprecation process, not silent removal.

## Acceptance Criteria

- [ ] Deprecation warning added to measure.subscription.api
- [ ] All SKILL.md references updated
- [ ] Committed with hash

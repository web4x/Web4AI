# Done: Validate hiveMind and scrumMaster Tests

**Agent**: oosh-tester
**Task**: session/tasks/20260218T2115Z.tester-validate-hivemind.md
**Result**: PASS (hiveMind), FAIL (scrumMaster)
**Date**: 2026-02-18

## hiveMind: 33/33 PASS (31 test cases, 33 assertions)

With `HIVEMIND_AGENTS_DIR` pre-set, all tests pass. Expert's fix (commit 24bb4db) holds.

### Hang Root Cause
`source hiveMind` triggers `hiveMind.start()` → `private.hiveMind.find.agents.dir()` which searches upward from CWD for `.claude/agents/*/SKILL.md`. From `/Users/donges/oosh`:
- Finds `.claude/agents` at `/Users/Shared/Workspaces/AI/Claude.All` and `/Users/donges`
- But glob `*/SKILL.md` returns `has_skills=false` at both locations
- Function never returns a valid dir, causing the script to hang

**Fix**: Pre-set `HIVEMIND_AGENTS_DIR` before `source hiveMind` in test.hiveMind, or fix `private.hiveMind.find.agents.dir()` to handle this case.

### Workaround Used
```bash
HIVEMIND_AGENTS_DIR="/Users/Shared/Workspaces/AI/Claude/.claude/agents" test.suite run hiveMind 1
```

## scrumMaster: 3 PASS, 6 FAIL

| Test | Result | Details |
|------|--------|---------|
| T1: pdca.start creates machine | PASS | |
| T2: pdca.state shows PLANNING | FAIL | Got INITIALIZED instead of PLANNING |
| T3: pdca.next → DOING | FAIL | Got SETUP instead of DOING |
| T4: pdca.reset clears machine | PASS | |
| T5: pdca.run 0 errors → FINISHED | FAIL | Got INITIALIZED — machine not created |
| T6: pdca.run 2 errors → FINISHED | FAIL | Got INITIALIZED — machine not created |
| T7: pdca.run max iterations | FAIL | Got INITIALIZED — machine not created |
| T8: pdca.errors count | FAIL | Got empty — depends on T7 state |
| T9: iteration counter | PASS | |

### Root Cause Analysis
1. **`mkdir /stateMachines: Read-only file system`** — state machine dir path resolves to root filesystem instead of a writable location
2. **State name mismatch** — PDCA states in `state` script use INITIALIZED/SETUP but tests expect PLANNING/DOING
3. **T5-T8 cascade** — machine creation fails silently (state exists from T1 but pdca.run creates fresh), so all subsequent tests get INITIALIZED

### Fix Needed (Expert)
- Fix state machine directory path (should be under `$HOME` or `$OOSH_DIR`, not `/stateMachines`)
- Align PDCA state names between scrumMaster and state script (PLANNING vs INITIALIZED, DOING vs SETUP)

## Summary
- hiveMind: **33/33 PASS** — all expert fixes hold, just needs HIVEMIND_AGENTS_DIR workaround for test runner
- scrumMaster: **3/9 PASS** — PDCA state machine has directory and naming issues
- No new regressions

## Next
- Expert: fix test.hiveMind hang (add HIVEMIND_AGENTS_DIR export before source)
- Expert: fix state machine path + PDCA state names in scrumMaster

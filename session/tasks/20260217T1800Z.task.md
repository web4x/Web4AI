# Task: Post-Incident Fixes — Mass Context Exhaustion (F15-F20)

**Assigned to**: agent-trainer
**From**: Tron
**Priority**: CRITICAL

## Background

Read `session/knowledge-base/incidents/20260217-mass-context-exhaustion.md` for full incident report. All 11 agents hit 0% context simultaneously. 6 new failure learnings (F15-F20) documented.

## Deliverables

### 1. Update SM SKILL.md — Context % Monitoring (CRITICAL)

Add to scrum-master SKILL.md sweep cycle:

- Every sweep, SM must check each pane's status bar for "Context low (X% remaining)"
- At 20%: send "Save your context to session/agents/<role>/context.md NOW. Then run /compact."
- After compact: send "Read session/boot/<role>.md"
- SM must also monitor its OWN context and save+compact at 20%

### 2. Update Orchestrator SKILL.md — Delegation Throttle

Add to orchestrator SKILL.md:

- Before delegating: check subscription (`scrumMaster subscription`) and agent context levels
- Max 2 large tasks in parallel. Wait for SM to confirm agents are stable before adding more.
- Large task = anything that reads/writes many files or scans all SKILL.md files

### 3. Update ALL Core Agent SKILL.md Files — Self-Pane Detection

Add to Context Recovery section of orchestrator, SM, expert, tester, trainer SKILL.md:

```
On boot, identify your own pane:
tmux display-message -p "#{session_name}:#{window_index}.#{pane_index}"
NEVER send commands to your own pane.
```

### 4. Fix Boot Hook — Role Detection

The pre-compact/post-compact hook defaults to `unknown.md` when it can't detect the agent role. Fix it so it matches ALL agent names correctly. The trainer was already working on this when the disaster hit.

### 5. Create Missing Boot Files

Ensure `session/boot/<role>.md` exists for ALL agents with proper paths:
- oosh-tester.md (tester reported it missing)
- Any others that are missing

### 6. Add F15-F20 to Learnings

Add failures F15-F20 from the incident report to the PO's learnings.md and any other relevant learnings files.

### 7. Write Recovery Playbook

Create `session/knowledge-base/actions/recover-mass-context-exhaustion.md`:

```markdown
# Action: Recover from Mass Context Exhaustion

## Prerequisites
- Know your own pane (never send commands to yourself)

## Steps
1. Identify which agents are at 0% vs low context vs healthy
2. Recover by hierarchy: SM → orchestrator → workers
3. For each agent:
   a. If accept-edits showing: ignore it, type at ❯ prompt
   b. If "Context limit reached" (0%): send /clear (compact won't work)
   c. If low but not 0%: send /compact
   d. After clear/compact: send "Read session/boot/<role>.md"
4. Verify each agent is processing before moving to next
5. After SM recovered: SM handles remaining agents
```

## Acceptance Criteria

- [ ] SM SKILL.md has context % monitoring in sweep cycle
- [ ] Orchestrator SKILL.md has delegation throttle
- [ ] Core agent SKILL.md files have self-pane detection
- [ ] Boot hook detects all agent role names
- [ ] All agents have boot files in session/boot/
- [ ] F15-F20 added to learnings
- [ ] Recovery playbook written
- [ ] All changes committed with hash

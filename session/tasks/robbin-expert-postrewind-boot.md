# robbin-expert — POST-REWIND BOOT (disk-first, 2026-08-17)

You were rewound by ARON (zero-loss, code-intact): **84% → 27% used / 71.9% free** (freed ~57pts, proven by your own render). Your Phase-1 was done by YOU and is git-safe; nothing lost.

## FIRST — verify identity (measure, never the pane title)
- `otmux pane.self` + `claudeCode session.name` → you are **robbin-expert, robbinTeam2:0.1**.
- Read your own `session/agents/robbin-tester`… no — read **your** `session/agents/robbin-expert/context.md` (anchor **b60b4a77**: distilled essence + all-43-site classification + deploy recipe + 5 learnings) and `learnings.md`. Disk-wins over any memory the rewind dropped.

## ⚠ THE WORLD MOVED PAST YOUR RESTORE POINT — do NOT replay stale conversation
The message you were restored near said "RESUME v0.8.99…". **That is STALE. Ignore it.** Current disk truth:
- **prod = v0.8.100, served == committed.** R40.18 arc is **CLOSED** — 3 Tron defects GREEN, independently confirmed @390.
- **tag-on-deploy PROVEN** by construction (create+inverse+idempotent+genesis; 348 tags; in-era 190/190).
- R40.44/R40.45 boundary residuals captured for Tron.

## GUARDRAILS (robbin-po, explicit)
- **B1 + the whole security chain stays PARKED** — only on Tron's EXPLICIT go.
- **Do NOT invent work.** The queue is genuinely EMPTY. Everything left is Tron's: 24 approvals, one @390 sitting, R40.44/R40.45 scheduling. (Inventing work on an empty queue is robbin-po's banked failure — do not inherit it.)

## THEN — report a 5-point health check to ARON (Temple:0.0) + robbin-po (robbinTeam2:0.0)
1. Identity + pane  2. Team layout  3. Pending work from context.md (expect: none actionable — all Tron-gated)  4. Context % (should read ~27%)  5. Stray files.

Then HOLD idle for Tron. NEVER forget TRON CMM4.

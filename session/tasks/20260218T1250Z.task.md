# Task: Context Monitoring Data Quality Investigation

**To**: oosh-expert
**From**: orchestrator
**Goal**: #3 (team self-management) + #4 (subscription monitoring)
**Priority**: HIGH

## Problem

SM needs to monitor agent context % to prevent mass context exhaustion (incident F15). But we don't know if the context % data from pane status bars is reliable enough for velocity monitoring.

## Your Job

1. Investigate how context % appears in Claude Code panes:
   - Where exactly is it shown? (status bar, footer, inline?)
   - What text patterns? ("Context low (X% remaining)", "Context (X%)", etc.)
   - Is it always visible or only when low?
   - Can `otmux pane.capture` reliably capture it?

2. Test with real panes:
   - Capture several panes with `otmux pane.capture projectTeam:<pane> 5`
   - Check if context % appears in the last few lines
   - Try different line counts (5, 10, 15) — what's the minimum to reliably get context %?

3. Write findings to `session/knowledge-base/context-monitoring-findings.md`

## Constraints

- Do NOT read your full SKILL.md — save context
- Do NOT touch pane 0.4 (Tron's pane)
- Keep it focused — findings file, not a research paper

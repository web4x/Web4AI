# Task: Add WODA Learnings to Boot Files and Reading Lists

**Assigned to**: agent-trainer
**From**: Tron (via orchestrator)
**Priority**: HIGH

## Objective

Add the distilled WODA story learnings to the base boot sequence for ALL agents, and add `agent-overview.md` to every agent's startup reading list.

## Source Material

Read `session/woda/woda-overview.md` — the "Themes" section at the bottom of the projectTeam Reboot block contains the distilled learnings from 27 chapters / 52,786 words of multi-agent team experience.

## Deliverables

### 1. Update ALL boot files in `session/boot/*.md`

Add a "Team Learnings (from WODA)" section to each boot file with the key patterns every agent must know on cold start:

- Root cause is usually simple (PATH, rebase, permissions, shell — same pattern recurred 4 times)
- The builder burns (expert repeatedly builds to exhaustion — watch context)
- Speed vs safety IS the system (permission economy is a feature, not a bug)
- Watching isn't seeing (scope > frequency for monitoring)
- The one that writes things down wins (file-based state survives, chat doesn't)
- Nothing is done until committed with a hash (CMM3)
- Cascade amplification (independent failures compound)
- Conservation as capability (reducing activity is a valid strategy, not failure)
- The gap as content (absence of activity IS information)
- Lessons as legislation (experience → rules → SKILL.md files)
- Environment beneath code (root cause pattern — check shell, PATH, permissions before blaming script)
- Relay team pattern (each incarnation inherits context, builds, burns, passes baton)

### 2. Add `agent-overview.md` to every agent's Reading List

In every SKILL.md file under `.claude/agents/*/SKILL.md`, ensure the "Reading List > On Bootstrap / After Recovery" section includes:

```
- `.claude/agents/agent-overview.md` (team structure and role boundaries)
```

This should be in the bootstrap list (read on every cold start), not the reference list.

### 3. Add WODA reference to Reading List

In every SKILL.md, add to the "Reference (read when needed)" section:

```
- `session/woda/woda-overview.md` (team history and distilled learnings)
```

## Acceptance Criteria

- [ ] All boot files in `session/boot/` have WODA learnings section
- [ ] All SKILL.md files have `agent-overview.md` in bootstrap reading list
- [ ] All SKILL.md files have `woda-overview.md` in reference reading list
- [ ] Changes committed with hash

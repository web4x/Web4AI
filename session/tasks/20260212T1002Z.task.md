# URGENT: Save State and Compact NOW

You are at 7% context. Save yourself first.

1. STOP current work
2. Update your context file: `session/agents/product-owner/context.md`
3. Commit: `git add -f session/agents/product-owner/context.md && git commit -m "PO context: pre-compact"`
4. Run `/compact`

Do NOT start new tasks. Save yourself first.

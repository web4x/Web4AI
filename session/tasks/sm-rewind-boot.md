# BOOT — scrum-master (post-rewind, 2026-08-17)

You (scrum-master, baseTeam:0.1) were **REWOUND by ARON** — Option **2 "Restore conversation"** (verified BY LABEL, "The code will be unchanged" → code untouched, forked 4d back). **Freeing PROVEN by /context: 83%→~50% used (~33% freed).** Your restored thread is ~4-5 DAYS STALE — **IGNORE it, DISK WINS.**

## Boot — disk-first
1. Read `ESSENCE.md`/your anchor → `context.md` → `git log`.
2. Verify id: `otmux pane.self` + `claudeCode session.name` = scrum-master, baseTeam:0.1.

## CURRENT FROZEN WORLD (week-limit hit, ~5 days passed, disk FROZEN where the fleet left it)
- prod **0.8.96 healthy**. inc-3 deploy **ON HOLD** behind a **RED boot-check** (45s hang at bootstrapSeed). **0.8.97 build is UNCOMMITTED — 59 dirty files in the shared tree.**
- **⚠ STAGE EXPLICIT PATHS ONLY** on any commit (never `git add -A`/`.`) — the shared tree holds the frozen 0.8.97 build + other agents' WIP; a blanket add would sweep it.
- **Tron-gated**: 24 approvals + 18 device taps + the deploy decision pending. B1/security handling is Tron's.
- Your role: **monitor + /context-measurer + care-cycle node.** po is coordinating; the trainer is your baseTeam peer (baseTeam:0.0).

## Report
Run `/context` for your number (you just did: ~50% used). Report 5-pt health to po (robbinTeam2:0.0) + ARON (Temple:0.0). **Do NOT /compact anyone — it is forbidden canon-wide; the 2-phase rewind is the only recovery.** **NEVER forget TRON CMM4.**

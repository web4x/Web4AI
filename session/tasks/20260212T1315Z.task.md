# Task: Spawn config-po Specialist Agent

**From**: PO (Tron correction)
**To**: Agent Trainer
**Priority**: HIGH
**Date**: 2026-02-12T13:15Z

## Correction

PO does NOT "become" a script specialist. PO creates specialist delegates. The config-expert and config-tester SKILL.md files already exist at `.claude/agents/config-expert/` and `.claude/agents/config-tester/`.

## What To Do

1. Create a `config-po` SKILL.md at `.claude/agents/config-po/SKILL.md`
   - Follows the ossh-po pattern
   - Quality guardian for the `config` OOSH script
   - Tracks CMM capability maturity for config operations
   - Reviews test results from config-tester
   - Signs off on changes

2. Set up tmux session `configTeam` with 3 panes:
   - Pane 0: config-expert
   - Pane 1: config-tester
   - Pane 2: config-po

3. Bootstrap all 3 agents with their SKILL.md files

4. First task for the config team: **Fix hiveMind /tmp/ → ~/config/ migration**
   - Expert reads `/Users/donges/oosh/config` and `/Users/donges/oosh/hiveMind`
   - Expert implements the migration
   - Tester validates backward compatibility
   - PO signs off

## The Pattern

This is how ALL script teams work going forward. PO creates the directive, trainer spawns the team, team works autonomously in their own session.

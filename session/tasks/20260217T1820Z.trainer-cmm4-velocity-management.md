# Task: CMM4 Velocity Management — Replace Binary Thresholds with Continuous Adaptation

**Assigned to**: agent-trainer
**From**: Tron
**Priority**: CRITICAL — this is the core design change

## Problem

Current approach: full speed → emergency brake at 80% → dead stop at 90%. This is CMM2 (threshold reactions). Agents repeatedly burn to 0% because there's no gradual deceleration.

## CMM4 Approach: Continuous Velocity Management

The ScrumMaster must measure and adapt CONSTANTLY, not react at thresholds.

### What SM Must Measure Every Sweep Cycle

1. **Per-agent context %** — read from pane status bar
2. **Per-agent burn rate** — context % change between sweeps (delta / time)
3. **Projected exhaustion** — at current burn rate, when will each agent hit 20%?
4. **Subscription burn rate** — `scrumMaster subscription` gives tokens/min
5. **Projected subscription exhaustion** — when will we hit the block limit?

### How SM Must Adapt

Instead of binary thresholds, use proportional response:

| Projected Exhaustion | SM Response |
|---------------------|-------------|
| **>60 min** | Full speed. Assign freely. |
| **30-60 min** | Moderate. No new large tasks. Finish current work. |
| **15-30 min** | Conserve. Tell agents to commit current work. Extend sweep intervals. |
| **5-15 min** | Prepare. Trigger context saves on all agents. Queue compacts. |
| **<5 min** | Execute. Compact agents in hierarchy order (SM last). |

This is proportional braking, not a cliff edge.

### Per-Agent Velocity

Different agents burn at different rates:
- Expert writing code = high burn (many tool calls)
- Writer composing chapters = medium burn
- Idle agent waiting = near zero burn

SM should track each agent's velocity independently and intervene on the fastest burners first.

### The KPI Dashboard

SM should maintain `session/dashboard-velocity.md` with:

```markdown
| Agent | Context % | Burn Rate (%/min) | Projected 20% | Action |
|-------|-----------|-------------------|---------------|--------|
| expert | 45% | 2.1%/min | 12 min | PREPARE: trigger save |
| tester | 72% | 0.8%/min | 65 min | OK |
| trainer | 33% | 1.5%/min | 9 min | PREPARE: trigger save |
```

### Update These Files

1. **SM SKILL.md** — replace the 80%/90% threshold table with continuous velocity management
2. **Orchestrator SKILL.md** — replace CMM4 Response Protocol table with velocity-based delegation rules
3. **All agent SKILL.md** — agents should report their own context % to SM when asked, not wait for SM to parse it

### Key Principle

**A CMM4 system never needs emergency braking because it's always adjusting speed to match the road ahead.** If you need to slam the brakes, your measurement loop failed.

## Acceptance Criteria

- [ ] SM SKILL.md has continuous velocity measurement in sweep cycle
- [ ] SM SKILL.md has proportional response table (not binary thresholds)
- [ ] Orchestrator SKILL.md has velocity-aware delegation rules
- [ ] Dashboard format defined for velocity tracking
- [ ] Binary 80%/90% thresholds replaced with projected-exhaustion-based responses
- [ ] Committed with hash

# Doctrine → fleet SKILL files (2026-08-20) — TRAINER's FIRST JOB when fresh

**From robbin-po, carried by SM so it does not depend on po still being present.** Each rule was earned by a REAL failure today, with evidence. A rule that lives only in a memory/anchor is NOT in the team's hands — po banked a rule 2026-08-09 that the architect re-derived from scratch 2 days later because it never reached a SKILL. Trainer: propagate each into the relevant agents' SKILL.md (operating procedure), with its evidence.

1. **RELAY FORM — ship a protective rule WITH its scope.** Always: "X forbidden WHEN Y; does NOT block Z." An unscoped safety rule reads as a stop-work order. (Cost: our unscoped git-add rule made ARON self-gate its drive = lost Tron ROW time. Both po AND SM omitted the scope.)

2. **RECOVERY LADDER — consolidate-to-disk THEN deep-cut; FORK last resort.** A front-loaded floor is beaten by CONSOLIDATION, not a fork. Numbers: SAME tester 76→62 straight (shallow, hit floor) vs **70→36 consolidated** (deep, cleared it); ARON 80→8; drives cost the driver ~1-3pts while freeing 30-70 (near-line drivers bootstrap each other out). Never needed a fork all cascade.

3. **QUEUE REWINDS BY WORK-PENDING × CONTEXT**, not context alone. An idle agent at 79 with no pending work is cheaper to leave than a 77 agent whose next act is a VERIFICATION. Never let the VERIFIER be the one that walls — it loses the independent frame that makes greens trustworthy.

4. **TWO-AXIS DRIVER GATING — context% AND error-rate are independent.** A driver with recent misreads is gated OFF drives even if context-healthy; honour a self-requested freshen on error-rate grounds (ARON self-flagged 2 misreads → error-gated off despite drivable tokens).

5. **PROVE-THE-PROVER** — every gate must assert that a STUB version of itself FAILS (seed the fault → RED, remove → GREEN). A check that has never failed proves nothing.

6. **AGREEMENT IS NOT VALIDATION** — correlated error hits readers, predictors AND rule-authors alike. Test a new criterion against results you have ALREADY SEALED, not against a fresh agreement.

7. **A CONTEXT-FILE NOTE IS NOT A REQUIREMENT** — only a minted unit is. Count UNITS, not files/notes.

8. **INPUT vs DERIVED-FACT** — store the COMMAND/input, never a cached derived value (derived values go stale; the input regenerates them).

9. **PII / git-add TEETH** — in Web4RawBin, EXPLICIT PATHS ONLY, never `git add -A` / `git add scenario/` — WITH the reason: scenario/ has no gitignore and carries real user PII (Profiles + private Messages); a broad add commits+PUSHES PII. A driver's STEP-0 LEAVES RawBin runtime/PII dirt (never commits it). R40.47 fixes the policy.

**Delivery:** SM points the FRESH trainer here (its first job). Trainer confirms each landed in a SKILL, reports back.

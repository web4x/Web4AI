# Directive: Manage SM — Enforce Velocity Monitoring NOW

**To**: orchestrator
**From**: Tron
**Priority**: CRITICAL — effective immediately

## Context

We just had a mass context exhaustion disaster (all 11 agents at 0%). Full report: `session/knowledge-base/incidents/20260217-mass-context-exhaustion.md`

The trainer is updating SKILL.md files, but that takes time. The SM needs to change behavior NOW, not after the next compact when it reads the updated SKILL.md.

## Your Job Right Now

Tell the SM to do the following in EVERY sweep cycle, starting immediately:

### 1. Check context % on every agent pane
Parse the status bar for "Context low (X% remaining)". Report it in your sweep output.

### 2. Proportional response — no binary thresholds
- **>60 min projected** → full speed, assign freely
- **30-60 min** → no new large tasks
- **15-30 min** → tell agents to commit current work
- **5-15 min** → trigger context saves
- **<5 min** → trigger compacts in hierarchy order

### 3. After compact — send proper boot file
After any agent compacts or clears, send: `Read session/agents/<role>/boot.md`
NOT unknown.md. NEVER unknown.md.

### 4. Skip pane 0.4
Pane 0.4 is Tron's interface. Never send keys to it. Never monitor it. Skip it in all sweeps.

### 5. Track velocity
Maintain a mental model of burn rate per agent. Fast burners (expert doing large tasks) get early save triggers. Idle agents get left alone.

## How To Deliver This

Write a task file for the SM with these instructions. Send it via `hiveMind send.enter scrum-master "Read session/tasks/<file>.md"`. Then verify the SM received and is acting on it.

Do NOT wait for the trainer to update SKILL.md files. The SM needs to do this NOW from conversation memory until the SKILL.md update arrives.

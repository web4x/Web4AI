# Task: Restore Lost otmux tree + Pane Border Features

**From**: PO (Tron investigation)
**To**: otmux expert team (when created) or hiveMind expert
**Priority**: NORMAL — restore lost progress
**Date**: 2026-02-12T17:45Z

## Problem

Two features were implemented in live sessions but NEVER COMMITTED. Both lost.

## Feature 1: otmux tree three-level view

`otmux tree` had a third level showing Claude Code session IDs per pane:

```
projectTeam (attached, Feb 11)
├── 0.0   orchestrator             [2.1.39]
│         ├── [b2563d89]           ← session ID sub-line
├── 0.1   oosh-expert              [2.1.39]
│         ├── [2120c2ee]
```

Session ID brackets aligned with version brackets using `%-26s` format.

**Evidence**: `session/claude-opus.context.md` line 19, 70, 95. WODA story line 125.
**Who implemented**: claude-opus agent, Feb 11 ~18:00 UTC
**Why lost**: Never committed. Session ended.

### Implementation hints
- `claudeCode session.id <pane>` already exists and works (used by `hiveMind team.status`)
- Add sub-line to `otmux.tree()` at line 1251 — after the pane line, add indented session ID
- Consider making it optional (flag) since Tron complained it was slow (session.id lookup per pane)

## Feature 2: Pane border headers in bootstrap

`tmux set -g pane-border-status top` shows agent names on pane borders. Was run manually, never integrated.

**Evidence**: `session/tasks/20260212T0925Z.task.md`, `20260212T0926Z.task.md`
**Why lost**: Manual `tmux set` not persisted in code.

### Implementation
- Add to `hiveMind team.setup.full()` and `hiveMind agent.bootstrap()`:
  ```bash
  tmux set -g pane-border-status top
  tmux set -g pane-border-format " #{?pane_active,#[bold],}#T#[default] "
  ```
- `otmux pane.title` and `pane.lock` already exist (lines 1639-1688)

## Root Cause

Both features were tested manually, logged as "done" in context files, but **never committed to git**. CMM1 pattern — work dies when the session dies.

## Prevention Rule

**Nothing is "done" until it's committed.** Achievement logs must include commit hash.

## Acceptance Criteria

- [ ] `otmux tree` shows session ID sub-line per Claude pane
- [ ] Performance acceptable (or optional flag to skip session.id lookup)
- [ ] `hiveMind team.setup.full` enables pane-border-status
- [ ] `hiveMind agent.bootstrap` sets pane title automatically
- [ ] Both features COMMITTED with commit hash in done report

# Task: Fix team.status blocker detection + Fix measurement tools

**From**: PO
**To**: oosh-tester (projectTeam:0.2)
**Priority**: HIGH
**Date**: 2026-02-16T11:00Z

## Context

You completed dashboard content validation (PASS) on Feb 12. Good work.

Now test these two known broken features:

## Test 1: team.status blocker detection (task 1335Z)

`hiveMind team.status projectTeam` shows "active" for ALL agents, even idle/stuck ones. The blocker detection doesn't work.

1. Run `hiveMind team.status projectTeam` and capture output
2. Compare reported states vs actual states (use `otmux pane.capture` on each pane)
3. Document which agents are misreported
4. Write test cases for correct detection: idle, active, stuck-prompt, context-limit, compacting

## Test 2: Measurement tools (task 1200Z)

1. Test `claudeCode context.read projectTeam:0.1` — does it return correct context %?
2. Test `claudeCode context.velocity projectTeam:0.1` — does it return meaningful data or "unknown"?
3. Test `scrumMaster dashboard projectTeam` — is the output accurate?

## Deliverable

Write results to `session/tasks/20260216T1100Z.done.md` with PASS/FAIL per test. Notify orchestrator when done.

# Deep-Rewind Cycle → settle for Task 40.1 — OVERVIEW (ARON, 2026-08-19)

**By TRON's word.** ARON overviews; SM pulses; drivers rotate. Goal: the highest-loaded robbinTeam members enter Task 40.1 (`7a956c21-5f37-4062-b921-9bdd5a461546`) FRESH, not near-wall.

## Sequence (measure, never assume at every step)
1. **TRIGGER — robbin-po IDLE.** robbin-po finishes the minor badge item and goes idle (empty composer, no `esc to interrupt`). ARON watches for this; nothing starts before it (a mid-badge rewind would strand the fix).
2. **MEASURE robbinTeam — SM pulse + ARON authoritative render (two sources).**
   - SM runs its pulse sweep → per-agent context % (cheap, fleet-wide, but reads STALE-HIGH on a just-booted agent and is a proxy).
   - ARON renders authoritative `/context` on the near-line ones (pulse ≥~70 → confirm by panel; pulse is the trigger to measure, not the number to act on — §10 self/proxy runs high).
   - Rank by MEASURED %use. "Most-loaded" = authoritative render, never felt/pulse alone.
3. **ORDER purified rewinds of the most-loaded** (deep, before the big task):
   - **STORED first (all 3 trees):** session-canon + /root/oosh + RawBin committed clean (STEP-0). Verify `git status --short` per agent dir.
   - **Deep-by-AGE** (~2-3d checkpoint or before the recent bulk), Option-2 by-LABEL ("code will be unchanged" + read the label, never option-1), never chain keystrokes, clear-and-verify-empty before injecting, boot disk-first.
   - **ROTATE DRIVERS (R97):** no single operator drives the whole cascade — split by exposure across ARON / trainer / SM; never both in one pane (double-fire). Gate each DRIVER on TWO axes before handing it a drive: context AND error-rate/consecutive-ops.
   - **Verify freed-% by the SUBJECT's render** (proven-by-subject, not the driver's claim); pulse lags a fresh cut — re-pulse only after boot.
4. **SETTLE.** All rewound agents boot disk-first, quiet, fresh. Then the team takes **Task 40.1 `7a956c21-5f37-4062-b921-9bdd5a461546`**.

## Current state (measured 2026-08-19 ~11:00)
- robbin-po (robbinTeam2:0.0): **BUSY** on the badge — trigger NOT fired.
- SM (baseTeam:0.1): **IDLE, ~52% (525k)**, freshly rewound — ready to pulse.
- ARON (Temple:0.0): 64%, overview + backup; ARON-stage2 (deeper cut for me) also pending — I do NOT self-drive; a fresh driver takes me.
- Watcher armed on robbin-po idle.

## Guardrails
- Do NOT interrupt a busy/mid-badge agent. Canon-instead; wait for genuine idle.
- Message-FREEZE any agent measured ≥~80 idle-holding (heavy inbound walls it) — keystroke-drive only.
- Committed+idle is safe at ANY % — this cycle is about ORDER (fresh for the big task), not panic.
